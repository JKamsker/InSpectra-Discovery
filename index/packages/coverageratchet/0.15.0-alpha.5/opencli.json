{
  "opencli": "0.1-draft",
  "info": {
    "title": "coverageratchet",
    "version": "0.15.0-alpha.5",
    "description": "Per-file coverage enforcement with automatic threshold ratcheting from Cobertura XML"
  },
  "x-inspectra": {
    "artifactSource": "crawled-from-help",
    "generator": "InSpectra.Gen",
    "helpDocumentCount": 4
  },
  "commands": [
    {
      "name": "check",
      "hidden": false,
      "description": "Check coverage against thresholds (exits non-zero if any file fails)",
      "arguments": [
        {
          "name": "CONFIG",
          "required": false,
          "hidden": false,
          "arity": {
            "minimum": 0,
            "maximum": 1
          },
          "description": "Path to the JSON config file (default: coverage-ratchet.json)\nExits 0 if every F# file meets its line+branch threshold, 1 otherwise.\nUse in CI. Files not listed in [config] must hit 100%/100%."
        }
      ]
    },
    {
      "name": "gaps",
      "hidden": false,
      "description": "List uncovered branch points per file\nloosen-from-ci [config] Pull CI coverage results into local platform-specific thresholds\nmerge <baseline> <partial> <output> Merge two Cobertura files by taking max hits per line\nrefresh-baseline Copy each coverage.cobertura.xml to coverage.baseline.xml in the search dir",
      "arguments": [
        {
          "name": "CONFIG",
          "required": false,
          "hidden": false,
          "arity": {
            "minimum": 0,
            "maximum": 1
          },
          "description": "Path to the JSON config file (default: coverage-ratchet.json)\nFor each file with branches < 100%, prints the source lines where\nuncovered branches sit. Use to plan tests for partial branch"
        },
        {
          "name": "COVERAGE",
          "required": false,
          "hidden": false,
          "arity": {
            "minimum": 0,
            "maximum": 1
          }
        }
      ]
    },
    {
      "name": "loosen",
      "hidden": false,
      "description": "Set thresholds down to current coverage (makes check pass immediately)\ncheck-json [config] [output] Write per-file coverage results as JSON for CI artifact upload\ntargets [config] List files sorted by coverage to find improvement targets",
      "arguments": [
        {
          "name": "CONFIG",
          "required": false,
          "hidden": false,
          "arity": {
            "minimum": 0,
            "maximum": 1
          },
          "description": "Path to the JSON config file (default: coverage-ratchet.json)\nLower thresholds in [config] to match current coverage so 'check'\npasses. Use sparingly — bootstrapping, or after a deliberate drop.\nUnlike 'ratchet' this can move thresholds DOWN."
        }
      ]
    }
  ],
  "options": [
    {
      "name": "--search-dir",
      "recursive": false,
      "hidden": false,
      "description": "directory to scan for coverage.cobertura.xml\n(default: current directory; recursive)",
      "arguments": [
        {
          "name": "PATH",
          "required": false,
          "arity": {
            "minimum": 0,
            "maximum": 1
          }
        }
      ]
    },
    {
      "name": "--merge-baselines",
      "recursive": false,
      "hidden": false,
      "description": "before reading coverage, merge each\ncoverage.cobertura.xml onto its sibling\ncoverage.baseline.xml (max hits per line) so\npartial test runs cannot lower the ratchet.\nBootstraps a baseline on first use.",
      "arguments": [
        {
          "name": "MERGE_BASELINES",
          "required": false,
          "arity": {
            "minimum": 0,
            "maximum": 1
          }
        }
      ]
    }
  ]
}
