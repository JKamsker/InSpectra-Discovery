{
  "opencli": "0.1-draft",
  "info": {
    "title": "hood-schema — apply the Hood CMS database schema (idempotent, forward-only).",
    "version": "7.0.0-rc.29",
    "description": "CLI to apply the Hood CMS database schema (idempotent, forward-only) in a deploy pipeline."
  },
  "x-inspectra": {
    "artifactSource": "crawled-from-help",
    "generator": "InSpectra.Gen",
    "helpDocumentCount": 2
  },
  "commands": [
    {
      "name": "upgrade",
      "hidden": false,
      "options": [
        {
          "name": "--connection",
          "recursive": false,
          "hidden": false,
          "description": "SQL Server connection string (or set the HOOD_CONNECTION env var).",
          "aliases": [
            "-c"
          ],
          "arguments": [
            {
              "name": "CONNECTION",
              "required": false,
              "arity": {
                "minimum": 0,
                "maximum": 1
              }
            }
          ]
        },
        {
          "name": "--scripts",
          "recursive": false,
          "hidden": false,
          "description": "Optional folder of the consuming project's own .sql scripts, applied\nafter Hood's core schema and journalled in the same SchemaVersions table.\nThe target database is created if it doesn't exist. Re-running is a clean no-op: DbUp records\nevery applied script in dbo.SchemaVersions and never re-runs one. No EF migrations are involved.",
          "aliases": [
            "-s"
          ],
          "arguments": [
            {
              "name": "SCRIPTS",
              "required": true,
              "arity": {
                "minimum": 1,
                "maximum": 1
              }
            }
          ]
        }
      ]
    }
  ],
  "options": [
    {
      "name": "--connection",
      "recursive": false,
      "hidden": false,
      "description": "SQL Server connection string (or set the HOOD_CONNECTION env var).",
      "aliases": [
        "-c"
      ],
      "arguments": [
        {
          "name": "CONNECTION",
          "required": false,
          "arity": {
            "minimum": 0,
            "maximum": 1
          }
        }
      ]
    },
    {
      "name": "--scripts",
      "recursive": false,
      "hidden": false,
      "description": "Optional folder of the consuming project's own .sql scripts, applied\nafter Hood's core schema and journalled in the same SchemaVersions table.\nThe target database is created if it doesn't exist. Re-running is a clean no-op: DbUp records\nevery applied script in dbo.SchemaVersions and never re-runs one. No EF migrations are involved.",
      "aliases": [
        "-s"
      ],
      "arguments": [
        {
          "name": "SCRIPTS",
          "required": true,
          "arity": {
            "minimum": 1,
            "maximum": 1
          }
        }
      ]
    }
  ]
}
