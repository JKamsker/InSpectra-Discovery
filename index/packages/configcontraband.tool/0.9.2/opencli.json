{
  "opencli": "0.1-draft",
  "info": {
    "title": "ConfigContraband.Tool — appsettings JSON Schema generator",
    "version": "0.9.2",
    "description": "Generate appsettings.schema.json from .NET Options types for editor IntelliSense, type checking, and required-key hints that match ValidateDataAnnotations and BindConfiguration."
  },
  "x-inspectra": {
    "artifactSource": "crawled-from-help",
    "generator": "InSpectra.Gen",
    "helpDocumentCount": 2,
    "cliParsedTitle": "ConfigContraband schema generator"
  },
  "commands": [
    {
      "name": "schema",
      "hidden": false,
      "options": [
        {
          "name": "--project",
          "recursive": false,
          "hidden": false,
          "description": "Project (.csproj) to analyze. Defaults to the single .csproj in the current directory.",
          "arguments": [
            {
              "name": "PATH",
              "required": true,
              "arity": {
                "minimum": 1,
                "maximum": 1
              }
            }
          ]
        },
        {
          "name": "--solution",
          "recursive": false,
          "hidden": false,
          "description": "Solution to analyze. A schema is written per project.",
          "arguments": [
            {
              "name": "PATH",
              "required": true,
              "arity": {
                "minimum": 1,
                "maximum": 1
              }
            }
          ]
        },
        {
          "name": "--output",
          "recursive": false,
          "hidden": false,
          "description": "Output schema path (single project only). Defaults to <projectDir>/appsettings.schema.json.",
          "arguments": [
            {
              "name": "PATH",
              "required": true,
              "arity": {
                "minimum": 1,
                "maximum": 1
              }
            }
          ]
        },
        {
          "name": "--check",
          "recursive": false,
          "hidden": false,
          "description": "Do not write; exit non-zero if any schema is missing or out of date (for CI)."
        }
      ]
    }
  ],
  "options": [
    {
      "name": "--project",
      "recursive": false,
      "hidden": false,
      "description": "Project (.csproj) to analyze. Defaults to the single .csproj in the current directory.",
      "arguments": [
        {
          "name": "PATH",
          "required": true,
          "arity": {
            "minimum": 1,
            "maximum": 1
          }
        }
      ]
    },
    {
      "name": "--solution",
      "recursive": false,
      "hidden": false,
      "description": "Solution to analyze. A schema is written per project.",
      "arguments": [
        {
          "name": "PATH",
          "required": true,
          "arity": {
            "minimum": 1,
            "maximum": 1
          }
        }
      ]
    },
    {
      "name": "--output",
      "recursive": false,
      "hidden": false,
      "description": "Output schema path (single project only). Defaults to <projectDir>/appsettings.schema.json.",
      "arguments": [
        {
          "name": "PATH",
          "required": true,
          "arity": {
            "minimum": 1,
            "maximum": 1
          }
        }
      ]
    },
    {
      "name": "--check",
      "recursive": false,
      "hidden": false,
      "description": "Do not write; exit non-zero if any schema is missing or out of date (for CI)."
    }
  ]
}
