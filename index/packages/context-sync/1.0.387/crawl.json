{
  "commands": [
    {
      "command": null,
      "helpInvocation": "--help",
      "result": {
        "status": "ok",
        "timedOut": false,
        "exitCode": 0,
        "durationMs": 34,
        "stdout": "context-sync - deliver artifacts to, and materialise knowledge from, a ContextHub workspace\n\nusage:\n  context-sync init     --url <base> --workspace <id> --layer <layer> [--platform github|azuredevops|gitlab] [--out <dir>]\n  context-sync init     --url <base> --organization <id> --workspace <handle> [--platform ...] [--out <dir>]\n  context-sync context  --out <dir> [--include-chunks] [--require <kind>[=<min>]]...\n  context-sync pull     --root <dir> [--layer <layer>] [--force]\n  context-sync validate --root <dir> [--layer <layer>] [--source-commit <sha>]\n  context-sync publish  --root <dir> [--layer <layer>] [--activate] [--source-commit <sha>]\n  context-sync gap      (--file <gaps.json> | --need <text> [--why <text>] [--considered a,b] | --list) [--strict]\n\ncommon options (env fallback in parentheses):\n  --url <base>        the API base URL              (CONTEXTHUB_URL)\n  --workspace <id>    the workspace to act in       (CONTEXTHUB_WORKSPACE)\n  --organization <id> the organization a tree belongs to (CONTEXTHUB_ORGANIZATION)\n  --config <path>     config file                   (default: <root>/context-sync.json)\n  credential          CONTEXTHUB_TOKEN only - never a flag, never a config key\nurl, workspace, layer and organization may come from context-sync.json; flags and environment\noverride it. A root whose context-sync.json names no layer, holding folders that do, is a\nTREE: validate, publish and pull walk it and each node's own config names its layer and the\nworkspace id its calls go through. --layer is refused against a tree - one flag must not\nretarget every node at once.\n\ninit      scaffold a customer workflow repository: workflows/skills/ontologies folders,\n          context-sync.json, README, an agent orientation file, and the pipeline for\n          --platform. Refuses to overwrite existing files.\n          With --organization it also needs --workspace (a handle inside that organization,\n          since no artifact route is organization-addressed) and a credential, and it\n          materialises a nested fo\n[truncated]",
        "stderr": null,
        "outputLimitExceeded": false
      },
      "parsed": true,
      "payload": "context-sync - deliver artifacts to, and materialise knowledge from, a ContextHub workspace\n\nusage:\n  context-sync init     --url <base> --workspace <id> --layer <layer> [--platform github|azuredevops|gitlab] [--out <dir>]\n  context-sync init     --url <base> --organization <id> --workspace <handle> [--platform ...] [--out <dir>]\n  context-sync context  --out <dir> [--include-chunks] [--require <kind>[=<min>]]...\n  context-sync pull     --root <dir> [--layer <layer>] [--force]\n  context-sync validate --root <dir> [--layer <layer>] [--source-commit <sha>]\n  context-sync publish  --root <dir> [--layer <layer>] [--activate] [--source-commit <sha>]\n  context-sync gap      (--file <gaps.json> | --need <text> [--why <text>] [--considered a,b] | --list) [--strict]\n\ncommon options (env fallback in parentheses):\n  --url <base>        the API base URL              (CONTEXTHUB_URL)\n  --workspace <id>    the workspace to act in       (CONTEXTHUB_WORKSPACE)\n  --organization <id> the organization a tree belongs to (CONTEXTHUB_ORGANIZATION)\n  --config <path>     config file                   (default: <root>/context-sync.json)\n  credential          CONTEXTHUB_TOKEN only - never a flag, never a config key\nurl, workspace, layer and organization may come from context-sync.json; flags and environment\noverride it. A root whose context-sync.json names no layer, holding folders that do, is a\nTREE: validate, publish and pull walk it and each node's own config names its layer and the\nworkspace id its calls go through. --layer is refused against a tree - one flag must not\nretarget every node at once.\n\ninit      scaffold a customer workflow repository: workflows/skills/ontologies folders,\n          context-sync.json, README, an agent orientation file, and the pipeline for\n          --platform. Refuses to overwrite existing files.\n          With --organization it also needs --workspace (a handle inside that organization,\n          since no artifact route is organization-addressed) and a credential, and it\n          materialises a nested fo\n[truncated]",
      "terminalNonHelp": false
    },
    {
      "command": "init",
      "helpInvocation": "init --help",
      "result": {
        "status": "failed",
        "timedOut": false,
        "exitCode": 2,
        "durationMs": 30,
        "stdout": null,
        "stderr": "context-sync: unknown option '--help'\ncontext-sync - deliver artifacts to, and materialise knowledge from, a ContextHub workspace\n\nusage:\n  context-sync init     --url <base> --workspace <id> --layer <layer> [--platform github|azuredevops|gitlab] [--out <dir>]\n  context-sync init     --url <base> --organization <id> --workspace <handle> [--platform ...] [--out <dir>]\n  context-sync context  --out <dir> [--include-chunks] [--require <kind>[=<min>]]...\n  context-sync pull     --root <dir> [--layer <layer>] [--force]\n  context-sync validate --root <dir> [--layer <layer>] [--source-commit <sha>]\n  context-sync publish  --root <dir> [--layer <layer>] [--activate] [--source-commit <sha>]\n  context-sync gap      (--file <gaps.json> | --need <text> [--why <text>] [--considered a,b] | --list) [--strict]\n\ncommon options (env fallback in parentheses):\n  --url <base>        the API base URL              (CONTEXTHUB_URL)\n  --workspace <id>    the workspace to act in       (CONTEXTHUB_WORKSPACE)\n  --organization <id> the organization a tree belongs to (CONTEXTHUB_ORGANIZATION)\n  --config <path>     config file                   (default: <root>/context-sync.json)\n  credential          CONTEXTHUB_TOKEN only - never a flag, never a config key\nurl, workspace, layer and organization may come from context-sync.json; flags and environment\noverride it. A root whose context-sync.json names no layer, holding folders that do, is a\nTREE: validate, publish and pull walk it and each node's own config names its layer and the\nworkspace id its calls go through. --layer is refused against a tree - one flag must not\nretarget every node at once.\n\ninit      scaffold a customer workflow repository: workflows/skills/ontologies folders,\n          context-sync.json, README, an agent orientation file, and the pipeline for\n          --platform. Refuses to overwrite existing files.\n          With --organization it also needs --workspace (a handle inside that organization,\n          since no artifact route is organization-addressed) and a credential, and\n[truncated]",
        "outputLimitExceeded": false
      },
      "parsed": true,
      "payload": "context-sync: unknown option '--help'\ncontext-sync - deliver artifacts to, and materialise knowledge from, a ContextHub workspace\n\nusage:\n  context-sync init     --url <base> --workspace <id> --layer <layer> [--platform github|azuredevops|gitlab] [--out <dir>]\n  context-sync init     --url <base> --organization <id> --workspace <handle> [--platform ...] [--out <dir>]\n  context-sync context  --out <dir> [--include-chunks] [--require <kind>[=<min>]]...\n  context-sync pull     --root <dir> [--layer <layer>] [--force]\n  context-sync validate --root <dir> [--layer <layer>] [--source-commit <sha>]\n  context-sync publish  --root <dir> [--layer <layer>] [--activate] [--source-commit <sha>]\n  context-sync gap      (--file <gaps.json> | --need <text> [--why <text>] [--considered a,b] | --list) [--strict]\n\ncommon options (env fallback in parentheses):\n  --url <base>        the API base URL              (CONTEXTHUB_URL)\n  --workspace <id>    the workspace to act in       (CONTEXTHUB_WORKSPACE)\n  --organization <id> the organization a tree belongs to (CONTEXTHUB_ORGANIZATION)\n  --config <path>     config file                   (default: <root>/context-sync.json)\n  credential          CONTEXTHUB_TOKEN only - never a flag, never a config key\nurl, workspace, layer and organization may come from context-sync.json; flags and environment\noverride it. A root whose context-sync.json names no layer, holding folders that do, is a\nTREE: validate, publish and pull walk it and each node's own config names its layer and the\nworkspace id its calls go through. --layer is refused against a tree - one flag must not\nretarget every node at once.\n\ninit      scaffold a customer workflow repository: workflows/skills/ontologies folders,\n          context-sync.json, README, an agent orientation file, and the pipeline for\n          --platform. Refuses to overwrite existing files.\n          With --organization it also needs --workspace (a handle inside that organization,\n          since no artifact route is organization-addressed) and a credential, and\n[truncated]",
      "terminalNonHelp": false
    },
    {
      "command": "context",
      "helpInvocation": "context --help",
      "result": {
        "status": "failed",
        "timedOut": false,
        "exitCode": 2,
        "durationMs": 25,
        "stdout": null,
        "stderr": "context-sync: unknown option '--help'\ncontext-sync - deliver artifacts to, and materialise knowledge from, a ContextHub workspace\n\nusage:\n  context-sync init     --url <base> --workspace <id> --layer <layer> [--platform github|azuredevops|gitlab] [--out <dir>]\n  context-sync init     --url <base> --organization <id> --workspace <handle> [--platform ...] [--out <dir>]\n  context-sync context  --out <dir> [--include-chunks] [--require <kind>[=<min>]]...\n  context-sync pull     --root <dir> [--layer <layer>] [--force]\n  context-sync validate --root <dir> [--layer <layer>] [--source-commit <sha>]\n  context-sync publish  --root <dir> [--layer <layer>] [--activate] [--source-commit <sha>]\n  context-sync gap      (--file <gaps.json> | --need <text> [--why <text>] [--considered a,b] | --list) [--strict]\n\ncommon options (env fallback in parentheses):\n  --url <base>        the API base URL              (CONTEXTHUB_URL)\n  --workspace <id>    the workspace to act in       (CONTEXTHUB_WORKSPACE)\n  --organization <id> the organization a tree belongs to (CONTEXTHUB_ORGANIZATION)\n  --config <path>     config file                   (default: <root>/context-sync.json)\n  credential          CONTEXTHUB_TOKEN only - never a flag, never a config key\nurl, workspace, layer and organization may come from context-sync.json; flags and environment\noverride it. A root whose context-sync.json names no layer, holding folders that do, is a\nTREE: validate, publish and pull walk it and each node's own config names its layer and the\nworkspace id its calls go through. --layer is refused against a tree - one flag must not\nretarget every node at once.\n\ninit      scaffold a customer workflow repository: workflows/skills/ontologies folders,\n          context-sync.json, README, an agent orientation file, and the pipeline for\n          --platform. Refuses to overwrite existing files.\n          With --organization it also needs --workspace (a handle inside that organization,\n          since no artifact route is organization-addressed) and a credential, and\n[truncated]",
        "outputLimitExceeded": false
      },
      "parsed": true,
      "payload": "context-sync: unknown option '--help'\ncontext-sync - deliver artifacts to, and materialise knowledge from, a ContextHub workspace\n\nusage:\n  context-sync init     --url <base> --workspace <id> --layer <layer> [--platform github|azuredevops|gitlab] [--out <dir>]\n  context-sync init     --url <base> --organization <id> --workspace <handle> [--platform ...] [--out <dir>]\n  context-sync context  --out <dir> [--include-chunks] [--require <kind>[=<min>]]...\n  context-sync pull     --root <dir> [--layer <layer>] [--force]\n  context-sync validate --root <dir> [--layer <layer>] [--source-commit <sha>]\n  context-sync publish  --root <dir> [--layer <layer>] [--activate] [--source-commit <sha>]\n  context-sync gap      (--file <gaps.json> | --need <text> [--why <text>] [--considered a,b] | --list) [--strict]\n\ncommon options (env fallback in parentheses):\n  --url <base>        the API base URL              (CONTEXTHUB_URL)\n  --workspace <id>    the workspace to act in       (CONTEXTHUB_WORKSPACE)\n  --organization <id> the organization a tree belongs to (CONTEXTHUB_ORGANIZATION)\n  --config <path>     config file                   (default: <root>/context-sync.json)\n  credential          CONTEXTHUB_TOKEN only - never a flag, never a config key\nurl, workspace, layer and organization may come from context-sync.json; flags and environment\noverride it. A root whose context-sync.json names no layer, holding folders that do, is a\nTREE: validate, publish and pull walk it and each node's own config names its layer and the\nworkspace id its calls go through. --layer is refused against a tree - one flag must not\nretarget every node at once.\n\ninit      scaffold a customer workflow repository: workflows/skills/ontologies folders,\n          context-sync.json, README, an agent orientation file, and the pipeline for\n          --platform. Refuses to overwrite existing files.\n          With --organization it also needs --workspace (a handle inside that organization,\n          since no artifact route is organization-addressed) and a credential, and\n[truncated]",
      "terminalNonHelp": false
    },
    {
      "command": "pull",
      "helpInvocation": "pull --help",
      "result": {
        "status": "failed",
        "timedOut": false,
        "exitCode": 2,
        "durationMs": 31,
        "stdout": null,
        "stderr": "context-sync: unknown option '--help'\ncontext-sync - deliver artifacts to, and materialise knowledge from, a ContextHub workspace\n\nusage:\n  context-sync init     --url <base> --workspace <id> --layer <layer> [--platform github|azuredevops|gitlab] [--out <dir>]\n  context-sync init     --url <base> --organization <id> --workspace <handle> [--platform ...] [--out <dir>]\n  context-sync context  --out <dir> [--include-chunks] [--require <kind>[=<min>]]...\n  context-sync pull     --root <dir> [--layer <layer>] [--force]\n  context-sync validate --root <dir> [--layer <layer>] [--source-commit <sha>]\n  context-sync publish  --root <dir> [--layer <layer>] [--activate] [--source-commit <sha>]\n  context-sync gap      (--file <gaps.json> | --need <text> [--why <text>] [--considered a,b] | --list) [--strict]\n\ncommon options (env fallback in parentheses):\n  --url <base>        the API base URL              (CONTEXTHUB_URL)\n  --workspace <id>    the workspace to act in       (CONTEXTHUB_WORKSPACE)\n  --organization <id> the organization a tree belongs to (CONTEXTHUB_ORGANIZATION)\n  --config <path>     config file                   (default: <root>/context-sync.json)\n  credential          CONTEXTHUB_TOKEN only - never a flag, never a config key\nurl, workspace, layer and organization may come from context-sync.json; flags and environment\noverride it. A root whose context-sync.json names no layer, holding folders that do, is a\nTREE: validate, publish and pull walk it and each node's own config names its layer and the\nworkspace id its calls go through. --layer is refused against a tree - one flag must not\nretarget every node at once.\n\ninit      scaffold a customer workflow repository: workflows/skills/ontologies folders,\n          context-sync.json, README, an agent orientation file, and the pipeline for\n          --platform. Refuses to overwrite existing files.\n          With --organization it also needs --workspace (a handle inside that organization,\n          since no artifact route is organization-addressed) and a credential, and\n[truncated]",
        "outputLimitExceeded": false
      },
      "parsed": true,
      "payload": "context-sync: unknown option '--help'\ncontext-sync - deliver artifacts to, and materialise knowledge from, a ContextHub workspace\n\nusage:\n  context-sync init     --url <base> --workspace <id> --layer <layer> [--platform github|azuredevops|gitlab] [--out <dir>]\n  context-sync init     --url <base> --organization <id> --workspace <handle> [--platform ...] [--out <dir>]\n  context-sync context  --out <dir> [--include-chunks] [--require <kind>[=<min>]]...\n  context-sync pull     --root <dir> [--layer <layer>] [--force]\n  context-sync validate --root <dir> [--layer <layer>] [--source-commit <sha>]\n  context-sync publish  --root <dir> [--layer <layer>] [--activate] [--source-commit <sha>]\n  context-sync gap      (--file <gaps.json> | --need <text> [--why <text>] [--considered a,b] | --list) [--strict]\n\ncommon options (env fallback in parentheses):\n  --url <base>        the API base URL              (CONTEXTHUB_URL)\n  --workspace <id>    the workspace to act in       (CONTEXTHUB_WORKSPACE)\n  --organization <id> the organization a tree belongs to (CONTEXTHUB_ORGANIZATION)\n  --config <path>     config file                   (default: <root>/context-sync.json)\n  credential          CONTEXTHUB_TOKEN only - never a flag, never a config key\nurl, workspace, layer and organization may come from context-sync.json; flags and environment\noverride it. A root whose context-sync.json names no layer, holding folders that do, is a\nTREE: validate, publish and pull walk it and each node's own config names its layer and the\nworkspace id its calls go through. --layer is refused against a tree - one flag must not\nretarget every node at once.\n\ninit      scaffold a customer workflow repository: workflows/skills/ontologies folders,\n          context-sync.json, README, an agent orientation file, and the pipeline for\n          --platform. Refuses to overwrite existing files.\n          With --organization it also needs --workspace (a handle inside that organization,\n          since no artifact route is organization-addressed) and a credential, and\n[truncated]",
      "terminalNonHelp": false
    },
    {
      "command": "validate",
      "helpInvocation": "validate --help",
      "result": {
        "status": "failed",
        "timedOut": false,
        "exitCode": 2,
        "durationMs": 30,
        "stdout": null,
        "stderr": "context-sync: unknown option '--help'\ncontext-sync - deliver artifacts to, and materialise knowledge from, a ContextHub workspace\n\nusage:\n  context-sync init     --url <base> --workspace <id> --layer <layer> [--platform github|azuredevops|gitlab] [--out <dir>]\n  context-sync init     --url <base> --organization <id> --workspace <handle> [--platform ...] [--out <dir>]\n  context-sync context  --out <dir> [--include-chunks] [--require <kind>[=<min>]]...\n  context-sync pull     --root <dir> [--layer <layer>] [--force]\n  context-sync validate --root <dir> [--layer <layer>] [--source-commit <sha>]\n  context-sync publish  --root <dir> [--layer <layer>] [--activate] [--source-commit <sha>]\n  context-sync gap      (--file <gaps.json> | --need <text> [--why <text>] [--considered a,b] | --list) [--strict]\n\ncommon options (env fallback in parentheses):\n  --url <base>        the API base URL              (CONTEXTHUB_URL)\n  --workspace <id>    the workspace to act in       (CONTEXTHUB_WORKSPACE)\n  --organization <id> the organization a tree belongs to (CONTEXTHUB_ORGANIZATION)\n  --config <path>     config file                   (default: <root>/context-sync.json)\n  credential          CONTEXTHUB_TOKEN only - never a flag, never a config key\nurl, workspace, layer and organization may come from context-sync.json; flags and environment\noverride it. A root whose context-sync.json names no layer, holding folders that do, is a\nTREE: validate, publish and pull walk it and each node's own config names its layer and the\nworkspace id its calls go through. --layer is refused against a tree - one flag must not\nretarget every node at once.\n\ninit      scaffold a customer workflow repository: workflows/skills/ontologies folders,\n          context-sync.json, README, an agent orientation file, and the pipeline for\n          --platform. Refuses to overwrite existing files.\n          With --organization it also needs --workspace (a handle inside that organization,\n          since no artifact route is organization-addressed) and a credential, and\n[truncated]",
        "outputLimitExceeded": false
      },
      "parsed": true,
      "payload": "context-sync: unknown option '--help'\ncontext-sync - deliver artifacts to, and materialise knowledge from, a ContextHub workspace\n\nusage:\n  context-sync init     --url <base> --workspace <id> --layer <layer> [--platform github|azuredevops|gitlab] [--out <dir>]\n  context-sync init     --url <base> --organization <id> --workspace <handle> [--platform ...] [--out <dir>]\n  context-sync context  --out <dir> [--include-chunks] [--require <kind>[=<min>]]...\n  context-sync pull     --root <dir> [--layer <layer>] [--force]\n  context-sync validate --root <dir> [--layer <layer>] [--source-commit <sha>]\n  context-sync publish  --root <dir> [--layer <layer>] [--activate] [--source-commit <sha>]\n  context-sync gap      (--file <gaps.json> | --need <text> [--why <text>] [--considered a,b] | --list) [--strict]\n\ncommon options (env fallback in parentheses):\n  --url <base>        the API base URL              (CONTEXTHUB_URL)\n  --workspace <id>    the workspace to act in       (CONTEXTHUB_WORKSPACE)\n  --organization <id> the organization a tree belongs to (CONTEXTHUB_ORGANIZATION)\n  --config <path>     config file                   (default: <root>/context-sync.json)\n  credential          CONTEXTHUB_TOKEN only - never a flag, never a config key\nurl, workspace, layer and organization may come from context-sync.json; flags and environment\noverride it. A root whose context-sync.json names no layer, holding folders that do, is a\nTREE: validate, publish and pull walk it and each node's own config names its layer and the\nworkspace id its calls go through. --layer is refused against a tree - one flag must not\nretarget every node at once.\n\ninit      scaffold a customer workflow repository: workflows/skills/ontologies folders,\n          context-sync.json, README, an agent orientation file, and the pipeline for\n          --platform. Refuses to overwrite existing files.\n          With --organization it also needs --workspace (a handle inside that organization,\n          since no artifact route is organization-addressed) and a credential, and\n[truncated]",
      "terminalNonHelp": false
    },
    {
      "command": "publish",
      "helpInvocation": "publish --help",
      "result": {
        "status": "failed",
        "timedOut": false,
        "exitCode": 2,
        "durationMs": 25,
        "stdout": null,
        "stderr": "context-sync: unknown option '--help'\ncontext-sync - deliver artifacts to, and materialise knowledge from, a ContextHub workspace\n\nusage:\n  context-sync init     --url <base> --workspace <id> --layer <layer> [--platform github|azuredevops|gitlab] [--out <dir>]\n  context-sync init     --url <base> --organization <id> --workspace <handle> [--platform ...] [--out <dir>]\n  context-sync context  --out <dir> [--include-chunks] [--require <kind>[=<min>]]...\n  context-sync pull     --root <dir> [--layer <layer>] [--force]\n  context-sync validate --root <dir> [--layer <layer>] [--source-commit <sha>]\n  context-sync publish  --root <dir> [--layer <layer>] [--activate] [--source-commit <sha>]\n  context-sync gap      (--file <gaps.json> | --need <text> [--why <text>] [--considered a,b] | --list) [--strict]\n\ncommon options (env fallback in parentheses):\n  --url <base>        the API base URL              (CONTEXTHUB_URL)\n  --workspace <id>    the workspace to act in       (CONTEXTHUB_WORKSPACE)\n  --organization <id> the organization a tree belongs to (CONTEXTHUB_ORGANIZATION)\n  --config <path>     config file                   (default: <root>/context-sync.json)\n  credential          CONTEXTHUB_TOKEN only - never a flag, never a config key\nurl, workspace, layer and organization may come from context-sync.json; flags and environment\noverride it. A root whose context-sync.json names no layer, holding folders that do, is a\nTREE: validate, publish and pull walk it and each node's own config names its layer and the\nworkspace id its calls go through. --layer is refused against a tree - one flag must not\nretarget every node at once.\n\ninit      scaffold a customer workflow repository: workflows/skills/ontologies folders,\n          context-sync.json, README, an agent orientation file, and the pipeline for\n          --platform. Refuses to overwrite existing files.\n          With --organization it also needs --workspace (a handle inside that organization,\n          since no artifact route is organization-addressed) and a credential, and\n[truncated]",
        "outputLimitExceeded": false
      },
      "parsed": true,
      "payload": "context-sync: unknown option '--help'\ncontext-sync - deliver artifacts to, and materialise knowledge from, a ContextHub workspace\n\nusage:\n  context-sync init     --url <base> --workspace <id> --layer <layer> [--platform github|azuredevops|gitlab] [--out <dir>]\n  context-sync init     --url <base> --organization <id> --workspace <handle> [--platform ...] [--out <dir>]\n  context-sync context  --out <dir> [--include-chunks] [--require <kind>[=<min>]]...\n  context-sync pull     --root <dir> [--layer <layer>] [--force]\n  context-sync validate --root <dir> [--layer <layer>] [--source-commit <sha>]\n  context-sync publish  --root <dir> [--layer <layer>] [--activate] [--source-commit <sha>]\n  context-sync gap      (--file <gaps.json> | --need <text> [--why <text>] [--considered a,b] | --list) [--strict]\n\ncommon options (env fallback in parentheses):\n  --url <base>        the API base URL              (CONTEXTHUB_URL)\n  --workspace <id>    the workspace to act in       (CONTEXTHUB_WORKSPACE)\n  --organization <id> the organization a tree belongs to (CONTEXTHUB_ORGANIZATION)\n  --config <path>     config file                   (default: <root>/context-sync.json)\n  credential          CONTEXTHUB_TOKEN only - never a flag, never a config key\nurl, workspace, layer and organization may come from context-sync.json; flags and environment\noverride it. A root whose context-sync.json names no layer, holding folders that do, is a\nTREE: validate, publish and pull walk it and each node's own config names its layer and the\nworkspace id its calls go through. --layer is refused against a tree - one flag must not\nretarget every node at once.\n\ninit      scaffold a customer workflow repository: workflows/skills/ontologies folders,\n          context-sync.json, README, an agent orientation file, and the pipeline for\n          --platform. Refuses to overwrite existing files.\n          With --organization it also needs --workspace (a handle inside that organization,\n          since no artifact route is organization-addressed) and a credential, and\n[truncated]",
      "terminalNonHelp": false
    },
    {
      "command": "gap",
      "helpInvocation": "gap --help",
      "result": {
        "status": "failed",
        "timedOut": false,
        "exitCode": 2,
        "durationMs": 34,
        "stdout": null,
        "stderr": "context-sync: unknown option '--help'\ncontext-sync - deliver artifacts to, and materialise knowledge from, a ContextHub workspace\n\nusage:\n  context-sync init     --url <base> --workspace <id> --layer <layer> [--platform github|azuredevops|gitlab] [--out <dir>]\n  context-sync init     --url <base> --organization <id> --workspace <handle> [--platform ...] [--out <dir>]\n  context-sync context  --out <dir> [--include-chunks] [--require <kind>[=<min>]]...\n  context-sync pull     --root <dir> [--layer <layer>] [--force]\n  context-sync validate --root <dir> [--layer <layer>] [--source-commit <sha>]\n  context-sync publish  --root <dir> [--layer <layer>] [--activate] [--source-commit <sha>]\n  context-sync gap      (--file <gaps.json> | --need <text> [--why <text>] [--considered a,b] | --list) [--strict]\n\ncommon options (env fallback in parentheses):\n  --url <base>        the API base URL              (CONTEXTHUB_URL)\n  --workspace <id>    the workspace to act in       (CONTEXTHUB_WORKSPACE)\n  --organization <id> the organization a tree belongs to (CONTEXTHUB_ORGANIZATION)\n  --config <path>     config file                   (default: <root>/context-sync.json)\n  credential          CONTEXTHUB_TOKEN only - never a flag, never a config key\nurl, workspace, layer and organization may come from context-sync.json; flags and environment\noverride it. A root whose context-sync.json names no layer, holding folders that do, is a\nTREE: validate, publish and pull walk it and each node's own config names its layer and the\nworkspace id its calls go through. --layer is refused against a tree - one flag must not\nretarget every node at once.\n\ninit      scaffold a customer workflow repository: workflows/skills/ontologies folders,\n          context-sync.json, README, an agent orientation file, and the pipeline for\n          --platform. Refuses to overwrite existing files.\n          With --organization it also needs --workspace (a handle inside that organization,\n          since no artifact route is organization-addressed) and a credential, and\n[truncated]",
        "outputLimitExceeded": false
      },
      "parsed": true,
      "payload": "context-sync: unknown option '--help'\ncontext-sync - deliver artifacts to, and materialise knowledge from, a ContextHub workspace\n\nusage:\n  context-sync init     --url <base> --workspace <id> --layer <layer> [--platform github|azuredevops|gitlab] [--out <dir>]\n  context-sync init     --url <base> --organization <id> --workspace <handle> [--platform ...] [--out <dir>]\n  context-sync context  --out <dir> [--include-chunks] [--require <kind>[=<min>]]...\n  context-sync pull     --root <dir> [--layer <layer>] [--force]\n  context-sync validate --root <dir> [--layer <layer>] [--source-commit <sha>]\n  context-sync publish  --root <dir> [--layer <layer>] [--activate] [--source-commit <sha>]\n  context-sync gap      (--file <gaps.json> | --need <text> [--why <text>] [--considered a,b] | --list) [--strict]\n\ncommon options (env fallback in parentheses):\n  --url <base>        the API base URL              (CONTEXTHUB_URL)\n  --workspace <id>    the workspace to act in       (CONTEXTHUB_WORKSPACE)\n  --organization <id> the organization a tree belongs to (CONTEXTHUB_ORGANIZATION)\n  --config <path>     config file                   (default: <root>/context-sync.json)\n  credential          CONTEXTHUB_TOKEN only - never a flag, never a config key\nurl, workspace, layer and organization may come from context-sync.json; flags and environment\noverride it. A root whose context-sync.json names no layer, holding folders that do, is a\nTREE: validate, publish and pull walk it and each node's own config names its layer and the\nworkspace id its calls go through. --layer is refused against a tree - one flag must not\nretarget every node at once.\n\ninit      scaffold a customer workflow repository: workflows/skills/ontologies folders,\n          context-sync.json, README, an agent orientation file, and the pipeline for\n          --platform. Refuses to overwrite existing files.\n          With --organization it also needs --workspace (a handle inside that organization,\n          since no artifact route is organization-addressed) and a credential, and\n[truncated]",
      "terminalNonHelp": false
    }
  ]
}
