{
  "opencli": "0.1-draft",
  "info": {
    "title": "ky-ai-dotnet — run a .NET backend with output mirrored for agents.",
    "version": "10.10.0",
    "description": ".NET CLI dev-loop runner for AI agents: runs dotnet run/build with build state and logs exposed over MCP."
  },
  "x-inspectra": {
    "artifactSource": "crawled-from-help",
    "generator": "InSpectra.Gen",
    "helpDocumentCount": 1,
    "cliParsedDescription": "SERVE — a backend's dev server (run one per app; registers with the hub for the agent):"
  },
  "commands": [],
  "options": [
    {
      "name": "--name",
      "recursive": false,
      "hidden": false,
      "description": "Project name in the hub (default: the .csproj / folder name)",
      "arguments": [
        {
          "name": "ID",
          "required": false,
          "arity": {
            "minimum": 0,
            "maximum": 1
          }
        }
      ]
    },
    {
      "name": "--log-lines",
      "recursive": false,
      "hidden": false,
      "description": "Lines kept in the rolling log (default: 200; 0 = unlimited)",
      "arguments": [
        {
          "name": "N",
          "required": false,
          "arity": {
            "minimum": 0,
            "maximum": 1
          }
        }
      ]
    },
    {
      "name": "--log-file",
      "recursive": false,
      "hidden": false,
      "description": "Also mirror the rolling log to a file (default: off — MCP serves logs)",
      "arguments": [
        {
          "name": "FILE",
          "required": false,
          "arity": {
            "minimum": 0,
            "maximum": 1
          }
        }
      ]
    },
    {
      "name": "--rest-port",
      "recursive": false,
      "hidden": false,
      "description": "Local REST control port (default: OS-assigned)",
      "arguments": [
        {
          "name": "N",
          "required": false,
          "arity": {
            "minimum": 0,
            "maximum": 1
          }
        }
      ]
    },
    {
      "name": "--no-watch",
      "recursive": false,
      "hidden": false,
      "description": "Use `dotnet run` instead of `dotnet watch run` (stable for debugging)"
    },
    {
      "name": "--hub-port",
      "recursive": false,
      "hidden": false,
      "description": "Hub port to register with (default: 5102; rarely needed — does not start a hub)",
      "arguments": [
        {
          "name": "N",
          "required": false,
          "arity": {
            "minimum": 0,
            "maximum": 1
          }
        }
      ]
    },
    {
      "name": "--no-hub",
      "recursive": false,
      "hidden": false,
      "description": "Standalone: tee + rolling log + local REST only; no hub, no agent access"
    },
    {
      "name": "--after-start",
      "recursive": false,
      "hidden": false,
      "description": "Run <cmd> once the first build settles (the backend is up).\nGreedy — everything after it is the command, so put it last.\nReplaces the PowerShell-unfriendly `serve & sleep 1 && cmd`.\nThe command shares this console and is killed when serve stops.\nAnything else after `serve` is forwarded to dotnet (e.g. --project ./Api.csproj).\n`dotnet watch run` hot-reloads; the agent verifies builds via the hub's wait_for_build.",
      "arguments": [
        {
          "name": "CMD",
          "required": true,
          "arity": {
            "minimum": 1,
            "maximum": 1
          }
        }
      ]
    }
  ]
}
