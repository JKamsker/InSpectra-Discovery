{
  "opencli": "0.1-draft",
  "info": {
    "title": "AWS .NET Core 5.0 Mock Lambda Test Tool (0.14.1)",
    "version": "0.14.1",
    "description": "The .NET Lambda Test Tool can be launched in 2 modes. The default mode is to launch a web interface to select the Lambda code\nto execute with in the Lambda test tool. The second mode skips using the web interface and the Lambda code is identified\nusing the commandline switches as described below. To switch to the no web interface mode use the --no-ui command line switch."
  },
  "x-inspectra": {
    "artifactSource": "crawled-from-help",
    "generator": "InSpectra.Discovery",
    "helpDocumentCount": 1
  },
  "commands": [],
  "options": [
    {
      "name": "--no-ui",
      "recursive": false,
      "hidden": false,
      "description": "Disable launching the web interface and immediately execute the Lambda code."
    },
    {
      "name": "--profile",
      "recursive": false,
      "hidden": false,
      "description": "Set the AWS credentials profile to provide credentials to the Lambda code.\nIf not set the profile from the config file will be used.",
      "arguments": [
        {
          "name": "PROFILE_NAME",
          "required": true,
          "arity": {
            "minimum": 1,
            "maximum": 1
          }
        }
      ]
    },
    {
      "name": "--region",
      "recursive": false,
      "hidden": false,
      "description": "Set the AWS region to as the default region for the Lambda code being executed.\nIf not set the region from the config file will be used.",
      "arguments": [
        {
          "name": "REGION_NAME",
          "required": true,
          "arity": {
            "minimum": 1,
            "maximum": 1
          }
        }
      ]
    },
    {
      "name": "--config-file",
      "recursive": false,
      "hidden": false,
      "description": "The config file to read for Lambda settings. If not set then aws-lambda-tools-defaults.json\nwill be used.",
      "arguments": [
        {
          "name": "FILE_NAME",
          "required": true,
          "arity": {
            "minimum": 1,
            "maximum": 1
          }
        }
      ]
    },
    {
      "name": "--function-handler",
      "recursive": false,
      "hidden": false,
      "description": "The Lambda function handler to identify the code to run. If not set then the function handler\nfrom the config file will be used. This is the format of <assembly::type-name::method-name>.",
      "arguments": [
        {
          "name": "HANDLER_STRING",
          "required": true,
          "arity": {
            "minimum": 1,
            "maximum": 1
          }
        }
      ]
    },
    {
      "name": "--payload",
      "recursive": false,
      "hidden": false,
      "description": "The JSON payload to send to the Lambda function. This can be either an inline string or a\nfile path to a JSON file.",
      "arguments": [
        {
          "name": "FILE_NAME",
          "required": true,
          "arity": {
            "minimum": 1,
            "maximum": 1
          }
        }
      ]
    },
    {
      "name": "--pause-exit",
      "recursive": false,
      "hidden": false,
      "description": "If set to true the test tool will pause waiting for a key input before exiting. The is useful\nwhen executing from an IDE so you can avoid having the output window immediately disappear after\nexecuting the Lambda code. The default value is true.",
      "arguments": [
        {
          "name": "TRUE",
          "required": true,
          "arity": {
            "minimum": 1,
            "maximum": 1
          }
        }
      ]
    },
    {
      "name": "--disable-logs",
      "recursive": false,
      "hidden": false,
      "description": "Logs response only or any exceptions (switch is valid when using --no-ui)."
    }
  ]
}
