{
  "opencli": "0.1-draft",
  "info": {
    "title": "JauntyQ CLI (Extrode.JauntyQ.Cli): schema extraction tool",
    "version": "0.5.0",
    "description": "The JauntyQ command-line tool: reflect a PostgreSQL, SQL Server, MySQL/MariaDB or SQLite database into the jaunty.schema.json snapshot the source generator compiles against. Install with 'dotnet tool install --global Extrode.JauntyQ.Cli' and invoke as 'jauntyq'. Contract testing, migration impact analysis, live EXPLAIN and the schema registry are in Extrode.JauntyQ.Cli.Premium."
  },
  "x-inspectra": {
    "artifactSource": "crawled-from-help",
    "generator": "InSpectra.Gen",
    "helpDocumentCount": 2
  },
  "commands": [
    {
      "name": "schema",
      "hidden": false,
      "options": [
        {
          "name": "--output",
          "recursive": false,
          "hidden": false,
          "description": "Output path, relative to the current directory (default: schema/jaunty.schema.json)\nJAUNTYQ_VERBOSE=1  Print full error detail on failure",
          "arguments": [
            {
              "name": "OUTPUT",
              "required": false,
              "arity": {
                "minimum": 0,
                "maximum": 1
              }
            }
          ]
        },
        {
          "name": "--connection-env",
          "recursive": false,
          "hidden": false,
          "description": "Read the connection string from environment variable VAR",
          "arguments": [
            {
              "name": "VAR",
              "required": true,
              "arity": {
                "minimum": 1,
                "maximum": 1
              }
            }
          ]
        },
        {
          "name": "--connection",
          "recursive": false,
          "hidden": false,
          "description": "Inline (avoid: visible in process list, shell history, CI logs)",
          "arguments": [
            {
              "name": "CONNSTR",
              "required": true,
              "arity": {
                "minimum": 1,
                "maximum": 1
              }
            }
          ]
        }
      ]
    }
  ],
  "options": [
    {
      "name": "--output",
      "recursive": false,
      "hidden": false,
      "description": "Output path, relative to the current directory (default: schema/jaunty.schema.json)\nJAUNTYQ_VERBOSE=1  Print full error detail on failure",
      "arguments": [
        {
          "name": "OUTPUT",
          "required": false,
          "arity": {
            "minimum": 0,
            "maximum": 1
          }
        }
      ]
    },
    {
      "name": "--connection-env",
      "recursive": false,
      "hidden": false,
      "description": "Read the connection string from environment variable VAR",
      "arguments": [
        {
          "name": "VAR",
          "required": true,
          "arity": {
            "minimum": 1,
            "maximum": 1
          }
        }
      ]
    },
    {
      "name": "--connection",
      "recursive": false,
      "hidden": false,
      "description": "Inline (avoid: visible in process list, shell history, CI logs)",
      "arguments": [
        {
          "name": "CONNSTR",
          "required": true,
          "arity": {
            "minimum": 1,
            "maximum": 1
          }
        }
      ]
    }
  ]
}
