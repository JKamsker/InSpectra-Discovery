{
  "opencli": "0.1-draft",
  "info": {
    "title": "vouchfx",
    "version": "1.0.0-alpha.6",
    "description": "End-to-end integration testing for distributed .NET systems, authored in declarative .e2e.yaml. vouchfx compiles YAML to memory-safe C# via Roslyn, orchestrates the required container topology with .NET Aspire + Testcontainers, and verifies one business transaction across HTTP (REST/SOAP), databases (PostgreSQL, MySQL, SQL Server, MongoDB, DynamoDB), message brokers (Kafka, RabbitMQ, NATS, Azure Service Bus, Redis Streams), cache/search (Redis, Elasticsearch), S3 object storage, Prometheus metrics, OTLP distributed traces, SMTP capture, webhooks and inline C# — twenty-five providers across eleven step families, with engine-owned RETRY polling, execution-time secret resolution with at-source redaction, and a four-verdict taxonomy where only Fail breaks CI by default."
  },
  "x-inspectra": {
    "artifactSource": "startup-hook",
    "generator": "InSpectra.Gen",
    "hookCapture": {
      "cliFramework": "System.CommandLine",
      "frameworkVersion": "2.0.0.0",
      "systemCommandLineVersion": "2.0.0.0",
      "patchTarget": "Parse-postfix ()"
    },
    "cliFramework": "System.CommandLine",
    "cliParsedDescription": "vouchfx — compile and run declarative .e2e.yaml integration tests end-to-end."
  },
  "options": [
    {
      "name": "--help",
      "recursive": true,
      "hidden": false,
      "description": "Show help and usage information",
      "aliases": [
        "-h",
        "/h",
        "-?",
        "/?"
      ]
    },
    {
      "name": "--version",
      "recursive": false,
      "hidden": false,
      "description": "Show version information"
    }
  ],
  "commands": [
    {
      "name": "run",
      "description": "Discover *.e2e.yaml scenarios under <path> and run them end-to-end against an orchestrated topology.",
      "options": [
        {
          "name": "--tag",
          "recursive": false,
          "hidden": false,
          "description": "Select scenarios whose metadata.tags contains this tag. Repeatable; a scenario matches if it has ANY of the supplied tags (OR).",
          "arguments": [
            {
              "name": "TAG",
              "required": true,
              "arity": {
                "minimum": 1,
                "maximum": 100000
              },
              "type": "String[]"
            }
          ]
        },
        {
          "name": "--owner",
          "recursive": false,
          "hidden": false,
          "description": "Select scenarios whose metadata.owner is this value. Repeatable; a scenario matches if its owner is ANY of the supplied owners (OR).",
          "arguments": [
            {
              "name": "OWNER",
              "required": true,
              "arity": {
                "minimum": 1,
                "maximum": 100000
              },
              "type": "String[]"
            }
          ]
        },
        {
          "name": "--path",
          "recursive": false,
          "hidden": false,
          "description": "Select scenarios whose (normalised) absolute path matches this glob. Supports *, ** and ?; a pattern with no wildcard is matched as a substring.",
          "arguments": [
            {
              "name": "PATH",
              "required": true,
              "arity": {
                "minimum": 1,
                "maximum": 1
              },
              "type": "String"
            }
          ]
        },
        {
          "name": "--changed-since",
          "recursive": false,
          "hidden": false,
          "description": "Select only scenarios whose file changed since this git ref (committed diff vs the ref plus the dirty working tree). Requires a git repository.",
          "arguments": [
            {
              "name": "CHANGED_SINCE",
              "required": true,
              "arity": {
                "minimum": 1,
                "maximum": 1
              },
              "type": "String"
            }
          ]
        },
        {
          "name": "--parallel",
          "recursive": false,
          "hidden": false,
          "description": "Run up to N scenarios concurrently, each owning its OWN container topology (S08). Must be 1 or greater. CAVEAT: each concurrent scenario stands up its own topology, so --parallel N uses up to N times the containers at once. Omit to run sequentially against a single shared topology (the default).",
          "arguments": [
            {
              "name": "PARALLEL",
              "required": true,
              "arity": {
                "minimum": 1,
                "maximum": 1
              },
              "type": "Int32"
            }
          ]
        },
        {
          "name": "--watch",
          "recursive": false,
          "hidden": false,
          "description": "Run once, then watch the .e2e.yaml file and re-run automatically on save, re-using the already-built topology while the `environment` block is unchanged (rebuilding it only when it changes). For local iteration on a single file; press Ctrl-C to stop. Cannot be combined with --parallel. [default: False]",
          "arguments": [
            {
              "name": "WATCH",
              "required": false,
              "arity": {
                "minimum": 0,
                "maximum": 1
              },
              "type": "Boolean"
            }
          ]
        },
        {
          "name": "--fail-on-env-error",
          "recursive": false,
          "hidden": false,
          "description": "Treat an Environment-error verdict as a CI failure (exit 3). Off by default — only Fail breaks CI. Use this to gate on infrastructure breakage (unhealthy container, image-pull / seed / tunnel failure); the distinct code 3 keeps it separable from a product Fail (1). [default: False]",
          "arguments": [
            {
              "name": "FAIL_ON_ENV_ERROR",
              "required": false,
              "arity": {
                "minimum": 0,
                "maximum": 1
              },
              "type": "Boolean"
            }
          ]
        },
        {
          "name": "--fail-on-inconclusive",
          "recursive": false,
          "hidden": false,
          "description": "Treat an Inconclusive verdict as a CI failure (exit 4). Off by default — only Fail breaks CI. Use this to gate on results the engine could not decide (timeout, partition outlasted grace, upstream capture unmet); the distinct code 4 keeps it separable from a product Fail (1) and an Environment error (3). [default: False]",
          "arguments": [
            {
              "name": "FAIL_ON_INCONCLUSIVE",
              "required": false,
              "arity": {
                "minimum": 0,
                "maximum": 1
              },
              "type": "Boolean"
            }
          ]
        },
        {
          "name": "--html",
          "recursive": false,
          "hidden": false,
          "description": "Write a self-contained HTML report to <path>. Rendered from the same event stream as the terminal output, so the two never disagree. Parent directories are created as needed; an existing file is overwritten. Omit for no HTML report (the default).",
          "arguments": [
            {
              "name": "HTML",
              "required": true,
              "arity": {
                "minimum": 1,
                "maximum": 1
              },
              "type": "String"
            }
          ]
        },
        {
          "name": "--junit",
          "recursive": false,
          "hidden": false,
          "description": "Write a JUnit XML results file to <path> for CI ingestion. Rendered from the same event stream as the terminal output; the four verdicts map to distinct JUnit primitives (Fail→failure, Environment-error→error, Inconclusive→skipped). Parent directories are created as needed; an existing file is overwritten. Omit for no JUnit report (the default).",
          "arguments": [
            {
              "name": "JUNIT",
              "required": true,
              "arity": {
                "minimum": 1,
                "maximum": 1
              },
              "type": "String"
            }
          ]
        },
        {
          "name": "--events",
          "recursive": false,
          "hidden": false,
          "description": "Write the raw JSON Lines event stream to the given path. Re-emits the same event stream the terminal / HTML / JUnit reports are rendered from, verbatim — one JSON object per line, UTF-8 without a BOM. Parent directories are created as needed; an existing file is overwritten. Aliased as --json. Omit for no events file (the default).",
          "aliases": [
            "--json"
          ],
          "arguments": [
            {
              "name": "EVENTS",
              "required": true,
              "arity": {
                "minimum": 1,
                "maximum": 1
              },
              "type": "String"
            }
          ]
        },
        {
          "name": "--no-decorations",
          "recursive": false,
          "hidden": false,
          "description": "Render the terminal report as plain text — no ANSI colour and no per-verdict shape glyph — for a screen-reader / CI-friendly view. Off by default; when off, colour + glyph are added only for an interactive TTY with NO_COLOR unset. The verdict text tokens (PASS / FAIL / ENV_ERROR / INCONCLUSIVE) are always shown, so this loses no information. [default: False]",
          "arguments": [
            {
              "name": "NO_DECORATIONS",
              "required": false,
              "arity": {
                "minimum": 0,
                "maximum": 1
              },
              "type": "Boolean"
            }
          ]
        },
        {
          "name": "--no-telemetry",
          "recursive": false,
          "hidden": false,
          "description": "Opt this run out of anonymous usage telemetry, even if telemetry is enabled. Telemetry is OFF by default and opt-in (vouchfx telemetry enable); set VOUCHFX_NO_TELEMETRY=1 for the same effect in CI / automation. [default: False]",
          "arguments": [
            {
              "name": "NO_TELEMETRY",
              "required": false,
              "arity": {
                "minimum": 0,
                "maximum": 1
              },
              "type": "Boolean"
            }
          ]
        }
      ],
      "arguments": [
        {
          "name": "PATH",
          "description": "Directory to search for *.e2e.yaml scenarios (recursively). Defaults to '.'. [default: .]",
          "required": false,
          "arity": {
            "minimum": 0,
            "maximum": 1
          },
          "type": "String"
        }
      ]
    },
    {
      "name": "telemetry",
      "description": "Manage opt-in, privacy-first usage telemetry. Telemetry is OFF by default; nothing is collected or sent until you run `telemetry enable`.",
      "commands": [
        {
          "name": "enable",
          "description": "Opt IN to anonymous, aggregate usage telemetry. Mints an install id if one does not exist and confirms the change."
        },
        {
          "name": "disable",
          "description": "Opt OUT of usage telemetry. Deletes the install id and clears the local outbox immediately."
        },
        {
          "name": "status",
          "description": "Show the current telemetry consent state, whether an install id exists, the outbox path, and how to opt in / out."
        }
      ]
    }
  ]
}
