{
  "commands": [
    {
      "command": null,
      "helpInvocation": "--help",
      "result": {
        "status": "ok",
        "timedOut": false,
        "exitCode": 0,
        "durationMs": 28,
        "stdout": "mockmcp — declarative mocking for MCP servers, tools, and CLIs.\r\n\r\nUsage:\r\n  mockmcp init [server|proxy|cli|strict|record] [path]\r\n                                           Scaffold an example fixture.\r\n  mockmcp serve <fixture.json> [--mode strict|lenient|passthrough]\r\n                                           Run the replacement MCP server (stdio).\r\n  mockmcp replay <cassette.jsonl>          Replay a recorded cassette as a mock server.\r\n  mockmcp proxy <proxy-config.json> [--record <cassette>]\r\n                                           Run the transparent interposing proxy (stdio).\r\n  mockmcp record <proxy-config.json> --cassette <path>\r\n                                           Proxy the real server and record calls + results.\r\n  mockmcp convert <cassette.jsonl> <out.json> [--cli]\r\n                                           Convert a cassette into a replayable fixture.\r\n  mockmcp make-shims <cli.json> <out-dir> [--record <cassette>]\r\n                                           Generate PATH shims for CLI mocking / recording.\r\n  mockmcp make-shims <cassette> <out-dir> --replay\r\n                                           Generate PATH shims that replay a CLI cassette directly.\r\n  mockmcp cli <record \"a,b\"|replay|clean|status> [--path <dir>]\r\n                                           Record/replay/clean CLI mocking (used by the shell wrapper).\r\n  mockmcp shell-init <pwsh|bash>           Emit the `mockcli` shell function (add to your profile).\r\n  mockmcp assert <log.jsonl> [checks...]   Assert over a JSONL call log.\r\n\r\nMock modes (serve):\r\n  strict        Calls must occur in the configured order and arguments.\r\n  lenient       Any order; expectations may be optional; unknown calls fail.\r\n  passthrough   Unknown calls fall back to the tool default.\r\n  After a strict/lenient run, the harness calls the 'mockmcp_finalize'\r\n  tool to verify no required expectation was missed.\r\n\r\nEnvironment:\r\n  MOCKMCP_LOG        JSONL request log path (serve/proxy).\r\n  MOCKMCP_MODE       Mock mode (strict|lenient|passthrough) for \n[truncated]",
        "stderr": null,
        "outputLimitExceeded": false
      },
      "parsed": true,
      "payload": "mockmcp — declarative mocking for MCP servers, tools, and CLIs.\r\n\r\nUsage:\r\n  mockmcp init [server|proxy|cli|strict|record] [path]\r\n                                           Scaffold an example fixture.\r\n  mockmcp serve <fixture.json> [--mode strict|lenient|passthrough]\r\n                                           Run the replacement MCP server (stdio).\r\n  mockmcp replay <cassette.jsonl>          Replay a recorded cassette as a mock server.\r\n  mockmcp proxy <proxy-config.json> [--record <cassette>]\r\n                                           Run the transparent interposing proxy (stdio).\r\n  mockmcp record <proxy-config.json> --cassette <path>\r\n                                           Proxy the real server and record calls + results.\r\n  mockmcp convert <cassette.jsonl> <out.json> [--cli]\r\n                                           Convert a cassette into a replayable fixture.\r\n  mockmcp make-shims <cli.json> <out-dir> [--record <cassette>]\r\n                                           Generate PATH shims for CLI mocking / recording.\r\n  mockmcp make-shims <cassette> <out-dir> --replay\r\n                                           Generate PATH shims that replay a CLI cassette directly.\r\n  mockmcp cli <record \"a,b\"|replay|clean|status> [--path <dir>]\r\n                                           Record/replay/clean CLI mocking (used by the shell wrapper).\r\n  mockmcp shell-init <pwsh|bash>           Emit the `mockcli` shell function (add to your profile).\r\n  mockmcp assert <log.jsonl> [checks...]   Assert over a JSONL call log.\r\n\r\nMock modes (serve):\r\n  strict        Calls must occur in the configured order and arguments.\r\n  lenient       Any order; expectations may be optional; unknown calls fail.\r\n  passthrough   Unknown calls fall back to the tool default.\r\n  After a strict/lenient run, the harness calls the 'mockmcp_finalize'\r\n  tool to verify no required expectation was missed.\r\n\r\nEnvironment:\r\n  MOCKMCP_LOG        JSONL request log path (serve/proxy).\r\n  MOCKMCP_MODE       Mock mode (strict|lenient|passthrough) for \n[truncated]",
      "terminalNonHelp": false
    },
    {
      "command": "init",
      "helpInvocation": "help init",
      "result": {
        "status": "ok",
        "timedOut": false,
        "exitCode": 0,
        "durationMs": 33,
        "stdout": "mockmcp — declarative mocking for MCP servers, tools, and CLIs.\r\n\r\nUsage:\r\n  mockmcp init [server|proxy|cli|strict|record] [path]\r\n                                           Scaffold an example fixture.\r\n  mockmcp serve <fixture.json> [--mode strict|lenient|passthrough]\r\n                                           Run the replacement MCP server (stdio).\r\n  mockmcp replay <cassette.jsonl>          Replay a recorded cassette as a mock server.\r\n  mockmcp proxy <proxy-config.json> [--record <cassette>]\r\n                                           Run the transparent interposing proxy (stdio).\r\n  mockmcp record <proxy-config.json> --cassette <path>\r\n                                           Proxy the real server and record calls + results.\r\n  mockmcp convert <cassette.jsonl> <out.json> [--cli]\r\n                                           Convert a cassette into a replayable fixture.\r\n  mockmcp make-shims <cli.json> <out-dir> [--record <cassette>]\r\n                                           Generate PATH shims for CLI mocking / recording.\r\n  mockmcp make-shims <cassette> <out-dir> --replay\r\n                                           Generate PATH shims that replay a CLI cassette directly.\r\n  mockmcp cli <record \"a,b\"|replay|clean|status> [--path <dir>]\r\n                                           Record/replay/clean CLI mocking (used by the shell wrapper).\r\n  mockmcp shell-init <pwsh|bash>           Emit the `mockcli` shell function (add to your profile).\r\n  mockmcp assert <log.jsonl> [checks...]   Assert over a JSONL call log.\r\n\r\nMock modes (serve):\r\n  strict        Calls must occur in the configured order and arguments.\r\n  lenient       Any order; expectations may be optional; unknown calls fail.\r\n  passthrough   Unknown calls fall back to the tool default.\r\n  After a strict/lenient run, the harness calls the 'mockmcp_finalize'\r\n  tool to verify no required expectation was missed.\r\n\r\nEnvironment:\r\n  MOCKMCP_LOG        JSONL request log path (serve/proxy).\r\n  MOCKMCP_MODE       Mock mode (strict|lenient|passthrough) for \n[truncated]",
        "stderr": null,
        "outputLimitExceeded": false
      },
      "parsed": true,
      "payload": "mockmcp — declarative mocking for MCP servers, tools, and CLIs.\r\n\r\nUsage:\r\n  mockmcp init [server|proxy|cli|strict|record] [path]\r\n                                           Scaffold an example fixture.\r\n  mockmcp serve <fixture.json> [--mode strict|lenient|passthrough]\r\n                                           Run the replacement MCP server (stdio).\r\n  mockmcp replay <cassette.jsonl>          Replay a recorded cassette as a mock server.\r\n  mockmcp proxy <proxy-config.json> [--record <cassette>]\r\n                                           Run the transparent interposing proxy (stdio).\r\n  mockmcp record <proxy-config.json> --cassette <path>\r\n                                           Proxy the real server and record calls + results.\r\n  mockmcp convert <cassette.jsonl> <out.json> [--cli]\r\n                                           Convert a cassette into a replayable fixture.\r\n  mockmcp make-shims <cli.json> <out-dir> [--record <cassette>]\r\n                                           Generate PATH shims for CLI mocking / recording.\r\n  mockmcp make-shims <cassette> <out-dir> --replay\r\n                                           Generate PATH shims that replay a CLI cassette directly.\r\n  mockmcp cli <record \"a,b\"|replay|clean|status> [--path <dir>]\r\n                                           Record/replay/clean CLI mocking (used by the shell wrapper).\r\n  mockmcp shell-init <pwsh|bash>           Emit the `mockcli` shell function (add to your profile).\r\n  mockmcp assert <log.jsonl> [checks...]   Assert over a JSONL call log.\r\n\r\nMock modes (serve):\r\n  strict        Calls must occur in the configured order and arguments.\r\n  lenient       Any order; expectations may be optional; unknown calls fail.\r\n  passthrough   Unknown calls fall back to the tool default.\r\n  After a strict/lenient run, the harness calls the 'mockmcp_finalize'\r\n  tool to verify no required expectation was missed.\r\n\r\nEnvironment:\r\n  MOCKMCP_LOG        JSONL request log path (serve/proxy).\r\n  MOCKMCP_MODE       Mock mode (strict|lenient|passthrough) for \n[truncated]",
      "terminalNonHelp": false
    },
    {
      "command": "serve",
      "helpInvocation": "serve -h",
      "result": {
        "status": "failed",
        "timedOut": false,
        "exitCode": 1,
        "durationMs": 47,
        "stdout": null,
        "stderr": "mockmcp: cannot read fixture '-h': Could not find file '/tmp/inspectra-help-mockmc-37f7a2d8-13d6b801/-h'.",
        "outputLimitExceeded": false
      },
      "parsed": false,
      "payload": "mockmcp: cannot read fixture '-h': Could not find file '/tmp/inspectra-help-mockmc-37f7a2d8-13d6b801/-h'.",
      "terminalNonHelp": true
    },
    {
      "command": "replay",
      "helpInvocation": "replay -h",
      "result": {
        "status": "failed",
        "timedOut": false,
        "exitCode": 1,
        "durationMs": 32,
        "stdout": null,
        "stderr": "mockmcp: cannot read cassette '-h': Could not find file '/tmp/inspectra-help-mockmc-37f7a2d8-13d6b801/-h'.",
        "outputLimitExceeded": false
      },
      "parsed": false,
      "payload": "mockmcp: cannot read cassette '-h': Could not find file '/tmp/inspectra-help-mockmc-37f7a2d8-13d6b801/-h'.",
      "terminalNonHelp": true
    },
    {
      "command": "proxy",
      "helpInvocation": "proxy -h",
      "result": {
        "status": "failed",
        "timedOut": false,
        "exitCode": 1,
        "durationMs": 43,
        "stdout": null,
        "stderr": "mockmcp: cannot read proxy config '-h': Could not find file '/tmp/inspectra-help-mockmc-37f7a2d8-13d6b801/-h'.",
        "outputLimitExceeded": false
      },
      "parsed": false,
      "payload": "mockmcp: cannot read proxy config '-h': Could not find file '/tmp/inspectra-help-mockmc-37f7a2d8-13d6b801/-h'.",
      "terminalNonHelp": true
    },
    {
      "command": "record",
      "helpInvocation": "record -h",
      "result": {
        "status": "failed",
        "timedOut": false,
        "exitCode": 1,
        "durationMs": 35,
        "stdout": null,
        "stderr": "mockmcp: cannot read proxy config '-h': Could not find file '/tmp/inspectra-help-mockmc-37f7a2d8-13d6b801/-h'.",
        "outputLimitExceeded": false
      },
      "parsed": false,
      "payload": "mockmcp: cannot read proxy config '-h': Could not find file '/tmp/inspectra-help-mockmc-37f7a2d8-13d6b801/-h'.",
      "terminalNonHelp": true
    },
    {
      "command": "convert",
      "helpInvocation": "help convert",
      "result": {
        "status": "ok",
        "timedOut": false,
        "exitCode": 0,
        "durationMs": 31,
        "stdout": "mockmcp — declarative mocking for MCP servers, tools, and CLIs.\r\n\r\nUsage:\r\n  mockmcp init [server|proxy|cli|strict|record] [path]\r\n                                           Scaffold an example fixture.\r\n  mockmcp serve <fixture.json> [--mode strict|lenient|passthrough]\r\n                                           Run the replacement MCP server (stdio).\r\n  mockmcp replay <cassette.jsonl>          Replay a recorded cassette as a mock server.\r\n  mockmcp proxy <proxy-config.json> [--record <cassette>]\r\n                                           Run the transparent interposing proxy (stdio).\r\n  mockmcp record <proxy-config.json> --cassette <path>\r\n                                           Proxy the real server and record calls + results.\r\n  mockmcp convert <cassette.jsonl> <out.json> [--cli]\r\n                                           Convert a cassette into a replayable fixture.\r\n  mockmcp make-shims <cli.json> <out-dir> [--record <cassette>]\r\n                                           Generate PATH shims for CLI mocking / recording.\r\n  mockmcp make-shims <cassette> <out-dir> --replay\r\n                                           Generate PATH shims that replay a CLI cassette directly.\r\n  mockmcp cli <record \"a,b\"|replay|clean|status> [--path <dir>]\r\n                                           Record/replay/clean CLI mocking (used by the shell wrapper).\r\n  mockmcp shell-init <pwsh|bash>           Emit the `mockcli` shell function (add to your profile).\r\n  mockmcp assert <log.jsonl> [checks...]   Assert over a JSONL call log.\r\n\r\nMock modes (serve):\r\n  strict        Calls must occur in the configured order and arguments.\r\n  lenient       Any order; expectations may be optional; unknown calls fail.\r\n  passthrough   Unknown calls fall back to the tool default.\r\n  After a strict/lenient run, the harness calls the 'mockmcp_finalize'\r\n  tool to verify no required expectation was missed.\r\n\r\nEnvironment:\r\n  MOCKMCP_LOG        JSONL request log path (serve/proxy).\r\n  MOCKMCP_MODE       Mock mode (strict|lenient|passthrough) for \n[truncated]",
        "stderr": null,
        "outputLimitExceeded": false
      },
      "parsed": true,
      "payload": "mockmcp — declarative mocking for MCP servers, tools, and CLIs.\r\n\r\nUsage:\r\n  mockmcp init [server|proxy|cli|strict|record] [path]\r\n                                           Scaffold an example fixture.\r\n  mockmcp serve <fixture.json> [--mode strict|lenient|passthrough]\r\n                                           Run the replacement MCP server (stdio).\r\n  mockmcp replay <cassette.jsonl>          Replay a recorded cassette as a mock server.\r\n  mockmcp proxy <proxy-config.json> [--record <cassette>]\r\n                                           Run the transparent interposing proxy (stdio).\r\n  mockmcp record <proxy-config.json> --cassette <path>\r\n                                           Proxy the real server and record calls + results.\r\n  mockmcp convert <cassette.jsonl> <out.json> [--cli]\r\n                                           Convert a cassette into a replayable fixture.\r\n  mockmcp make-shims <cli.json> <out-dir> [--record <cassette>]\r\n                                           Generate PATH shims for CLI mocking / recording.\r\n  mockmcp make-shims <cassette> <out-dir> --replay\r\n                                           Generate PATH shims that replay a CLI cassette directly.\r\n  mockmcp cli <record \"a,b\"|replay|clean|status> [--path <dir>]\r\n                                           Record/replay/clean CLI mocking (used by the shell wrapper).\r\n  mockmcp shell-init <pwsh|bash>           Emit the `mockcli` shell function (add to your profile).\r\n  mockmcp assert <log.jsonl> [checks...]   Assert over a JSONL call log.\r\n\r\nMock modes (serve):\r\n  strict        Calls must occur in the configured order and arguments.\r\n  lenient       Any order; expectations may be optional; unknown calls fail.\r\n  passthrough   Unknown calls fall back to the tool default.\r\n  After a strict/lenient run, the harness calls the 'mockmcp_finalize'\r\n  tool to verify no required expectation was missed.\r\n\r\nEnvironment:\r\n  MOCKMCP_LOG        JSONL request log path (serve/proxy).\r\n  MOCKMCP_MODE       Mock mode (strict|lenient|passthrough) for \n[truncated]",
      "terminalNonHelp": false
    },
    {
      "command": "make-shims",
      "helpInvocation": "help make-shims",
      "result": {
        "status": "ok",
        "timedOut": false,
        "exitCode": 0,
        "durationMs": 26,
        "stdout": "mockmcp — declarative mocking for MCP servers, tools, and CLIs.\r\n\r\nUsage:\r\n  mockmcp init [server|proxy|cli|strict|record] [path]\r\n                                           Scaffold an example fixture.\r\n  mockmcp serve <fixture.json> [--mode strict|lenient|passthrough]\r\n                                           Run the replacement MCP server (stdio).\r\n  mockmcp replay <cassette.jsonl>          Replay a recorded cassette as a mock server.\r\n  mockmcp proxy <proxy-config.json> [--record <cassette>]\r\n                                           Run the transparent interposing proxy (stdio).\r\n  mockmcp record <proxy-config.json> --cassette <path>\r\n                                           Proxy the real server and record calls + results.\r\n  mockmcp convert <cassette.jsonl> <out.json> [--cli]\r\n                                           Convert a cassette into a replayable fixture.\r\n  mockmcp make-shims <cli.json> <out-dir> [--record <cassette>]\r\n                                           Generate PATH shims for CLI mocking / recording.\r\n  mockmcp make-shims <cassette> <out-dir> --replay\r\n                                           Generate PATH shims that replay a CLI cassette directly.\r\n  mockmcp cli <record \"a,b\"|replay|clean|status> [--path <dir>]\r\n                                           Record/replay/clean CLI mocking (used by the shell wrapper).\r\n  mockmcp shell-init <pwsh|bash>           Emit the `mockcli` shell function (add to your profile).\r\n  mockmcp assert <log.jsonl> [checks...]   Assert over a JSONL call log.\r\n\r\nMock modes (serve):\r\n  strict        Calls must occur in the configured order and arguments.\r\n  lenient       Any order; expectations may be optional; unknown calls fail.\r\n  passthrough   Unknown calls fall back to the tool default.\r\n  After a strict/lenient run, the harness calls the 'mockmcp_finalize'\r\n  tool to verify no required expectation was missed.\r\n\r\nEnvironment:\r\n  MOCKMCP_LOG        JSONL request log path (serve/proxy).\r\n  MOCKMCP_MODE       Mock mode (strict|lenient|passthrough) for \n[truncated]",
        "stderr": null,
        "outputLimitExceeded": false
      },
      "parsed": true,
      "payload": "mockmcp — declarative mocking for MCP servers, tools, and CLIs.\r\n\r\nUsage:\r\n  mockmcp init [server|proxy|cli|strict|record] [path]\r\n                                           Scaffold an example fixture.\r\n  mockmcp serve <fixture.json> [--mode strict|lenient|passthrough]\r\n                                           Run the replacement MCP server (stdio).\r\n  mockmcp replay <cassette.jsonl>          Replay a recorded cassette as a mock server.\r\n  mockmcp proxy <proxy-config.json> [--record <cassette>]\r\n                                           Run the transparent interposing proxy (stdio).\r\n  mockmcp record <proxy-config.json> --cassette <path>\r\n                                           Proxy the real server and record calls + results.\r\n  mockmcp convert <cassette.jsonl> <out.json> [--cli]\r\n                                           Convert a cassette into a replayable fixture.\r\n  mockmcp make-shims <cli.json> <out-dir> [--record <cassette>]\r\n                                           Generate PATH shims for CLI mocking / recording.\r\n  mockmcp make-shims <cassette> <out-dir> --replay\r\n                                           Generate PATH shims that replay a CLI cassette directly.\r\n  mockmcp cli <record \"a,b\"|replay|clean|status> [--path <dir>]\r\n                                           Record/replay/clean CLI mocking (used by the shell wrapper).\r\n  mockmcp shell-init <pwsh|bash>           Emit the `mockcli` shell function (add to your profile).\r\n  mockmcp assert <log.jsonl> [checks...]   Assert over a JSONL call log.\r\n\r\nMock modes (serve):\r\n  strict        Calls must occur in the configured order and arguments.\r\n  lenient       Any order; expectations may be optional; unknown calls fail.\r\n  passthrough   Unknown calls fall back to the tool default.\r\n  After a strict/lenient run, the harness calls the 'mockmcp_finalize'\r\n  tool to verify no required expectation was missed.\r\n\r\nEnvironment:\r\n  MOCKMCP_LOG        JSONL request log path (serve/proxy).\r\n  MOCKMCP_MODE       Mock mode (strict|lenient|passthrough) for \n[truncated]",
      "terminalNonHelp": false
    },
    {
      "command": "cli",
      "helpInvocation": "help cli",
      "result": {
        "status": "ok",
        "timedOut": false,
        "exitCode": 0,
        "durationMs": 32,
        "stdout": "mockmcp — declarative mocking for MCP servers, tools, and CLIs.\r\n\r\nUsage:\r\n  mockmcp init [server|proxy|cli|strict|record] [path]\r\n                                           Scaffold an example fixture.\r\n  mockmcp serve <fixture.json> [--mode strict|lenient|passthrough]\r\n                                           Run the replacement MCP server (stdio).\r\n  mockmcp replay <cassette.jsonl>          Replay a recorded cassette as a mock server.\r\n  mockmcp proxy <proxy-config.json> [--record <cassette>]\r\n                                           Run the transparent interposing proxy (stdio).\r\n  mockmcp record <proxy-config.json> --cassette <path>\r\n                                           Proxy the real server and record calls + results.\r\n  mockmcp convert <cassette.jsonl> <out.json> [--cli]\r\n                                           Convert a cassette into a replayable fixture.\r\n  mockmcp make-shims <cli.json> <out-dir> [--record <cassette>]\r\n                                           Generate PATH shims for CLI mocking / recording.\r\n  mockmcp make-shims <cassette> <out-dir> --replay\r\n                                           Generate PATH shims that replay a CLI cassette directly.\r\n  mockmcp cli <record \"a,b\"|replay|clean|status> [--path <dir>]\r\n                                           Record/replay/clean CLI mocking (used by the shell wrapper).\r\n  mockmcp shell-init <pwsh|bash>           Emit the `mockcli` shell function (add to your profile).\r\n  mockmcp assert <log.jsonl> [checks...]   Assert over a JSONL call log.\r\n\r\nMock modes (serve):\r\n  strict        Calls must occur in the configured order and arguments.\r\n  lenient       Any order; expectations may be optional; unknown calls fail.\r\n  passthrough   Unknown calls fall back to the tool default.\r\n  After a strict/lenient run, the harness calls the 'mockmcp_finalize'\r\n  tool to verify no required expectation was missed.\r\n\r\nEnvironment:\r\n  MOCKMCP_LOG        JSONL request log path (serve/proxy).\r\n  MOCKMCP_MODE       Mock mode (strict|lenient|passthrough) for \n[truncated]",
        "stderr": null,
        "outputLimitExceeded": false
      },
      "parsed": true,
      "payload": "mockmcp — declarative mocking for MCP servers, tools, and CLIs.\r\n\r\nUsage:\r\n  mockmcp init [server|proxy|cli|strict|record] [path]\r\n                                           Scaffold an example fixture.\r\n  mockmcp serve <fixture.json> [--mode strict|lenient|passthrough]\r\n                                           Run the replacement MCP server (stdio).\r\n  mockmcp replay <cassette.jsonl>          Replay a recorded cassette as a mock server.\r\n  mockmcp proxy <proxy-config.json> [--record <cassette>]\r\n                                           Run the transparent interposing proxy (stdio).\r\n  mockmcp record <proxy-config.json> --cassette <path>\r\n                                           Proxy the real server and record calls + results.\r\n  mockmcp convert <cassette.jsonl> <out.json> [--cli]\r\n                                           Convert a cassette into a replayable fixture.\r\n  mockmcp make-shims <cli.json> <out-dir> [--record <cassette>]\r\n                                           Generate PATH shims for CLI mocking / recording.\r\n  mockmcp make-shims <cassette> <out-dir> --replay\r\n                                           Generate PATH shims that replay a CLI cassette directly.\r\n  mockmcp cli <record \"a,b\"|replay|clean|status> [--path <dir>]\r\n                                           Record/replay/clean CLI mocking (used by the shell wrapper).\r\n  mockmcp shell-init <pwsh|bash>           Emit the `mockcli` shell function (add to your profile).\r\n  mockmcp assert <log.jsonl> [checks...]   Assert over a JSONL call log.\r\n\r\nMock modes (serve):\r\n  strict        Calls must occur in the configured order and arguments.\r\n  lenient       Any order; expectations may be optional; unknown calls fail.\r\n  passthrough   Unknown calls fall back to the tool default.\r\n  After a strict/lenient run, the harness calls the 'mockmcp_finalize'\r\n  tool to verify no required expectation was missed.\r\n\r\nEnvironment:\r\n  MOCKMCP_LOG        JSONL request log path (serve/proxy).\r\n  MOCKMCP_MODE       Mock mode (strict|lenient|passthrough) for \n[truncated]",
      "terminalNonHelp": false
    },
    {
      "command": "shell-init",
      "helpInvocation": "help shell-init",
      "result": {
        "status": "ok",
        "timedOut": false,
        "exitCode": 0,
        "durationMs": 29,
        "stdout": "mockmcp — declarative mocking for MCP servers, tools, and CLIs.\r\n\r\nUsage:\r\n  mockmcp init [server|proxy|cli|strict|record] [path]\r\n                                           Scaffold an example fixture.\r\n  mockmcp serve <fixture.json> [--mode strict|lenient|passthrough]\r\n                                           Run the replacement MCP server (stdio).\r\n  mockmcp replay <cassette.jsonl>          Replay a recorded cassette as a mock server.\r\n  mockmcp proxy <proxy-config.json> [--record <cassette>]\r\n                                           Run the transparent interposing proxy (stdio).\r\n  mockmcp record <proxy-config.json> --cassette <path>\r\n                                           Proxy the real server and record calls + results.\r\n  mockmcp convert <cassette.jsonl> <out.json> [--cli]\r\n                                           Convert a cassette into a replayable fixture.\r\n  mockmcp make-shims <cli.json> <out-dir> [--record <cassette>]\r\n                                           Generate PATH shims for CLI mocking / recording.\r\n  mockmcp make-shims <cassette> <out-dir> --replay\r\n                                           Generate PATH shims that replay a CLI cassette directly.\r\n  mockmcp cli <record \"a,b\"|replay|clean|status> [--path <dir>]\r\n                                           Record/replay/clean CLI mocking (used by the shell wrapper).\r\n  mockmcp shell-init <pwsh|bash>           Emit the `mockcli` shell function (add to your profile).\r\n  mockmcp assert <log.jsonl> [checks...]   Assert over a JSONL call log.\r\n\r\nMock modes (serve):\r\n  strict        Calls must occur in the configured order and arguments.\r\n  lenient       Any order; expectations may be optional; unknown calls fail.\r\n  passthrough   Unknown calls fall back to the tool default.\r\n  After a strict/lenient run, the harness calls the 'mockmcp_finalize'\r\n  tool to verify no required expectation was missed.\r\n\r\nEnvironment:\r\n  MOCKMCP_LOG        JSONL request log path (serve/proxy).\r\n  MOCKMCP_MODE       Mock mode (strict|lenient|passthrough) for \n[truncated]",
        "stderr": null,
        "outputLimitExceeded": false
      },
      "parsed": true,
      "payload": "mockmcp — declarative mocking for MCP servers, tools, and CLIs.\r\n\r\nUsage:\r\n  mockmcp init [server|proxy|cli|strict|record] [path]\r\n                                           Scaffold an example fixture.\r\n  mockmcp serve <fixture.json> [--mode strict|lenient|passthrough]\r\n                                           Run the replacement MCP server (stdio).\r\n  mockmcp replay <cassette.jsonl>          Replay a recorded cassette as a mock server.\r\n  mockmcp proxy <proxy-config.json> [--record <cassette>]\r\n                                           Run the transparent interposing proxy (stdio).\r\n  mockmcp record <proxy-config.json> --cassette <path>\r\n                                           Proxy the real server and record calls + results.\r\n  mockmcp convert <cassette.jsonl> <out.json> [--cli]\r\n                                           Convert a cassette into a replayable fixture.\r\n  mockmcp make-shims <cli.json> <out-dir> [--record <cassette>]\r\n                                           Generate PATH shims for CLI mocking / recording.\r\n  mockmcp make-shims <cassette> <out-dir> --replay\r\n                                           Generate PATH shims that replay a CLI cassette directly.\r\n  mockmcp cli <record \"a,b\"|replay|clean|status> [--path <dir>]\r\n                                           Record/replay/clean CLI mocking (used by the shell wrapper).\r\n  mockmcp shell-init <pwsh|bash>           Emit the `mockcli` shell function (add to your profile).\r\n  mockmcp assert <log.jsonl> [checks...]   Assert over a JSONL call log.\r\n\r\nMock modes (serve):\r\n  strict        Calls must occur in the configured order and arguments.\r\n  lenient       Any order; expectations may be optional; unknown calls fail.\r\n  passthrough   Unknown calls fall back to the tool default.\r\n  After a strict/lenient run, the harness calls the 'mockmcp_finalize'\r\n  tool to verify no required expectation was missed.\r\n\r\nEnvironment:\r\n  MOCKMCP_LOG        JSONL request log path (serve/proxy).\r\n  MOCKMCP_MODE       Mock mode (strict|lenient|passthrough) for \n[truncated]",
      "terminalNonHelp": false
    },
    {
      "command": "assert",
      "helpInvocation": "help assert",
      "result": {
        "status": "ok",
        "timedOut": false,
        "exitCode": 0,
        "durationMs": 37,
        "stdout": "mockmcp — declarative mocking for MCP servers, tools, and CLIs.\r\n\r\nUsage:\r\n  mockmcp init [server|proxy|cli|strict|record] [path]\r\n                                           Scaffold an example fixture.\r\n  mockmcp serve <fixture.json> [--mode strict|lenient|passthrough]\r\n                                           Run the replacement MCP server (stdio).\r\n  mockmcp replay <cassette.jsonl>          Replay a recorded cassette as a mock server.\r\n  mockmcp proxy <proxy-config.json> [--record <cassette>]\r\n                                           Run the transparent interposing proxy (stdio).\r\n  mockmcp record <proxy-config.json> --cassette <path>\r\n                                           Proxy the real server and record calls + results.\r\n  mockmcp convert <cassette.jsonl> <out.json> [--cli]\r\n                                           Convert a cassette into a replayable fixture.\r\n  mockmcp make-shims <cli.json> <out-dir> [--record <cassette>]\r\n                                           Generate PATH shims for CLI mocking / recording.\r\n  mockmcp make-shims <cassette> <out-dir> --replay\r\n                                           Generate PATH shims that replay a CLI cassette directly.\r\n  mockmcp cli <record \"a,b\"|replay|clean|status> [--path <dir>]\r\n                                           Record/replay/clean CLI mocking (used by the shell wrapper).\r\n  mockmcp shell-init <pwsh|bash>           Emit the `mockcli` shell function (add to your profile).\r\n  mockmcp assert <log.jsonl> [checks...]   Assert over a JSONL call log.\r\n\r\nMock modes (serve):\r\n  strict        Calls must occur in the configured order and arguments.\r\n  lenient       Any order; expectations may be optional; unknown calls fail.\r\n  passthrough   Unknown calls fall back to the tool default.\r\n  After a strict/lenient run, the harness calls the 'mockmcp_finalize'\r\n  tool to verify no required expectation was missed.\r\n\r\nEnvironment:\r\n  MOCKMCP_LOG        JSONL request log path (serve/proxy).\r\n  MOCKMCP_MODE       Mock mode (strict|lenient|passthrough) for \n[truncated]",
        "stderr": null,
        "outputLimitExceeded": false
      },
      "parsed": true,
      "payload": "mockmcp — declarative mocking for MCP servers, tools, and CLIs.\r\n\r\nUsage:\r\n  mockmcp init [server|proxy|cli|strict|record] [path]\r\n                                           Scaffold an example fixture.\r\n  mockmcp serve <fixture.json> [--mode strict|lenient|passthrough]\r\n                                           Run the replacement MCP server (stdio).\r\n  mockmcp replay <cassette.jsonl>          Replay a recorded cassette as a mock server.\r\n  mockmcp proxy <proxy-config.json> [--record <cassette>]\r\n                                           Run the transparent interposing proxy (stdio).\r\n  mockmcp record <proxy-config.json> --cassette <path>\r\n                                           Proxy the real server and record calls + results.\r\n  mockmcp convert <cassette.jsonl> <out.json> [--cli]\r\n                                           Convert a cassette into a replayable fixture.\r\n  mockmcp make-shims <cli.json> <out-dir> [--record <cassette>]\r\n                                           Generate PATH shims for CLI mocking / recording.\r\n  mockmcp make-shims <cassette> <out-dir> --replay\r\n                                           Generate PATH shims that replay a CLI cassette directly.\r\n  mockmcp cli <record \"a,b\"|replay|clean|status> [--path <dir>]\r\n                                           Record/replay/clean CLI mocking (used by the shell wrapper).\r\n  mockmcp shell-init <pwsh|bash>           Emit the `mockcli` shell function (add to your profile).\r\n  mockmcp assert <log.jsonl> [checks...]   Assert over a JSONL call log.\r\n\r\nMock modes (serve):\r\n  strict        Calls must occur in the configured order and arguments.\r\n  lenient       Any order; expectations may be optional; unknown calls fail.\r\n  passthrough   Unknown calls fall back to the tool default.\r\n  After a strict/lenient run, the harness calls the 'mockmcp_finalize'\r\n  tool to verify no required expectation was missed.\r\n\r\nEnvironment:\r\n  MOCKMCP_LOG        JSONL request log path (serve/proxy).\r\n  MOCKMCP_MODE       Mock mode (strict|lenient|passthrough) for \n[truncated]",
      "terminalNonHelp": false
    }
  ]
}
