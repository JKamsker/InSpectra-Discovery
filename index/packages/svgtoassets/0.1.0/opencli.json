{
  "opencli": "0.1-draft",
  "info": {
    "title": "SvgToAssets",
    "version": "0.1.0",
    "description": "Rasterizes SVGs into WinUI app assets and multi-size .ico files."
  },
  "x-inspectra": {
    "artifactSource": "startup-hook",
    "generator": "InSpectra.Gen",
    "hookCapture": {
      "cliFramework": "System.CommandLine",
      "frameworkVersion": "2.0.12.0",
      "systemCommandLineVersion": "2.0.12.0",
      "patchTarget": "Parse-postfix ()"
    },
    "cliFramework": "System.CommandLine",
    "cliParsedDescription": "Converts SVG images into the visual assets of a WinUI 3 app."
  },
  "options": [
    {
      "name": "--help",
      "recursive": true,
      "hidden": false,
      "description": "Show help and usage information",
      "aliases": [
        "-h",
        "/h",
        "-?",
        "/?"
      ]
    },
    {
      "name": "--version",
      "recursive": false,
      "hidden": false,
      "description": "Show version information"
    },
    {
      "name": "--output",
      "recursive": false,
      "hidden": false,
      "description": "Directory to write the assets to; created if missing. [default: <path>]",
      "aliases": [
        "-o"
      ],
      "arguments": [
        {
          "name": "OUTPUT",
          "required": true,
          "arity": {
            "minimum": 1,
            "maximum": 1
          },
          "type": "FullPath"
        }
      ]
    },
    {
      "name": "--type",
      "recursive": false,
      "hidden": false,
      "description": "What to generate: the PNG assets, AppIcon.ico, or both. [default: All]",
      "aliases": [
        "-t"
      ],
      "arguments": [
        {
          "name": "TYPE",
          "required": true,
          "arity": {
            "minimum": 1,
            "maximum": 1
          },
          "type": "AssetsType",
          "allowedValues": [
            "Icon",
            "Assets",
            "All"
          ]
        }
      ]
    },
    {
      "name": "--category",
      "recursive": false,
      "hidden": false,
      "description": "Basic: the WinUI template's 7 files. Required: every scale of the assets the default manifest references. Optional: the unreferenced tiles. All: Required + Optional and a 14-size AppIcon.ico. [default: Required]",
      "aliases": [
        "-c"
      ],
      "arguments": [
        {
          "name": "CATEGORY",
          "required": true,
          "arity": {
            "minimum": 1,
            "maximum": 1
          },
          "type": "AssetCategory",
          "allowedValues": [
            "Basic",
            "Required",
            "Optional",
            "All"
          ]
        }
      ]
    }
  ],
  "arguments": [
    {
      "name": "SVG",
      "description": "SVG files or glob patterns (e.g. icons/**/*.svg). When several SVGs are converted, each gets an output subfolder named after it (e.g. dark/App Logo.svg -> dark-app-logo).",
      "required": true,
      "arity": {
        "minimum": 1,
        "maximum": 100000
      },
      "type": "SvgInput[]"
    }
  ]
}
