{
  "commands": [
    {
      "command": null,
      "helpInvocation": "--help",
      "result": {
        "status": "ok",
        "timedOut": false,
        "exitCode": 0,
        "durationMs": 259,
        "stdout": "Usage: [options...] [-h|--help] [--version]\n\nTranspile C# types to TypeScript.\n\nOptions:\n  -p, --project <string>      Path to the C# project file (.csproj) [Required]\n  -o, --output <string>       Output directory for generated TypeScript files [Required]\n  -t, --time                  Show compilation and transpilation timings\n  -c, --clean                 Clean output directory before generating\n  --package-root <string?>    Root directory of the consumer package (default: parent of --output) [Default: null]\n  --dist <string>             Path (relative to packageRoot) where the JS build output lives (default: ./dist) [Default: @\"./dist\"]\n  --src-root <string?>        TypeScript source root relative to packageRoot (default: inferred from first segment of output path). Used to compute the dist prefix when output targets a subdirectory (e.g., src/domain/). [Default: null]\n  --skip-package-json         Skip generating/updating package.json\n  --namespace-barrels         Emit an additional src/index.ts root barrel mirroring the C# namespace hierarchy via `export namespace` blocks (opt-in; see ADR-0006).\n  --strip-interface-prefix    Drop the C# `I` prefix from generated interface names when it is followed by another uppercase letter (opt-in). Collisions with sibling types in the same namespace keep the prefix and emit MS0017.\n  --file-prefix <string?>     Opaque text written verbatim at the top of every generated file, followed by a single newline. Use to inject formatter / lint directives (e.g., a Biome `biome-ignore-all` block, a Prettier `// @prettier-ignore`, a `// @generated` provenance marker). [Default: null]\n  --dry-run                   Run the full pipeline but do NOT write any files. Print a preflight summary (file count + total line count + per-file paths) instead. Useful for CI preflight checks and exploratory runs. Skips the package.json update too.",
        "stderr": null,
        "outputLimitExceeded": false
      },
      "parsed": true,
      "payload": "Usage: [options...] [-h|--help] [--version]\n\nTranspile C# types to TypeScript.\n\nOptions:\n  -p, --project <string>      Path to the C# project file (.csproj) [Required]\n  -o, --output <string>       Output directory for generated TypeScript files [Required]\n  -t, --time                  Show compilation and transpilation timings\n  -c, --clean                 Clean output directory before generating\n  --package-root <string?>    Root directory of the consumer package (default: parent of --output) [Default: null]\n  --dist <string>             Path (relative to packageRoot) where the JS build output lives (default: ./dist) [Default: @\"./dist\"]\n  --src-root <string?>        TypeScript source root relative to packageRoot (default: inferred from first segment of output path). Used to compute the dist prefix when output targets a subdirectory (e.g., src/domain/). [Default: null]\n  --skip-package-json         Skip generating/updating package.json\n  --namespace-barrels         Emit an additional src/index.ts root barrel mirroring the C# namespace hierarchy via `export namespace` blocks (opt-in; see ADR-0006).\n  --strip-interface-prefix    Drop the C# `I` prefix from generated interface names when it is followed by another uppercase letter (opt-in). Collisions with sibling types in the same namespace keep the prefix and emit MS0017.\n  --file-prefix <string?>     Opaque text written verbatim at the top of every generated file, followed by a single newline. Use to inject formatter / lint directives (e.g., a Biome `biome-ignore-all` block, a Prettier `// @prettier-ignore`, a `// @generated` provenance marker). [Default: null]\n  --dry-run                   Run the full pipeline but do NOT write any files. Print a preflight summary (file count + total line count + per-file paths) instead. Useful for CI preflight checks and exploratory runs. Skips the package.json update too.",
      "terminalNonHelp": false
    }
  ]
}
