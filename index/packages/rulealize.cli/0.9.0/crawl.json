{
  "commands": [
    {
      "command": null,
      "helpInvocation": "--help",
      "result": {
        "status": "failed",
        "timedOut": false,
        "exitCode": 2,
        "durationMs": 33,
        "stdout": null,
        "stderr": "usage:\n  rulealize restore <rule set>\n  rulealize plugins\n  rulealize check   <rule set>\n  rulealize moves   <rule set>\n  rulealize apply   <rule set> <input> | --input <file>\n  rulealize play    <rule set>\n\n  --plugins <folder>   where the vocabularies are        (default 'plugin')\n  --rulesets <folder>  where fetched components go and are read (default 'component')\n  --state <file>       the position to start from        (default the rule set's)\n  --limit <n>          candidates GetValidInputs may try (default 10000)\n  --outcomes <n>       outcomes GetOutcomes may return   (default 64)\n  --outcome <file>     what was drawn, for an input that resolves something nobody chose\n  --write              amend --state in place instead of writing to standard output\n  --json               moves, as the runtime writes them\n\nrestore fetches what `requires` and `uses` name, into --plugins and --rulesets.",
        "outputLimitExceeded": false
      },
      "parsed": true,
      "payload": "usage:\n  rulealize restore <rule set>\n  rulealize plugins\n  rulealize check   <rule set>\n  rulealize moves   <rule set>\n  rulealize apply   <rule set> <input> | --input <file>\n  rulealize play    <rule set>\n\n  --plugins <folder>   where the vocabularies are        (default 'plugin')\n  --rulesets <folder>  where fetched components go and are read (default 'component')\n  --state <file>       the position to start from        (default the rule set's)\n  --limit <n>          candidates GetValidInputs may try (default 10000)\n  --outcomes <n>       outcomes GetOutcomes may return   (default 64)\n  --outcome <file>     what was drawn, for an input that resolves something nobody chose\n  --write              amend --state in place instead of writing to standard output\n  --json               moves, as the runtime writes them\n\nrestore fetches what `requires` and `uses` name, into --plugins and --rulesets.",
      "terminalNonHelp": false
    },
    {
      "command": "restore",
      "helpInvocation": "help restore",
      "result": {
        "status": "failed",
        "timedOut": false,
        "exitCode": 2,
        "durationMs": 33,
        "stdout": null,
        "stderr": "usage:\n  rulealize restore <rule set>\n  rulealize plugins\n  rulealize check   <rule set>\n  rulealize moves   <rule set>\n  rulealize apply   <rule set> <input> | --input <file>\n  rulealize play    <rule set>\n\n  --plugins <folder>   where the vocabularies are        (default 'plugin')\n  --rulesets <folder>  where fetched components go and are read (default 'component')\n  --state <file>       the position to start from        (default the rule set's)\n  --limit <n>          candidates GetValidInputs may try (default 10000)\n  --outcomes <n>       outcomes GetOutcomes may return   (default 64)\n  --outcome <file>     what was drawn, for an input that resolves something nobody chose\n  --write              amend --state in place instead of writing to standard output\n  --json               moves, as the runtime writes them\n\nrestore fetches what `requires` and `uses` name, into --plugins and --rulesets.",
        "outputLimitExceeded": false
      },
      "parsed": true,
      "payload": "usage:\n  rulealize restore <rule set>\n  rulealize plugins\n  rulealize check   <rule set>\n  rulealize moves   <rule set>\n  rulealize apply   <rule set> <input> | --input <file>\n  rulealize play    <rule set>\n\n  --plugins <folder>   where the vocabularies are        (default 'plugin')\n  --rulesets <folder>  where fetched components go and are read (default 'component')\n  --state <file>       the position to start from        (default the rule set's)\n  --limit <n>          candidates GetValidInputs may try (default 10000)\n  --outcomes <n>       outcomes GetOutcomes may return   (default 64)\n  --outcome <file>     what was drawn, for an input that resolves something nobody chose\n  --write              amend --state in place instead of writing to standard output\n  --json               moves, as the runtime writes them\n\nrestore fetches what `requires` and `uses` name, into --plugins and --rulesets.",
      "terminalNonHelp": false
    },
    {
      "command": "check",
      "helpInvocation": "help check",
      "result": {
        "status": "failed",
        "timedOut": false,
        "exitCode": 2,
        "durationMs": 33,
        "stdout": null,
        "stderr": "usage:\n  rulealize restore <rule set>\n  rulealize plugins\n  rulealize check   <rule set>\n  rulealize moves   <rule set>\n  rulealize apply   <rule set> <input> | --input <file>\n  rulealize play    <rule set>\n\n  --plugins <folder>   where the vocabularies are        (default 'plugin')\n  --rulesets <folder>  where fetched components go and are read (default 'component')\n  --state <file>       the position to start from        (default the rule set's)\n  --limit <n>          candidates GetValidInputs may try (default 10000)\n  --outcomes <n>       outcomes GetOutcomes may return   (default 64)\n  --outcome <file>     what was drawn, for an input that resolves something nobody chose\n  --write              amend --state in place instead of writing to standard output\n  --json               moves, as the runtime writes them\n\nrestore fetches what `requires` and `uses` name, into --plugins and --rulesets.",
        "outputLimitExceeded": false
      },
      "parsed": true,
      "payload": "usage:\n  rulealize restore <rule set>\n  rulealize plugins\n  rulealize check   <rule set>\n  rulealize moves   <rule set>\n  rulealize apply   <rule set> <input> | --input <file>\n  rulealize play    <rule set>\n\n  --plugins <folder>   where the vocabularies are        (default 'plugin')\n  --rulesets <folder>  where fetched components go and are read (default 'component')\n  --state <file>       the position to start from        (default the rule set's)\n  --limit <n>          candidates GetValidInputs may try (default 10000)\n  --outcomes <n>       outcomes GetOutcomes may return   (default 64)\n  --outcome <file>     what was drawn, for an input that resolves something nobody chose\n  --write              amend --state in place instead of writing to standard output\n  --json               moves, as the runtime writes them\n\nrestore fetches what `requires` and `uses` name, into --plugins and --rulesets.",
      "terminalNonHelp": false
    },
    {
      "command": "moves",
      "helpInvocation": "help moves",
      "result": {
        "status": "failed",
        "timedOut": false,
        "exitCode": 2,
        "durationMs": 33,
        "stdout": null,
        "stderr": "usage:\n  rulealize restore <rule set>\n  rulealize plugins\n  rulealize check   <rule set>\n  rulealize moves   <rule set>\n  rulealize apply   <rule set> <input> | --input <file>\n  rulealize play    <rule set>\n\n  --plugins <folder>   where the vocabularies are        (default 'plugin')\n  --rulesets <folder>  where fetched components go and are read (default 'component')\n  --state <file>       the position to start from        (default the rule set's)\n  --limit <n>          candidates GetValidInputs may try (default 10000)\n  --outcomes <n>       outcomes GetOutcomes may return   (default 64)\n  --outcome <file>     what was drawn, for an input that resolves something nobody chose\n  --write              amend --state in place instead of writing to standard output\n  --json               moves, as the runtime writes them\n\nrestore fetches what `requires` and `uses` name, into --plugins and --rulesets.",
        "outputLimitExceeded": false
      },
      "parsed": true,
      "payload": "usage:\n  rulealize restore <rule set>\n  rulealize plugins\n  rulealize check   <rule set>\n  rulealize moves   <rule set>\n  rulealize apply   <rule set> <input> | --input <file>\n  rulealize play    <rule set>\n\n  --plugins <folder>   where the vocabularies are        (default 'plugin')\n  --rulesets <folder>  where fetched components go and are read (default 'component')\n  --state <file>       the position to start from        (default the rule set's)\n  --limit <n>          candidates GetValidInputs may try (default 10000)\n  --outcomes <n>       outcomes GetOutcomes may return   (default 64)\n  --outcome <file>     what was drawn, for an input that resolves something nobody chose\n  --write              amend --state in place instead of writing to standard output\n  --json               moves, as the runtime writes them\n\nrestore fetches what `requires` and `uses` name, into --plugins and --rulesets.",
      "terminalNonHelp": false
    },
    {
      "command": "apply",
      "helpInvocation": "help apply",
      "result": {
        "status": "failed",
        "timedOut": false,
        "exitCode": 2,
        "durationMs": 38,
        "stdout": null,
        "stderr": "usage:\n  rulealize restore <rule set>\n  rulealize plugins\n  rulealize check   <rule set>\n  rulealize moves   <rule set>\n  rulealize apply   <rule set> <input> | --input <file>\n  rulealize play    <rule set>\n\n  --plugins <folder>   where the vocabularies are        (default 'plugin')\n  --rulesets <folder>  where fetched components go and are read (default 'component')\n  --state <file>       the position to start from        (default the rule set's)\n  --limit <n>          candidates GetValidInputs may try (default 10000)\n  --outcomes <n>       outcomes GetOutcomes may return   (default 64)\n  --outcome <file>     what was drawn, for an input that resolves something nobody chose\n  --write              amend --state in place instead of writing to standard output\n  --json               moves, as the runtime writes them\n\nrestore fetches what `requires` and `uses` name, into --plugins and --rulesets.",
        "outputLimitExceeded": false
      },
      "parsed": true,
      "payload": "usage:\n  rulealize restore <rule set>\n  rulealize plugins\n  rulealize check   <rule set>\n  rulealize moves   <rule set>\n  rulealize apply   <rule set> <input> | --input <file>\n  rulealize play    <rule set>\n\n  --plugins <folder>   where the vocabularies are        (default 'plugin')\n  --rulesets <folder>  where fetched components go and are read (default 'component')\n  --state <file>       the position to start from        (default the rule set's)\n  --limit <n>          candidates GetValidInputs may try (default 10000)\n  --outcomes <n>       outcomes GetOutcomes may return   (default 64)\n  --outcome <file>     what was drawn, for an input that resolves something nobody chose\n  --write              amend --state in place instead of writing to standard output\n  --json               moves, as the runtime writes them\n\nrestore fetches what `requires` and `uses` name, into --plugins and --rulesets.",
      "terminalNonHelp": false
    },
    {
      "command": "play",
      "helpInvocation": "help play",
      "result": {
        "status": "failed",
        "timedOut": false,
        "exitCode": 2,
        "durationMs": 41,
        "stdout": null,
        "stderr": "usage:\n  rulealize restore <rule set>\n  rulealize plugins\n  rulealize check   <rule set>\n  rulealize moves   <rule set>\n  rulealize apply   <rule set> <input> | --input <file>\n  rulealize play    <rule set>\n\n  --plugins <folder>   where the vocabularies are        (default 'plugin')\n  --rulesets <folder>  where fetched components go and are read (default 'component')\n  --state <file>       the position to start from        (default the rule set's)\n  --limit <n>          candidates GetValidInputs may try (default 10000)\n  --outcomes <n>       outcomes GetOutcomes may return   (default 64)\n  --outcome <file>     what was drawn, for an input that resolves something nobody chose\n  --write              amend --state in place instead of writing to standard output\n  --json               moves, as the runtime writes them\n\nrestore fetches what `requires` and `uses` name, into --plugins and --rulesets.",
        "outputLimitExceeded": false
      },
      "parsed": true,
      "payload": "usage:\n  rulealize restore <rule set>\n  rulealize plugins\n  rulealize check   <rule set>\n  rulealize moves   <rule set>\n  rulealize apply   <rule set> <input> | --input <file>\n  rulealize play    <rule set>\n\n  --plugins <folder>   where the vocabularies are        (default 'plugin')\n  --rulesets <folder>  where fetched components go and are read (default 'component')\n  --state <file>       the position to start from        (default the rule set's)\n  --limit <n>          candidates GetValidInputs may try (default 10000)\n  --outcomes <n>       outcomes GetOutcomes may return   (default 64)\n  --outcome <file>     what was drawn, for an input that resolves something nobody chose\n  --write              amend --state in place instead of writing to standard output\n  --json               moves, as the runtime writes them\n\nrestore fetches what `requires` and `uses` name, into --plugins and --rulesets.",
      "terminalNonHelp": false
    }
  ]
}
