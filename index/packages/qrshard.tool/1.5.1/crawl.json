{
  "commands": [
    {
      "command": null,
      "helpInvocation": "--help",
      "result": {
        "status": "ok",
        "timedOut": false,
        "exitCode": 0,
        "durationMs": 128,
        "stdout": "QrShard — encode any file into dense QR-style images and back.\n\nusage:\n  qrshard encode <file|folder> [options]   Split a file (or a folder, tar-ed\n                             automatically and extracted on decode) into shard images.\n    -o, --out <dir>          Output folder (default: <file>.shards next to the input)\n    -r, --resolution <px>    Image size: \"auto\" (the default) uses the primary\n                             monitor's native resolution so shards fill the screen\n                             they'll be captured from; or one number (square) or\n                             WxH, 700-16384, to override (e.g. a smaller size shows\n                             the code surrounded by padding)\n    -c, --cell <px>          Data cell size in pixels, 1-64 (default: 3)\n    -b, --bits <n>           Bits per cell / color density, 1-8 (default: 4)\n    -e, --ecc <n>            Reed-Solomon parity per 255-byte block, even, 0-64\n                             (default: 16 ≈ 6% overhead, fixes 8 bad bytes per block)\n    -R, --recovery <pct>     Add parity IMAGES so whole missing/damaged images can be\n                             rebuilt without recapture; pct% extra images, 0-100\n                             (default: 0; e.g. 15 tolerates losing ~15% of the images)\n    -F, --fountain <pct>     Fountain coding for video mode: pct% extra CODED frames\n                             (random linear combinations); ANY enough captured frames\n                             per stripe reconstruct the data — ideal with --video,\n                             where torn/glared frames simply don't count\n    -p, --password <pw>      AES-256-GCM encrypt the payload; decode needs the same\n                             password (wrong password fails cleanly, nothing leaks)\n    -f, --format <fmt>       Lossless image format: png, bmp, tga, qoi, webp, tiff\n                             (default: png, written by the built-in fast PNG writer)\n    --camera                 Camera profile: adds finder patterns so images decode\n                       \n[truncated]",
        "stderr": null,
        "outputLimitExceeded": false
      },
      "parsed": true,
      "payload": "QrShard — encode any file into dense QR-style images and back.\n\nusage:\n  qrshard encode <file|folder> [options]   Split a file (or a folder, tar-ed\n                             automatically and extracted on decode) into shard images.\n    -o, --out <dir>          Output folder (default: <file>.shards next to the input)\n    -r, --resolution <px>    Image size: \"auto\" (the default) uses the primary\n                             monitor's native resolution so shards fill the screen\n                             they'll be captured from; or one number (square) or\n                             WxH, 700-16384, to override (e.g. a smaller size shows\n                             the code surrounded by padding)\n    -c, --cell <px>          Data cell size in pixels, 1-64 (default: 3)\n    -b, --bits <n>           Bits per cell / color density, 1-8 (default: 4)\n    -e, --ecc <n>            Reed-Solomon parity per 255-byte block, even, 0-64\n                             (default: 16 ≈ 6% overhead, fixes 8 bad bytes per block)\n    -R, --recovery <pct>     Add parity IMAGES so whole missing/damaged images can be\n                             rebuilt without recapture; pct% extra images, 0-100\n                             (default: 0; e.g. 15 tolerates losing ~15% of the images)\n    -F, --fountain <pct>     Fountain coding for video mode: pct% extra CODED frames\n                             (random linear combinations); ANY enough captured frames\n                             per stripe reconstruct the data — ideal with --video,\n                             where torn/glared frames simply don't count\n    -p, --password <pw>      AES-256-GCM encrypt the payload; decode needs the same\n                             password (wrong password fails cleanly, nothing leaks)\n    -f, --format <fmt>       Lossless image format: png, bmp, tga, qoi, webp, tiff\n                             (default: png, written by the built-in fast PNG writer)\n    --camera                 Camera profile: adds finder patterns so images decode\n                       \n[truncated]",
      "terminalNonHelp": false
    },
    {
      "command": "encode",
      "helpInvocation": "encode --help",
      "result": {
        "status": "failed",
        "timedOut": false,
        "exitCode": 2,
        "durationMs": 117,
        "stdout": "QrShard — encode any file into dense QR-style images and back.\n\nusage:\n  qrshard encode <file|folder> [options]   Split a file (or a folder, tar-ed\n                             automatically and extracted on decode) into shard images.\n    -o, --out <dir>          Output folder (default: <file>.shards next to the input)\n    -r, --resolution <px>    Image size: \"auto\" (the default) uses the primary\n                             monitor's native resolution so shards fill the screen\n                             they'll be captured from; or one number (square) or\n                             WxH, 700-16384, to override (e.g. a smaller size shows\n                             the code surrounded by padding)\n    -c, --cell <px>          Data cell size in pixels, 1-64 (default: 3)\n    -b, --bits <n>           Bits per cell / color density, 1-8 (default: 4)\n    -e, --ecc <n>            Reed-Solomon parity per 255-byte block, even, 0-64\n                             (default: 16 ≈ 6% overhead, fixes 8 bad bytes per block)\n    -R, --recovery <pct>     Add parity IMAGES so whole missing/damaged images can be\n                             rebuilt without recapture; pct% extra images, 0-100\n                             (default: 0; e.g. 15 tolerates losing ~15% of the images)\n    -F, --fountain <pct>     Fountain coding for video mode: pct% extra CODED frames\n                             (random linear combinations); ANY enough captured frames\n                             per stripe reconstruct the data — ideal with --video,\n                             where torn/glared frames simply don't count\n    -p, --password <pw>      AES-256-GCM encrypt the payload; decode needs the same\n                             password (wrong password fails cleanly, nothing leaks)\n    -f, --format <fmt>       Lossless image format: png, bmp, tga, qoi, webp, tiff\n                             (default: png, written by the built-in fast PNG writer)\n    --camera                 Camera profile: adds finder patterns so images decode\n                       \n[truncated]",
        "stderr": "error: unknown option '--help'. Did you mean '--cell'?",
        "outputLimitExceeded": false
      },
      "parsed": true,
      "payload": "QrShard — encode any file into dense QR-style images and back.\n\nusage:\n  qrshard encode <file|folder> [options]   Split a file (or a folder, tar-ed\n                             automatically and extracted on decode) into shard images.\n    -o, --out <dir>          Output folder (default: <file>.shards next to the input)\n    -r, --resolution <px>    Image size: \"auto\" (the default) uses the primary\n                             monitor's native resolution so shards fill the screen\n                             they'll be captured from; or one number (square) or\n                             WxH, 700-16384, to override (e.g. a smaller size shows\n                             the code surrounded by padding)\n    -c, --cell <px>          Data cell size in pixels, 1-64 (default: 3)\n    -b, --bits <n>           Bits per cell / color density, 1-8 (default: 4)\n    -e, --ecc <n>            Reed-Solomon parity per 255-byte block, even, 0-64\n                             (default: 16 ≈ 6% overhead, fixes 8 bad bytes per block)\n    -R, --recovery <pct>     Add parity IMAGES so whole missing/damaged images can be\n                             rebuilt without recapture; pct% extra images, 0-100\n                             (default: 0; e.g. 15 tolerates losing ~15% of the images)\n    -F, --fountain <pct>     Fountain coding for video mode: pct% extra CODED frames\n                             (random linear combinations); ANY enough captured frames\n                             per stripe reconstruct the data — ideal with --video,\n                             where torn/glared frames simply don't count\n    -p, --password <pw>      AES-256-GCM encrypt the payload; decode needs the same\n                             password (wrong password fails cleanly, nothing leaks)\n    -f, --format <fmt>       Lossless image format: png, bmp, tga, qoi, webp, tiff\n                             (default: png, written by the built-in fast PNG writer)\n    --camera                 Camera profile: adds finder patterns so images decode\n                       \n[truncated]",
      "terminalNonHelp": false
    },
    {
      "command": "send",
      "helpInvocation": "send --help",
      "result": {
        "status": "failed",
        "timedOut": false,
        "exitCode": 2,
        "durationMs": 120,
        "stdout": "QrShard — encode any file into dense QR-style images and back.\n\nusage:\n  qrshard encode <file|folder> [options]   Split a file (or a folder, tar-ed\n                             automatically and extracted on decode) into shard images.\n    -o, --out <dir>          Output folder (default: <file>.shards next to the input)\n    -r, --resolution <px>    Image size: \"auto\" (the default) uses the primary\n                             monitor's native resolution so shards fill the screen\n                             they'll be captured from; or one number (square) or\n                             WxH, 700-16384, to override (e.g. a smaller size shows\n                             the code surrounded by padding)\n    -c, --cell <px>          Data cell size in pixels, 1-64 (default: 3)\n    -b, --bits <n>           Bits per cell / color density, 1-8 (default: 4)\n    -e, --ecc <n>            Reed-Solomon parity per 255-byte block, even, 0-64\n                             (default: 16 ≈ 6% overhead, fixes 8 bad bytes per block)\n    -R, --recovery <pct>     Add parity IMAGES so whole missing/damaged images can be\n                             rebuilt without recapture; pct% extra images, 0-100\n                             (default: 0; e.g. 15 tolerates losing ~15% of the images)\n    -F, --fountain <pct>     Fountain coding for video mode: pct% extra CODED frames\n                             (random linear combinations); ANY enough captured frames\n                             per stripe reconstruct the data — ideal with --video,\n                             where torn/glared frames simply don't count\n    -p, --password <pw>      AES-256-GCM encrypt the payload; decode needs the same\n                             password (wrong password fails cleanly, nothing leaks)\n    -f, --format <fmt>       Lossless image format: png, bmp, tga, qoi, webp, tiff\n                             (default: png, written by the built-in fast PNG writer)\n    --camera                 Camera profile: adds finder patterns so images decode\n                       \n[truncated]",
        "stderr": "error: unknown option '--help'. Did you mean '--cell'?",
        "outputLimitExceeded": false
      },
      "parsed": true,
      "payload": "QrShard — encode any file into dense QR-style images and back.\n\nusage:\n  qrshard encode <file|folder> [options]   Split a file (or a folder, tar-ed\n                             automatically and extracted on decode) into shard images.\n    -o, --out <dir>          Output folder (default: <file>.shards next to the input)\n    -r, --resolution <px>    Image size: \"auto\" (the default) uses the primary\n                             monitor's native resolution so shards fill the screen\n                             they'll be captured from; or one number (square) or\n                             WxH, 700-16384, to override (e.g. a smaller size shows\n                             the code surrounded by padding)\n    -c, --cell <px>          Data cell size in pixels, 1-64 (default: 3)\n    -b, --bits <n>           Bits per cell / color density, 1-8 (default: 4)\n    -e, --ecc <n>            Reed-Solomon parity per 255-byte block, even, 0-64\n                             (default: 16 ≈ 6% overhead, fixes 8 bad bytes per block)\n    -R, --recovery <pct>     Add parity IMAGES so whole missing/damaged images can be\n                             rebuilt without recapture; pct% extra images, 0-100\n                             (default: 0; e.g. 15 tolerates losing ~15% of the images)\n    -F, --fountain <pct>     Fountain coding for video mode: pct% extra CODED frames\n                             (random linear combinations); ANY enough captured frames\n                             per stripe reconstruct the data — ideal with --video,\n                             where torn/glared frames simply don't count\n    -p, --password <pw>      AES-256-GCM encrypt the payload; decode needs the same\n                             password (wrong password fails cleanly, nothing leaks)\n    -f, --format <fmt>       Lossless image format: png, bmp, tga, qoi, webp, tiff\n                             (default: png, written by the built-in fast PNG writer)\n    --camera                 Camera profile: adds finder patterns so images decode\n                       \n[truncated]",
      "terminalNonHelp": false
    },
    {
      "command": "decode",
      "helpInvocation": "decode --help",
      "result": {
        "status": "failed",
        "timedOut": false,
        "exitCode": 2,
        "durationMs": 125,
        "stdout": "QrShard — encode any file into dense QR-style images and back.\n\nusage:\n  qrshard encode <file|folder> [options]   Split a file (or a folder, tar-ed\n                             automatically and extracted on decode) into shard images.\n    -o, --out <dir>          Output folder (default: <file>.shards next to the input)\n    -r, --resolution <px>    Image size: \"auto\" (the default) uses the primary\n                             monitor's native resolution so shards fill the screen\n                             they'll be captured from; or one number (square) or\n                             WxH, 700-16384, to override (e.g. a smaller size shows\n                             the code surrounded by padding)\n    -c, --cell <px>          Data cell size in pixels, 1-64 (default: 3)\n    -b, --bits <n>           Bits per cell / color density, 1-8 (default: 4)\n    -e, --ecc <n>            Reed-Solomon parity per 255-byte block, even, 0-64\n                             (default: 16 ≈ 6% overhead, fixes 8 bad bytes per block)\n    -R, --recovery <pct>     Add parity IMAGES so whole missing/damaged images can be\n                             rebuilt without recapture; pct% extra images, 0-100\n                             (default: 0; e.g. 15 tolerates losing ~15% of the images)\n    -F, --fountain <pct>     Fountain coding for video mode: pct% extra CODED frames\n                             (random linear combinations); ANY enough captured frames\n                             per stripe reconstruct the data — ideal with --video,\n                             where torn/glared frames simply don't count\n    -p, --password <pw>      AES-256-GCM encrypt the payload; decode needs the same\n                             password (wrong password fails cleanly, nothing leaks)\n    -f, --format <fmt>       Lossless image format: png, bmp, tga, qoi, webp, tiff\n                             (default: png, written by the built-in fast PNG writer)\n    --camera                 Camera profile: adds finder patterns so images decode\n                       \n[truncated]",
        "stderr": "error: unknown option '--help'.",
        "outputLimitExceeded": false
      },
      "parsed": true,
      "payload": "QrShard — encode any file into dense QR-style images and back.\n\nusage:\n  qrshard encode <file|folder> [options]   Split a file (or a folder, tar-ed\n                             automatically and extracted on decode) into shard images.\n    -o, --out <dir>          Output folder (default: <file>.shards next to the input)\n    -r, --resolution <px>    Image size: \"auto\" (the default) uses the primary\n                             monitor's native resolution so shards fill the screen\n                             they'll be captured from; or one number (square) or\n                             WxH, 700-16384, to override (e.g. a smaller size shows\n                             the code surrounded by padding)\n    -c, --cell <px>          Data cell size in pixels, 1-64 (default: 3)\n    -b, --bits <n>           Bits per cell / color density, 1-8 (default: 4)\n    -e, --ecc <n>            Reed-Solomon parity per 255-byte block, even, 0-64\n                             (default: 16 ≈ 6% overhead, fixes 8 bad bytes per block)\n    -R, --recovery <pct>     Add parity IMAGES so whole missing/damaged images can be\n                             rebuilt without recapture; pct% extra images, 0-100\n                             (default: 0; e.g. 15 tolerates losing ~15% of the images)\n    -F, --fountain <pct>     Fountain coding for video mode: pct% extra CODED frames\n                             (random linear combinations); ANY enough captured frames\n                             per stripe reconstruct the data — ideal with --video,\n                             where torn/glared frames simply don't count\n    -p, --password <pw>      AES-256-GCM encrypt the payload; decode needs the same\n                             password (wrong password fails cleanly, nothing leaks)\n    -f, --format <fmt>       Lossless image format: png, bmp, tga, qoi, webp, tiff\n                             (default: png, written by the built-in fast PNG writer)\n    --camera                 Camera profile: adds finder patterns so images decode\n                       \n[truncated]",
      "terminalNonHelp": false
    },
    {
      "command": "receive",
      "helpInvocation": "receive --help",
      "result": {
        "status": "failed",
        "timedOut": false,
        "exitCode": 2,
        "durationMs": 123,
        "stdout": "QrShard — encode any file into dense QR-style images and back.\n\nusage:\n  qrshard encode <file|folder> [options]   Split a file (or a folder, tar-ed\n                             automatically and extracted on decode) into shard images.\n    -o, --out <dir>          Output folder (default: <file>.shards next to the input)\n    -r, --resolution <px>    Image size: \"auto\" (the default) uses the primary\n                             monitor's native resolution so shards fill the screen\n                             they'll be captured from; or one number (square) or\n                             WxH, 700-16384, to override (e.g. a smaller size shows\n                             the code surrounded by padding)\n    -c, --cell <px>          Data cell size in pixels, 1-64 (default: 3)\n    -b, --bits <n>           Bits per cell / color density, 1-8 (default: 4)\n    -e, --ecc <n>            Reed-Solomon parity per 255-byte block, even, 0-64\n                             (default: 16 ≈ 6% overhead, fixes 8 bad bytes per block)\n    -R, --recovery <pct>     Add parity IMAGES so whole missing/damaged images can be\n                             rebuilt without recapture; pct% extra images, 0-100\n                             (default: 0; e.g. 15 tolerates losing ~15% of the images)\n    -F, --fountain <pct>     Fountain coding for video mode: pct% extra CODED frames\n                             (random linear combinations); ANY enough captured frames\n                             per stripe reconstruct the data — ideal with --video,\n                             where torn/glared frames simply don't count\n    -p, --password <pw>      AES-256-GCM encrypt the payload; decode needs the same\n                             password (wrong password fails cleanly, nothing leaks)\n    -f, --format <fmt>       Lossless image format: png, bmp, tga, qoi, webp, tiff\n                             (default: png, written by the built-in fast PNG writer)\n    --camera                 Camera profile: adds finder patterns so images decode\n                       \n[truncated]",
        "stderr": "error: unknown option '--help'.",
        "outputLimitExceeded": false
      },
      "parsed": true,
      "payload": "QrShard — encode any file into dense QR-style images and back.\n\nusage:\n  qrshard encode <file|folder> [options]   Split a file (or a folder, tar-ed\n                             automatically and extracted on decode) into shard images.\n    -o, --out <dir>          Output folder (default: <file>.shards next to the input)\n    -r, --resolution <px>    Image size: \"auto\" (the default) uses the primary\n                             monitor's native resolution so shards fill the screen\n                             they'll be captured from; or one number (square) or\n                             WxH, 700-16384, to override (e.g. a smaller size shows\n                             the code surrounded by padding)\n    -c, --cell <px>          Data cell size in pixels, 1-64 (default: 3)\n    -b, --bits <n>           Bits per cell / color density, 1-8 (default: 4)\n    -e, --ecc <n>            Reed-Solomon parity per 255-byte block, even, 0-64\n                             (default: 16 ≈ 6% overhead, fixes 8 bad bytes per block)\n    -R, --recovery <pct>     Add parity IMAGES so whole missing/damaged images can be\n                             rebuilt without recapture; pct% extra images, 0-100\n                             (default: 0; e.g. 15 tolerates losing ~15% of the images)\n    -F, --fountain <pct>     Fountain coding for video mode: pct% extra CODED frames\n                             (random linear combinations); ANY enough captured frames\n                             per stripe reconstruct the data — ideal with --video,\n                             where torn/glared frames simply don't count\n    -p, --password <pw>      AES-256-GCM encrypt the payload; decode needs the same\n                             password (wrong password fails cleanly, nothing leaks)\n    -f, --format <fmt>       Lossless image format: png, bmp, tga, qoi, webp, tiff\n                             (default: png, written by the built-in fast PNG writer)\n    --camera                 Camera profile: adds finder patterns so images decode\n                       \n[truncated]",
      "terminalNonHelp": false
    },
    {
      "command": "calibrate",
      "helpInvocation": "calibrate --help",
      "result": {
        "status": "failed",
        "timedOut": false,
        "exitCode": 2,
        "durationMs": 115,
        "stdout": "QrShard — encode any file into dense QR-style images and back.\n\nusage:\n  qrshard encode <file|folder> [options]   Split a file (or a folder, tar-ed\n                             automatically and extracted on decode) into shard images.\n    -o, --out <dir>          Output folder (default: <file>.shards next to the input)\n    -r, --resolution <px>    Image size: \"auto\" (the default) uses the primary\n                             monitor's native resolution so shards fill the screen\n                             they'll be captured from; or one number (square) or\n                             WxH, 700-16384, to override (e.g. a smaller size shows\n                             the code surrounded by padding)\n    -c, --cell <px>          Data cell size in pixels, 1-64 (default: 3)\n    -b, --bits <n>           Bits per cell / color density, 1-8 (default: 4)\n    -e, --ecc <n>            Reed-Solomon parity per 255-byte block, even, 0-64\n                             (default: 16 ≈ 6% overhead, fixes 8 bad bytes per block)\n    -R, --recovery <pct>     Add parity IMAGES so whole missing/damaged images can be\n                             rebuilt without recapture; pct% extra images, 0-100\n                             (default: 0; e.g. 15 tolerates losing ~15% of the images)\n    -F, --fountain <pct>     Fountain coding for video mode: pct% extra CODED frames\n                             (random linear combinations); ANY enough captured frames\n                             per stripe reconstruct the data — ideal with --video,\n                             where torn/glared frames simply don't count\n    -p, --password <pw>      AES-256-GCM encrypt the payload; decode needs the same\n                             password (wrong password fails cleanly, nothing leaks)\n    -f, --format <fmt>       Lossless image format: png, bmp, tga, qoi, webp, tiff\n                             (default: png, written by the built-in fast PNG writer)\n    --camera                 Camera profile: adds finder patterns so images decode\n                       \n[truncated]",
        "stderr": "error: unknown option '--help'.",
        "outputLimitExceeded": false
      },
      "parsed": true,
      "payload": "QrShard — encode any file into dense QR-style images and back.\n\nusage:\n  qrshard encode <file|folder> [options]   Split a file (or a folder, tar-ed\n                             automatically and extracted on decode) into shard images.\n    -o, --out <dir>          Output folder (default: <file>.shards next to the input)\n    -r, --resolution <px>    Image size: \"auto\" (the default) uses the primary\n                             monitor's native resolution so shards fill the screen\n                             they'll be captured from; or one number (square) or\n                             WxH, 700-16384, to override (e.g. a smaller size shows\n                             the code surrounded by padding)\n    -c, --cell <px>          Data cell size in pixels, 1-64 (default: 3)\n    -b, --bits <n>           Bits per cell / color density, 1-8 (default: 4)\n    -e, --ecc <n>            Reed-Solomon parity per 255-byte block, even, 0-64\n                             (default: 16 ≈ 6% overhead, fixes 8 bad bytes per block)\n    -R, --recovery <pct>     Add parity IMAGES so whole missing/damaged images can be\n                             rebuilt without recapture; pct% extra images, 0-100\n                             (default: 0; e.g. 15 tolerates losing ~15% of the images)\n    -F, --fountain <pct>     Fountain coding for video mode: pct% extra CODED frames\n                             (random linear combinations); ANY enough captured frames\n                             per stripe reconstruct the data — ideal with --video,\n                             where torn/glared frames simply don't count\n    -p, --password <pw>      AES-256-GCM encrypt the payload; decode needs the same\n                             password (wrong password fails cleanly, nothing leaks)\n    -f, --format <fmt>       Lossless image format: png, bmp, tga, qoi, webp, tiff\n                             (default: png, written by the built-in fast PNG writer)\n    --camera                 Camera profile: adds finder patterns so images decode\n                       \n[truncated]",
      "terminalNonHelp": false
    },
    {
      "command": "verify",
      "helpInvocation": "verify --help",
      "result": {
        "status": "failed",
        "timedOut": false,
        "exitCode": 2,
        "durationMs": 114,
        "stdout": "QrShard — encode any file into dense QR-style images and back.\n\nusage:\n  qrshard encode <file|folder> [options]   Split a file (or a folder, tar-ed\n                             automatically and extracted on decode) into shard images.\n    -o, --out <dir>          Output folder (default: <file>.shards next to the input)\n    -r, --resolution <px>    Image size: \"auto\" (the default) uses the primary\n                             monitor's native resolution so shards fill the screen\n                             they'll be captured from; or one number (square) or\n                             WxH, 700-16384, to override (e.g. a smaller size shows\n                             the code surrounded by padding)\n    -c, --cell <px>          Data cell size in pixels, 1-64 (default: 3)\n    -b, --bits <n>           Bits per cell / color density, 1-8 (default: 4)\n    -e, --ecc <n>            Reed-Solomon parity per 255-byte block, even, 0-64\n                             (default: 16 ≈ 6% overhead, fixes 8 bad bytes per block)\n    -R, --recovery <pct>     Add parity IMAGES so whole missing/damaged images can be\n                             rebuilt without recapture; pct% extra images, 0-100\n                             (default: 0; e.g. 15 tolerates losing ~15% of the images)\n    -F, --fountain <pct>     Fountain coding for video mode: pct% extra CODED frames\n                             (random linear combinations); ANY enough captured frames\n                             per stripe reconstruct the data — ideal with --video,\n                             where torn/glared frames simply don't count\n    -p, --password <pw>      AES-256-GCM encrypt the payload; decode needs the same\n                             password (wrong password fails cleanly, nothing leaks)\n    -f, --format <fmt>       Lossless image format: png, bmp, tga, qoi, webp, tiff\n                             (default: png, written by the built-in fast PNG writer)\n    --camera                 Camera profile: adds finder patterns so images decode\n                       \n[truncated]",
        "stderr": "error: unknown option '--help'.",
        "outputLimitExceeded": false
      },
      "parsed": true,
      "payload": "QrShard — encode any file into dense QR-style images and back.\n\nusage:\n  qrshard encode <file|folder> [options]   Split a file (or a folder, tar-ed\n                             automatically and extracted on decode) into shard images.\n    -o, --out <dir>          Output folder (default: <file>.shards next to the input)\n    -r, --resolution <px>    Image size: \"auto\" (the default) uses the primary\n                             monitor's native resolution so shards fill the screen\n                             they'll be captured from; or one number (square) or\n                             WxH, 700-16384, to override (e.g. a smaller size shows\n                             the code surrounded by padding)\n    -c, --cell <px>          Data cell size in pixels, 1-64 (default: 3)\n    -b, --bits <n>           Bits per cell / color density, 1-8 (default: 4)\n    -e, --ecc <n>            Reed-Solomon parity per 255-byte block, even, 0-64\n                             (default: 16 ≈ 6% overhead, fixes 8 bad bytes per block)\n    -R, --recovery <pct>     Add parity IMAGES so whole missing/damaged images can be\n                             rebuilt without recapture; pct% extra images, 0-100\n                             (default: 0; e.g. 15 tolerates losing ~15% of the images)\n    -F, --fountain <pct>     Fountain coding for video mode: pct% extra CODED frames\n                             (random linear combinations); ANY enough captured frames\n                             per stripe reconstruct the data — ideal with --video,\n                             where torn/glared frames simply don't count\n    -p, --password <pw>      AES-256-GCM encrypt the payload; decode needs the same\n                             password (wrong password fails cleanly, nothing leaks)\n    -f, --format <fmt>       Lossless image format: png, bmp, tga, qoi, webp, tiff\n                             (default: png, written by the built-in fast PNG writer)\n    --camera                 Camera profile: adds finder patterns so images decode\n                       \n[truncated]",
      "terminalNonHelp": false
    },
    {
      "command": "info",
      "helpInvocation": "info --help",
      "result": {
        "status": "failed",
        "timedOut": false,
        "exitCode": 2,
        "durationMs": 115,
        "stdout": "QrShard — encode any file into dense QR-style images and back.\n\nusage:\n  qrshard encode <file|folder> [options]   Split a file (or a folder, tar-ed\n                             automatically and extracted on decode) into shard images.\n    -o, --out <dir>          Output folder (default: <file>.shards next to the input)\n    -r, --resolution <px>    Image size: \"auto\" (the default) uses the primary\n                             monitor's native resolution so shards fill the screen\n                             they'll be captured from; or one number (square) or\n                             WxH, 700-16384, to override (e.g. a smaller size shows\n                             the code surrounded by padding)\n    -c, --cell <px>          Data cell size in pixels, 1-64 (default: 3)\n    -b, --bits <n>           Bits per cell / color density, 1-8 (default: 4)\n    -e, --ecc <n>            Reed-Solomon parity per 255-byte block, even, 0-64\n                             (default: 16 ≈ 6% overhead, fixes 8 bad bytes per block)\n    -R, --recovery <pct>     Add parity IMAGES so whole missing/damaged images can be\n                             rebuilt without recapture; pct% extra images, 0-100\n                             (default: 0; e.g. 15 tolerates losing ~15% of the images)\n    -F, --fountain <pct>     Fountain coding for video mode: pct% extra CODED frames\n                             (random linear combinations); ANY enough captured frames\n                             per stripe reconstruct the data — ideal with --video,\n                             where torn/glared frames simply don't count\n    -p, --password <pw>      AES-256-GCM encrypt the payload; decode needs the same\n                             password (wrong password fails cleanly, nothing leaks)\n    -f, --format <fmt>       Lossless image format: png, bmp, tga, qoi, webp, tiff\n                             (default: png, written by the built-in fast PNG writer)\n    --camera                 Camera profile: adds finder patterns so images decode\n                       \n[truncated]",
        "stderr": "error: unknown option '--help'.",
        "outputLimitExceeded": false
      },
      "parsed": true,
      "payload": "QrShard — encode any file into dense QR-style images and back.\n\nusage:\n  qrshard encode <file|folder> [options]   Split a file (or a folder, tar-ed\n                             automatically and extracted on decode) into shard images.\n    -o, --out <dir>          Output folder (default: <file>.shards next to the input)\n    -r, --resolution <px>    Image size: \"auto\" (the default) uses the primary\n                             monitor's native resolution so shards fill the screen\n                             they'll be captured from; or one number (square) or\n                             WxH, 700-16384, to override (e.g. a smaller size shows\n                             the code surrounded by padding)\n    -c, --cell <px>          Data cell size in pixels, 1-64 (default: 3)\n    -b, --bits <n>           Bits per cell / color density, 1-8 (default: 4)\n    -e, --ecc <n>            Reed-Solomon parity per 255-byte block, even, 0-64\n                             (default: 16 ≈ 6% overhead, fixes 8 bad bytes per block)\n    -R, --recovery <pct>     Add parity IMAGES so whole missing/damaged images can be\n                             rebuilt without recapture; pct% extra images, 0-100\n                             (default: 0; e.g. 15 tolerates losing ~15% of the images)\n    -F, --fountain <pct>     Fountain coding for video mode: pct% extra CODED frames\n                             (random linear combinations); ANY enough captured frames\n                             per stripe reconstruct the data — ideal with --video,\n                             where torn/glared frames simply don't count\n    -p, --password <pw>      AES-256-GCM encrypt the payload; decode needs the same\n                             password (wrong password fails cleanly, nothing leaks)\n    -f, --format <fmt>       Lossless image format: png, bmp, tga, qoi, webp, tiff\n                             (default: png, written by the built-in fast PNG writer)\n    --camera                 Camera profile: adds finder patterns so images decode\n                       \n[truncated]",
      "terminalNonHelp": false
    },
    {
      "command": "test",
      "helpInvocation": "test --help",
      "result": {
        "status": "failed",
        "timedOut": false,
        "exitCode": 2,
        "durationMs": 129,
        "stdout": "QrShard — encode any file into dense QR-style images and back.\n\nusage:\n  qrshard encode <file|folder> [options]   Split a file (or a folder, tar-ed\n                             automatically and extracted on decode) into shard images.\n    -o, --out <dir>          Output folder (default: <file>.shards next to the input)\n    -r, --resolution <px>    Image size: \"auto\" (the default) uses the primary\n                             monitor's native resolution so shards fill the screen\n                             they'll be captured from; or one number (square) or\n                             WxH, 700-16384, to override (e.g. a smaller size shows\n                             the code surrounded by padding)\n    -c, --cell <px>          Data cell size in pixels, 1-64 (default: 3)\n    -b, --bits <n>           Bits per cell / color density, 1-8 (default: 4)\n    -e, --ecc <n>            Reed-Solomon parity per 255-byte block, even, 0-64\n                             (default: 16 ≈ 6% overhead, fixes 8 bad bytes per block)\n    -R, --recovery <pct>     Add parity IMAGES so whole missing/damaged images can be\n                             rebuilt without recapture; pct% extra images, 0-100\n                             (default: 0; e.g. 15 tolerates losing ~15% of the images)\n    -F, --fountain <pct>     Fountain coding for video mode: pct% extra CODED frames\n                             (random linear combinations); ANY enough captured frames\n                             per stripe reconstruct the data — ideal with --video,\n                             where torn/glared frames simply don't count\n    -p, --password <pw>      AES-256-GCM encrypt the payload; decode needs the same\n                             password (wrong password fails cleanly, nothing leaks)\n    -f, --format <fmt>       Lossless image format: png, bmp, tga, qoi, webp, tiff\n                             (default: png, written by the built-in fast PNG writer)\n    --camera                 Camera profile: adds finder patterns so images decode\n                       \n[truncated]",
        "stderr": "error: unknown option '--help'. Did you mean '--cell'?",
        "outputLimitExceeded": false
      },
      "parsed": true,
      "payload": "QrShard — encode any file into dense QR-style images and back.\n\nusage:\n  qrshard encode <file|folder> [options]   Split a file (or a folder, tar-ed\n                             automatically and extracted on decode) into shard images.\n    -o, --out <dir>          Output folder (default: <file>.shards next to the input)\n    -r, --resolution <px>    Image size: \"auto\" (the default) uses the primary\n                             monitor's native resolution so shards fill the screen\n                             they'll be captured from; or one number (square) or\n                             WxH, 700-16384, to override (e.g. a smaller size shows\n                             the code surrounded by padding)\n    -c, --cell <px>          Data cell size in pixels, 1-64 (default: 3)\n    -b, --bits <n>           Bits per cell / color density, 1-8 (default: 4)\n    -e, --ecc <n>            Reed-Solomon parity per 255-byte block, even, 0-64\n                             (default: 16 ≈ 6% overhead, fixes 8 bad bytes per block)\n    -R, --recovery <pct>     Add parity IMAGES so whole missing/damaged images can be\n                             rebuilt without recapture; pct% extra images, 0-100\n                             (default: 0; e.g. 15 tolerates losing ~15% of the images)\n    -F, --fountain <pct>     Fountain coding for video mode: pct% extra CODED frames\n                             (random linear combinations); ANY enough captured frames\n                             per stripe reconstruct the data — ideal with --video,\n                             where torn/glared frames simply don't count\n    -p, --password <pw>      AES-256-GCM encrypt the payload; decode needs the same\n                             password (wrong password fails cleanly, nothing leaks)\n    -f, --format <fmt>       Lossless image format: png, bmp, tga, qoi, webp, tiff\n                             (default: png, written by the built-in fast PNG writer)\n    --camera                 Camera profile: adds finder patterns so images decode\n                       \n[truncated]",
      "terminalNonHelp": false
    }
  ]
}
