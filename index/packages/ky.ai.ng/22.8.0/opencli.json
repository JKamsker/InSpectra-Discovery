{
  "opencli": "0.1-draft",
  "info": {
    "title": "ky-ai-ng — run the Angular CLI with output mirrored for agents.",
    "version": "22.8.0",
    "description": "Angular CLI dev-loop runner for AI agents: runs ng serve/build with build state and logs exposed over MCP."
  },
  "x-inspectra": {
    "artifactSource": "crawled-from-help",
    "generator": "InSpectra.Gen",
    "helpDocumentCount": 1,
    "cliParsedDescription": "SERVE — a frontend's dev server (run one per app; registers with the hub for the agent):"
  },
  "commands": [],
  "options": [
    {
      "name": "--name",
      "recursive": false,
      "hidden": false,
      "description": "Project name in the hub (default: parent folder of ClientApp)",
      "arguments": [
        {
          "name": "ID",
          "required": false,
          "arity": {
            "minimum": 0,
            "maximum": 1
          }
        }
      ]
    },
    {
      "name": "--log-lines",
      "recursive": false,
      "hidden": false,
      "description": "Lines kept in the in-memory log buffer (default: 200; 0 = unlimited)",
      "arguments": [
        {
          "name": "N",
          "required": false,
          "arity": {
            "minimum": 0,
            "maximum": 1
          }
        }
      ]
    },
    {
      "name": "--log-file",
      "recursive": false,
      "hidden": false,
      "description": "Also mirror the buffer to a file (default: off — MCP serves logs)",
      "arguments": [
        {
          "name": "FILE",
          "required": false,
          "arity": {
            "minimum": 0,
            "maximum": 1
          }
        }
      ]
    },
    {
      "name": "--rest-port",
      "recursive": false,
      "hidden": false,
      "description": "Local REST control port (default: OS-assigned)",
      "arguments": [
        {
          "name": "N",
          "required": false,
          "arity": {
            "minimum": 0,
            "maximum": 1
          }
        }
      ]
    },
    {
      "name": "--hub-port",
      "recursive": false,
      "hidden": false,
      "description": "Hub port to register with (default: 5101; rarely needed — does not start a hub)",
      "arguments": [
        {
          "name": "N",
          "required": false,
          "arity": {
            "minimum": 0,
            "maximum": 1
          }
        }
      ]
    },
    {
      "name": "--no-hub",
      "recursive": false,
      "hidden": false,
      "description": "Standalone: buffer + local REST only; no hub, no agent access"
    },
    {
      "name": "--after-start",
      "recursive": false,
      "hidden": false,
      "description": "Run <cmd> once the first build settles (the dev server is up).\nGreedy — everything after it is the command, so put it last.\nReplaces the PowerShell-unfriendly `serve & sleep 1 && cmd`.\nThe command shares this console and is killed when serve stops.\nAnything else after `serve` is forwarded to `ng serve` (e.g. --port 4015).\nStopping (Ctrl+C or a hard kill) reaps the whole ng tree and deregisters from the hub.\nExample: ky-ai-ng serve --port 4015 --after-start ky-ai-browser -y",
      "arguments": [
        {
          "name": "CMD",
          "required": true,
          "arity": {
            "minimum": 1,
            "maximum": 1
          }
        }
      ]
    }
  ]
}
