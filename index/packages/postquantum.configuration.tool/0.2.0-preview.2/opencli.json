{
  "opencli": "0.1-draft",
  "info": {
    "title": "pqc-config — CLI for PostQuantum.Configuration",
    "version": "0.2.0-preview.2",
    "description": "Command-line companion to PostQuantum.Configuration. Seal (protect), open (unprotect), and rotate keys for encrypted configuration values from a shell or CI pipeline, backed by a persisted PostQuantum.KeyManagement keyring. Read values from stdin to keep secrets out of shell history. See the PostQuantum.Configuration package for the library."
  },
  "x-inspectra": {
    "artifactSource": "crawled-from-help",
    "generator": "InSpectra.Gen",
    "helpDocumentCount": 1,
    "cliParsedTitle": "pqc-config — encrypt configuration values with PostQuantum.Configuration"
  },
  "commands": [
    {
      "name": "protect",
      "hidden": false,
      "description": "Seal a value into a pqc.v1 token (creates the keyring on first use)."
    },
    {
      "name": "rotate",
      "hidden": false,
      "description": "Add a fresh active key (same passphrase, new salt); old tokens still open."
    },
    {
      "name": "unprotect",
      "hidden": false,
      "description": "Recover the plaintext from a token."
    }
  ],
  "options": [
    {
      "name": "--keyring",
      "recursive": false,
      "hidden": false,
      "description": "Path to the keyring file (required).",
      "arguments": [
        {
          "name": "PATH",
          "required": true,
          "arity": {
            "minimum": 1,
            "maximum": 1
          }
        }
      ]
    },
    {
      "name": "--passphrase",
      "recursive": false,
      "hidden": false,
      "description": "KEK passphrase. Prefer the PQC_PASSPHRASE env var instead.",
      "arguments": [
        {
          "name": "VALUE",
          "required": true,
          "arity": {
            "minimum": 1,
            "maximum": 1
          }
        }
      ]
    },
    {
      "name": "--value",
      "recursive": false,
      "hidden": false,
      "description": "Value to protect. If omitted, read from stdin.\nvalue on stdin avoids the issue entirely and keeps it out of your shell history.",
      "arguments": [
        {
          "name": "VALUE",
          "required": false,
          "arity": {
            "minimum": 0,
            "maximum": 1
          }
        }
      ]
    },
    {
      "name": "--token",
      "recursive": false,
      "hidden": false,
      "description": "Token to unprotect. If omitted, read from stdin.",
      "arguments": [
        {
          "name": "TEXT",
          "required": true,
          "arity": {
            "minimum": 1,
            "maximum": 1
          }
        }
      ]
    },
    {
      "name": "--context",
      "recursive": false,
      "hidden": false,
      "description": "Optional context bound into the token (swap resistance).\nUse --option=value (with '=') for any value that starts with '-', e.g.",
      "arguments": [
        {
          "name": "TEXT",
          "required": true,
          "arity": {
            "minimum": 1,
            "maximum": 1
          }
        }
      ]
    }
  ]
}
