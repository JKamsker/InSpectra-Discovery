{
  "opencli": "0.1-draft",
  "info": {
    "title": "xquery",
    "version": "1.1.0.10",
    "description": "Command-line XQuery 3.1/4.0 processor — query XML documents from the terminal"
  },
  "x-inspectra": {
    "artifactSource": "crawled-from-help",
    "generator": "InSpectra.Discovery",
    "helpDocumentCount": 1
  },
  "commands": [],
  "options": [
    {
      "name": "--file",
      "recursive": false,
      "hidden": false,
      "description": "Read XQuery from a file instead of inline",
      "aliases": [
        "-f"
      ],
      "arguments": [
        {
          "name": "PATH",
          "required": true,
          "arity": {
            "minimum": 1,
            "maximum": 1
          }
        }
      ]
    },
    {
      "name": "--output",
      "recursive": false,
      "hidden": false,
      "description": "Output method: adaptive (default), xml, text, json",
      "aliases": [
        "-o"
      ],
      "arguments": [
        {
          "name": "METHOD",
          "required": true,
          "arity": {
            "minimum": 1,
            "maximum": 1
          }
        }
      ]
    },
    {
      "name": "--stdin",
      "recursive": false,
      "hidden": false,
      "description": "Read XML input from stdin (waits indefinitely)"
    },
    {
      "name": "--timeout",
      "recursive": false,
      "hidden": false,
      "description": "Stdin auto-detection timeout in ms (default: 200)",
      "arguments": [
        {
          "name": "MS",
          "required": false,
          "arity": {
            "minimum": 0,
            "maximum": 1
          }
        }
      ]
    },
    {
      "name": "--timing",
      "recursive": false,
      "hidden": false,
      "description": "Show parse/compile/execute timing breakdown"
    },
    {
      "name": "--plan",
      "recursive": false,
      "hidden": false,
      "description": "Show the execution plan before running"
    },
    {
      "name": "--dry-run",
      "recursive": false,
      "hidden": false,
      "description": "Parse and compile only, do not execute"
    },
    {
      "name": "--verbose",
      "recursive": false,
      "hidden": false,
      "description": "Show detailed error information",
      "aliases": [
        "-v"
      ]
    },
    {
      "name": "--help",
      "recursive": false,
      "hidden": false,
      "description": "Show this help message",
      "aliases": [
        "-h"
      ]
    },
    {
      "name": "--version",
      "recursive": false,
      "hidden": false,
      "description": "Show version information\nSources:\nFiles              Path to an XML file (loaded as fn:doc() target)\nDirectories        Path to a directory (all *.xml files loaded)\nURLs               HTTP/HTTPS URL to fetch XML from\nWhen a single source is provided, it becomes the context item (.).\nAll sources are available via fn:doc($uri) by their path or URL.\nfn:collection() returns all loaded documents."
    }
  ],
  "arguments": [
    {
      "name": "EXPRESSION",
      "required": false,
      "hidden": false,
      "arity": {
        "minimum": 0,
        "maximum": 1
      },
      "description": "Inline XQuery expression to execute"
    },
    {
      "name": "SOURCES",
      "required": false,
      "hidden": false,
      "arity": {
        "minimum": 0
      },
      "description": "XML files, directories, or URLs to query"
    }
  ]
}
