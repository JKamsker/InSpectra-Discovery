{
  "opencli": "0.1-draft",
  "info": {
    "title": "WorktreeInitializer (wi) - Copy gitignored files to a new worktree",
    "version": "1.2.0"
  },
  "x-inspectra": {
    "artifactSource": "crawled-from-help",
    "generator": "InSpectra.Discovery",
    "helpDocumentCount": 1
  },
  "commands": [],
  "options": [
    {
      "name": "--ignore",
      "recursive": false,
      "hidden": false,
      "description": "Exclude files under <path> from copying (can be repeated)\nThe init command copies all git-ignored files (e.g. build outputs, .env files)\nfrom a source repository to a destination directory, preserving the directory structure.\nYou can also place a WorktreeConfig.json in the source repo root to define\ndefault ignores:\n{\n\"ignores\": [\"node_modules\", \".venv\", \"dist\"]\n}\nCLI --ignore flags are merged with config file ignores.",
      "arguments": [
        {
          "name": "PATH",
          "required": true,
          "arity": {
            "minimum": 1,
            "maximum": 1
          }
        }
      ]
    }
  ],
  "arguments": [
    {
      "name": "SOURCE_PATH",
      "required": true,
      "hidden": false,
      "arity": {
        "minimum": 1,
        "maximum": 1
      }
    }
  ]
}
