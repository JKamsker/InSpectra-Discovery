{
  "opencli": "0.1-draft",
  "info": {
    "title": "PowerPortalsPro MCP server.",
    "version": "8.0.0",
    "description": "Model Context Protocol server for PowerPortalsPro. Gives AI coding agents the component API reference, working code samples and conventions for both the Blazor and React stacks, plus Dataverse metadata read live from the environment the project is already configured against. Reads are metadata-only; writes reach only the framework's own portal-role and website configuration tables, and deletions ask the user to confirm."
  },
  "x-inspectra": {
    "artifactSource": "crawled-from-help",
    "generator": "InSpectra.Gen",
    "helpDocumentCount": 1,
    "cliParsedDescription": "With no arguments it speaks the Model Context Protocol over stdio; MCP clients launch\nit that way. The commands below are for setting up the Dataverse connection."
  },
  "commands": [
    {
      "name": "environments",
      "hidden": false,
      "description": "List the Power Platform CLI profiles available to mirror"
    },
    {
      "name": "login",
      "hidden": false,
      "description": "Sign in to a Dataverse environment and bind it to this folder"
    },
    {
      "name": "logout",
      "hidden": false,
      "description": "Unbind this folder"
    },
    {
      "name": "status",
      "hidden": false,
      "description": "Show which environment this folder uses, and whether it connects"
    }
  ],
  "options": [
    {
      "name": "--environment",
      "recursive": false,
      "hidden": false,
      "description": "The environment to bind, e.g. https://contoso.crm.dynamics.com",
      "arguments": [
        {
          "name": "URL",
          "required": true,
          "arity": {
            "minimum": 1,
            "maximum": 1
          }
        }
      ]
    },
    {
      "name": "--pac-profile",
      "recursive": false,
      "hidden": false,
      "description": "Take the environment and account from a Power Platform CLI profile",
      "arguments": [
        {
          "name": "NAME",
          "required": true,
          "arity": {
            "minimum": 1,
            "maximum": 1
          }
        }
      ]
    },
    {
      "name": "--username",
      "recursive": false,
      "hidden": false,
      "description": "Sign in as a specific account",
      "arguments": [
        {
          "name": "UPN",
          "required": true,
          "arity": {
            "minimum": 1,
            "maximum": 1
          }
        }
      ]
    },
    {
      "name": "--tenant",
      "recursive": false,
      "hidden": false,
      "description": "Authenticate against a specific Entra tenant",
      "arguments": [
        {
          "name": "ID",
          "required": true,
          "arity": {
            "minimum": 1,
            "maximum": 1
          }
        }
      ]
    },
    {
      "name": "--browser",
      "recursive": false,
      "hidden": false,
      "description": "Open a browser instead of showing a device code"
    },
    {
      "name": "--workspace",
      "recursive": false,
      "hidden": false,
      "description": "Act on another folder",
      "arguments": [
        {
          "name": "PATH",
          "required": true,
          "arity": {
            "minimum": 1,
            "maximum": 1
          }
        }
      ]
    }
  ]
}
