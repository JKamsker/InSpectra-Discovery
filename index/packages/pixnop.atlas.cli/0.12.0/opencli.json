{
  "opencli": "0.1-draft",
  "info": {
    "title": "Atlas CLI: run Atlas scenarios from a compiled test assembly, without VSTest.",
    "version": "0.12.0",
    "description": "Command line runner for the Atlas integration-test harness: executes Atlas scenarios from a compiled test assembly without VSTest (atlas run path/to/Scenarios.dll)."
  },
  "x-inspectra": {
    "artifactSource": "crawled-from-help",
    "generator": "InSpectra.Gen",
    "helpDocumentCount": 5
  },
  "commands": [
    {
      "name": "diff",
      "hidden": false,
      "description": "Compare two TRX reports by test name (baseline vs candidate; no server, no\nVINTAGE_STORY) and report what changed: new failures, fixed, vanished, new\ntests, still failing, and notable duration shifts (a passing test at least\n2x and at least 500 ms away from its baseline). Works on the TRX atlas\nwrites (`--parallel --trx`) and on any spec-conforming TRX (`dotnet test\n--logger trx`). Exit 0 when the candidate has no regressions, 1 when it\nhas (a regression is a new failure or a vanished test), 2 when a file\ncannot be read as TRX. Contract: docs/specs/2026-07-14-diff-command.md.",
      "options": [
        {
          "name": "--help",
          "recursive": false,
          "hidden": false,
          "description": "Show this help.",
          "aliases": [
            "-h"
          ]
        },
        {
          "name": "--version",
          "recursive": false,
          "hidden": false,
          "description": "Print the atlas version and exit (no assembly or\nVINTAGE_STORY required). `atlas version` works too.\nEnvironment:\nVINTAGE_STORY          Required by `run`, `fixture` and `stage`: the Vintage Story\ninstall directory containing VintagestoryLib.dll.\nExit codes:\n0  every scenario passed (or the listing succeeded, or the fixture was written, or\nthe diff found no regressions, or the stage found nothing to do or staged\ncleanly)\n1  at least one scenario failed or errored, or nothing matched the filter, or the\nfixture builder failed (no fixture is written then), or the diff found\nregressions (a new failure or a vanished test)\n2  usage or environment error (bad arguments, VINTAGE_STORY missing, a diff input\nthat cannot be read as TRX, or a stage target that cannot be staged: an\nunwritable output, an install without its pdb, a diverged copy already bound, or\nthe Newtonsoft direction refusal)"
        },
        {
          "name": "--filter",
          "recursive": false,
          "hidden": false,
          "description": "Only scenarios whose display name contains the substring\n(ordinal, case-insensitive).",
          "arguments": [
            {
              "name": "SUBSTRING",
              "required": true,
              "arity": {
                "minimum": 1,
                "maximum": 1
              }
            }
          ]
        },
        {
          "name": "--list",
          "recursive": false,
          "hidden": false,
          "description": "Print the discovered scenarios and exit without booting\nanything (VINTAGE_STORY not required)."
        },
        {
          "name": "--worker",
          "recursive": false,
          "hidden": false,
          "description": "Report exclusively as line-delimited JSON events on stdout\n(the orchestrator protocol; see\ndocs/specs/2026-07-06-worker-protocol.md). With --list,\nemits one 'discovered' event per scenario instead."
        },
        {
          "name": "--classes",
          "recursive": false,
          "hidden": false,
          "description": "Worker mode only: run only these scenario classes\n(comma-separated fully qualified names, exact match).",
          "arguments": [
            {
              "name": "LIST",
              "required": true,
              "arity": {
                "minimum": 1,
                "maximum": 1
              }
            }
          ]
        },
        {
          "name": "--parallel",
          "recursive": false,
          "hidden": false,
          "description": "Run the scenario classes on N worker subprocesses (one live\nserver per worker, one class per dispatch). N defaults to\nmin(cores / 2, class count). Incompatible with --worker\nand --list."
        },
        {
          "name": "--worker-timeout",
          "recursive": false,
          "hidden": false,
          "description": "Parallel mode only: kill a worker stuck on one class for\nmore than <s> seconds and report the class as failed\n(default 600).",
          "arguments": [
            {
              "name": "S",
              "required": true,
              "arity": {
                "minimum": 1,
                "maximum": 1
              }
            }
          ]
        },
        {
          "name": "--trx",
          "recursive": false,
          "hidden": false,
          "description": "Parallel mode only: write one aggregated VSTest-style TRX\nreport covering every class.",
          "arguments": [
            {
              "name": "PATH",
              "required": true,
              "arity": {
                "minimum": 1,
                "maximum": 1
              }
            }
          ]
        }
      ],
      "arguments": [
        {
          "name": "PATHTOSCENARIOSDLL",
          "required": true,
          "hidden": false,
          "arity": {
            "minimum": 1,
            "maximum": 1
          }
        },
        {
          "name": "BASELINETRX",
          "required": true,
          "hidden": false,
          "arity": {
            "minimum": 1,
            "maximum": 1
          }
        },
        {
          "name": "CANDIDATETRX",
          "required": true,
          "hidden": false,
          "arity": {
            "minimum": 1,
            "maximum": 1
          }
        },
        {
          "name": "PATHTOTEST_OUTPUT_OR_ASSEMBLYDLL",
          "required": true,
          "hidden": false,
          "arity": {
            "minimum": 1,
            "maximum": 1
          }
        }
      ],
      "commands": [
        {
          "name": "fixture",
          "hidden": false,
          "description": "Run exactly ONE matching scenario as a world builder, then copy the world\nsave its graceful shutdown persisted to --out. The builder is an ordinary\n[AtlasScenario] whose side effect is building the world (place blocks, run\ncommands, seed data); a class then boots the produced file with\n[AtlasWorld(SaveFile = \"fixtures/myworld.vcdbs\")]. If the builder fails,\nno fixture is written."
        },
        {
          "name": "Options",
          "hidden": false,
          "description": "--filter <substring>   Only scenarios whose display name contains the substring\n(ordinal, case-insensitive).\n--list                 Print the discovered scenarios and exit without booting\nanything (VINTAGE_STORY not required).\n--worker               Report exclusively as line-delimited JSON events on stdout\n(the orchestrator protocol; see\ndocs/specs/2026-07-06-worker-protocol.md). With --list,\nemits one 'discovered' event per scenario instead.\n--classes <list>       Worker mode only: run only these scenario classes\n(comma-separated fully qualified names, exact match).\n--parallel [N]         Run the scenario classes on N worker subprocesses (one live\nserver per worker, one class per dispatch). N defaults to\nmin(cores / 2, class count). Incompatible with --worker\nand --list.\n--worker-timeout <s>   Parallel mode only: kill a worker stuck on one class for\nmore than <s> seconds and report the class as failed\n(default 600).\n--trx <path>           Parallel mode only: write one aggregated VSTest-style TRX\nreport covering every class."
        },
        {
          "name": "run",
          "hidden": false,
          "description": "Build nothing, boot the embedded server(s) in-process, and execute the\nassembly's [AtlasScenario] methods sequentially, like `dotnet test` would."
        },
        {
          "name": "stage",
          "hidden": false,
          "description": "Pre-stage a compiled test output's VintagestoryAPI.dll+pdb (and, if the\ndirection calls for it, Newtonsoft.Json.dll) from VINTAGE_STORY, then\nexit: the same decision the module initializers run at boot (issue #49),\nbut BEFORE any process binds the engine assembly, so a one-shot script\nthat only runs each install once (no fail-then-rerun budget) can do\n`atlas stage out/ && VINTAGE_STORY=... dotnet test --no-build` and boot\ngreen on the first try. Prints one line per file (pair): staged, already\nidentical, or nothing to stage. Never boots a server and never loads\nVintagestoryAPI.dll or VintagestoryLib.dll itself."
        }
      ]
    },
    {
      "name": "fixture",
      "hidden": false,
      "description": "Run exactly ONE matching scenario as a world builder, then copy the world\nsave its graceful shutdown persisted to --out. The builder is an ordinary\n[AtlasScenario] whose side effect is building the world (place blocks, run\ncommands, seed data); a class then boots the produced file with\n[AtlasWorld(SaveFile = \"fixtures/myworld.vcdbs\")]. If the builder fails,\nno fixture is written.",
      "options": [
        {
          "name": "--help",
          "recursive": false,
          "hidden": false,
          "description": "Show this help.",
          "aliases": [
            "-h"
          ]
        },
        {
          "name": "--version",
          "recursive": false,
          "hidden": false,
          "description": "Print the atlas version and exit (no assembly or\nVINTAGE_STORY required). `atlas version` works too.\nEnvironment:\nVINTAGE_STORY          Required by `run`, `fixture` and `stage`: the Vintage Story\ninstall directory containing VintagestoryLib.dll.\nExit codes:\n0  every scenario passed (or the listing succeeded, or the fixture was written, or\nthe diff found no regressions, or the stage found nothing to do or staged\ncleanly)\n1  at least one scenario failed or errored, or nothing matched the filter, or the\nfixture builder failed (no fixture is written then), or the diff found\nregressions (a new failure or a vanished test)\n2  usage or environment error (bad arguments, VINTAGE_STORY missing, a diff input\nthat cannot be read as TRX, or a stage target that cannot be staged: an\nunwritable output, an install without its pdb, a diverged copy already bound, or\nthe Newtonsoft direction refusal)"
        },
        {
          "name": "--filter",
          "recursive": false,
          "hidden": false,
          "description": "Only scenarios whose display name contains the substring\n(ordinal, case-insensitive).",
          "arguments": [
            {
              "name": "SUBSTRING",
              "required": true,
              "arity": {
                "minimum": 1,
                "maximum": 1
              }
            }
          ]
        },
        {
          "name": "--list",
          "recursive": false,
          "hidden": false,
          "description": "Print the discovered scenarios and exit without booting\nanything (VINTAGE_STORY not required)."
        },
        {
          "name": "--worker",
          "recursive": false,
          "hidden": false,
          "description": "Report exclusively as line-delimited JSON events on stdout\n(the orchestrator protocol; see\ndocs/specs/2026-07-06-worker-protocol.md). With --list,\nemits one 'discovered' event per scenario instead."
        },
        {
          "name": "--classes",
          "recursive": false,
          "hidden": false,
          "description": "Worker mode only: run only these scenario classes\n(comma-separated fully qualified names, exact match).",
          "arguments": [
            {
              "name": "LIST",
              "required": true,
              "arity": {
                "minimum": 1,
                "maximum": 1
              }
            }
          ]
        },
        {
          "name": "--parallel",
          "recursive": false,
          "hidden": false,
          "description": "Run the scenario classes on N worker subprocesses (one live\nserver per worker, one class per dispatch). N defaults to\nmin(cores / 2, class count). Incompatible with --worker\nand --list."
        },
        {
          "name": "--worker-timeout",
          "recursive": false,
          "hidden": false,
          "description": "Parallel mode only: kill a worker stuck on one class for\nmore than <s> seconds and report the class as failed\n(default 600).",
          "arguments": [
            {
              "name": "S",
              "required": true,
              "arity": {
                "minimum": 1,
                "maximum": 1
              }
            }
          ]
        },
        {
          "name": "--trx",
          "recursive": false,
          "hidden": false,
          "description": "Parallel mode only: write one aggregated VSTest-style TRX\nreport covering every class.",
          "arguments": [
            {
              "name": "PATH",
              "required": true,
              "arity": {
                "minimum": 1,
                "maximum": 1
              }
            }
          ]
        }
      ],
      "arguments": [
        {
          "name": "PATHTOSCENARIOSDLL",
          "required": true,
          "hidden": false,
          "arity": {
            "minimum": 1,
            "maximum": 1
          }
        },
        {
          "name": "BASELINETRX",
          "required": true,
          "hidden": false,
          "arity": {
            "minimum": 1,
            "maximum": 1
          }
        },
        {
          "name": "CANDIDATETRX",
          "required": true,
          "hidden": false,
          "arity": {
            "minimum": 1,
            "maximum": 1
          }
        },
        {
          "name": "PATHTOTEST_OUTPUT_OR_ASSEMBLYDLL",
          "required": true,
          "hidden": false,
          "arity": {
            "minimum": 1,
            "maximum": 1
          }
        }
      ],
      "commands": [
        {
          "name": "diff",
          "hidden": false,
          "description": "Compare two TRX reports by test name (baseline vs candidate; no server, no\nVINTAGE_STORY) and report what changed: new failures, fixed, vanished, new\ntests, still failing, and notable duration shifts (a passing test at least\n2x and at least 500 ms away from its baseline). Works on the TRX atlas\nwrites (`--parallel --trx`) and on any spec-conforming TRX (`dotnet test\n--logger trx`). Exit 0 when the candidate has no regressions, 1 when it\nhas (a regression is a new failure or a vanished test), 2 when a file\ncannot be read as TRX. Contract: docs/specs/2026-07-14-diff-command.md."
        },
        {
          "name": "Options",
          "hidden": false,
          "description": "--filter <substring>   Only scenarios whose display name contains the substring\n(ordinal, case-insensitive).\n--list                 Print the discovered scenarios and exit without booting\nanything (VINTAGE_STORY not required).\n--worker               Report exclusively as line-delimited JSON events on stdout\n(the orchestrator protocol; see\ndocs/specs/2026-07-06-worker-protocol.md). With --list,\nemits one 'discovered' event per scenario instead.\n--classes <list>       Worker mode only: run only these scenario classes\n(comma-separated fully qualified names, exact match).\n--parallel [N]         Run the scenario classes on N worker subprocesses (one live\nserver per worker, one class per dispatch). N defaults to\nmin(cores / 2, class count). Incompatible with --worker\nand --list.\n--worker-timeout <s>   Parallel mode only: kill a worker stuck on one class for\nmore than <s> seconds and report the class as failed\n(default 600).\n--trx <path>           Parallel mode only: write one aggregated VSTest-style TRX\nreport covering every class."
        },
        {
          "name": "run",
          "hidden": false,
          "description": "Build nothing, boot the embedded server(s) in-process, and execute the\nassembly's [AtlasScenario] methods sequentially, like `dotnet test` would."
        },
        {
          "name": "stage",
          "hidden": false,
          "description": "Pre-stage a compiled test output's VintagestoryAPI.dll+pdb (and, if the\ndirection calls for it, Newtonsoft.Json.dll) from VINTAGE_STORY, then\nexit: the same decision the module initializers run at boot (issue #49),\nbut BEFORE any process binds the engine assembly, so a one-shot script\nthat only runs each install once (no fail-then-rerun budget) can do\n`atlas stage out/ && VINTAGE_STORY=... dotnet test --no-build` and boot\ngreen on the first try. Prints one line per file (pair): staged, already\nidentical, or nothing to stage. Never boots a server and never loads\nVintagestoryAPI.dll or VintagestoryLib.dll itself."
        }
      ]
    },
    {
      "name": "Options",
      "hidden": false,
      "description": "--filter <substring>   Only scenarios whose display name contains the substring\n(ordinal, case-insensitive).\n--list                 Print the discovered scenarios and exit without booting\nanything (VINTAGE_STORY not required).\n--worker               Report exclusively as line-delimited JSON events on stdout\n(the orchestrator protocol; see\ndocs/specs/2026-07-06-worker-protocol.md). With --list,\nemits one 'discovered' event per scenario instead.\n--classes <list>       Worker mode only: run only these scenario classes\n(comma-separated fully qualified names, exact match).\n--parallel [N]         Run the scenario classes on N worker subprocesses (one live\nserver per worker, one class per dispatch). N defaults to\nmin(cores / 2, class count). Incompatible with --worker\nand --list.\n--worker-timeout <s>   Parallel mode only: kill a worker stuck on one class for\nmore than <s> seconds and report the class as failed\n(default 600).\n--trx <path>           Parallel mode only: write one aggregated VSTest-style TRX\nreport covering every class."
    },
    {
      "name": "run",
      "hidden": false,
      "description": "Build nothing, boot the embedded server(s) in-process, and execute the\nassembly's [AtlasScenario] methods sequentially, like `dotnet test` would.",
      "options": [
        {
          "name": "--help",
          "recursive": false,
          "hidden": false,
          "description": "Show this help.",
          "aliases": [
            "-h"
          ]
        },
        {
          "name": "--version",
          "recursive": false,
          "hidden": false,
          "description": "Print the atlas version and exit (no assembly or\nVINTAGE_STORY required). `atlas version` works too.\nEnvironment:\nVINTAGE_STORY          Required by `run`, `fixture` and `stage`: the Vintage Story\ninstall directory containing VintagestoryLib.dll.\nExit codes:\n0  every scenario passed (or the listing succeeded, or the fixture was written, or\nthe diff found no regressions, or the stage found nothing to do or staged\ncleanly)\n1  at least one scenario failed or errored, or nothing matched the filter, or the\nfixture builder failed (no fixture is written then), or the diff found\nregressions (a new failure or a vanished test)\n2  usage or environment error (bad arguments, VINTAGE_STORY missing, a diff input\nthat cannot be read as TRX, or a stage target that cannot be staged: an\nunwritable output, an install without its pdb, a diverged copy already bound, or\nthe Newtonsoft direction refusal)"
        },
        {
          "name": "--filter",
          "recursive": false,
          "hidden": false,
          "description": "Only scenarios whose display name contains the substring\n(ordinal, case-insensitive).",
          "arguments": [
            {
              "name": "SUBSTRING",
              "required": true,
              "arity": {
                "minimum": 1,
                "maximum": 1
              }
            }
          ]
        },
        {
          "name": "--list",
          "recursive": false,
          "hidden": false,
          "description": "Print the discovered scenarios and exit without booting\nanything (VINTAGE_STORY not required)."
        },
        {
          "name": "--worker",
          "recursive": false,
          "hidden": false,
          "description": "Report exclusively as line-delimited JSON events on stdout\n(the orchestrator protocol; see\ndocs/specs/2026-07-06-worker-protocol.md). With --list,\nemits one 'discovered' event per scenario instead."
        },
        {
          "name": "--classes",
          "recursive": false,
          "hidden": false,
          "description": "Worker mode only: run only these scenario classes\n(comma-separated fully qualified names, exact match).",
          "arguments": [
            {
              "name": "LIST",
              "required": true,
              "arity": {
                "minimum": 1,
                "maximum": 1
              }
            }
          ]
        },
        {
          "name": "--parallel",
          "recursive": false,
          "hidden": false,
          "description": "Run the scenario classes on N worker subprocesses (one live\nserver per worker, one class per dispatch). N defaults to\nmin(cores / 2, class count). Incompatible with --worker\nand --list."
        },
        {
          "name": "--worker-timeout",
          "recursive": false,
          "hidden": false,
          "description": "Parallel mode only: kill a worker stuck on one class for\nmore than <s> seconds and report the class as failed\n(default 600).",
          "arguments": [
            {
              "name": "S",
              "required": true,
              "arity": {
                "minimum": 1,
                "maximum": 1
              }
            }
          ]
        },
        {
          "name": "--trx",
          "recursive": false,
          "hidden": false,
          "description": "Parallel mode only: write one aggregated VSTest-style TRX\nreport covering every class.",
          "arguments": [
            {
              "name": "PATH",
              "required": true,
              "arity": {
                "minimum": 1,
                "maximum": 1
              }
            }
          ]
        }
      ],
      "arguments": [
        {
          "name": "PATHTOSCENARIOSDLL",
          "required": true,
          "hidden": false,
          "arity": {
            "minimum": 1,
            "maximum": 1
          }
        },
        {
          "name": "BASELINETRX",
          "required": true,
          "hidden": false,
          "arity": {
            "minimum": 1,
            "maximum": 1
          }
        },
        {
          "name": "CANDIDATETRX",
          "required": true,
          "hidden": false,
          "arity": {
            "minimum": 1,
            "maximum": 1
          }
        },
        {
          "name": "PATHTOTEST_OUTPUT_OR_ASSEMBLYDLL",
          "required": true,
          "hidden": false,
          "arity": {
            "minimum": 1,
            "maximum": 1
          }
        }
      ],
      "commands": [
        {
          "name": "diff",
          "hidden": false,
          "description": "Compare two TRX reports by test name (baseline vs candidate; no server, no\nVINTAGE_STORY) and report what changed: new failures, fixed, vanished, new\ntests, still failing, and notable duration shifts (a passing test at least\n2x and at least 500 ms away from its baseline). Works on the TRX atlas\nwrites (`--parallel --trx`) and on any spec-conforming TRX (`dotnet test\n--logger trx`). Exit 0 when the candidate has no regressions, 1 when it\nhas (a regression is a new failure or a vanished test), 2 when a file\ncannot be read as TRX. Contract: docs/specs/2026-07-14-diff-command.md."
        },
        {
          "name": "fixture",
          "hidden": false,
          "description": "Run exactly ONE matching scenario as a world builder, then copy the world\nsave its graceful shutdown persisted to --out. The builder is an ordinary\n[AtlasScenario] whose side effect is building the world (place blocks, run\ncommands, seed data); a class then boots the produced file with\n[AtlasWorld(SaveFile = \"fixtures/myworld.vcdbs\")]. If the builder fails,\nno fixture is written."
        },
        {
          "name": "Options",
          "hidden": false,
          "description": "--filter <substring>   Only scenarios whose display name contains the substring\n(ordinal, case-insensitive).\n--list                 Print the discovered scenarios and exit without booting\nanything (VINTAGE_STORY not required).\n--worker               Report exclusively as line-delimited JSON events on stdout\n(the orchestrator protocol; see\ndocs/specs/2026-07-06-worker-protocol.md). With --list,\nemits one 'discovered' event per scenario instead.\n--classes <list>       Worker mode only: run only these scenario classes\n(comma-separated fully qualified names, exact match).\n--parallel [N]         Run the scenario classes on N worker subprocesses (one live\nserver per worker, one class per dispatch). N defaults to\nmin(cores / 2, class count). Incompatible with --worker\nand --list.\n--worker-timeout <s>   Parallel mode only: kill a worker stuck on one class for\nmore than <s> seconds and report the class as failed\n(default 600).\n--trx <path>           Parallel mode only: write one aggregated VSTest-style TRX\nreport covering every class."
        },
        {
          "name": "stage",
          "hidden": false,
          "description": "Pre-stage a compiled test output's VintagestoryAPI.dll+pdb (and, if the\ndirection calls for it, Newtonsoft.Json.dll) from VINTAGE_STORY, then\nexit: the same decision the module initializers run at boot (issue #49),\nbut BEFORE any process binds the engine assembly, so a one-shot script\nthat only runs each install once (no fail-then-rerun budget) can do\n`atlas stage out/ && VINTAGE_STORY=... dotnet test --no-build` and boot\ngreen on the first try. Prints one line per file (pair): staged, already\nidentical, or nothing to stage. Never boots a server and never loads\nVintagestoryAPI.dll or VintagestoryLib.dll itself."
        }
      ]
    },
    {
      "name": "stage",
      "hidden": false,
      "description": "Pre-stage a compiled test output's VintagestoryAPI.dll+pdb (and, if the\ndirection calls for it, Newtonsoft.Json.dll) from VINTAGE_STORY, then\nexit: the same decision the module initializers run at boot (issue #49),\nbut BEFORE any process binds the engine assembly, so a one-shot script\nthat only runs each install once (no fail-then-rerun budget) can do\n`atlas stage out/ && VINTAGE_STORY=... dotnet test --no-build` and boot\ngreen on the first try. Prints one line per file (pair): staged, already\nidentical, or nothing to stage. Never boots a server and never loads\nVintagestoryAPI.dll or VintagestoryLib.dll itself.",
      "options": [
        {
          "name": "--help",
          "recursive": false,
          "hidden": false,
          "description": "Show this help.",
          "aliases": [
            "-h"
          ]
        },
        {
          "name": "--version",
          "recursive": false,
          "hidden": false,
          "description": "Print the atlas version and exit (no assembly or\nVINTAGE_STORY required). `atlas version` works too.\nEnvironment:\nVINTAGE_STORY          Required by `run`, `fixture` and `stage`: the Vintage Story\ninstall directory containing VintagestoryLib.dll.\nExit codes:\n0  every scenario passed (or the listing succeeded, or the fixture was written, or\nthe diff found no regressions, or the stage found nothing to do or staged\ncleanly)\n1  at least one scenario failed or errored, or nothing matched the filter, or the\nfixture builder failed (no fixture is written then), or the diff found\nregressions (a new failure or a vanished test)\n2  usage or environment error (bad arguments, VINTAGE_STORY missing, a diff input\nthat cannot be read as TRX, or a stage target that cannot be staged: an\nunwritable output, an install without its pdb, a diverged copy already bound, or\nthe Newtonsoft direction refusal)"
        },
        {
          "name": "--filter",
          "recursive": false,
          "hidden": false,
          "description": "Only scenarios whose display name contains the substring\n(ordinal, case-insensitive).",
          "arguments": [
            {
              "name": "SUBSTRING",
              "required": true,
              "arity": {
                "minimum": 1,
                "maximum": 1
              }
            }
          ]
        },
        {
          "name": "--list",
          "recursive": false,
          "hidden": false,
          "description": "Print the discovered scenarios and exit without booting\nanything (VINTAGE_STORY not required)."
        },
        {
          "name": "--worker",
          "recursive": false,
          "hidden": false,
          "description": "Report exclusively as line-delimited JSON events on stdout\n(the orchestrator protocol; see\ndocs/specs/2026-07-06-worker-protocol.md). With --list,\nemits one 'discovered' event per scenario instead."
        },
        {
          "name": "--classes",
          "recursive": false,
          "hidden": false,
          "description": "Worker mode only: run only these scenario classes\n(comma-separated fully qualified names, exact match).",
          "arguments": [
            {
              "name": "LIST",
              "required": true,
              "arity": {
                "minimum": 1,
                "maximum": 1
              }
            }
          ]
        },
        {
          "name": "--parallel",
          "recursive": false,
          "hidden": false,
          "description": "Run the scenario classes on N worker subprocesses (one live\nserver per worker, one class per dispatch). N defaults to\nmin(cores / 2, class count). Incompatible with --worker\nand --list."
        },
        {
          "name": "--worker-timeout",
          "recursive": false,
          "hidden": false,
          "description": "Parallel mode only: kill a worker stuck on one class for\nmore than <s> seconds and report the class as failed\n(default 600).",
          "arguments": [
            {
              "name": "S",
              "required": true,
              "arity": {
                "minimum": 1,
                "maximum": 1
              }
            }
          ]
        },
        {
          "name": "--trx",
          "recursive": false,
          "hidden": false,
          "description": "Parallel mode only: write one aggregated VSTest-style TRX\nreport covering every class.",
          "arguments": [
            {
              "name": "PATH",
              "required": true,
              "arity": {
                "minimum": 1,
                "maximum": 1
              }
            }
          ]
        }
      ],
      "arguments": [
        {
          "name": "PATHTOSCENARIOSDLL",
          "required": true,
          "hidden": false,
          "arity": {
            "minimum": 1,
            "maximum": 1
          }
        },
        {
          "name": "BASELINETRX",
          "required": true,
          "hidden": false,
          "arity": {
            "minimum": 1,
            "maximum": 1
          }
        },
        {
          "name": "CANDIDATETRX",
          "required": true,
          "hidden": false,
          "arity": {
            "minimum": 1,
            "maximum": 1
          }
        },
        {
          "name": "PATHTOTEST_OUTPUT_OR_ASSEMBLYDLL",
          "required": true,
          "hidden": false,
          "arity": {
            "minimum": 1,
            "maximum": 1
          }
        }
      ],
      "commands": [
        {
          "name": "diff",
          "hidden": false,
          "description": "Compare two TRX reports by test name (baseline vs candidate; no server, no\nVINTAGE_STORY) and report what changed: new failures, fixed, vanished, new\ntests, still failing, and notable duration shifts (a passing test at least\n2x and at least 500 ms away from its baseline). Works on the TRX atlas\nwrites (`--parallel --trx`) and on any spec-conforming TRX (`dotnet test\n--logger trx`). Exit 0 when the candidate has no regressions, 1 when it\nhas (a regression is a new failure or a vanished test), 2 when a file\ncannot be read as TRX. Contract: docs/specs/2026-07-14-diff-command.md."
        },
        {
          "name": "fixture",
          "hidden": false,
          "description": "Run exactly ONE matching scenario as a world builder, then copy the world\nsave its graceful shutdown persisted to --out. The builder is an ordinary\n[AtlasScenario] whose side effect is building the world (place blocks, run\ncommands, seed data); a class then boots the produced file with\n[AtlasWorld(SaveFile = \"fixtures/myworld.vcdbs\")]. If the builder fails,\nno fixture is written."
        },
        {
          "name": "Options",
          "hidden": false,
          "description": "--filter <substring>   Only scenarios whose display name contains the substring\n(ordinal, case-insensitive).\n--list                 Print the discovered scenarios and exit without booting\nanything (VINTAGE_STORY not required).\n--worker               Report exclusively as line-delimited JSON events on stdout\n(the orchestrator protocol; see\ndocs/specs/2026-07-06-worker-protocol.md). With --list,\nemits one 'discovered' event per scenario instead.\n--classes <list>       Worker mode only: run only these scenario classes\n(comma-separated fully qualified names, exact match).\n--parallel [N]         Run the scenario classes on N worker subprocesses (one live\nserver per worker, one class per dispatch). N defaults to\nmin(cores / 2, class count). Incompatible with --worker\nand --list.\n--worker-timeout <s>   Parallel mode only: kill a worker stuck on one class for\nmore than <s> seconds and report the class as failed\n(default 600).\n--trx <path>           Parallel mode only: write one aggregated VSTest-style TRX\nreport covering every class."
        },
        {
          "name": "run",
          "hidden": false,
          "description": "Build nothing, boot the embedded server(s) in-process, and execute the\nassembly's [AtlasScenario] methods sequentially, like `dotnet test` would."
        }
      ]
    }
  ],
  "options": [
    {
      "name": "--help",
      "recursive": false,
      "hidden": false,
      "description": "Show this help.",
      "aliases": [
        "-h"
      ]
    },
    {
      "name": "--version",
      "recursive": false,
      "hidden": false,
      "description": "Print the atlas version and exit (no assembly or\nVINTAGE_STORY required). `atlas version` works too.\nEnvironment:\nVINTAGE_STORY          Required by `run`, `fixture` and `stage`: the Vintage Story\ninstall directory containing VintagestoryLib.dll.\nExit codes:\n0  every scenario passed (or the listing succeeded, or the fixture was written, or\nthe diff found no regressions, or the stage found nothing to do or staged\ncleanly)\n1  at least one scenario failed or errored, or nothing matched the filter, or the\nfixture builder failed (no fixture is written then), or the diff found\nregressions (a new failure or a vanished test)\n2  usage or environment error (bad arguments, VINTAGE_STORY missing, a diff input\nthat cannot be read as TRX, or a stage target that cannot be staged: an\nunwritable output, an install without its pdb, a diverged copy already bound, or\nthe Newtonsoft direction refusal)"
    },
    {
      "name": "--filter",
      "recursive": false,
      "hidden": false,
      "description": "Only scenarios whose display name contains the substring\n(ordinal, case-insensitive).",
      "arguments": [
        {
          "name": "SUBSTRING",
          "required": true,
          "arity": {
            "minimum": 1,
            "maximum": 1
          }
        }
      ]
    },
    {
      "name": "--list",
      "recursive": false,
      "hidden": false,
      "description": "Print the discovered scenarios and exit without booting\nanything (VINTAGE_STORY not required)."
    },
    {
      "name": "--worker",
      "recursive": false,
      "hidden": false,
      "description": "Report exclusively as line-delimited JSON events on stdout\n(the orchestrator protocol; see\ndocs/specs/2026-07-06-worker-protocol.md). With --list,\nemits one 'discovered' event per scenario instead."
    },
    {
      "name": "--classes",
      "recursive": false,
      "hidden": false,
      "description": "Worker mode only: run only these scenario classes\n(comma-separated fully qualified names, exact match).",
      "arguments": [
        {
          "name": "LIST",
          "required": true,
          "arity": {
            "minimum": 1,
            "maximum": 1
          }
        }
      ]
    },
    {
      "name": "--parallel",
      "recursive": false,
      "hidden": false,
      "description": "Run the scenario classes on N worker subprocesses (one live\nserver per worker, one class per dispatch). N defaults to\nmin(cores / 2, class count). Incompatible with --worker\nand --list."
    },
    {
      "name": "--worker-timeout",
      "recursive": false,
      "hidden": false,
      "description": "Parallel mode only: kill a worker stuck on one class for\nmore than <s> seconds and report the class as failed\n(default 600).",
      "arguments": [
        {
          "name": "S",
          "required": true,
          "arity": {
            "minimum": 1,
            "maximum": 1
          }
        }
      ]
    },
    {
      "name": "--trx",
      "recursive": false,
      "hidden": false,
      "description": "Parallel mode only: write one aggregated VSTest-style TRX\nreport covering every class.",
      "arguments": [
        {
          "name": "PATH",
          "required": true,
          "arity": {
            "minimum": 1,
            "maximum": 1
          }
        }
      ]
    }
  ]
}
