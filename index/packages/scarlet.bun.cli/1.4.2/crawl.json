{
  "commands": [
    {
      "command": null,
      "helpInvocation": "--help",
      "result": {
        "status": "ok",
        "timedOut": false,
        "exitCode": 0,
        "durationMs": 45,
        "stdout": "Bun is a fast JavaScript runtime, package manager, bundler, and test runner. (1.4.2+744846f84)\n\nUsage: bun <command> [...flags] [...args]\n\nCommands:\n  run       ./my-script.ts       Execute a file with Bun\n            lint                 Run a package.json script\n  test                           Run unit tests with Bun\n  x         eslint               Execute a package binary (CLI), installing if needed (bunx)\n  repl                           Start a REPL session with Bun\n  exec                           Run a shell script directly with Bun\n\n  install                        Install dependencies for a package.json (bun i)\n  add       lyra                 Add a dependency to package.json (bun a)\n  remove    is-array             Remove a dependency from package.json (bun rm)\n  update    @remix-run/dev       Update outdated dependencies\n  audit                          Check installed packages for vulnerabilities\n  dedupe                         Remove duplicate versions from the lockfile\n  prune                          Remove packages that are not in the lockfile from node_modules\n  outdated                       Display latest versions of outdated dependencies\n  link      [<package>]          Register or link a local npm package\n  unlink                         Unregister a local npm package\n  publish                        Publish a package to the npm registry\n  patch <pkg>                    Prepare a package for patching\n  pm <subcommand>                Additional package management utilities\n  info      @evan/duckdb         Display package metadata from the registry\n  why       @zarfjs/zarf         Explain why a package is installed\n\n  build     ./a.ts ./b.jsx       Bundle TypeScript & JavaScript into a single file\n\n  init                           Start an empty Bun project from a built-in template\n  create    vite                 Create a new project from a template (bun c)\n  upgrade                        Upgrade to latest version of Bun.\n\n  <command> --help               Print help text for command.\n\nFlag\n[truncated]",
        "stderr": null,
        "outputLimitExceeded": false
      },
      "parsed": true,
      "payload": "Bun is a fast JavaScript runtime, package manager, bundler, and test runner. (1.4.2+744846f84)\n\nUsage: bun <command> [...flags] [...args]\n\nCommands:\n  run       ./my-script.ts       Execute a file with Bun\n            lint                 Run a package.json script\n  test                           Run unit tests with Bun\n  x         eslint               Execute a package binary (CLI), installing if needed (bunx)\n  repl                           Start a REPL session with Bun\n  exec                           Run a shell script directly with Bun\n\n  install                        Install dependencies for a package.json (bun i)\n  add       lyra                 Add a dependency to package.json (bun a)\n  remove    is-array             Remove a dependency from package.json (bun rm)\n  update    @remix-run/dev       Update outdated dependencies\n  audit                          Check installed packages for vulnerabilities\n  dedupe                         Remove duplicate versions from the lockfile\n  prune                          Remove packages that are not in the lockfile from node_modules\n  outdated                       Display latest versions of outdated dependencies\n  link      [<package>]          Register or link a local npm package\n  unlink                         Unregister a local npm package\n  publish                        Publish a package to the npm registry\n  patch <pkg>                    Prepare a package for patching\n  pm <subcommand>                Additional package management utilities\n  info      @evan/duckdb         Display package metadata from the registry\n  why       @zarfjs/zarf         Explain why a package is installed\n\n  build     ./a.ts ./b.jsx       Bundle TypeScript & JavaScript into a single file\n\n  init                           Start an empty Bun project from a built-in template\n  create    vite                 Create a new project from a template (bun c)\n  upgrade                        Upgrade to latest version of Bun.\n\n  <command> --help               Print help text for command.\n\nFlag\n[truncated]",
      "terminalNonHelp": false
    },
    {
      "command": "lint",
      "helpInvocation": "lint --help",
      "result": {
        "status": "failed",
        "timedOut": false,
        "exitCode": 1,
        "durationMs": 40,
        "stdout": null,
        "stderr": "error: Script not found \"lint\"",
        "outputLimitExceeded": false
      },
      "parsed": false,
      "payload": "error: Script not found \"lint\"",
      "terminalNonHelp": false
    },
    {
      "command": "test",
      "helpInvocation": "test --help",
      "result": {
        "status": "ok",
        "timedOut": false,
        "exitCode": 0,
        "durationMs": 44,
        "stdout": "Usage: bun test [flags] [<patterns>]\n  Run all matching test files and print the results to stdout\n\nFlags:\n      --no-orphans                    Exit when the parent process dies, and on exit kill every descendant.\n      --timeout=<val>                 Set the per-test timeout in milliseconds, default is 5000.\n  -u, --update-snapshots              Update snapshot files\n      --rerun-each=<val>              Re-run each test file <NUMBER> times, helps catch certain bugs\n      --retry=<val>                   Default retry count for all tests, overridden by per-test { retry: N }\n      --todo                          Include tests that are marked with \"test.todo()\"\n      --only                          Run only tests that are marked with \"test.only()\" or \"describe.only()\"\n      --pass-with-no-tests            Exit with code 0 when no tests are found\n      --concurrent                    Treat all tests as `test.concurrent()` tests\n      --randomize                     Run tests in random order\n      --seed=<val>                    Set the random seed for test randomization\n      --coverage                      Generate a coverage profile\n      --coverage-reporter=<val>       Report coverage in 'text' and/or 'lcov'. Defaults to 'text'.\n      --coverage-dir=<val>            Directory for coverage files. Defaults to 'coverage'.\n      --bail=<val>                    Exit the test suite after <NUMBER> failures. If you do not specify a number, it defaults to 1.\n  -t, --test-name-pattern=<val>       Run only tests with a name that matches the given regex.\n      --reporter=<val>                Test output reporter format. Available: 'junit' (requires --reporter-outfile), 'dots'. Default: console output.\n      --reporter-outfile=<val>        Output file path for the reporter format (required with --reporter).\n      --dots                          Enable dots reporter. Shorthand for --reporter=dots.\n      --only-failures                 Only display test failures, hiding passing tests.\n      --max-concurrency=<val>         Maxi\n[truncated]",
        "stderr": null,
        "outputLimitExceeded": false
      },
      "parsed": true,
      "payload": "Usage: bun test [flags] [<patterns>]\n  Run all matching test files and print the results to stdout\n\nFlags:\n      --no-orphans                    Exit when the parent process dies, and on exit kill every descendant.\n      --timeout=<val>                 Set the per-test timeout in milliseconds, default is 5000.\n  -u, --update-snapshots              Update snapshot files\n      --rerun-each=<val>              Re-run each test file <NUMBER> times, helps catch certain bugs\n      --retry=<val>                   Default retry count for all tests, overridden by per-test { retry: N }\n      --todo                          Include tests that are marked with \"test.todo()\"\n      --only                          Run only tests that are marked with \"test.only()\" or \"describe.only()\"\n      --pass-with-no-tests            Exit with code 0 when no tests are found\n      --concurrent                    Treat all tests as `test.concurrent()` tests\n      --randomize                     Run tests in random order\n      --seed=<val>                    Set the random seed for test randomization\n      --coverage                      Generate a coverage profile\n      --coverage-reporter=<val>       Report coverage in 'text' and/or 'lcov'. Defaults to 'text'.\n      --coverage-dir=<val>            Directory for coverage files. Defaults to 'coverage'.\n      --bail=<val>                    Exit the test suite after <NUMBER> failures. If you do not specify a number, it defaults to 1.\n  -t, --test-name-pattern=<val>       Run only tests with a name that matches the given regex.\n      --reporter=<val>                Test output reporter format. Available: 'junit' (requires --reporter-outfile), 'dots'. Default: console output.\n      --reporter-outfile=<val>        Output file path for the reporter format (required with --reporter).\n      --dots                          Enable dots reporter. Shorthand for --reporter=dots.\n      --only-failures                 Only display test failures, hiding passing tests.\n      --max-concurrency=<val>         Maxi\n[truncated]",
      "terminalNonHelp": false
    },
    {
      "command": "x",
      "helpInvocation": "x --help",
      "result": {
        "status": "failed",
        "timedOut": false,
        "exitCode": 1,
        "durationMs": 40,
        "stdout": null,
        "stderr": "Usage: bunx [flags] <package><@version> [flags and arguments for the package]\nExecute an npm package executable (CLI), automatically installing into a global shared cache if not installed in node_modules.\n\nFlags:\n  --bun                  Force the command to run with Bun instead of Node.js\n  -p, --package <package>    Specify package to install when binary name differs from package name\n  --no-install           Skip installation if package is not already installed\n  --verbose              Enable verbose output during installation\n  --silent               Suppress output during installation\n\nExamples:\n  bunx prisma migrate\n  bunx prettier foo.js\n  bunx -p @angular/cli ng new my-app\n  bunx --bun vite dev foo.js",
        "outputLimitExceeded": false
      },
      "parsed": true,
      "payload": "Usage: bunx [flags] <package><@version> [flags and arguments for the package]\nExecute an npm package executable (CLI), automatically installing into a global shared cache if not installed in node_modules.\n\nFlags:\n  --bun                  Force the command to run with Bun instead of Node.js\n  -p, --package <package>    Specify package to install when binary name differs from package name\n  --no-install           Skip installation if package is not already installed\n  --verbose              Enable verbose output during installation\n  --silent               Suppress output during installation\n\nExamples:\n  bunx prisma migrate\n  bunx prettier foo.js\n  bunx -p @angular/cli ng new my-app\n  bunx --bun vite dev foo.js",
      "terminalNonHelp": false
    },
    {
      "command": "repl",
      "helpInvocation": "repl --help",
      "result": {
        "status": "ok",
        "timedOut": false,
        "exitCode": 0,
        "durationMs": 43,
        "stdout": "Usage: bun run [flags] <file or script>\n\nFlags:\n      --silent                        Don't print the script command\n      --elide-lines=<val>             Number of lines of script output shown when using --filter (default: 0, show all lines)\n  -F, --filter=<val>                  Run a script in all workspace packages matching the pattern\n  -b, --bun                           Force a script or package to use Bun's runtime instead of Node.js (via symlinking node)\n      --no-orphans                    Exit when the parent process dies, and on exit kill every descendant.\n      --shell=<val>                   Control the shell used for package.json scripts. Supports either 'bun' or 'system'\n      --workspaces                    Run a script in all workspace packages (from the \"workspaces\" field in package.json)\n      --parallel                      Run multiple scripts concurrently with Foreman-style output\n      --sequential                    Run multiple scripts sequentially with Foreman-style output\n      --no-exit-on-error              Continue running other scripts when one fails (with --parallel/--sequential)\n      --watch                         Automatically restart the process on file change\n      --watch-kill-signal=<val>       Signal whose handlers run when --watch restarts the process (default: \"SIGTERM\")\n      --hot                           Enable auto reload in the Bun runtime, test runner, or bundler\n      --no-clear-screen               Disable clearing the terminal screen on reload when --hot or --watch is enabled\n      --smol                          Use less memory, but run garbage collection more often\n      --interactive                   Start a Node.js-compatible REPL, like node --interactive\n  -r, --preload=<val>                 Import a module before other modules are loaded\n      --require=<val>                 Alias of --preload, for Node.js compatibility\n      --import=<val>                  Alias of --preload, for Node.js compatibility\n      --inspect=<val>                 Activate Bun'\n[truncated]",
        "stderr": null,
        "outputLimitExceeded": false
      },
      "parsed": true,
      "payload": "Usage: bun run [flags] <file or script>\n\nFlags:\n      --silent                        Don't print the script command\n      --elide-lines=<val>             Number of lines of script output shown when using --filter (default: 0, show all lines)\n  -F, --filter=<val>                  Run a script in all workspace packages matching the pattern\n  -b, --bun                           Force a script or package to use Bun's runtime instead of Node.js (via symlinking node)\n      --no-orphans                    Exit when the parent process dies, and on exit kill every descendant.\n      --shell=<val>                   Control the shell used for package.json scripts. Supports either 'bun' or 'system'\n      --workspaces                    Run a script in all workspace packages (from the \"workspaces\" field in package.json)\n      --parallel                      Run multiple scripts concurrently with Foreman-style output\n      --sequential                    Run multiple scripts sequentially with Foreman-style output\n      --no-exit-on-error              Continue running other scripts when one fails (with --parallel/--sequential)\n      --watch                         Automatically restart the process on file change\n      --watch-kill-signal=<val>       Signal whose handlers run when --watch restarts the process (default: \"SIGTERM\")\n      --hot                           Enable auto reload in the Bun runtime, test runner, or bundler\n      --no-clear-screen               Disable clearing the terminal screen on reload when --hot or --watch is enabled\n      --smol                          Use less memory, but run garbage collection more often\n      --interactive                   Start a Node.js-compatible REPL, like node --interactive\n  -r, --preload=<val>                 Import a module before other modules are loaded\n      --require=<val>                 Alias of --preload, for Node.js compatibility\n      --import=<val>                  Alias of --preload, for Node.js compatibility\n      --inspect=<val>                 Activate Bun'\n[truncated]",
      "terminalNonHelp": false
    },
    {
      "command": "exec",
      "helpInvocation": "exec --help",
      "result": {
        "status": "ok",
        "timedOut": false,
        "exitCode": 0,
        "durationMs": 45,
        "stdout": "Usage: bun exec <script>\n\nExecute a shell script directly from Bun.\n\nNote: If executing this from a shell, make sure to escape the string!\n\nExamples:\n  bun exec \"echo hi\"\n  bun exec \"echo \\\"hey friends\\\"!\"",
        "stderr": null,
        "outputLimitExceeded": false
      },
      "parsed": true,
      "payload": "Usage: bun exec <script>\n\nExecute a shell script directly from Bun.\n\nNote: If executing this from a shell, make sure to escape the string!\n\nExamples:\n  bun exec \"echo hi\"\n  bun exec \"echo \\\"hey friends\\\"!\"",
      "terminalNonHelp": false
    },
    {
      "command": "install",
      "helpInvocation": "install --help",
      "result": {
        "status": "ok",
        "timedOut": false,
        "exitCode": 0,
        "durationMs": 43,
        "stdout": "Usage: bun install [flags] <name>@<version>\nAlias: bun i\n\n  Install the dependencies listed in package.json.\n\nFlags:\n  -c, --config=<val>                 Specify path to config file (bunfig.toml)\n  -y, --yarn                         Write a yarn.lock file (yarn v1)\n  -p, --production                   Don't install devDependencies\n      --no-save                      Don't update package.json or save a lockfile\n      --save                         Save to package.json (true by default)\n      --ca=<val>                     Provide a Certificate Authority signing certificate\n      --cafile=<val>                 The same as `--ca`, but is a file path to the certificate\n      --dry-run                      Perform a dry run without making changes\n      --frozen-lockfile              Disallow changes to lockfile\n  -f, --force                        Always request the latest versions from the registry & reinstall all dependencies\n      --cache-dir=<val>              Store & load cached data from a specific directory path\n      --no-cache                     Ignore manifest cache entirely\n      --silent                       Don't log anything\n      --quiet                        Only show tarball name when packing\n      --verbose                      Excessively verbose logging\n      --no-progress                  Disable the progress bar\n      --no-summary                   Don't print a summary\n      --no-verify                    Skip verifying integrity of newly downloaded packages\n      --offline                      Never touch the network: resolve and install only from the local cache\n      --prefer-offline               Use cached package metadata regardless of age; only fetch what is missing\n      --ignore-scripts               Skip lifecycle scripts in the project's package.json (dependency scripts are never run)\n      --trust                        Add to trustedDependencies in the project's package.json and install the package(s)\n  -g, --global                       Install globally\n      --cwd=<val>       \n[truncated]",
        "stderr": null,
        "outputLimitExceeded": false
      },
      "parsed": false,
      "payload": "Usage: bun install [flags] <name>@<version>\nAlias: bun i\n\n  Install the dependencies listed in package.json.\n\nFlags:\n  -c, --config=<val>                 Specify path to config file (bunfig.toml)\n  -y, --yarn                         Write a yarn.lock file (yarn v1)\n  -p, --production                   Don't install devDependencies\n      --no-save                      Don't update package.json or save a lockfile\n      --save                         Save to package.json (true by default)\n      --ca=<val>                     Provide a Certificate Authority signing certificate\n      --cafile=<val>                 The same as `--ca`, but is a file path to the certificate\n      --dry-run                      Perform a dry run without making changes\n      --frozen-lockfile              Disallow changes to lockfile\n  -f, --force                        Always request the latest versions from the registry & reinstall all dependencies\n      --cache-dir=<val>              Store & load cached data from a specific directory path\n      --no-cache                     Ignore manifest cache entirely\n      --silent                       Don't log anything\n      --quiet                        Only show tarball name when packing\n      --verbose                      Excessively verbose logging\n      --no-progress                  Disable the progress bar\n      --no-summary                   Don't print a summary\n      --no-verify                    Skip verifying integrity of newly downloaded packages\n      --offline                      Never touch the network: resolve and install only from the local cache\n      --prefer-offline               Use cached package metadata regardless of age; only fetch what is missing\n      --ignore-scripts               Skip lifecycle scripts in the project's package.json (dependency scripts are never run)\n      --trust                        Add to trustedDependencies in the project's package.json and install the package(s)\n  -g, --global                       Install globally\n      --cwd=<val>       \n[truncated]",
      "terminalNonHelp": true
    },
    {
      "command": "add",
      "helpInvocation": "add --help",
      "result": {
        "status": "ok",
        "timedOut": false,
        "exitCode": 0,
        "durationMs": 40,
        "stdout": "Usage: bun add [flags] <package><@version>\nAlias: bun a\n\n  Add a new dependency to package.json and install it.\n\nFlags:\n  -c, --config=<val>                 Specify path to config file (bunfig.toml)\n  -y, --yarn                         Write a yarn.lock file (yarn v1)\n  -p, --production                   Don't install devDependencies\n      --no-save                      Don't update package.json or save a lockfile\n      --save                         Save to package.json (true by default)\n      --ca=<val>                     Provide a Certificate Authority signing certificate\n      --cafile=<val>                 The same as `--ca`, but is a file path to the certificate\n      --dry-run                      Perform a dry run without making changes\n      --frozen-lockfile              Disallow changes to lockfile\n  -f, --force                        Always request the latest versions from the registry & reinstall all dependencies\n      --cache-dir=<val>              Store & load cached data from a specific directory path\n      --no-cache                     Ignore manifest cache entirely\n      --silent                       Don't log anything\n      --quiet                        Only show tarball name when packing\n      --verbose                      Excessively verbose logging\n      --no-progress                  Disable the progress bar\n      --no-summary                   Don't print a summary\n      --no-verify                    Skip verifying integrity of newly downloaded packages\n      --offline                      Never touch the network: resolve and install only from the local cache\n      --prefer-offline               Use cached package metadata regardless of age; only fetch what is missing\n      --ignore-scripts               Skip lifecycle scripts in the project's package.json (dependency scripts are never run)\n      --trust                        Add to trustedDependencies in the project's package.json and install the package(s)\n  -g, --global                       Install globally\n      --cwd=<val>    \n[truncated]",
        "stderr": null,
        "outputLimitExceeded": false
      },
      "parsed": false,
      "payload": "Usage: bun add [flags] <package><@version>\nAlias: bun a\n\n  Add a new dependency to package.json and install it.\n\nFlags:\n  -c, --config=<val>                 Specify path to config file (bunfig.toml)\n  -y, --yarn                         Write a yarn.lock file (yarn v1)\n  -p, --production                   Don't install devDependencies\n      --no-save                      Don't update package.json or save a lockfile\n      --save                         Save to package.json (true by default)\n      --ca=<val>                     Provide a Certificate Authority signing certificate\n      --cafile=<val>                 The same as `--ca`, but is a file path to the certificate\n      --dry-run                      Perform a dry run without making changes\n      --frozen-lockfile              Disallow changes to lockfile\n  -f, --force                        Always request the latest versions from the registry & reinstall all dependencies\n      --cache-dir=<val>              Store & load cached data from a specific directory path\n      --no-cache                     Ignore manifest cache entirely\n      --silent                       Don't log anything\n      --quiet                        Only show tarball name when packing\n      --verbose                      Excessively verbose logging\n      --no-progress                  Disable the progress bar\n      --no-summary                   Don't print a summary\n      --no-verify                    Skip verifying integrity of newly downloaded packages\n      --offline                      Never touch the network: resolve and install only from the local cache\n      --prefer-offline               Use cached package metadata regardless of age; only fetch what is missing\n      --ignore-scripts               Skip lifecycle scripts in the project's package.json (dependency scripts are never run)\n      --trust                        Add to trustedDependencies in the project's package.json and install the package(s)\n  -g, --global                       Install globally\n      --cwd=<val>    \n[truncated]",
      "terminalNonHelp": true
    },
    {
      "command": "remove",
      "helpInvocation": "remove --help",
      "result": {
        "status": "ok",
        "timedOut": false,
        "exitCode": 0,
        "durationMs": 40,
        "stdout": "Usage: bun remove [flags] [<packages>]\nAlias: bun r\n\n  Remove a package from package.json and uninstall from node_modules.\n\nFlags:\n  -c, --config=<val>                 Specify path to config file (bunfig.toml)\n  -y, --yarn                         Write a yarn.lock file (yarn v1)\n  -p, --production                   Don't install devDependencies\n      --no-save                      Don't update package.json or save a lockfile\n      --save                         Save to package.json (true by default)\n      --ca=<val>                     Provide a Certificate Authority signing certificate\n      --cafile=<val>                 The same as `--ca`, but is a file path to the certificate\n      --dry-run                      Perform a dry run without making changes\n      --frozen-lockfile              Disallow changes to lockfile\n  -f, --force                        Always request the latest versions from the registry & reinstall all dependencies\n      --cache-dir=<val>              Store & load cached data from a specific directory path\n      --no-cache                     Ignore manifest cache entirely\n      --silent                       Don't log anything\n      --quiet                        Only show tarball name when packing\n      --verbose                      Excessively verbose logging\n      --no-progress                  Disable the progress bar\n      --no-summary                   Don't print a summary\n      --no-verify                    Skip verifying integrity of newly downloaded packages\n      --offline                      Never touch the network: resolve and install only from the local cache\n      --prefer-offline               Use cached package metadata regardless of age; only fetch what is missing\n      --ignore-scripts               Skip lifecycle scripts in the project's package.json (dependency scripts are never run)\n      --trust                        Add to trustedDependencies in the project's package.json and install the package(s)\n  -g, --global                       Install globally\n      --cw\n[truncated]",
        "stderr": null,
        "outputLimitExceeded": false
      },
      "parsed": false,
      "payload": "Usage: bun remove [flags] [<packages>]\nAlias: bun r\n\n  Remove a package from package.json and uninstall from node_modules.\n\nFlags:\n  -c, --config=<val>                 Specify path to config file (bunfig.toml)\n  -y, --yarn                         Write a yarn.lock file (yarn v1)\n  -p, --production                   Don't install devDependencies\n      --no-save                      Don't update package.json or save a lockfile\n      --save                         Save to package.json (true by default)\n      --ca=<val>                     Provide a Certificate Authority signing certificate\n      --cafile=<val>                 The same as `--ca`, but is a file path to the certificate\n      --dry-run                      Perform a dry run without making changes\n      --frozen-lockfile              Disallow changes to lockfile\n  -f, --force                        Always request the latest versions from the registry & reinstall all dependencies\n      --cache-dir=<val>              Store & load cached data from a specific directory path\n      --no-cache                     Ignore manifest cache entirely\n      --silent                       Don't log anything\n      --quiet                        Only show tarball name when packing\n      --verbose                      Excessively verbose logging\n      --no-progress                  Disable the progress bar\n      --no-summary                   Don't print a summary\n      --no-verify                    Skip verifying integrity of newly downloaded packages\n      --offline                      Never touch the network: resolve and install only from the local cache\n      --prefer-offline               Use cached package metadata regardless of age; only fetch what is missing\n      --ignore-scripts               Skip lifecycle scripts in the project's package.json (dependency scripts are never run)\n      --trust                        Add to trustedDependencies in the project's package.json and install the package(s)\n  -g, --global                       Install globally\n      --cw\n[truncated]",
      "terminalNonHelp": true
    },
    {
      "command": "audit",
      "helpInvocation": "audit --help",
      "result": {
        "status": "ok",
        "timedOut": false,
        "exitCode": 0,
        "durationMs": 47,
        "stdout": "Usage: bun audit [flags]\n\n  Check installed packages for vulnerabilities.\n  bun audit fix upgrades vulnerable packages to the lowest safe version that still satisfies every dependent's range.\n\nFlags:\n      --json                 Output in JSON format\n      --audit-level=<val>    Only print advisories with severity greater than or equal to <level> (low, moderate, high, critical)\n      --ignore=<val>         Ignore advisories by GHSA or numeric advisory ID (repeatable)\n  -L, --latest               Also apply fixes your declared ranges exclude, rewriting package.json\n\nExamples:\n  Check the current project's packages for vulnerabilities.\n  bun audit\n\n  Output package vulnerabilities in JSON format.\n  bun audit --json\n\n  Upgrade vulnerable packages in bun.lock and node_modules; package.json is only changed when an exact pin has to be bumped.\n  bun audit fix\n\n  Show what bun audit fix would change without changing anything.\n  bun audit fix --dry-run\n\n  Also apply fixes that your package.json ranges exclude, rewriting those ranges.\n  bun audit fix --latest\n\nFull documentation is available at https://bun.com/docs/install/audit.",
        "stderr": null,
        "outputLimitExceeded": false
      },
      "parsed": true,
      "payload": "Usage: bun audit [flags]\n\n  Check installed packages for vulnerabilities.\n  bun audit fix upgrades vulnerable packages to the lowest safe version that still satisfies every dependent's range.\n\nFlags:\n      --json                 Output in JSON format\n      --audit-level=<val>    Only print advisories with severity greater than or equal to <level> (low, moderate, high, critical)\n      --ignore=<val>         Ignore advisories by GHSA or numeric advisory ID (repeatable)\n  -L, --latest               Also apply fixes your declared ranges exclude, rewriting package.json\n\nExamples:\n  Check the current project's packages for vulnerabilities.\n  bun audit\n\n  Output package vulnerabilities in JSON format.\n  bun audit --json\n\n  Upgrade vulnerable packages in bun.lock and node_modules; package.json is only changed when an exact pin has to be bumped.\n  bun audit fix\n\n  Show what bun audit fix would change without changing anything.\n  bun audit fix --dry-run\n\n  Also apply fixes that your package.json ranges exclude, rewriting those ranges.\n  bun audit fix --latest\n\nFull documentation is available at https://bun.com/docs/install/audit.",
      "terminalNonHelp": false
    },
    {
      "command": "dedupe",
      "helpInvocation": "dedupe --help",
      "result": {
        "status": "ok",
        "timedOut": false,
        "exitCode": 0,
        "durationMs": 39,
        "stdout": "Usage: bun dedupe [flags]\n\n  Remove duplicate versions from bun.lock by re-resolving dependency ranges onto versions that are already in the lockfile, then install.\n\nFlags:\n      --check              Exit with code 1 if the lockfile has duplicate versions that can be removed, without changing anything\n      --dry-run            Print the duplicate versions that would be removed without changing anything\n      --lockfile-only      Rewrite bun.lock without installing\n      --frozen-lockfile    Fail instead of rewriting bun.lock when duplicate versions can be removed\n      --linker=<val>       Install with the given linker (one of \"isolated\" or \"hoisted\")\n      --silent             Don't log anything\n      --cwd=<val>          Set a specific cwd\n  -h, --help               Print this help menu\n\nExamples:\n  Remove duplicate versions and install\n  bun dedupe\n\n  Only report removable duplicates; exit code 1 if there are any (for CI)\n  bun dedupe --check\n\n  Show what would be removed without changing anything\n  bun dedupe --dry-run\n\n  Rewrite bun.lock without installing\n  bun dedupe --lockfile-only\n\nFull documentation is available at https://bun.com/docs/pm/cli/dedupe.",
        "stderr": null,
        "outputLimitExceeded": false
      },
      "parsed": true,
      "payload": "Usage: bun dedupe [flags]\n\n  Remove duplicate versions from bun.lock by re-resolving dependency ranges onto versions that are already in the lockfile, then install.\n\nFlags:\n      --check              Exit with code 1 if the lockfile has duplicate versions that can be removed, without changing anything\n      --dry-run            Print the duplicate versions that would be removed without changing anything\n      --lockfile-only      Rewrite bun.lock without installing\n      --frozen-lockfile    Fail instead of rewriting bun.lock when duplicate versions can be removed\n      --linker=<val>       Install with the given linker (one of \"isolated\" or \"hoisted\")\n      --silent             Don't log anything\n      --cwd=<val>          Set a specific cwd\n  -h, --help               Print this help menu\n\nExamples:\n  Remove duplicate versions and install\n  bun dedupe\n\n  Only report removable duplicates; exit code 1 if there are any (for CI)\n  bun dedupe --check\n\n  Show what would be removed without changing anything\n  bun dedupe --dry-run\n\n  Rewrite bun.lock without installing\n  bun dedupe --lockfile-only\n\nFull documentation is available at https://bun.com/docs/pm/cli/dedupe.",
      "terminalNonHelp": false
    },
    {
      "command": "prune",
      "helpInvocation": "prune --help",
      "result": {
        "status": "ok",
        "timedOut": false,
        "exitCode": 0,
        "durationMs": 53,
        "stdout": "Usage: bun prune [flags]\n\n  Remove packages from node_modules that are not in bun.lock. With --production, also remove packages that are only needed by devDependencies.\n\nFlags:\n  -p, --production      Also remove packages that are only needed by devDependencies (alias: --prod)\n      --omit=<val>      Also remove packages that are only needed by the given dependency types\n      --dry-run         Print what would be removed without deleting anything\n      --os=<val>        Prune for a different operating system than the current one\n      --cpu=<val>       Prune for a different CPU architecture than the current one\n      --linker=<val>    Linker to assume when node_modules mixes isolated and hoisted installs (one of \"isolated\" or \"hoisted\")\n  -F, --filter=<val>    Only prune the node_modules folders of the matching workspaces\n      --silent          Don't log anything\n      --cwd=<val>       Set a specific cwd\n  -h, --help            Print this help menu\n\nExamples:\n  Remove packages that are not in bun.lock from node_modules\n  bun prune\n\n  Also remove devDependencies, e.g. after the build step in a Dockerfile\n  bun prune --production\n\n  Show what would be removed without deleting anything\n  bun prune --dry-run\n\n  Only prune what the app workspace no longer needs\n  bun prune --production --filter app\n\nFull documentation is available at https://bun.com/docs/pm/cli/prune.",
        "stderr": null,
        "outputLimitExceeded": false
      },
      "parsed": true,
      "payload": "Usage: bun prune [flags]\n\n  Remove packages from node_modules that are not in bun.lock. With --production, also remove packages that are only needed by devDependencies.\n\nFlags:\n  -p, --production      Also remove packages that are only needed by devDependencies (alias: --prod)\n      --omit=<val>      Also remove packages that are only needed by the given dependency types\n      --dry-run         Print what would be removed without deleting anything\n      --os=<val>        Prune for a different operating system than the current one\n      --cpu=<val>       Prune for a different CPU architecture than the current one\n      --linker=<val>    Linker to assume when node_modules mixes isolated and hoisted installs (one of \"isolated\" or \"hoisted\")\n  -F, --filter=<val>    Only prune the node_modules folders of the matching workspaces\n      --silent          Don't log anything\n      --cwd=<val>       Set a specific cwd\n  -h, --help            Print this help menu\n\nExamples:\n  Remove packages that are not in bun.lock from node_modules\n  bun prune\n\n  Also remove devDependencies, e.g. after the build step in a Dockerfile\n  bun prune --production\n\n  Show what would be removed without deleting anything\n  bun prune --dry-run\n\n  Only prune what the app workspace no longer needs\n  bun prune --production --filter app\n\nFull documentation is available at https://bun.com/docs/pm/cli/prune.",
      "terminalNonHelp": false
    },
    {
      "command": "outdated",
      "helpInvocation": "outdated --help",
      "result": {
        "status": "ok",
        "timedOut": false,
        "exitCode": 0,
        "durationMs": 40,
        "stdout": "bun outdated v1.4.2 (744846f84)\n\nUsage: bun outdated [flags] [filter]\n\n  Display outdated dependencies for each matching workspace.\n\nFlags:\n  -c, --config=<val>                 Specify path to config file (bunfig.toml)\n  -y, --yarn                         Write a yarn.lock file (yarn v1)\n  -p, --production                   Don't install devDependencies\n      --no-save                      Don't update package.json or save a lockfile\n      --save                         Save to package.json (true by default)\n      --ca=<val>                     Provide a Certificate Authority signing certificate\n      --cafile=<val>                 The same as `--ca`, but is a file path to the certificate\n      --dry-run                      Perform a dry run without making changes\n      --frozen-lockfile              Disallow changes to lockfile\n  -f, --force                        Always request the latest versions from the registry & reinstall all dependencies\n      --cache-dir=<val>              Store & load cached data from a specific directory path\n      --no-cache                     Ignore manifest cache entirely\n      --silent                       Don't log anything\n      --quiet                        Only show tarball name when packing\n      --verbose                      Excessively verbose logging\n      --no-progress                  Disable the progress bar\n      --no-summary                   Don't print a summary\n      --no-verify                    Skip verifying integrity of newly downloaded packages\n      --offline                      Never touch the network: resolve and install only from the local cache\n      --prefer-offline               Use cached package metadata regardless of age; only fetch what is missing\n      --ignore-scripts               Skip lifecycle scripts in the project's package.json (dependency scripts are never run)\n      --trust                        Add to trustedDependencies in the project's package.json and install the package(s)\n  -g, --global                       Install globally\n \n[truncated]",
        "stderr": null,
        "outputLimitExceeded": false
      },
      "parsed": false,
      "payload": "bun outdated v1.4.2 (744846f84)\n\nUsage: bun outdated [flags] [filter]\n\n  Display outdated dependencies for each matching workspace.\n\nFlags:\n  -c, --config=<val>                 Specify path to config file (bunfig.toml)\n  -y, --yarn                         Write a yarn.lock file (yarn v1)\n  -p, --production                   Don't install devDependencies\n      --no-save                      Don't update package.json or save a lockfile\n      --save                         Save to package.json (true by default)\n      --ca=<val>                     Provide a Certificate Authority signing certificate\n      --cafile=<val>                 The same as `--ca`, but is a file path to the certificate\n      --dry-run                      Perform a dry run without making changes\n      --frozen-lockfile              Disallow changes to lockfile\n  -f, --force                        Always request the latest versions from the registry & reinstall all dependencies\n      --cache-dir=<val>              Store & load cached data from a specific directory path\n      --no-cache                     Ignore manifest cache entirely\n      --silent                       Don't log anything\n      --quiet                        Only show tarball name when packing\n      --verbose                      Excessively verbose logging\n      --no-progress                  Disable the progress bar\n      --no-summary                   Don't print a summary\n      --no-verify                    Skip verifying integrity of newly downloaded packages\n      --offline                      Never touch the network: resolve and install only from the local cache\n      --prefer-offline               Use cached package metadata regardless of age; only fetch what is missing\n      --ignore-scripts               Skip lifecycle scripts in the project's package.json (dependency scripts are never run)\n      --trust                        Add to trustedDependencies in the project's package.json and install the package(s)\n  -g, --global                       Install globally\n \n[truncated]",
      "terminalNonHelp": true
    },
    {
      "command": "unlink",
      "helpInvocation": "unlink --help",
      "result": {
        "status": "ok",
        "timedOut": false,
        "exitCode": 0,
        "durationMs": 44,
        "stdout": "Usage: bun unlink [flags]\n\n  Unregister the current directory as a \"linkable\" package.\n\nFlags:\n  -c, --config=<val>                 Specify path to config file (bunfig.toml)\n  -y, --yarn                         Write a yarn.lock file (yarn v1)\n  -p, --production                   Don't install devDependencies\n      --no-save                      Don't update package.json or save a lockfile\n      --save                         Save to package.json (true by default)\n      --ca=<val>                     Provide a Certificate Authority signing certificate\n      --cafile=<val>                 The same as `--ca`, but is a file path to the certificate\n      --dry-run                      Perform a dry run without making changes\n      --frozen-lockfile              Disallow changes to lockfile\n  -f, --force                        Always request the latest versions from the registry & reinstall all dependencies\n      --cache-dir=<val>              Store & load cached data from a specific directory path\n      --no-cache                     Ignore manifest cache entirely\n      --silent                       Don't log anything\n      --quiet                        Only show tarball name when packing\n      --verbose                      Excessively verbose logging\n      --no-progress                  Disable the progress bar\n      --no-summary                   Don't print a summary\n      --no-verify                    Skip verifying integrity of newly downloaded packages\n      --offline                      Never touch the network: resolve and install only from the local cache\n      --prefer-offline               Use cached package metadata regardless of age; only fetch what is missing\n      --ignore-scripts               Skip lifecycle scripts in the project's package.json (dependency scripts are never run)\n      --trust                        Add to trustedDependencies in the project's package.json and install the package(s)\n  -g, --global                       Install globally\n      --cwd=<val>                    Set a spe\n[truncated]",
        "stderr": null,
        "outputLimitExceeded": false
      },
      "parsed": false,
      "payload": "Usage: bun unlink [flags]\n\n  Unregister the current directory as a \"linkable\" package.\n\nFlags:\n  -c, --config=<val>                 Specify path to config file (bunfig.toml)\n  -y, --yarn                         Write a yarn.lock file (yarn v1)\n  -p, --production                   Don't install devDependencies\n      --no-save                      Don't update package.json or save a lockfile\n      --save                         Save to package.json (true by default)\n      --ca=<val>                     Provide a Certificate Authority signing certificate\n      --cafile=<val>                 The same as `--ca`, but is a file path to the certificate\n      --dry-run                      Perform a dry run without making changes\n      --frozen-lockfile              Disallow changes to lockfile\n  -f, --force                        Always request the latest versions from the registry & reinstall all dependencies\n      --cache-dir=<val>              Store & load cached data from a specific directory path\n      --no-cache                     Ignore manifest cache entirely\n      --silent                       Don't log anything\n      --quiet                        Only show tarball name when packing\n      --verbose                      Excessively verbose logging\n      --no-progress                  Disable the progress bar\n      --no-summary                   Don't print a summary\n      --no-verify                    Skip verifying integrity of newly downloaded packages\n      --offline                      Never touch the network: resolve and install only from the local cache\n      --prefer-offline               Use cached package metadata regardless of age; only fetch what is missing\n      --ignore-scripts               Skip lifecycle scripts in the project's package.json (dependency scripts are never run)\n      --trust                        Add to trustedDependencies in the project's package.json and install the package(s)\n  -g, --global                       Install globally\n      --cwd=<val>                    Set a spe\n[truncated]",
      "terminalNonHelp": true
    },
    {
      "command": "publish",
      "helpInvocation": "publish --help",
      "result": {
        "status": "ok",
        "timedOut": false,
        "exitCode": 0,
        "durationMs": 43,
        "stdout": "bun publish v1.4.2 (744846f84)\n\nUsage: bun publish [flags] [dist]\n\n  Publish a package to the npm registry.\n\nFlags:\n  -c, --config=<val>                 Specify path to config file (bunfig.toml)\n  -y, --yarn                         Write a yarn.lock file (yarn v1)\n  -p, --production                   Don't install devDependencies\n      --no-save                      Don't update package.json or save a lockfile\n      --save                         Save to package.json (true by default)\n      --ca=<val>                     Provide a Certificate Authority signing certificate\n      --cafile=<val>                 The same as `--ca`, but is a file path to the certificate\n      --dry-run                      Perform a dry run without making changes\n      --frozen-lockfile              Disallow changes to lockfile\n  -f, --force                        Always request the latest versions from the registry & reinstall all dependencies\n      --cache-dir=<val>              Store & load cached data from a specific directory path\n      --no-cache                     Ignore manifest cache entirely\n      --silent                       Don't log anything\n      --quiet                        Only show tarball name when packing\n      --verbose                      Excessively verbose logging\n      --no-progress                  Disable the progress bar\n      --no-summary                   Don't print a summary\n      --no-verify                    Skip verifying integrity of newly downloaded packages\n      --offline                      Never touch the network: resolve and install only from the local cache\n      --prefer-offline               Use cached package metadata regardless of age; only fetch what is missing\n      --ignore-scripts               Skip lifecycle scripts in the project's package.json (dependency scripts are never run)\n      --trust                        Add to trustedDependencies in the project's package.json and install the package(s)\n  -g, --global                       Install globally\n      --cwd=<val>        \n[truncated]",
        "stderr": null,
        "outputLimitExceeded": false
      },
      "parsed": false,
      "payload": "bun publish v1.4.2 (744846f84)\n\nUsage: bun publish [flags] [dist]\n\n  Publish a package to the npm registry.\n\nFlags:\n  -c, --config=<val>                 Specify path to config file (bunfig.toml)\n  -y, --yarn                         Write a yarn.lock file (yarn v1)\n  -p, --production                   Don't install devDependencies\n      --no-save                      Don't update package.json or save a lockfile\n      --save                         Save to package.json (true by default)\n      --ca=<val>                     Provide a Certificate Authority signing certificate\n      --cafile=<val>                 The same as `--ca`, but is a file path to the certificate\n      --dry-run                      Perform a dry run without making changes\n      --frozen-lockfile              Disallow changes to lockfile\n  -f, --force                        Always request the latest versions from the registry & reinstall all dependencies\n      --cache-dir=<val>              Store & load cached data from a specific directory path\n      --no-cache                     Ignore manifest cache entirely\n      --silent                       Don't log anything\n      --quiet                        Only show tarball name when packing\n      --verbose                      Excessively verbose logging\n      --no-progress                  Disable the progress bar\n      --no-summary                   Don't print a summary\n      --no-verify                    Skip verifying integrity of newly downloaded packages\n      --offline                      Never touch the network: resolve and install only from the local cache\n      --prefer-offline               Use cached package metadata regardless of age; only fetch what is missing\n      --ignore-scripts               Skip lifecycle scripts in the project's package.json (dependency scripts are never run)\n      --trust                        Add to trustedDependencies in the project's package.json and install the package(s)\n  -g, --global                       Install globally\n      --cwd=<val>        \n[truncated]",
      "terminalNonHelp": true
    },
    {
      "command": "patch",
      "helpInvocation": "patch --help",
      "result": {
        "status": "ok",
        "timedOut": false,
        "exitCode": 0,
        "durationMs": 40,
        "stdout": "Usage: bun patch [flags or options] <package>@<version>\n\n  Prepare a package for patching, or generate and save a patch.\n\nFlags:\n  -c, --config=<val>                 Specify path to config file (bunfig.toml)\n  -y, --yarn                         Write a yarn.lock file (yarn v1)\n  -p, --production                   Don't install devDependencies\n      --no-save                      Don't update package.json or save a lockfile\n      --save                         Save to package.json (true by default)\n      --ca=<val>                     Provide a Certificate Authority signing certificate\n      --cafile=<val>                 The same as `--ca`, but is a file path to the certificate\n      --dry-run                      Perform a dry run without making changes\n      --frozen-lockfile              Disallow changes to lockfile\n  -f, --force                        Always request the latest versions from the registry & reinstall all dependencies\n      --cache-dir=<val>              Store & load cached data from a specific directory path\n      --no-cache                     Ignore manifest cache entirely\n      --silent                       Don't log anything\n      --quiet                        Only show tarball name when packing\n      --verbose                      Excessively verbose logging\n      --no-progress                  Disable the progress bar\n      --no-summary                   Don't print a summary\n      --no-verify                    Skip verifying integrity of newly downloaded packages\n      --offline                      Never touch the network: resolve and install only from the local cache\n      --prefer-offline               Use cached package metadata regardless of age; only fetch what is missing\n      --ignore-scripts               Skip lifecycle scripts in the project's package.json (dependency scripts are never run)\n      --trust                        Add to trustedDependencies in the project's package.json and install the package(s)\n  -g, --global                       Install globally\n      --cwd=\n[truncated]",
        "stderr": null,
        "outputLimitExceeded": false
      },
      "parsed": false,
      "payload": "Usage: bun patch [flags or options] <package>@<version>\n\n  Prepare a package for patching, or generate and save a patch.\n\nFlags:\n  -c, --config=<val>                 Specify path to config file (bunfig.toml)\n  -y, --yarn                         Write a yarn.lock file (yarn v1)\n  -p, --production                   Don't install devDependencies\n      --no-save                      Don't update package.json or save a lockfile\n      --save                         Save to package.json (true by default)\n      --ca=<val>                     Provide a Certificate Authority signing certificate\n      --cafile=<val>                 The same as `--ca`, but is a file path to the certificate\n      --dry-run                      Perform a dry run without making changes\n      --frozen-lockfile              Disallow changes to lockfile\n  -f, --force                        Always request the latest versions from the registry & reinstall all dependencies\n      --cache-dir=<val>              Store & load cached data from a specific directory path\n      --no-cache                     Ignore manifest cache entirely\n      --silent                       Don't log anything\n      --quiet                        Only show tarball name when packing\n      --verbose                      Excessively verbose logging\n      --no-progress                  Disable the progress bar\n      --no-summary                   Don't print a summary\n      --no-verify                    Skip verifying integrity of newly downloaded packages\n      --offline                      Never touch the network: resolve and install only from the local cache\n      --prefer-offline               Use cached package metadata regardless of age; only fetch what is missing\n      --ignore-scripts               Skip lifecycle scripts in the project's package.json (dependency scripts are never run)\n      --trust                        Add to trustedDependencies in the project's package.json and install the package(s)\n  -g, --global                       Install globally\n      --cwd=\n[truncated]",
      "terminalNonHelp": true
    },
    {
      "command": "pm",
      "helpInvocation": "pm --help",
      "result": {
        "status": "ok",
        "timedOut": false,
        "exitCode": 0,
        "durationMs": 51,
        "stdout": "Usage: bun pm [flags] [<command>]\n\n  Run package manager utilities.\n\n\nCommands:\n\n  bun pm scan                 scan all packages in lockfile for security vulnerabilities\n  bun pm pack                 create a tarball of the current workspace\n  ├ --dry-run                 do everything except for writing the tarball to disk\n  ├ --destination             the directory the tarball will be saved in\n  ├ --filename                the name of the tarball\n  ├ --ignore-scripts          don't run pre/postpack and prepare scripts\n  ├ --gzip-level              specify a custom compression level for gzip (0-9, default is 9)\n  └ --quiet                   only output the tarball filename\n  bun pm bin                  print the path to bin folder\n  └ -g                        print the global path to bin folder\n  bun pm ls                   list the dependency tree according to the current lockfile\n  ├ --all                     list the entire dependency tree according to the current lockfile\n  └ --trusted                 list only trusted dependencies\n  bun pm why <pkg>            show dependency tree explaining why a package is installed\n  bun pm licenses             list installed packages grouped by license\n  ├ --json                    output as JSON\n  ├ --prod                    omit devDependencies\n  ├ --dev                     list only what devDependencies pull in\n  ├ --long                    also print author, description and homepage\n  └ --filter <pattern>        list only the matching workspaces' dependencies\n  bun pm whoami               print the current npm username\n  bun pm view name[@version]  view package metadata from the registry (use `bun info` instead)\n  bun pm version [increment]  bump the version in package.json and create a git tag\n  └ increment                 patch, minor, major, prepatch, preminor, premajor, prerelease, from-git, or a specific version\n  bun pm pkg                  manage data in package.json\n  ├ get [key ...]\n  ├ set key=value ...\n  ├ delete key ...\n  └ fix                       auto\n[truncated]",
        "stderr": null,
        "outputLimitExceeded": false
      },
      "parsed": true,
      "payload": "Usage: bun pm [flags] [<command>]\n\n  Run package manager utilities.\n\n\nCommands:\n\n  bun pm scan                 scan all packages in lockfile for security vulnerabilities\n  bun pm pack                 create a tarball of the current workspace\n  ├ --dry-run                 do everything except for writing the tarball to disk\n  ├ --destination             the directory the tarball will be saved in\n  ├ --filename                the name of the tarball\n  ├ --ignore-scripts          don't run pre/postpack and prepare scripts\n  ├ --gzip-level              specify a custom compression level for gzip (0-9, default is 9)\n  └ --quiet                   only output the tarball filename\n  bun pm bin                  print the path to bin folder\n  └ -g                        print the global path to bin folder\n  bun pm ls                   list the dependency tree according to the current lockfile\n  ├ --all                     list the entire dependency tree according to the current lockfile\n  └ --trusted                 list only trusted dependencies\n  bun pm why <pkg>            show dependency tree explaining why a package is installed\n  bun pm licenses             list installed packages grouped by license\n  ├ --json                    output as JSON\n  ├ --prod                    omit devDependencies\n  ├ --dev                     list only what devDependencies pull in\n  ├ --long                    also print author, description and homepage\n  └ --filter <pattern>        list only the matching workspaces' dependencies\n  bun pm whoami               print the current npm username\n  bun pm view name[@version]  view package metadata from the registry (use `bun info` instead)\n  bun pm version [increment]  bump the version in package.json and create a git tag\n  └ increment                 patch, minor, major, prepatch, preminor, premajor, prerelease, from-git, or a specific version\n  bun pm pkg                  manage data in package.json\n  ├ get [key ...]\n  ├ set key=value ...\n  ├ delete key ...\n  └ fix                       auto\n[truncated]",
      "terminalNonHelp": false
    },
    {
      "command": "init",
      "helpInvocation": "init --help",
      "result": {
        "status": "ok",
        "timedOut": false,
        "exitCode": 0,
        "durationMs": 40,
        "stdout": "Usage: bun init [flags] [<folder>]\n  Initialize a Bun project in the current directory.\n  Creates a package.json, tsconfig.json, and bunfig.toml if they don't exist.\n\nFlags:\n      --help             Print this menu\n  -y, --yes              Accept all default options\n  -m, --minimal          Only initialize type definitions\n  -r, --react            Initialize a React project\n      --react=tailwind   Initialize a React project with TailwindCSS\n      --react=shadcn     Initialize a React project with @shadcn/ui and TailwindCSS\n\nExamples:\n  bun init\n  bun init --yes\n  bun init --react\n  bun init --react=tailwind my-app",
        "stderr": null,
        "outputLimitExceeded": false
      },
      "parsed": true,
      "payload": "Usage: bun init [flags] [<folder>]\n  Initialize a Bun project in the current directory.\n  Creates a package.json, tsconfig.json, and bunfig.toml if they don't exist.\n\nFlags:\n      --help             Print this menu\n  -y, --yes              Accept all default options\n  -m, --minimal          Only initialize type definitions\n  -r, --react            Initialize a React project\n      --react=tailwind   Initialize a React project with TailwindCSS\n      --react=shadcn     Initialize a React project with @shadcn/ui and TailwindCSS\n\nExamples:\n  bun init\n  bun init --yes\n  bun init --react\n  bun init --react=tailwind my-app",
      "terminalNonHelp": false
    },
    {
      "command": "create",
      "helpInvocation": "create --help",
      "result": {
        "status": "ok",
        "timedOut": false,
        "exitCode": 0,
        "durationMs": 40,
        "stdout": "Usage:\n  bun create <MyReactComponent.(jsx|tsx)>\n  bun create <template> [...flags] dest\n  bun create <github-org/repo> [...flags] dest\n\nEnvironment variables:\n  GITHUB_TOKEN         Supply a token to download code from GitHub with a higher rate limit\n  GITHUB_API_DOMAIN    Configure custom/enterprise GitHub domain. Default \"api.github.com\"\n  NPM_CLIENT           Absolute path to the npm client executable\n  BUN_CREATE_DIR       Custom path for global templates (default: $HOME/.bun-create)\n\nReact Component Projects:\n  • Turn an existing React component into a complete frontend dev environment\n  • Automatically starts a hot-reloading dev server\n  • Auto-detects & configures TailwindCSS and shadcn/ui\n\n  bun create <MyReactComponent.(jsx|tsx)>\n\nTemplates:\n  • NPM: Runs bunx create-<template> with given arguments\n  • GitHub: Downloads repository contents as template\n  • Local: Uses templates from $HOME/.bun-create/<name> or ./.bun-create/<name>\n\nLearn more: https://bun.com/docs/cli/bun-create",
        "stderr": null,
        "outputLimitExceeded": false
      },
      "parsed": true,
      "payload": "Usage:\n  bun create <MyReactComponent.(jsx|tsx)>\n  bun create <template> [...flags] dest\n  bun create <github-org/repo> [...flags] dest\n\nEnvironment variables:\n  GITHUB_TOKEN         Supply a token to download code from GitHub with a higher rate limit\n  GITHUB_API_DOMAIN    Configure custom/enterprise GitHub domain. Default \"api.github.com\"\n  NPM_CLIENT           Absolute path to the npm client executable\n  BUN_CREATE_DIR       Custom path for global templates (default: $HOME/.bun-create)\n\nReact Component Projects:\n  • Turn an existing React component into a complete frontend dev environment\n  • Automatically starts a hot-reloading dev server\n  • Auto-detects & configures TailwindCSS and shadcn/ui\n\n  bun create <MyReactComponent.(jsx|tsx)>\n\nTemplates:\n  • NPM: Runs bunx create-<template> with given arguments\n  • GitHub: Downloads repository contents as template\n  • Local: Uses templates from $HOME/.bun-create/<name> or ./.bun-create/<name>\n\nLearn more: https://bun.com/docs/cli/bun-create",
      "terminalNonHelp": false
    },
    {
      "command": "upgrade",
      "helpInvocation": "upgrade --help",
      "result": {
        "status": "ok",
        "timedOut": false,
        "exitCode": 0,
        "durationMs": 41,
        "stdout": "Usage: bun upgrade [flags]\n  Upgrade Bun\n\nExamples:\n  Install the latest stable version\n  bun upgrade\n\n  Install the most recent canary version of Bun\n  bun upgrade --canary\n\nFull documentation is available at https://bun.com/docs/installation#upgrading",
        "stderr": null,
        "outputLimitExceeded": false
      },
      "parsed": true,
      "payload": "Usage: bun upgrade [flags]\n  Upgrade Bun\n\nExamples:\n  Install the latest stable version\n  bun upgrade\n\n  Install the most recent canary version of Bun\n  bun upgrade --canary\n\nFull documentation is available at https://bun.com/docs/installation#upgrading",
      "terminalNonHelp": false
    },
    {
      "command": "pm bun pm scan",
      "helpInvocation": "pm bun pm scan --help",
      "result": {
        "status": "ok",
        "timedOut": false,
        "exitCode": 0,
        "durationMs": 40,
        "stdout": "Usage: bun pm [flags] [<command>]\n\n  Run package manager utilities.\n\n\nCommands:\n\n  bun pm scan                 scan all packages in lockfile for security vulnerabilities\n  bun pm pack                 create a tarball of the current workspace\n  ├ --dry-run                 do everything except for writing the tarball to disk\n  ├ --destination             the directory the tarball will be saved in\n  ├ --filename                the name of the tarball\n  ├ --ignore-scripts          don't run pre/postpack and prepare scripts\n  ├ --gzip-level              specify a custom compression level for gzip (0-9, default is 9)\n  └ --quiet                   only output the tarball filename\n  bun pm bin                  print the path to bin folder\n  └ -g                        print the global path to bin folder\n  bun pm ls                   list the dependency tree according to the current lockfile\n  ├ --all                     list the entire dependency tree according to the current lockfile\n  └ --trusted                 list only trusted dependencies\n  bun pm why <pkg>            show dependency tree explaining why a package is installed\n  bun pm licenses             list installed packages grouped by license\n  ├ --json                    output as JSON\n  ├ --prod                    omit devDependencies\n  ├ --dev                     list only what devDependencies pull in\n  ├ --long                    also print author, description and homepage\n  └ --filter <pattern>        list only the matching workspaces' dependencies\n  bun pm whoami               print the current npm username\n  bun pm view name[@version]  view package metadata from the registry (use `bun info` instead)\n  bun pm version [increment]  bump the version in package.json and create a git tag\n  └ increment                 patch, minor, major, prepatch, preminor, premajor, prerelease, from-git, or a specific version\n  bun pm pkg                  manage data in package.json\n  ├ get [key ...]\n  ├ set key=value ...\n  ├ delete key ...\n  └ fix                       auto\n[truncated]",
        "stderr": null,
        "outputLimitExceeded": false
      },
      "parsed": true,
      "payload": "Usage: bun pm [flags] [<command>]\n\n  Run package manager utilities.\n\n\nCommands:\n\n  bun pm scan                 scan all packages in lockfile for security vulnerabilities\n  bun pm pack                 create a tarball of the current workspace\n  ├ --dry-run                 do everything except for writing the tarball to disk\n  ├ --destination             the directory the tarball will be saved in\n  ├ --filename                the name of the tarball\n  ├ --ignore-scripts          don't run pre/postpack and prepare scripts\n  ├ --gzip-level              specify a custom compression level for gzip (0-9, default is 9)\n  └ --quiet                   only output the tarball filename\n  bun pm bin                  print the path to bin folder\n  └ -g                        print the global path to bin folder\n  bun pm ls                   list the dependency tree according to the current lockfile\n  ├ --all                     list the entire dependency tree according to the current lockfile\n  └ --trusted                 list only trusted dependencies\n  bun pm why <pkg>            show dependency tree explaining why a package is installed\n  bun pm licenses             list installed packages grouped by license\n  ├ --json                    output as JSON\n  ├ --prod                    omit devDependencies\n  ├ --dev                     list only what devDependencies pull in\n  ├ --long                    also print author, description and homepage\n  └ --filter <pattern>        list only the matching workspaces' dependencies\n  bun pm whoami               print the current npm username\n  bun pm view name[@version]  view package metadata from the registry (use `bun info` instead)\n  bun pm version [increment]  bump the version in package.json and create a git tag\n  └ increment                 patch, minor, major, prepatch, preminor, premajor, prerelease, from-git, or a specific version\n  bun pm pkg                  manage data in package.json\n  ├ get [key ...]\n  ├ set key=value ...\n  ├ delete key ...\n  └ fix                       auto\n[truncated]",
      "terminalNonHelp": false
    },
    {
      "command": "pm bun pm pack",
      "helpInvocation": "pm bun pm pack --help",
      "result": {
        "status": "ok",
        "timedOut": false,
        "exitCode": 0,
        "durationMs": 42,
        "stdout": "Usage: bun pm [flags] [<command>]\n\n  Run package manager utilities.\n\n\nCommands:\n\n  bun pm scan                 scan all packages in lockfile for security vulnerabilities\n  bun pm pack                 create a tarball of the current workspace\n  ├ --dry-run                 do everything except for writing the tarball to disk\n  ├ --destination             the directory the tarball will be saved in\n  ├ --filename                the name of the tarball\n  ├ --ignore-scripts          don't run pre/postpack and prepare scripts\n  ├ --gzip-level              specify a custom compression level for gzip (0-9, default is 9)\n  └ --quiet                   only output the tarball filename\n  bun pm bin                  print the path to bin folder\n  └ -g                        print the global path to bin folder\n  bun pm ls                   list the dependency tree according to the current lockfile\n  ├ --all                     list the entire dependency tree according to the current lockfile\n  └ --trusted                 list only trusted dependencies\n  bun pm why <pkg>            show dependency tree explaining why a package is installed\n  bun pm licenses             list installed packages grouped by license\n  ├ --json                    output as JSON\n  ├ --prod                    omit devDependencies\n  ├ --dev                     list only what devDependencies pull in\n  ├ --long                    also print author, description and homepage\n  └ --filter <pattern>        list only the matching workspaces' dependencies\n  bun pm whoami               print the current npm username\n  bun pm view name[@version]  view package metadata from the registry (use `bun info` instead)\n  bun pm version [increment]  bump the version in package.json and create a git tag\n  └ increment                 patch, minor, major, prepatch, preminor, premajor, prerelease, from-git, or a specific version\n  bun pm pkg                  manage data in package.json\n  ├ get [key ...]\n  ├ set key=value ...\n  ├ delete key ...\n  └ fix                       auto\n[truncated]",
        "stderr": null,
        "outputLimitExceeded": false
      },
      "parsed": true,
      "payload": "Usage: bun pm [flags] [<command>]\n\n  Run package manager utilities.\n\n\nCommands:\n\n  bun pm scan                 scan all packages in lockfile for security vulnerabilities\n  bun pm pack                 create a tarball of the current workspace\n  ├ --dry-run                 do everything except for writing the tarball to disk\n  ├ --destination             the directory the tarball will be saved in\n  ├ --filename                the name of the tarball\n  ├ --ignore-scripts          don't run pre/postpack and prepare scripts\n  ├ --gzip-level              specify a custom compression level for gzip (0-9, default is 9)\n  └ --quiet                   only output the tarball filename\n  bun pm bin                  print the path to bin folder\n  └ -g                        print the global path to bin folder\n  bun pm ls                   list the dependency tree according to the current lockfile\n  ├ --all                     list the entire dependency tree according to the current lockfile\n  └ --trusted                 list only trusted dependencies\n  bun pm why <pkg>            show dependency tree explaining why a package is installed\n  bun pm licenses             list installed packages grouped by license\n  ├ --json                    output as JSON\n  ├ --prod                    omit devDependencies\n  ├ --dev                     list only what devDependencies pull in\n  ├ --long                    also print author, description and homepage\n  └ --filter <pattern>        list only the matching workspaces' dependencies\n  bun pm whoami               print the current npm username\n  bun pm view name[@version]  view package metadata from the registry (use `bun info` instead)\n  bun pm version [increment]  bump the version in package.json and create a git tag\n  └ increment                 patch, minor, major, prepatch, preminor, premajor, prerelease, from-git, or a specific version\n  bun pm pkg                  manage data in package.json\n  ├ get [key ...]\n  ├ set key=value ...\n  ├ delete key ...\n  └ fix                       auto\n[truncated]",
      "terminalNonHelp": false
    },
    {
      "command": "pm bun pm bin",
      "helpInvocation": "pm bun pm bin --help",
      "result": {
        "status": "ok",
        "timedOut": false,
        "exitCode": 0,
        "durationMs": 39,
        "stdout": "Usage: bun pm [flags] [<command>]\n\n  Run package manager utilities.\n\n\nCommands:\n\n  bun pm scan                 scan all packages in lockfile for security vulnerabilities\n  bun pm pack                 create a tarball of the current workspace\n  ├ --dry-run                 do everything except for writing the tarball to disk\n  ├ --destination             the directory the tarball will be saved in\n  ├ --filename                the name of the tarball\n  ├ --ignore-scripts          don't run pre/postpack and prepare scripts\n  ├ --gzip-level              specify a custom compression level for gzip (0-9, default is 9)\n  └ --quiet                   only output the tarball filename\n  bun pm bin                  print the path to bin folder\n  └ -g                        print the global path to bin folder\n  bun pm ls                   list the dependency tree according to the current lockfile\n  ├ --all                     list the entire dependency tree according to the current lockfile\n  └ --trusted                 list only trusted dependencies\n  bun pm why <pkg>            show dependency tree explaining why a package is installed\n  bun pm licenses             list installed packages grouped by license\n  ├ --json                    output as JSON\n  ├ --prod                    omit devDependencies\n  ├ --dev                     list only what devDependencies pull in\n  ├ --long                    also print author, description and homepage\n  └ --filter <pattern>        list only the matching workspaces' dependencies\n  bun pm whoami               print the current npm username\n  bun pm view name[@version]  view package metadata from the registry (use `bun info` instead)\n  bun pm version [increment]  bump the version in package.json and create a git tag\n  └ increment                 patch, minor, major, prepatch, preminor, premajor, prerelease, from-git, or a specific version\n  bun pm pkg                  manage data in package.json\n  ├ get [key ...]\n  ├ set key=value ...\n  ├ delete key ...\n  └ fix                       auto\n[truncated]",
        "stderr": null,
        "outputLimitExceeded": false
      },
      "parsed": true,
      "payload": "Usage: bun pm [flags] [<command>]\n\n  Run package manager utilities.\n\n\nCommands:\n\n  bun pm scan                 scan all packages in lockfile for security vulnerabilities\n  bun pm pack                 create a tarball of the current workspace\n  ├ --dry-run                 do everything except for writing the tarball to disk\n  ├ --destination             the directory the tarball will be saved in\n  ├ --filename                the name of the tarball\n  ├ --ignore-scripts          don't run pre/postpack and prepare scripts\n  ├ --gzip-level              specify a custom compression level for gzip (0-9, default is 9)\n  └ --quiet                   only output the tarball filename\n  bun pm bin                  print the path to bin folder\n  └ -g                        print the global path to bin folder\n  bun pm ls                   list the dependency tree according to the current lockfile\n  ├ --all                     list the entire dependency tree according to the current lockfile\n  └ --trusted                 list only trusted dependencies\n  bun pm why <pkg>            show dependency tree explaining why a package is installed\n  bun pm licenses             list installed packages grouped by license\n  ├ --json                    output as JSON\n  ├ --prod                    omit devDependencies\n  ├ --dev                     list only what devDependencies pull in\n  ├ --long                    also print author, description and homepage\n  └ --filter <pattern>        list only the matching workspaces' dependencies\n  bun pm whoami               print the current npm username\n  bun pm view name[@version]  view package metadata from the registry (use `bun info` instead)\n  bun pm version [increment]  bump the version in package.json and create a git tag\n  └ increment                 patch, minor, major, prepatch, preminor, premajor, prerelease, from-git, or a specific version\n  bun pm pkg                  manage data in package.json\n  ├ get [key ...]\n  ├ set key=value ...\n  ├ delete key ...\n  └ fix                       auto\n[truncated]",
      "terminalNonHelp": false
    },
    {
      "command": "pm bun pm ls",
      "helpInvocation": "pm bun pm ls --help",
      "result": {
        "status": "ok",
        "timedOut": false,
        "exitCode": 0,
        "durationMs": 47,
        "stdout": "Usage: bun pm [flags] [<command>]\n\n  Run package manager utilities.\n\n\nCommands:\n\n  bun pm scan                 scan all packages in lockfile for security vulnerabilities\n  bun pm pack                 create a tarball of the current workspace\n  ├ --dry-run                 do everything except for writing the tarball to disk\n  ├ --destination             the directory the tarball will be saved in\n  ├ --filename                the name of the tarball\n  ├ --ignore-scripts          don't run pre/postpack and prepare scripts\n  ├ --gzip-level              specify a custom compression level for gzip (0-9, default is 9)\n  └ --quiet                   only output the tarball filename\n  bun pm bin                  print the path to bin folder\n  └ -g                        print the global path to bin folder\n  bun pm ls                   list the dependency tree according to the current lockfile\n  ├ --all                     list the entire dependency tree according to the current lockfile\n  └ --trusted                 list only trusted dependencies\n  bun pm why <pkg>            show dependency tree explaining why a package is installed\n  bun pm licenses             list installed packages grouped by license\n  ├ --json                    output as JSON\n  ├ --prod                    omit devDependencies\n  ├ --dev                     list only what devDependencies pull in\n  ├ --long                    also print author, description and homepage\n  └ --filter <pattern>        list only the matching workspaces' dependencies\n  bun pm whoami               print the current npm username\n  bun pm view name[@version]  view package metadata from the registry (use `bun info` instead)\n  bun pm version [increment]  bump the version in package.json and create a git tag\n  └ increment                 patch, minor, major, prepatch, preminor, premajor, prerelease, from-git, or a specific version\n  bun pm pkg                  manage data in package.json\n  ├ get [key ...]\n  ├ set key=value ...\n  ├ delete key ...\n  └ fix                       auto\n[truncated]",
        "stderr": null,
        "outputLimitExceeded": false
      },
      "parsed": true,
      "payload": "Usage: bun pm [flags] [<command>]\n\n  Run package manager utilities.\n\n\nCommands:\n\n  bun pm scan                 scan all packages in lockfile for security vulnerabilities\n  bun pm pack                 create a tarball of the current workspace\n  ├ --dry-run                 do everything except for writing the tarball to disk\n  ├ --destination             the directory the tarball will be saved in\n  ├ --filename                the name of the tarball\n  ├ --ignore-scripts          don't run pre/postpack and prepare scripts\n  ├ --gzip-level              specify a custom compression level for gzip (0-9, default is 9)\n  └ --quiet                   only output the tarball filename\n  bun pm bin                  print the path to bin folder\n  └ -g                        print the global path to bin folder\n  bun pm ls                   list the dependency tree according to the current lockfile\n  ├ --all                     list the entire dependency tree according to the current lockfile\n  └ --trusted                 list only trusted dependencies\n  bun pm why <pkg>            show dependency tree explaining why a package is installed\n  bun pm licenses             list installed packages grouped by license\n  ├ --json                    output as JSON\n  ├ --prod                    omit devDependencies\n  ├ --dev                     list only what devDependencies pull in\n  ├ --long                    also print author, description and homepage\n  └ --filter <pattern>        list only the matching workspaces' dependencies\n  bun pm whoami               print the current npm username\n  bun pm view name[@version]  view package metadata from the registry (use `bun info` instead)\n  bun pm version [increment]  bump the version in package.json and create a git tag\n  └ increment                 patch, minor, major, prepatch, preminor, premajor, prerelease, from-git, or a specific version\n  bun pm pkg                  manage data in package.json\n  ├ get [key ...]\n  ├ set key=value ...\n  ├ delete key ...\n  └ fix                       auto\n[truncated]",
      "terminalNonHelp": false
    },
    {
      "command": "pm bun pm why",
      "helpInvocation": "pm bun pm why --help",
      "result": {
        "status": "ok",
        "timedOut": false,
        "exitCode": 0,
        "durationMs": 46,
        "stdout": "Usage: bun pm [flags] [<command>]\n\n  Run package manager utilities.\n\n\nCommands:\n\n  bun pm scan                 scan all packages in lockfile for security vulnerabilities\n  bun pm pack                 create a tarball of the current workspace\n  ├ --dry-run                 do everything except for writing the tarball to disk\n  ├ --destination             the directory the tarball will be saved in\n  ├ --filename                the name of the tarball\n  ├ --ignore-scripts          don't run pre/postpack and prepare scripts\n  ├ --gzip-level              specify a custom compression level for gzip (0-9, default is 9)\n  └ --quiet                   only output the tarball filename\n  bun pm bin                  print the path to bin folder\n  └ -g                        print the global path to bin folder\n  bun pm ls                   list the dependency tree according to the current lockfile\n  ├ --all                     list the entire dependency tree according to the current lockfile\n  └ --trusted                 list only trusted dependencies\n  bun pm why <pkg>            show dependency tree explaining why a package is installed\n  bun pm licenses             list installed packages grouped by license\n  ├ --json                    output as JSON\n  ├ --prod                    omit devDependencies\n  ├ --dev                     list only what devDependencies pull in\n  ├ --long                    also print author, description and homepage\n  └ --filter <pattern>        list only the matching workspaces' dependencies\n  bun pm whoami               print the current npm username\n  bun pm view name[@version]  view package metadata from the registry (use `bun info` instead)\n  bun pm version [increment]  bump the version in package.json and create a git tag\n  └ increment                 patch, minor, major, prepatch, preminor, premajor, prerelease, from-git, or a specific version\n  bun pm pkg                  manage data in package.json\n  ├ get [key ...]\n  ├ set key=value ...\n  ├ delete key ...\n  └ fix                       auto\n[truncated]",
        "stderr": null,
        "outputLimitExceeded": false
      },
      "parsed": true,
      "payload": "Usage: bun pm [flags] [<command>]\n\n  Run package manager utilities.\n\n\nCommands:\n\n  bun pm scan                 scan all packages in lockfile for security vulnerabilities\n  bun pm pack                 create a tarball of the current workspace\n  ├ --dry-run                 do everything except for writing the tarball to disk\n  ├ --destination             the directory the tarball will be saved in\n  ├ --filename                the name of the tarball\n  ├ --ignore-scripts          don't run pre/postpack and prepare scripts\n  ├ --gzip-level              specify a custom compression level for gzip (0-9, default is 9)\n  └ --quiet                   only output the tarball filename\n  bun pm bin                  print the path to bin folder\n  └ -g                        print the global path to bin folder\n  bun pm ls                   list the dependency tree according to the current lockfile\n  ├ --all                     list the entire dependency tree according to the current lockfile\n  └ --trusted                 list only trusted dependencies\n  bun pm why <pkg>            show dependency tree explaining why a package is installed\n  bun pm licenses             list installed packages grouped by license\n  ├ --json                    output as JSON\n  ├ --prod                    omit devDependencies\n  ├ --dev                     list only what devDependencies pull in\n  ├ --long                    also print author, description and homepage\n  └ --filter <pattern>        list only the matching workspaces' dependencies\n  bun pm whoami               print the current npm username\n  bun pm view name[@version]  view package metadata from the registry (use `bun info` instead)\n  bun pm version [increment]  bump the version in package.json and create a git tag\n  └ increment                 patch, minor, major, prepatch, preminor, premajor, prerelease, from-git, or a specific version\n  bun pm pkg                  manage data in package.json\n  ├ get [key ...]\n  ├ set key=value ...\n  ├ delete key ...\n  └ fix                       auto\n[truncated]",
      "terminalNonHelp": false
    },
    {
      "command": "pm bun pm licenses",
      "helpInvocation": "pm bun pm licenses --help",
      "result": {
        "status": "ok",
        "timedOut": false,
        "exitCode": 0,
        "durationMs": 43,
        "stdout": "Usage: bun pm [flags] [<command>]\n\n  Run package manager utilities.\n\n\nCommands:\n\n  bun pm scan                 scan all packages in lockfile for security vulnerabilities\n  bun pm pack                 create a tarball of the current workspace\n  ├ --dry-run                 do everything except for writing the tarball to disk\n  ├ --destination             the directory the tarball will be saved in\n  ├ --filename                the name of the tarball\n  ├ --ignore-scripts          don't run pre/postpack and prepare scripts\n  ├ --gzip-level              specify a custom compression level for gzip (0-9, default is 9)\n  └ --quiet                   only output the tarball filename\n  bun pm bin                  print the path to bin folder\n  └ -g                        print the global path to bin folder\n  bun pm ls                   list the dependency tree according to the current lockfile\n  ├ --all                     list the entire dependency tree according to the current lockfile\n  └ --trusted                 list only trusted dependencies\n  bun pm why <pkg>            show dependency tree explaining why a package is installed\n  bun pm licenses             list installed packages grouped by license\n  ├ --json                    output as JSON\n  ├ --prod                    omit devDependencies\n  ├ --dev                     list only what devDependencies pull in\n  ├ --long                    also print author, description and homepage\n  └ --filter <pattern>        list only the matching workspaces' dependencies\n  bun pm whoami               print the current npm username\n  bun pm view name[@version]  view package metadata from the registry (use `bun info` instead)\n  bun pm version [increment]  bump the version in package.json and create a git tag\n  └ increment                 patch, minor, major, prepatch, preminor, premajor, prerelease, from-git, or a specific version\n  bun pm pkg                  manage data in package.json\n  ├ get [key ...]\n  ├ set key=value ...\n  ├ delete key ...\n  └ fix                       auto\n[truncated]",
        "stderr": null,
        "outputLimitExceeded": false
      },
      "parsed": true,
      "payload": "Usage: bun pm [flags] [<command>]\n\n  Run package manager utilities.\n\n\nCommands:\n\n  bun pm scan                 scan all packages in lockfile for security vulnerabilities\n  bun pm pack                 create a tarball of the current workspace\n  ├ --dry-run                 do everything except for writing the tarball to disk\n  ├ --destination             the directory the tarball will be saved in\n  ├ --filename                the name of the tarball\n  ├ --ignore-scripts          don't run pre/postpack and prepare scripts\n  ├ --gzip-level              specify a custom compression level for gzip (0-9, default is 9)\n  └ --quiet                   only output the tarball filename\n  bun pm bin                  print the path to bin folder\n  └ -g                        print the global path to bin folder\n  bun pm ls                   list the dependency tree according to the current lockfile\n  ├ --all                     list the entire dependency tree according to the current lockfile\n  └ --trusted                 list only trusted dependencies\n  bun pm why <pkg>            show dependency tree explaining why a package is installed\n  bun pm licenses             list installed packages grouped by license\n  ├ --json                    output as JSON\n  ├ --prod                    omit devDependencies\n  ├ --dev                     list only what devDependencies pull in\n  ├ --long                    also print author, description and homepage\n  └ --filter <pattern>        list only the matching workspaces' dependencies\n  bun pm whoami               print the current npm username\n  bun pm view name[@version]  view package metadata from the registry (use `bun info` instead)\n  bun pm version [increment]  bump the version in package.json and create a git tag\n  └ increment                 patch, minor, major, prepatch, preminor, premajor, prerelease, from-git, or a specific version\n  bun pm pkg                  manage data in package.json\n  ├ get [key ...]\n  ├ set key=value ...\n  ├ delete key ...\n  └ fix                       auto\n[truncated]",
      "terminalNonHelp": false
    },
    {
      "command": "pm bun pm whoami",
      "helpInvocation": "pm bun pm whoami --help",
      "result": {
        "status": "ok",
        "timedOut": false,
        "exitCode": 0,
        "durationMs": 39,
        "stdout": "Usage: bun pm [flags] [<command>]\n\n  Run package manager utilities.\n\n\nCommands:\n\n  bun pm scan                 scan all packages in lockfile for security vulnerabilities\n  bun pm pack                 create a tarball of the current workspace\n  ├ --dry-run                 do everything except for writing the tarball to disk\n  ├ --destination             the directory the tarball will be saved in\n  ├ --filename                the name of the tarball\n  ├ --ignore-scripts          don't run pre/postpack and prepare scripts\n  ├ --gzip-level              specify a custom compression level for gzip (0-9, default is 9)\n  └ --quiet                   only output the tarball filename\n  bun pm bin                  print the path to bin folder\n  └ -g                        print the global path to bin folder\n  bun pm ls                   list the dependency tree according to the current lockfile\n  ├ --all                     list the entire dependency tree according to the current lockfile\n  └ --trusted                 list only trusted dependencies\n  bun pm why <pkg>            show dependency tree explaining why a package is installed\n  bun pm licenses             list installed packages grouped by license\n  ├ --json                    output as JSON\n  ├ --prod                    omit devDependencies\n  ├ --dev                     list only what devDependencies pull in\n  ├ --long                    also print author, description and homepage\n  └ --filter <pattern>        list only the matching workspaces' dependencies\n  bun pm whoami               print the current npm username\n  bun pm view name[@version]  view package metadata from the registry (use `bun info` instead)\n  bun pm version [increment]  bump the version in package.json and create a git tag\n  └ increment                 patch, minor, major, prepatch, preminor, premajor, prerelease, from-git, or a specific version\n  bun pm pkg                  manage data in package.json\n  ├ get [key ...]\n  ├ set key=value ...\n  ├ delete key ...\n  └ fix                       auto\n[truncated]",
        "stderr": null,
        "outputLimitExceeded": false
      },
      "parsed": true,
      "payload": "Usage: bun pm [flags] [<command>]\n\n  Run package manager utilities.\n\n\nCommands:\n\n  bun pm scan                 scan all packages in lockfile for security vulnerabilities\n  bun pm pack                 create a tarball of the current workspace\n  ├ --dry-run                 do everything except for writing the tarball to disk\n  ├ --destination             the directory the tarball will be saved in\n  ├ --filename                the name of the tarball\n  ├ --ignore-scripts          don't run pre/postpack and prepare scripts\n  ├ --gzip-level              specify a custom compression level for gzip (0-9, default is 9)\n  └ --quiet                   only output the tarball filename\n  bun pm bin                  print the path to bin folder\n  └ -g                        print the global path to bin folder\n  bun pm ls                   list the dependency tree according to the current lockfile\n  ├ --all                     list the entire dependency tree according to the current lockfile\n  └ --trusted                 list only trusted dependencies\n  bun pm why <pkg>            show dependency tree explaining why a package is installed\n  bun pm licenses             list installed packages grouped by license\n  ├ --json                    output as JSON\n  ├ --prod                    omit devDependencies\n  ├ --dev                     list only what devDependencies pull in\n  ├ --long                    also print author, description and homepage\n  └ --filter <pattern>        list only the matching workspaces' dependencies\n  bun pm whoami               print the current npm username\n  bun pm view name[@version]  view package metadata from the registry (use `bun info` instead)\n  bun pm version [increment]  bump the version in package.json and create a git tag\n  └ increment                 patch, minor, major, prepatch, preminor, premajor, prerelease, from-git, or a specific version\n  bun pm pkg                  manage data in package.json\n  ├ get [key ...]\n  ├ set key=value ...\n  ├ delete key ...\n  └ fix                       auto\n[truncated]",
      "terminalNonHelp": false
    },
    {
      "command": "pm bun pm pkg",
      "helpInvocation": "pm bun pm pkg --help",
      "result": {
        "status": "ok",
        "timedOut": false,
        "exitCode": 0,
        "durationMs": 47,
        "stdout": "Usage: bun pm [flags] [<command>]\n\n  Run package manager utilities.\n\n\nCommands:\n\n  bun pm scan                 scan all packages in lockfile for security vulnerabilities\n  bun pm pack                 create a tarball of the current workspace\n  ├ --dry-run                 do everything except for writing the tarball to disk\n  ├ --destination             the directory the tarball will be saved in\n  ├ --filename                the name of the tarball\n  ├ --ignore-scripts          don't run pre/postpack and prepare scripts\n  ├ --gzip-level              specify a custom compression level for gzip (0-9, default is 9)\n  └ --quiet                   only output the tarball filename\n  bun pm bin                  print the path to bin folder\n  └ -g                        print the global path to bin folder\n  bun pm ls                   list the dependency tree according to the current lockfile\n  ├ --all                     list the entire dependency tree according to the current lockfile\n  └ --trusted                 list only trusted dependencies\n  bun pm why <pkg>            show dependency tree explaining why a package is installed\n  bun pm licenses             list installed packages grouped by license\n  ├ --json                    output as JSON\n  ├ --prod                    omit devDependencies\n  ├ --dev                     list only what devDependencies pull in\n  ├ --long                    also print author, description and homepage\n  └ --filter <pattern>        list only the matching workspaces' dependencies\n  bun pm whoami               print the current npm username\n  bun pm view name[@version]  view package metadata from the registry (use `bun info` instead)\n  bun pm version [increment]  bump the version in package.json and create a git tag\n  └ increment                 patch, minor, major, prepatch, preminor, premajor, prerelease, from-git, or a specific version\n  bun pm pkg                  manage data in package.json\n  ├ get [key ...]\n  ├ set key=value ...\n  ├ delete key ...\n  └ fix                       auto\n[truncated]",
        "stderr": null,
        "outputLimitExceeded": false
      },
      "parsed": true,
      "payload": "Usage: bun pm [flags] [<command>]\n\n  Run package manager utilities.\n\n\nCommands:\n\n  bun pm scan                 scan all packages in lockfile for security vulnerabilities\n  bun pm pack                 create a tarball of the current workspace\n  ├ --dry-run                 do everything except for writing the tarball to disk\n  ├ --destination             the directory the tarball will be saved in\n  ├ --filename                the name of the tarball\n  ├ --ignore-scripts          don't run pre/postpack and prepare scripts\n  ├ --gzip-level              specify a custom compression level for gzip (0-9, default is 9)\n  └ --quiet                   only output the tarball filename\n  bun pm bin                  print the path to bin folder\n  └ -g                        print the global path to bin folder\n  bun pm ls                   list the dependency tree according to the current lockfile\n  ├ --all                     list the entire dependency tree according to the current lockfile\n  └ --trusted                 list only trusted dependencies\n  bun pm why <pkg>            show dependency tree explaining why a package is installed\n  bun pm licenses             list installed packages grouped by license\n  ├ --json                    output as JSON\n  ├ --prod                    omit devDependencies\n  ├ --dev                     list only what devDependencies pull in\n  ├ --long                    also print author, description and homepage\n  └ --filter <pattern>        list only the matching workspaces' dependencies\n  bun pm whoami               print the current npm username\n  bun pm view name[@version]  view package metadata from the registry (use `bun info` instead)\n  bun pm version [increment]  bump the version in package.json and create a git tag\n  └ increment                 patch, minor, major, prepatch, preminor, premajor, prerelease, from-git, or a specific version\n  bun pm pkg                  manage data in package.json\n  ├ get [key ...]\n  ├ set key=value ...\n  ├ delete key ...\n  └ fix                       auto\n[truncated]",
      "terminalNonHelp": false
    },
    {
      "command": "pm bun pm hash",
      "helpInvocation": "pm bun pm hash --help",
      "result": {
        "status": "ok",
        "timedOut": false,
        "exitCode": 0,
        "durationMs": 41,
        "stdout": "Usage: bun pm [flags] [<command>]\n\n  Run package manager utilities.\n\n\nCommands:\n\n  bun pm scan                 scan all packages in lockfile for security vulnerabilities\n  bun pm pack                 create a tarball of the current workspace\n  ├ --dry-run                 do everything except for writing the tarball to disk\n  ├ --destination             the directory the tarball will be saved in\n  ├ --filename                the name of the tarball\n  ├ --ignore-scripts          don't run pre/postpack and prepare scripts\n  ├ --gzip-level              specify a custom compression level for gzip (0-9, default is 9)\n  └ --quiet                   only output the tarball filename\n  bun pm bin                  print the path to bin folder\n  └ -g                        print the global path to bin folder\n  bun pm ls                   list the dependency tree according to the current lockfile\n  ├ --all                     list the entire dependency tree according to the current lockfile\n  └ --trusted                 list only trusted dependencies\n  bun pm why <pkg>            show dependency tree explaining why a package is installed\n  bun pm licenses             list installed packages grouped by license\n  ├ --json                    output as JSON\n  ├ --prod                    omit devDependencies\n  ├ --dev                     list only what devDependencies pull in\n  ├ --long                    also print author, description and homepage\n  └ --filter <pattern>        list only the matching workspaces' dependencies\n  bun pm whoami               print the current npm username\n  bun pm view name[@version]  view package metadata from the registry (use `bun info` instead)\n  bun pm version [increment]  bump the version in package.json and create a git tag\n  └ increment                 patch, minor, major, prepatch, preminor, premajor, prerelease, from-git, or a specific version\n  bun pm pkg                  manage data in package.json\n  ├ get [key ...]\n  ├ set key=value ...\n  ├ delete key ...\n  └ fix                       auto\n[truncated]",
        "stderr": null,
        "outputLimitExceeded": false
      },
      "parsed": true,
      "payload": "Usage: bun pm [flags] [<command>]\n\n  Run package manager utilities.\n\n\nCommands:\n\n  bun pm scan                 scan all packages in lockfile for security vulnerabilities\n  bun pm pack                 create a tarball of the current workspace\n  ├ --dry-run                 do everything except for writing the tarball to disk\n  ├ --destination             the directory the tarball will be saved in\n  ├ --filename                the name of the tarball\n  ├ --ignore-scripts          don't run pre/postpack and prepare scripts\n  ├ --gzip-level              specify a custom compression level for gzip (0-9, default is 9)\n  └ --quiet                   only output the tarball filename\n  bun pm bin                  print the path to bin folder\n  └ -g                        print the global path to bin folder\n  bun pm ls                   list the dependency tree according to the current lockfile\n  ├ --all                     list the entire dependency tree according to the current lockfile\n  └ --trusted                 list only trusted dependencies\n  bun pm why <pkg>            show dependency tree explaining why a package is installed\n  bun pm licenses             list installed packages grouped by license\n  ├ --json                    output as JSON\n  ├ --prod                    omit devDependencies\n  ├ --dev                     list only what devDependencies pull in\n  ├ --long                    also print author, description and homepage\n  └ --filter <pattern>        list only the matching workspaces' dependencies\n  bun pm whoami               print the current npm username\n  bun pm view name[@version]  view package metadata from the registry (use `bun info` instead)\n  bun pm version [increment]  bump the version in package.json and create a git tag\n  └ increment                 patch, minor, major, prepatch, preminor, premajor, prerelease, from-git, or a specific version\n  bun pm pkg                  manage data in package.json\n  ├ get [key ...]\n  ├ set key=value ...\n  ├ delete key ...\n  └ fix                       auto\n[truncated]",
      "terminalNonHelp": false
    },
    {
      "command": "pm bun pm hash-string",
      "helpInvocation": "pm bun pm hash-string --help",
      "result": {
        "status": "ok",
        "timedOut": false,
        "exitCode": 0,
        "durationMs": 48,
        "stdout": "Usage: bun pm [flags] [<command>]\n\n  Run package manager utilities.\n\n\nCommands:\n\n  bun pm scan                 scan all packages in lockfile for security vulnerabilities\n  bun pm pack                 create a tarball of the current workspace\n  ├ --dry-run                 do everything except for writing the tarball to disk\n  ├ --destination             the directory the tarball will be saved in\n  ├ --filename                the name of the tarball\n  ├ --ignore-scripts          don't run pre/postpack and prepare scripts\n  ├ --gzip-level              specify a custom compression level for gzip (0-9, default is 9)\n  └ --quiet                   only output the tarball filename\n  bun pm bin                  print the path to bin folder\n  └ -g                        print the global path to bin folder\n  bun pm ls                   list the dependency tree according to the current lockfile\n  ├ --all                     list the entire dependency tree according to the current lockfile\n  └ --trusted                 list only trusted dependencies\n  bun pm why <pkg>            show dependency tree explaining why a package is installed\n  bun pm licenses             list installed packages grouped by license\n  ├ --json                    output as JSON\n  ├ --prod                    omit devDependencies\n  ├ --dev                     list only what devDependencies pull in\n  ├ --long                    also print author, description and homepage\n  └ --filter <pattern>        list only the matching workspaces' dependencies\n  bun pm whoami               print the current npm username\n  bun pm view name[@version]  view package metadata from the registry (use `bun info` instead)\n  bun pm version [increment]  bump the version in package.json and create a git tag\n  └ increment                 patch, minor, major, prepatch, preminor, premajor, prerelease, from-git, or a specific version\n  bun pm pkg                  manage data in package.json\n  ├ get [key ...]\n  ├ set key=value ...\n  ├ delete key ...\n  └ fix                       auto\n[truncated]",
        "stderr": null,
        "outputLimitExceeded": false
      },
      "parsed": true,
      "payload": "Usage: bun pm [flags] [<command>]\n\n  Run package manager utilities.\n\n\nCommands:\n\n  bun pm scan                 scan all packages in lockfile for security vulnerabilities\n  bun pm pack                 create a tarball of the current workspace\n  ├ --dry-run                 do everything except for writing the tarball to disk\n  ├ --destination             the directory the tarball will be saved in\n  ├ --filename                the name of the tarball\n  ├ --ignore-scripts          don't run pre/postpack and prepare scripts\n  ├ --gzip-level              specify a custom compression level for gzip (0-9, default is 9)\n  └ --quiet                   only output the tarball filename\n  bun pm bin                  print the path to bin folder\n  └ -g                        print the global path to bin folder\n  bun pm ls                   list the dependency tree according to the current lockfile\n  ├ --all                     list the entire dependency tree according to the current lockfile\n  └ --trusted                 list only trusted dependencies\n  bun pm why <pkg>            show dependency tree explaining why a package is installed\n  bun pm licenses             list installed packages grouped by license\n  ├ --json                    output as JSON\n  ├ --prod                    omit devDependencies\n  ├ --dev                     list only what devDependencies pull in\n  ├ --long                    also print author, description and homepage\n  └ --filter <pattern>        list only the matching workspaces' dependencies\n  bun pm whoami               print the current npm username\n  bun pm view name[@version]  view package metadata from the registry (use `bun info` instead)\n  bun pm version [increment]  bump the version in package.json and create a git tag\n  └ increment                 patch, minor, major, prepatch, preminor, premajor, prerelease, from-git, or a specific version\n  bun pm pkg                  manage data in package.json\n  ├ get [key ...]\n  ├ set key=value ...\n  ├ delete key ...\n  └ fix                       auto\n[truncated]",
      "terminalNonHelp": false
    },
    {
      "command": "pm bun pm hash-print",
      "helpInvocation": "pm bun pm hash-print --help",
      "result": {
        "status": "ok",
        "timedOut": false,
        "exitCode": 0,
        "durationMs": 40,
        "stdout": "Usage: bun pm [flags] [<command>]\n\n  Run package manager utilities.\n\n\nCommands:\n\n  bun pm scan                 scan all packages in lockfile for security vulnerabilities\n  bun pm pack                 create a tarball of the current workspace\n  ├ --dry-run                 do everything except for writing the tarball to disk\n  ├ --destination             the directory the tarball will be saved in\n  ├ --filename                the name of the tarball\n  ├ --ignore-scripts          don't run pre/postpack and prepare scripts\n  ├ --gzip-level              specify a custom compression level for gzip (0-9, default is 9)\n  └ --quiet                   only output the tarball filename\n  bun pm bin                  print the path to bin folder\n  └ -g                        print the global path to bin folder\n  bun pm ls                   list the dependency tree according to the current lockfile\n  ├ --all                     list the entire dependency tree according to the current lockfile\n  └ --trusted                 list only trusted dependencies\n  bun pm why <pkg>            show dependency tree explaining why a package is installed\n  bun pm licenses             list installed packages grouped by license\n  ├ --json                    output as JSON\n  ├ --prod                    omit devDependencies\n  ├ --dev                     list only what devDependencies pull in\n  ├ --long                    also print author, description and homepage\n  └ --filter <pattern>        list only the matching workspaces' dependencies\n  bun pm whoami               print the current npm username\n  bun pm view name[@version]  view package metadata from the registry (use `bun info` instead)\n  bun pm version [increment]  bump the version in package.json and create a git tag\n  └ increment                 patch, minor, major, prepatch, preminor, premajor, prerelease, from-git, or a specific version\n  bun pm pkg                  manage data in package.json\n  ├ get [key ...]\n  ├ set key=value ...\n  ├ delete key ...\n  └ fix                       auto\n[truncated]",
        "stderr": null,
        "outputLimitExceeded": false
      },
      "parsed": true,
      "payload": "Usage: bun pm [flags] [<command>]\n\n  Run package manager utilities.\n\n\nCommands:\n\n  bun pm scan                 scan all packages in lockfile for security vulnerabilities\n  bun pm pack                 create a tarball of the current workspace\n  ├ --dry-run                 do everything except for writing the tarball to disk\n  ├ --destination             the directory the tarball will be saved in\n  ├ --filename                the name of the tarball\n  ├ --ignore-scripts          don't run pre/postpack and prepare scripts\n  ├ --gzip-level              specify a custom compression level for gzip (0-9, default is 9)\n  └ --quiet                   only output the tarball filename\n  bun pm bin                  print the path to bin folder\n  └ -g                        print the global path to bin folder\n  bun pm ls                   list the dependency tree according to the current lockfile\n  ├ --all                     list the entire dependency tree according to the current lockfile\n  └ --trusted                 list only trusted dependencies\n  bun pm why <pkg>            show dependency tree explaining why a package is installed\n  bun pm licenses             list installed packages grouped by license\n  ├ --json                    output as JSON\n  ├ --prod                    omit devDependencies\n  ├ --dev                     list only what devDependencies pull in\n  ├ --long                    also print author, description and homepage\n  └ --filter <pattern>        list only the matching workspaces' dependencies\n  bun pm whoami               print the current npm username\n  bun pm view name[@version]  view package metadata from the registry (use `bun info` instead)\n  bun pm version [increment]  bump the version in package.json and create a git tag\n  └ increment                 patch, minor, major, prepatch, preminor, premajor, prerelease, from-git, or a specific version\n  bun pm pkg                  manage data in package.json\n  ├ get [key ...]\n  ├ set key=value ...\n  ├ delete key ...\n  └ fix                       auto\n[truncated]",
      "terminalNonHelp": false
    },
    {
      "command": "pm bun pm cache",
      "helpInvocation": "pm bun pm cache --help",
      "result": {
        "status": "ok",
        "timedOut": false,
        "exitCode": 0,
        "durationMs": 46,
        "stdout": "Usage: bun pm [flags] [<command>]\n\n  Run package manager utilities.\n\n\nCommands:\n\n  bun pm scan                 scan all packages in lockfile for security vulnerabilities\n  bun pm pack                 create a tarball of the current workspace\n  ├ --dry-run                 do everything except for writing the tarball to disk\n  ├ --destination             the directory the tarball will be saved in\n  ├ --filename                the name of the tarball\n  ├ --ignore-scripts          don't run pre/postpack and prepare scripts\n  ├ --gzip-level              specify a custom compression level for gzip (0-9, default is 9)\n  └ --quiet                   only output the tarball filename\n  bun pm bin                  print the path to bin folder\n  └ -g                        print the global path to bin folder\n  bun pm ls                   list the dependency tree according to the current lockfile\n  ├ --all                     list the entire dependency tree according to the current lockfile\n  └ --trusted                 list only trusted dependencies\n  bun pm why <pkg>            show dependency tree explaining why a package is installed\n  bun pm licenses             list installed packages grouped by license\n  ├ --json                    output as JSON\n  ├ --prod                    omit devDependencies\n  ├ --dev                     list only what devDependencies pull in\n  ├ --long                    also print author, description and homepage\n  └ --filter <pattern>        list only the matching workspaces' dependencies\n  bun pm whoami               print the current npm username\n  bun pm view name[@version]  view package metadata from the registry (use `bun info` instead)\n  bun pm version [increment]  bump the version in package.json and create a git tag\n  └ increment                 patch, minor, major, prepatch, preminor, premajor, prerelease, from-git, or a specific version\n  bun pm pkg                  manage data in package.json\n  ├ get [key ...]\n  ├ set key=value ...\n  ├ delete key ...\n  └ fix                       auto\n[truncated]",
        "stderr": null,
        "outputLimitExceeded": false
      },
      "parsed": true,
      "payload": "Usage: bun pm [flags] [<command>]\n\n  Run package manager utilities.\n\n\nCommands:\n\n  bun pm scan                 scan all packages in lockfile for security vulnerabilities\n  bun pm pack                 create a tarball of the current workspace\n  ├ --dry-run                 do everything except for writing the tarball to disk\n  ├ --destination             the directory the tarball will be saved in\n  ├ --filename                the name of the tarball\n  ├ --ignore-scripts          don't run pre/postpack and prepare scripts\n  ├ --gzip-level              specify a custom compression level for gzip (0-9, default is 9)\n  └ --quiet                   only output the tarball filename\n  bun pm bin                  print the path to bin folder\n  └ -g                        print the global path to bin folder\n  bun pm ls                   list the dependency tree according to the current lockfile\n  ├ --all                     list the entire dependency tree according to the current lockfile\n  └ --trusted                 list only trusted dependencies\n  bun pm why <pkg>            show dependency tree explaining why a package is installed\n  bun pm licenses             list installed packages grouped by license\n  ├ --json                    output as JSON\n  ├ --prod                    omit devDependencies\n  ├ --dev                     list only what devDependencies pull in\n  ├ --long                    also print author, description and homepage\n  └ --filter <pattern>        list only the matching workspaces' dependencies\n  bun pm whoami               print the current npm username\n  bun pm view name[@version]  view package metadata from the registry (use `bun info` instead)\n  bun pm version [increment]  bump the version in package.json and create a git tag\n  └ increment                 patch, minor, major, prepatch, preminor, premajor, prerelease, from-git, or a specific version\n  bun pm pkg                  manage data in package.json\n  ├ get [key ...]\n  ├ set key=value ...\n  ├ delete key ...\n  └ fix                       auto\n[truncated]",
      "terminalNonHelp": false
    },
    {
      "command": "pm bun pm cache rm",
      "helpInvocation": "pm bun pm cache rm --help",
      "result": {
        "status": "ok",
        "timedOut": false,
        "exitCode": 0,
        "durationMs": 40,
        "stdout": "Usage: bun pm [flags] [<command>]\n\n  Run package manager utilities.\n\n\nCommands:\n\n  bun pm scan                 scan all packages in lockfile for security vulnerabilities\n  bun pm pack                 create a tarball of the current workspace\n  ├ --dry-run                 do everything except for writing the tarball to disk\n  ├ --destination             the directory the tarball will be saved in\n  ├ --filename                the name of the tarball\n  ├ --ignore-scripts          don't run pre/postpack and prepare scripts\n  ├ --gzip-level              specify a custom compression level for gzip (0-9, default is 9)\n  └ --quiet                   only output the tarball filename\n  bun pm bin                  print the path to bin folder\n  └ -g                        print the global path to bin folder\n  bun pm ls                   list the dependency tree according to the current lockfile\n  ├ --all                     list the entire dependency tree according to the current lockfile\n  └ --trusted                 list only trusted dependencies\n  bun pm why <pkg>            show dependency tree explaining why a package is installed\n  bun pm licenses             list installed packages grouped by license\n  ├ --json                    output as JSON\n  ├ --prod                    omit devDependencies\n  ├ --dev                     list only what devDependencies pull in\n  ├ --long                    also print author, description and homepage\n  └ --filter <pattern>        list only the matching workspaces' dependencies\n  bun pm whoami               print the current npm username\n  bun pm view name[@version]  view package metadata from the registry (use `bun info` instead)\n  bun pm version [increment]  bump the version in package.json and create a git tag\n  └ increment                 patch, minor, major, prepatch, preminor, premajor, prerelease, from-git, or a specific version\n  bun pm pkg                  manage data in package.json\n  ├ get [key ...]\n  ├ set key=value ...\n  ├ delete key ...\n  └ fix                       auto\n[truncated]",
        "stderr": null,
        "outputLimitExceeded": false
      },
      "parsed": true,
      "payload": "Usage: bun pm [flags] [<command>]\n\n  Run package manager utilities.\n\n\nCommands:\n\n  bun pm scan                 scan all packages in lockfile for security vulnerabilities\n  bun pm pack                 create a tarball of the current workspace\n  ├ --dry-run                 do everything except for writing the tarball to disk\n  ├ --destination             the directory the tarball will be saved in\n  ├ --filename                the name of the tarball\n  ├ --ignore-scripts          don't run pre/postpack and prepare scripts\n  ├ --gzip-level              specify a custom compression level for gzip (0-9, default is 9)\n  └ --quiet                   only output the tarball filename\n  bun pm bin                  print the path to bin folder\n  └ -g                        print the global path to bin folder\n  bun pm ls                   list the dependency tree according to the current lockfile\n  ├ --all                     list the entire dependency tree according to the current lockfile\n  └ --trusted                 list only trusted dependencies\n  bun pm why <pkg>            show dependency tree explaining why a package is installed\n  bun pm licenses             list installed packages grouped by license\n  ├ --json                    output as JSON\n  ├ --prod                    omit devDependencies\n  ├ --dev                     list only what devDependencies pull in\n  ├ --long                    also print author, description and homepage\n  └ --filter <pattern>        list only the matching workspaces' dependencies\n  bun pm whoami               print the current npm username\n  bun pm view name[@version]  view package metadata from the registry (use `bun info` instead)\n  bun pm version [increment]  bump the version in package.json and create a git tag\n  └ increment                 patch, minor, major, prepatch, preminor, premajor, prerelease, from-git, or a specific version\n  bun pm pkg                  manage data in package.json\n  ├ get [key ...]\n  ├ set key=value ...\n  ├ delete key ...\n  └ fix                       auto\n[truncated]",
      "terminalNonHelp": false
    },
    {
      "command": "pm bun pm migrate",
      "helpInvocation": "pm bun pm migrate --help",
      "result": {
        "status": "ok",
        "timedOut": false,
        "exitCode": 0,
        "durationMs": 40,
        "stdout": "Usage: bun pm [flags] [<command>]\n\n  Run package manager utilities.\n\n\nCommands:\n\n  bun pm scan                 scan all packages in lockfile for security vulnerabilities\n  bun pm pack                 create a tarball of the current workspace\n  ├ --dry-run                 do everything except for writing the tarball to disk\n  ├ --destination             the directory the tarball will be saved in\n  ├ --filename                the name of the tarball\n  ├ --ignore-scripts          don't run pre/postpack and prepare scripts\n  ├ --gzip-level              specify a custom compression level for gzip (0-9, default is 9)\n  └ --quiet                   only output the tarball filename\n  bun pm bin                  print the path to bin folder\n  └ -g                        print the global path to bin folder\n  bun pm ls                   list the dependency tree according to the current lockfile\n  ├ --all                     list the entire dependency tree according to the current lockfile\n  └ --trusted                 list only trusted dependencies\n  bun pm why <pkg>            show dependency tree explaining why a package is installed\n  bun pm licenses             list installed packages grouped by license\n  ├ --json                    output as JSON\n  ├ --prod                    omit devDependencies\n  ├ --dev                     list only what devDependencies pull in\n  ├ --long                    also print author, description and homepage\n  └ --filter <pattern>        list only the matching workspaces' dependencies\n  bun pm whoami               print the current npm username\n  bun pm view name[@version]  view package metadata from the registry (use `bun info` instead)\n  bun pm version [increment]  bump the version in package.json and create a git tag\n  └ increment                 patch, minor, major, prepatch, preminor, premajor, prerelease, from-git, or a specific version\n  bun pm pkg                  manage data in package.json\n  ├ get [key ...]\n  ├ set key=value ...\n  ├ delete key ...\n  └ fix                       auto\n[truncated]",
        "stderr": null,
        "outputLimitExceeded": false
      },
      "parsed": true,
      "payload": "Usage: bun pm [flags] [<command>]\n\n  Run package manager utilities.\n\n\nCommands:\n\n  bun pm scan                 scan all packages in lockfile for security vulnerabilities\n  bun pm pack                 create a tarball of the current workspace\n  ├ --dry-run                 do everything except for writing the tarball to disk\n  ├ --destination             the directory the tarball will be saved in\n  ├ --filename                the name of the tarball\n  ├ --ignore-scripts          don't run pre/postpack and prepare scripts\n  ├ --gzip-level              specify a custom compression level for gzip (0-9, default is 9)\n  └ --quiet                   only output the tarball filename\n  bun pm bin                  print the path to bin folder\n  └ -g                        print the global path to bin folder\n  bun pm ls                   list the dependency tree according to the current lockfile\n  ├ --all                     list the entire dependency tree according to the current lockfile\n  └ --trusted                 list only trusted dependencies\n  bun pm why <pkg>            show dependency tree explaining why a package is installed\n  bun pm licenses             list installed packages grouped by license\n  ├ --json                    output as JSON\n  ├ --prod                    omit devDependencies\n  ├ --dev                     list only what devDependencies pull in\n  ├ --long                    also print author, description and homepage\n  └ --filter <pattern>        list only the matching workspaces' dependencies\n  bun pm whoami               print the current npm username\n  bun pm view name[@version]  view package metadata from the registry (use `bun info` instead)\n  bun pm version [increment]  bump the version in package.json and create a git tag\n  └ increment                 patch, minor, major, prepatch, preminor, premajor, prerelease, from-git, or a specific version\n  bun pm pkg                  manage data in package.json\n  ├ get [key ...]\n  ├ set key=value ...\n  ├ delete key ...\n  └ fix                       auto\n[truncated]",
      "terminalNonHelp": false
    },
    {
      "command": "pm bun pm untrusted",
      "helpInvocation": "pm bun pm untrusted --help",
      "result": {
        "status": "ok",
        "timedOut": false,
        "exitCode": 0,
        "durationMs": 40,
        "stdout": "Usage: bun pm [flags] [<command>]\n\n  Run package manager utilities.\n\n\nCommands:\n\n  bun pm scan                 scan all packages in lockfile for security vulnerabilities\n  bun pm pack                 create a tarball of the current workspace\n  ├ --dry-run                 do everything except for writing the tarball to disk\n  ├ --destination             the directory the tarball will be saved in\n  ├ --filename                the name of the tarball\n  ├ --ignore-scripts          don't run pre/postpack and prepare scripts\n  ├ --gzip-level              specify a custom compression level for gzip (0-9, default is 9)\n  └ --quiet                   only output the tarball filename\n  bun pm bin                  print the path to bin folder\n  └ -g                        print the global path to bin folder\n  bun pm ls                   list the dependency tree according to the current lockfile\n  ├ --all                     list the entire dependency tree according to the current lockfile\n  └ --trusted                 list only trusted dependencies\n  bun pm why <pkg>            show dependency tree explaining why a package is installed\n  bun pm licenses             list installed packages grouped by license\n  ├ --json                    output as JSON\n  ├ --prod                    omit devDependencies\n  ├ --dev                     list only what devDependencies pull in\n  ├ --long                    also print author, description and homepage\n  └ --filter <pattern>        list only the matching workspaces' dependencies\n  bun pm whoami               print the current npm username\n  bun pm view name[@version]  view package metadata from the registry (use `bun info` instead)\n  bun pm version [increment]  bump the version in package.json and create a git tag\n  └ increment                 patch, minor, major, prepatch, preminor, premajor, prerelease, from-git, or a specific version\n  bun pm pkg                  manage data in package.json\n  ├ get [key ...]\n  ├ set key=value ...\n  ├ delete key ...\n  └ fix                       auto\n[truncated]",
        "stderr": null,
        "outputLimitExceeded": false
      },
      "parsed": true,
      "payload": "Usage: bun pm [flags] [<command>]\n\n  Run package manager utilities.\n\n\nCommands:\n\n  bun pm scan                 scan all packages in lockfile for security vulnerabilities\n  bun pm pack                 create a tarball of the current workspace\n  ├ --dry-run                 do everything except for writing the tarball to disk\n  ├ --destination             the directory the tarball will be saved in\n  ├ --filename                the name of the tarball\n  ├ --ignore-scripts          don't run pre/postpack and prepare scripts\n  ├ --gzip-level              specify a custom compression level for gzip (0-9, default is 9)\n  └ --quiet                   only output the tarball filename\n  bun pm bin                  print the path to bin folder\n  └ -g                        print the global path to bin folder\n  bun pm ls                   list the dependency tree according to the current lockfile\n  ├ --all                     list the entire dependency tree according to the current lockfile\n  └ --trusted                 list only trusted dependencies\n  bun pm why <pkg>            show dependency tree explaining why a package is installed\n  bun pm licenses             list installed packages grouped by license\n  ├ --json                    output as JSON\n  ├ --prod                    omit devDependencies\n  ├ --dev                     list only what devDependencies pull in\n  ├ --long                    also print author, description and homepage\n  └ --filter <pattern>        list only the matching workspaces' dependencies\n  bun pm whoami               print the current npm username\n  bun pm view name[@version]  view package metadata from the registry (use `bun info` instead)\n  bun pm version [increment]  bump the version in package.json and create a git tag\n  └ increment                 patch, minor, major, prepatch, preminor, premajor, prerelease, from-git, or a specific version\n  bun pm pkg                  manage data in package.json\n  ├ get [key ...]\n  ├ set key=value ...\n  ├ delete key ...\n  └ fix                       auto\n[truncated]",
      "terminalNonHelp": false
    },
    {
      "command": "pm bun pm default-trusted",
      "helpInvocation": "pm bun pm default-trusted --help",
      "result": {
        "status": "ok",
        "timedOut": false,
        "exitCode": 0,
        "durationMs": 40,
        "stdout": "Usage: bun pm [flags] [<command>]\n\n  Run package manager utilities.\n\n\nCommands:\n\n  bun pm scan                 scan all packages in lockfile for security vulnerabilities\n  bun pm pack                 create a tarball of the current workspace\n  ├ --dry-run                 do everything except for writing the tarball to disk\n  ├ --destination             the directory the tarball will be saved in\n  ├ --filename                the name of the tarball\n  ├ --ignore-scripts          don't run pre/postpack and prepare scripts\n  ├ --gzip-level              specify a custom compression level for gzip (0-9, default is 9)\n  └ --quiet                   only output the tarball filename\n  bun pm bin                  print the path to bin folder\n  └ -g                        print the global path to bin folder\n  bun pm ls                   list the dependency tree according to the current lockfile\n  ├ --all                     list the entire dependency tree according to the current lockfile\n  └ --trusted                 list only trusted dependencies\n  bun pm why <pkg>            show dependency tree explaining why a package is installed\n  bun pm licenses             list installed packages grouped by license\n  ├ --json                    output as JSON\n  ├ --prod                    omit devDependencies\n  ├ --dev                     list only what devDependencies pull in\n  ├ --long                    also print author, description and homepage\n  └ --filter <pattern>        list only the matching workspaces' dependencies\n  bun pm whoami               print the current npm username\n  bun pm view name[@version]  view package metadata from the registry (use `bun info` instead)\n  bun pm version [increment]  bump the version in package.json and create a git tag\n  └ increment                 patch, minor, major, prepatch, preminor, premajor, prerelease, from-git, or a specific version\n  bun pm pkg                  manage data in package.json\n  ├ get [key ...]\n  ├ set key=value ...\n  ├ delete key ...\n  └ fix                       auto\n[truncated]",
        "stderr": null,
        "outputLimitExceeded": false
      },
      "parsed": true,
      "payload": "Usage: bun pm [flags] [<command>]\n\n  Run package manager utilities.\n\n\nCommands:\n\n  bun pm scan                 scan all packages in lockfile for security vulnerabilities\n  bun pm pack                 create a tarball of the current workspace\n  ├ --dry-run                 do everything except for writing the tarball to disk\n  ├ --destination             the directory the tarball will be saved in\n  ├ --filename                the name of the tarball\n  ├ --ignore-scripts          don't run pre/postpack and prepare scripts\n  ├ --gzip-level              specify a custom compression level for gzip (0-9, default is 9)\n  └ --quiet                   only output the tarball filename\n  bun pm bin                  print the path to bin folder\n  └ -g                        print the global path to bin folder\n  bun pm ls                   list the dependency tree according to the current lockfile\n  ├ --all                     list the entire dependency tree according to the current lockfile\n  └ --trusted                 list only trusted dependencies\n  bun pm why <pkg>            show dependency tree explaining why a package is installed\n  bun pm licenses             list installed packages grouped by license\n  ├ --json                    output as JSON\n  ├ --prod                    omit devDependencies\n  ├ --dev                     list only what devDependencies pull in\n  ├ --long                    also print author, description and homepage\n  └ --filter <pattern>        list only the matching workspaces' dependencies\n  bun pm whoami               print the current npm username\n  bun pm view name[@version]  view package metadata from the registry (use `bun info` instead)\n  bun pm version [increment]  bump the version in package.json and create a git tag\n  └ increment                 patch, minor, major, prepatch, preminor, premajor, prerelease, from-git, or a specific version\n  bun pm pkg                  manage data in package.json\n  ├ get [key ...]\n  ├ set key=value ...\n  ├ delete key ...\n  └ fix                       auto\n[truncated]",
      "terminalNonHelp": false
    }
  ]
}
