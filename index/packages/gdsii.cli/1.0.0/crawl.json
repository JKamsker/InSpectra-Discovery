{
  "commands": [
    {
      "command": null,
      "helpInvocation": "--help",
      "result": {
        "status": "ok",
        "timedOut": false,
        "exitCode": 0,
        "durationMs": 30,
        "stdout": "gds - read, check and convert GDSII layout files.\n\nUsage:\n  gds info <file.gds>                  What the file holds: units, structures, elements, layers\n  gds dump <file.gds> [-o <file>]      Every record as text, one per line\n  gds build <file.txt> [-o <file>]     Read that text back into a GDSII file\n  gds validate <path...>               Parse and report; a directory is searched for .gds files\n  gds cells <file.gds> [--tree]        The library's cells: what places what, and what is in each\n  gds nets <file.gds> --at <x,y>       Everything joined to the shape at that point. Needs --layermap\n  gds measure <file.gds> --from --to   The distance between two points, the way the ruler reads it\n  gds drc <file.gds> --deck <file>     Check it against design rules. Exit 3 if any are broken,\n                                       4 if some rule could not be run\n  gds layers <file.gds> [--area]       Layer/datatype pairs, with a count of what is on each, named\n                                       if a layermap says so\n  gds svg <file.gds> [-o <file>]       The layout as an SVG\n  gds model <file.gds> -o <file>       The layout as a 3D model: .stl, .obj, .gltf or .glb\n  gds boolean <file.gds> -o <file>     Combine two layers into a third: --op, --a, --b, --into\n  gds size <file.gds> -o <file>        Move every edge of a layer out or in: --a, --by\n  gds convert <file> -o <file>         Between GDSII, OASIS and DXF\n  gds generate -o <file>               A layout of a chosen size, to measure against\n  gds bench [<file.gds>]               Time parse, flatten, svg and merge over one\n\nOptions:\n  -o, --output <file>   Write here rather than to standard output. \"-\" means standard output.\n      --layermap <file> layers, svg, model: a CSV of layer,datatype,name and more - see below\n      --write-layermap <file>\n                        layers only: this file's own layers as a mapping to edit\n      --layers <list>   svg, model: draw only these, e.g. 65/20,66/44,68\n      --hide <list>     svg, model: draw everything except these\n    \n[truncated]",
        "stderr": null,
        "outputLimitExceeded": false
      },
      "parsed": true,
      "payload": "gds - read, check and convert GDSII layout files.\n\nUsage:\n  gds info <file.gds>                  What the file holds: units, structures, elements, layers\n  gds dump <file.gds> [-o <file>]      Every record as text, one per line\n  gds build <file.txt> [-o <file>]     Read that text back into a GDSII file\n  gds validate <path...>               Parse and report; a directory is searched for .gds files\n  gds cells <file.gds> [--tree]        The library's cells: what places what, and what is in each\n  gds nets <file.gds> --at <x,y>       Everything joined to the shape at that point. Needs --layermap\n  gds measure <file.gds> --from --to   The distance between two points, the way the ruler reads it\n  gds drc <file.gds> --deck <file>     Check it against design rules. Exit 3 if any are broken,\n                                       4 if some rule could not be run\n  gds layers <file.gds> [--area]       Layer/datatype pairs, with a count of what is on each, named\n                                       if a layermap says so\n  gds svg <file.gds> [-o <file>]       The layout as an SVG\n  gds model <file.gds> -o <file>       The layout as a 3D model: .stl, .obj, .gltf or .glb\n  gds boolean <file.gds> -o <file>     Combine two layers into a third: --op, --a, --b, --into\n  gds size <file.gds> -o <file>        Move every edge of a layer out or in: --a, --by\n  gds convert <file> -o <file>         Between GDSII, OASIS and DXF\n  gds generate -o <file>               A layout of a chosen size, to measure against\n  gds bench [<file.gds>]               Time parse, flatten, svg and merge over one\n\nOptions:\n  -o, --output <file>   Write here rather than to standard output. \"-\" means standard output.\n      --layermap <file> layers, svg, model: a CSV of layer,datatype,name and more - see below\n      --write-layermap <file>\n                        layers only: this file's own layers as a mapping to edit\n      --layers <list>   svg, model: draw only these, e.g. 65/20,66/44,68\n      --hide <list>     svg, model: draw everything except these\n    \n[truncated]",
      "terminalNonHelp": false
    },
    {
      "command": "info",
      "helpInvocation": "help info",
      "result": {
        "status": "ok",
        "timedOut": false,
        "exitCode": 0,
        "durationMs": 28,
        "stdout": "gds - read, check and convert GDSII layout files.\n\nUsage:\n  gds info <file.gds>                  What the file holds: units, structures, elements, layers\n  gds dump <file.gds> [-o <file>]      Every record as text, one per line\n  gds build <file.txt> [-o <file>]     Read that text back into a GDSII file\n  gds validate <path...>               Parse and report; a directory is searched for .gds files\n  gds cells <file.gds> [--tree]        The library's cells: what places what, and what is in each\n  gds nets <file.gds> --at <x,y>       Everything joined to the shape at that point. Needs --layermap\n  gds measure <file.gds> --from --to   The distance between two points, the way the ruler reads it\n  gds drc <file.gds> --deck <file>     Check it against design rules. Exit 3 if any are broken,\n                                       4 if some rule could not be run\n  gds layers <file.gds> [--area]       Layer/datatype pairs, with a count of what is on each, named\n                                       if a layermap says so\n  gds svg <file.gds> [-o <file>]       The layout as an SVG\n  gds model <file.gds> -o <file>       The layout as a 3D model: .stl, .obj, .gltf or .glb\n  gds boolean <file.gds> -o <file>     Combine two layers into a third: --op, --a, --b, --into\n  gds size <file.gds> -o <file>        Move every edge of a layer out or in: --a, --by\n  gds convert <file> -o <file>         Between GDSII, OASIS and DXF\n  gds generate -o <file>               A layout of a chosen size, to measure against\n  gds bench [<file.gds>]               Time parse, flatten, svg and merge over one\n\nOptions:\n  -o, --output <file>   Write here rather than to standard output. \"-\" means standard output.\n      --layermap <file> layers, svg, model: a CSV of layer,datatype,name and more - see below\n      --write-layermap <file>\n                        layers only: this file's own layers as a mapping to edit\n      --layers <list>   svg, model: draw only these, e.g. 65/20,66/44,68\n      --hide <list>     svg, model: draw everything except these\n    \n[truncated]",
        "stderr": null,
        "outputLimitExceeded": false
      },
      "parsed": true,
      "payload": "gds - read, check and convert GDSII layout files.\n\nUsage:\n  gds info <file.gds>                  What the file holds: units, structures, elements, layers\n  gds dump <file.gds> [-o <file>]      Every record as text, one per line\n  gds build <file.txt> [-o <file>]     Read that text back into a GDSII file\n  gds validate <path...>               Parse and report; a directory is searched for .gds files\n  gds cells <file.gds> [--tree]        The library's cells: what places what, and what is in each\n  gds nets <file.gds> --at <x,y>       Everything joined to the shape at that point. Needs --layermap\n  gds measure <file.gds> --from --to   The distance between two points, the way the ruler reads it\n  gds drc <file.gds> --deck <file>     Check it against design rules. Exit 3 if any are broken,\n                                       4 if some rule could not be run\n  gds layers <file.gds> [--area]       Layer/datatype pairs, with a count of what is on each, named\n                                       if a layermap says so\n  gds svg <file.gds> [-o <file>]       The layout as an SVG\n  gds model <file.gds> -o <file>       The layout as a 3D model: .stl, .obj, .gltf or .glb\n  gds boolean <file.gds> -o <file>     Combine two layers into a third: --op, --a, --b, --into\n  gds size <file.gds> -o <file>        Move every edge of a layer out or in: --a, --by\n  gds convert <file> -o <file>         Between GDSII, OASIS and DXF\n  gds generate -o <file>               A layout of a chosen size, to measure against\n  gds bench [<file.gds>]               Time parse, flatten, svg and merge over one\n\nOptions:\n  -o, --output <file>   Write here rather than to standard output. \"-\" means standard output.\n      --layermap <file> layers, svg, model: a CSV of layer,datatype,name and more - see below\n      --write-layermap <file>\n                        layers only: this file's own layers as a mapping to edit\n      --layers <list>   svg, model: draw only these, e.g. 65/20,66/44,68\n      --hide <list>     svg, model: draw everything except these\n    \n[truncated]",
      "terminalNonHelp": false
    },
    {
      "command": "dump",
      "helpInvocation": "help dump",
      "result": {
        "status": "ok",
        "timedOut": false,
        "exitCode": 0,
        "durationMs": 29,
        "stdout": "gds - read, check and convert GDSII layout files.\n\nUsage:\n  gds info <file.gds>                  What the file holds: units, structures, elements, layers\n  gds dump <file.gds> [-o <file>]      Every record as text, one per line\n  gds build <file.txt> [-o <file>]     Read that text back into a GDSII file\n  gds validate <path...>               Parse and report; a directory is searched for .gds files\n  gds cells <file.gds> [--tree]        The library's cells: what places what, and what is in each\n  gds nets <file.gds> --at <x,y>       Everything joined to the shape at that point. Needs --layermap\n  gds measure <file.gds> --from --to   The distance between two points, the way the ruler reads it\n  gds drc <file.gds> --deck <file>     Check it against design rules. Exit 3 if any are broken,\n                                       4 if some rule could not be run\n  gds layers <file.gds> [--area]       Layer/datatype pairs, with a count of what is on each, named\n                                       if a layermap says so\n  gds svg <file.gds> [-o <file>]       The layout as an SVG\n  gds model <file.gds> -o <file>       The layout as a 3D model: .stl, .obj, .gltf or .glb\n  gds boolean <file.gds> -o <file>     Combine two layers into a third: --op, --a, --b, --into\n  gds size <file.gds> -o <file>        Move every edge of a layer out or in: --a, --by\n  gds convert <file> -o <file>         Between GDSII, OASIS and DXF\n  gds generate -o <file>               A layout of a chosen size, to measure against\n  gds bench [<file.gds>]               Time parse, flatten, svg and merge over one\n\nOptions:\n  -o, --output <file>   Write here rather than to standard output. \"-\" means standard output.\n      --layermap <file> layers, svg, model: a CSV of layer,datatype,name and more - see below\n      --write-layermap <file>\n                        layers only: this file's own layers as a mapping to edit\n      --layers <list>   svg, model: draw only these, e.g. 65/20,66/44,68\n      --hide <list>     svg, model: draw everything except these\n    \n[truncated]",
        "stderr": null,
        "outputLimitExceeded": false
      },
      "parsed": true,
      "payload": "gds - read, check and convert GDSII layout files.\n\nUsage:\n  gds info <file.gds>                  What the file holds: units, structures, elements, layers\n  gds dump <file.gds> [-o <file>]      Every record as text, one per line\n  gds build <file.txt> [-o <file>]     Read that text back into a GDSII file\n  gds validate <path...>               Parse and report; a directory is searched for .gds files\n  gds cells <file.gds> [--tree]        The library's cells: what places what, and what is in each\n  gds nets <file.gds> --at <x,y>       Everything joined to the shape at that point. Needs --layermap\n  gds measure <file.gds> --from --to   The distance between two points, the way the ruler reads it\n  gds drc <file.gds> --deck <file>     Check it against design rules. Exit 3 if any are broken,\n                                       4 if some rule could not be run\n  gds layers <file.gds> [--area]       Layer/datatype pairs, with a count of what is on each, named\n                                       if a layermap says so\n  gds svg <file.gds> [-o <file>]       The layout as an SVG\n  gds model <file.gds> -o <file>       The layout as a 3D model: .stl, .obj, .gltf or .glb\n  gds boolean <file.gds> -o <file>     Combine two layers into a third: --op, --a, --b, --into\n  gds size <file.gds> -o <file>        Move every edge of a layer out or in: --a, --by\n  gds convert <file> -o <file>         Between GDSII, OASIS and DXF\n  gds generate -o <file>               A layout of a chosen size, to measure against\n  gds bench [<file.gds>]               Time parse, flatten, svg and merge over one\n\nOptions:\n  -o, --output <file>   Write here rather than to standard output. \"-\" means standard output.\n      --layermap <file> layers, svg, model: a CSV of layer,datatype,name and more - see below\n      --write-layermap <file>\n                        layers only: this file's own layers as a mapping to edit\n      --layers <list>   svg, model: draw only these, e.g. 65/20,66/44,68\n      --hide <list>     svg, model: draw everything except these\n    \n[truncated]",
      "terminalNonHelp": false
    },
    {
      "command": "build",
      "helpInvocation": "help build",
      "result": {
        "status": "ok",
        "timedOut": false,
        "exitCode": 0,
        "durationMs": 31,
        "stdout": "gds - read, check and convert GDSII layout files.\n\nUsage:\n  gds info <file.gds>                  What the file holds: units, structures, elements, layers\n  gds dump <file.gds> [-o <file>]      Every record as text, one per line\n  gds build <file.txt> [-o <file>]     Read that text back into a GDSII file\n  gds validate <path...>               Parse and report; a directory is searched for .gds files\n  gds cells <file.gds> [--tree]        The library's cells: what places what, and what is in each\n  gds nets <file.gds> --at <x,y>       Everything joined to the shape at that point. Needs --layermap\n  gds measure <file.gds> --from --to   The distance between two points, the way the ruler reads it\n  gds drc <file.gds> --deck <file>     Check it against design rules. Exit 3 if any are broken,\n                                       4 if some rule could not be run\n  gds layers <file.gds> [--area]       Layer/datatype pairs, with a count of what is on each, named\n                                       if a layermap says so\n  gds svg <file.gds> [-o <file>]       The layout as an SVG\n  gds model <file.gds> -o <file>       The layout as a 3D model: .stl, .obj, .gltf or .glb\n  gds boolean <file.gds> -o <file>     Combine two layers into a third: --op, --a, --b, --into\n  gds size <file.gds> -o <file>        Move every edge of a layer out or in: --a, --by\n  gds convert <file> -o <file>         Between GDSII, OASIS and DXF\n  gds generate -o <file>               A layout of a chosen size, to measure against\n  gds bench [<file.gds>]               Time parse, flatten, svg and merge over one\n\nOptions:\n  -o, --output <file>   Write here rather than to standard output. \"-\" means standard output.\n      --layermap <file> layers, svg, model: a CSV of layer,datatype,name and more - see below\n      --write-layermap <file>\n                        layers only: this file's own layers as a mapping to edit\n      --layers <list>   svg, model: draw only these, e.g. 65/20,66/44,68\n      --hide <list>     svg, model: draw everything except these\n    \n[truncated]",
        "stderr": null,
        "outputLimitExceeded": false
      },
      "parsed": true,
      "payload": "gds - read, check and convert GDSII layout files.\n\nUsage:\n  gds info <file.gds>                  What the file holds: units, structures, elements, layers\n  gds dump <file.gds> [-o <file>]      Every record as text, one per line\n  gds build <file.txt> [-o <file>]     Read that text back into a GDSII file\n  gds validate <path...>               Parse and report; a directory is searched for .gds files\n  gds cells <file.gds> [--tree]        The library's cells: what places what, and what is in each\n  gds nets <file.gds> --at <x,y>       Everything joined to the shape at that point. Needs --layermap\n  gds measure <file.gds> --from --to   The distance between two points, the way the ruler reads it\n  gds drc <file.gds> --deck <file>     Check it against design rules. Exit 3 if any are broken,\n                                       4 if some rule could not be run\n  gds layers <file.gds> [--area]       Layer/datatype pairs, with a count of what is on each, named\n                                       if a layermap says so\n  gds svg <file.gds> [-o <file>]       The layout as an SVG\n  gds model <file.gds> -o <file>       The layout as a 3D model: .stl, .obj, .gltf or .glb\n  gds boolean <file.gds> -o <file>     Combine two layers into a third: --op, --a, --b, --into\n  gds size <file.gds> -o <file>        Move every edge of a layer out or in: --a, --by\n  gds convert <file> -o <file>         Between GDSII, OASIS and DXF\n  gds generate -o <file>               A layout of a chosen size, to measure against\n  gds bench [<file.gds>]               Time parse, flatten, svg and merge over one\n\nOptions:\n  -o, --output <file>   Write here rather than to standard output. \"-\" means standard output.\n      --layermap <file> layers, svg, model: a CSV of layer,datatype,name and more - see below\n      --write-layermap <file>\n                        layers only: this file's own layers as a mapping to edit\n      --layers <list>   svg, model: draw only these, e.g. 65/20,66/44,68\n      --hide <list>     svg, model: draw everything except these\n    \n[truncated]",
      "terminalNonHelp": false
    },
    {
      "command": "validate",
      "helpInvocation": "help validate",
      "result": {
        "status": "ok",
        "timedOut": false,
        "exitCode": 0,
        "durationMs": 32,
        "stdout": "gds - read, check and convert GDSII layout files.\n\nUsage:\n  gds info <file.gds>                  What the file holds: units, structures, elements, layers\n  gds dump <file.gds> [-o <file>]      Every record as text, one per line\n  gds build <file.txt> [-o <file>]     Read that text back into a GDSII file\n  gds validate <path...>               Parse and report; a directory is searched for .gds files\n  gds cells <file.gds> [--tree]        The library's cells: what places what, and what is in each\n  gds nets <file.gds> --at <x,y>       Everything joined to the shape at that point. Needs --layermap\n  gds measure <file.gds> --from --to   The distance between two points, the way the ruler reads it\n  gds drc <file.gds> --deck <file>     Check it against design rules. Exit 3 if any are broken,\n                                       4 if some rule could not be run\n  gds layers <file.gds> [--area]       Layer/datatype pairs, with a count of what is on each, named\n                                       if a layermap says so\n  gds svg <file.gds> [-o <file>]       The layout as an SVG\n  gds model <file.gds> -o <file>       The layout as a 3D model: .stl, .obj, .gltf or .glb\n  gds boolean <file.gds> -o <file>     Combine two layers into a third: --op, --a, --b, --into\n  gds size <file.gds> -o <file>        Move every edge of a layer out or in: --a, --by\n  gds convert <file> -o <file>         Between GDSII, OASIS and DXF\n  gds generate -o <file>               A layout of a chosen size, to measure against\n  gds bench [<file.gds>]               Time parse, flatten, svg and merge over one\n\nOptions:\n  -o, --output <file>   Write here rather than to standard output. \"-\" means standard output.\n      --layermap <file> layers, svg, model: a CSV of layer,datatype,name and more - see below\n      --write-layermap <file>\n                        layers only: this file's own layers as a mapping to edit\n      --layers <list>   svg, model: draw only these, e.g. 65/20,66/44,68\n      --hide <list>     svg, model: draw everything except these\n    \n[truncated]",
        "stderr": null,
        "outputLimitExceeded": false
      },
      "parsed": true,
      "payload": "gds - read, check and convert GDSII layout files.\n\nUsage:\n  gds info <file.gds>                  What the file holds: units, structures, elements, layers\n  gds dump <file.gds> [-o <file>]      Every record as text, one per line\n  gds build <file.txt> [-o <file>]     Read that text back into a GDSII file\n  gds validate <path...>               Parse and report; a directory is searched for .gds files\n  gds cells <file.gds> [--tree]        The library's cells: what places what, and what is in each\n  gds nets <file.gds> --at <x,y>       Everything joined to the shape at that point. Needs --layermap\n  gds measure <file.gds> --from --to   The distance between two points, the way the ruler reads it\n  gds drc <file.gds> --deck <file>     Check it against design rules. Exit 3 if any are broken,\n                                       4 if some rule could not be run\n  gds layers <file.gds> [--area]       Layer/datatype pairs, with a count of what is on each, named\n                                       if a layermap says so\n  gds svg <file.gds> [-o <file>]       The layout as an SVG\n  gds model <file.gds> -o <file>       The layout as a 3D model: .stl, .obj, .gltf or .glb\n  gds boolean <file.gds> -o <file>     Combine two layers into a third: --op, --a, --b, --into\n  gds size <file.gds> -o <file>        Move every edge of a layer out or in: --a, --by\n  gds convert <file> -o <file>         Between GDSII, OASIS and DXF\n  gds generate -o <file>               A layout of a chosen size, to measure against\n  gds bench [<file.gds>]               Time parse, flatten, svg and merge over one\n\nOptions:\n  -o, --output <file>   Write here rather than to standard output. \"-\" means standard output.\n      --layermap <file> layers, svg, model: a CSV of layer,datatype,name and more - see below\n      --write-layermap <file>\n                        layers only: this file's own layers as a mapping to edit\n      --layers <list>   svg, model: draw only these, e.g. 65/20,66/44,68\n      --hide <list>     svg, model: draw everything except these\n    \n[truncated]",
      "terminalNonHelp": false
    },
    {
      "command": "cells",
      "helpInvocation": "help cells",
      "result": {
        "status": "ok",
        "timedOut": false,
        "exitCode": 0,
        "durationMs": 27,
        "stdout": "gds - read, check and convert GDSII layout files.\n\nUsage:\n  gds info <file.gds>                  What the file holds: units, structures, elements, layers\n  gds dump <file.gds> [-o <file>]      Every record as text, one per line\n  gds build <file.txt> [-o <file>]     Read that text back into a GDSII file\n  gds validate <path...>               Parse and report; a directory is searched for .gds files\n  gds cells <file.gds> [--tree]        The library's cells: what places what, and what is in each\n  gds nets <file.gds> --at <x,y>       Everything joined to the shape at that point. Needs --layermap\n  gds measure <file.gds> --from --to   The distance between two points, the way the ruler reads it\n  gds drc <file.gds> --deck <file>     Check it against design rules. Exit 3 if any are broken,\n                                       4 if some rule could not be run\n  gds layers <file.gds> [--area]       Layer/datatype pairs, with a count of what is on each, named\n                                       if a layermap says so\n  gds svg <file.gds> [-o <file>]       The layout as an SVG\n  gds model <file.gds> -o <file>       The layout as a 3D model: .stl, .obj, .gltf or .glb\n  gds boolean <file.gds> -o <file>     Combine two layers into a third: --op, --a, --b, --into\n  gds size <file.gds> -o <file>        Move every edge of a layer out or in: --a, --by\n  gds convert <file> -o <file>         Between GDSII, OASIS and DXF\n  gds generate -o <file>               A layout of a chosen size, to measure against\n  gds bench [<file.gds>]               Time parse, flatten, svg and merge over one\n\nOptions:\n  -o, --output <file>   Write here rather than to standard output. \"-\" means standard output.\n      --layermap <file> layers, svg, model: a CSV of layer,datatype,name and more - see below\n      --write-layermap <file>\n                        layers only: this file's own layers as a mapping to edit\n      --layers <list>   svg, model: draw only these, e.g. 65/20,66/44,68\n      --hide <list>     svg, model: draw everything except these\n    \n[truncated]",
        "stderr": null,
        "outputLimitExceeded": false
      },
      "parsed": true,
      "payload": "gds - read, check and convert GDSII layout files.\n\nUsage:\n  gds info <file.gds>                  What the file holds: units, structures, elements, layers\n  gds dump <file.gds> [-o <file>]      Every record as text, one per line\n  gds build <file.txt> [-o <file>]     Read that text back into a GDSII file\n  gds validate <path...>               Parse and report; a directory is searched for .gds files\n  gds cells <file.gds> [--tree]        The library's cells: what places what, and what is in each\n  gds nets <file.gds> --at <x,y>       Everything joined to the shape at that point. Needs --layermap\n  gds measure <file.gds> --from --to   The distance between two points, the way the ruler reads it\n  gds drc <file.gds> --deck <file>     Check it against design rules. Exit 3 if any are broken,\n                                       4 if some rule could not be run\n  gds layers <file.gds> [--area]       Layer/datatype pairs, with a count of what is on each, named\n                                       if a layermap says so\n  gds svg <file.gds> [-o <file>]       The layout as an SVG\n  gds model <file.gds> -o <file>       The layout as a 3D model: .stl, .obj, .gltf or .glb\n  gds boolean <file.gds> -o <file>     Combine two layers into a third: --op, --a, --b, --into\n  gds size <file.gds> -o <file>        Move every edge of a layer out or in: --a, --by\n  gds convert <file> -o <file>         Between GDSII, OASIS and DXF\n  gds generate -o <file>               A layout of a chosen size, to measure against\n  gds bench [<file.gds>]               Time parse, flatten, svg and merge over one\n\nOptions:\n  -o, --output <file>   Write here rather than to standard output. \"-\" means standard output.\n      --layermap <file> layers, svg, model: a CSV of layer,datatype,name and more - see below\n      --write-layermap <file>\n                        layers only: this file's own layers as a mapping to edit\n      --layers <list>   svg, model: draw only these, e.g. 65/20,66/44,68\n      --hide <list>     svg, model: draw everything except these\n    \n[truncated]",
      "terminalNonHelp": false
    },
    {
      "command": "nets",
      "helpInvocation": "help nets",
      "result": {
        "status": "ok",
        "timedOut": false,
        "exitCode": 0,
        "durationMs": 31,
        "stdout": "gds - read, check and convert GDSII layout files.\n\nUsage:\n  gds info <file.gds>                  What the file holds: units, structures, elements, layers\n  gds dump <file.gds> [-o <file>]      Every record as text, one per line\n  gds build <file.txt> [-o <file>]     Read that text back into a GDSII file\n  gds validate <path...>               Parse and report; a directory is searched for .gds files\n  gds cells <file.gds> [--tree]        The library's cells: what places what, and what is in each\n  gds nets <file.gds> --at <x,y>       Everything joined to the shape at that point. Needs --layermap\n  gds measure <file.gds> --from --to   The distance between two points, the way the ruler reads it\n  gds drc <file.gds> --deck <file>     Check it against design rules. Exit 3 if any are broken,\n                                       4 if some rule could not be run\n  gds layers <file.gds> [--area]       Layer/datatype pairs, with a count of what is on each, named\n                                       if a layermap says so\n  gds svg <file.gds> [-o <file>]       The layout as an SVG\n  gds model <file.gds> -o <file>       The layout as a 3D model: .stl, .obj, .gltf or .glb\n  gds boolean <file.gds> -o <file>     Combine two layers into a third: --op, --a, --b, --into\n  gds size <file.gds> -o <file>        Move every edge of a layer out or in: --a, --by\n  gds convert <file> -o <file>         Between GDSII, OASIS and DXF\n  gds generate -o <file>               A layout of a chosen size, to measure against\n  gds bench [<file.gds>]               Time parse, flatten, svg and merge over one\n\nOptions:\n  -o, --output <file>   Write here rather than to standard output. \"-\" means standard output.\n      --layermap <file> layers, svg, model: a CSV of layer,datatype,name and more - see below\n      --write-layermap <file>\n                        layers only: this file's own layers as a mapping to edit\n      --layers <list>   svg, model: draw only these, e.g. 65/20,66/44,68\n      --hide <list>     svg, model: draw everything except these\n    \n[truncated]",
        "stderr": null,
        "outputLimitExceeded": false
      },
      "parsed": true,
      "payload": "gds - read, check and convert GDSII layout files.\n\nUsage:\n  gds info <file.gds>                  What the file holds: units, structures, elements, layers\n  gds dump <file.gds> [-o <file>]      Every record as text, one per line\n  gds build <file.txt> [-o <file>]     Read that text back into a GDSII file\n  gds validate <path...>               Parse and report; a directory is searched for .gds files\n  gds cells <file.gds> [--tree]        The library's cells: what places what, and what is in each\n  gds nets <file.gds> --at <x,y>       Everything joined to the shape at that point. Needs --layermap\n  gds measure <file.gds> --from --to   The distance between two points, the way the ruler reads it\n  gds drc <file.gds> --deck <file>     Check it against design rules. Exit 3 if any are broken,\n                                       4 if some rule could not be run\n  gds layers <file.gds> [--area]       Layer/datatype pairs, with a count of what is on each, named\n                                       if a layermap says so\n  gds svg <file.gds> [-o <file>]       The layout as an SVG\n  gds model <file.gds> -o <file>       The layout as a 3D model: .stl, .obj, .gltf or .glb\n  gds boolean <file.gds> -o <file>     Combine two layers into a third: --op, --a, --b, --into\n  gds size <file.gds> -o <file>        Move every edge of a layer out or in: --a, --by\n  gds convert <file> -o <file>         Between GDSII, OASIS and DXF\n  gds generate -o <file>               A layout of a chosen size, to measure against\n  gds bench [<file.gds>]               Time parse, flatten, svg and merge over one\n\nOptions:\n  -o, --output <file>   Write here rather than to standard output. \"-\" means standard output.\n      --layermap <file> layers, svg, model: a CSV of layer,datatype,name and more - see below\n      --write-layermap <file>\n                        layers only: this file's own layers as a mapping to edit\n      --layers <list>   svg, model: draw only these, e.g. 65/20,66/44,68\n      --hide <list>     svg, model: draw everything except these\n    \n[truncated]",
      "terminalNonHelp": false
    },
    {
      "command": "measure",
      "helpInvocation": "help measure",
      "result": {
        "status": "ok",
        "timedOut": false,
        "exitCode": 0,
        "durationMs": 34,
        "stdout": "gds - read, check and convert GDSII layout files.\n\nUsage:\n  gds info <file.gds>                  What the file holds: units, structures, elements, layers\n  gds dump <file.gds> [-o <file>]      Every record as text, one per line\n  gds build <file.txt> [-o <file>]     Read that text back into a GDSII file\n  gds validate <path...>               Parse and report; a directory is searched for .gds files\n  gds cells <file.gds> [--tree]        The library's cells: what places what, and what is in each\n  gds nets <file.gds> --at <x,y>       Everything joined to the shape at that point. Needs --layermap\n  gds measure <file.gds> --from --to   The distance between two points, the way the ruler reads it\n  gds drc <file.gds> --deck <file>     Check it against design rules. Exit 3 if any are broken,\n                                       4 if some rule could not be run\n  gds layers <file.gds> [--area]       Layer/datatype pairs, with a count of what is on each, named\n                                       if a layermap says so\n  gds svg <file.gds> [-o <file>]       The layout as an SVG\n  gds model <file.gds> -o <file>       The layout as a 3D model: .stl, .obj, .gltf or .glb\n  gds boolean <file.gds> -o <file>     Combine two layers into a third: --op, --a, --b, --into\n  gds size <file.gds> -o <file>        Move every edge of a layer out or in: --a, --by\n  gds convert <file> -o <file>         Between GDSII, OASIS and DXF\n  gds generate -o <file>               A layout of a chosen size, to measure against\n  gds bench [<file.gds>]               Time parse, flatten, svg and merge over one\n\nOptions:\n  -o, --output <file>   Write here rather than to standard output. \"-\" means standard output.\n      --layermap <file> layers, svg, model: a CSV of layer,datatype,name and more - see below\n      --write-layermap <file>\n                        layers only: this file's own layers as a mapping to edit\n      --layers <list>   svg, model: draw only these, e.g. 65/20,66/44,68\n      --hide <list>     svg, model: draw everything except these\n    \n[truncated]",
        "stderr": null,
        "outputLimitExceeded": false
      },
      "parsed": true,
      "payload": "gds - read, check and convert GDSII layout files.\n\nUsage:\n  gds info <file.gds>                  What the file holds: units, structures, elements, layers\n  gds dump <file.gds> [-o <file>]      Every record as text, one per line\n  gds build <file.txt> [-o <file>]     Read that text back into a GDSII file\n  gds validate <path...>               Parse and report; a directory is searched for .gds files\n  gds cells <file.gds> [--tree]        The library's cells: what places what, and what is in each\n  gds nets <file.gds> --at <x,y>       Everything joined to the shape at that point. Needs --layermap\n  gds measure <file.gds> --from --to   The distance between two points, the way the ruler reads it\n  gds drc <file.gds> --deck <file>     Check it against design rules. Exit 3 if any are broken,\n                                       4 if some rule could not be run\n  gds layers <file.gds> [--area]       Layer/datatype pairs, with a count of what is on each, named\n                                       if a layermap says so\n  gds svg <file.gds> [-o <file>]       The layout as an SVG\n  gds model <file.gds> -o <file>       The layout as a 3D model: .stl, .obj, .gltf or .glb\n  gds boolean <file.gds> -o <file>     Combine two layers into a third: --op, --a, --b, --into\n  gds size <file.gds> -o <file>        Move every edge of a layer out or in: --a, --by\n  gds convert <file> -o <file>         Between GDSII, OASIS and DXF\n  gds generate -o <file>               A layout of a chosen size, to measure against\n  gds bench [<file.gds>]               Time parse, flatten, svg and merge over one\n\nOptions:\n  -o, --output <file>   Write here rather than to standard output. \"-\" means standard output.\n      --layermap <file> layers, svg, model: a CSV of layer,datatype,name and more - see below\n      --write-layermap <file>\n                        layers only: this file's own layers as a mapping to edit\n      --layers <list>   svg, model: draw only these, e.g. 65/20,66/44,68\n      --hide <list>     svg, model: draw everything except these\n    \n[truncated]",
      "terminalNonHelp": false
    },
    {
      "command": "drc",
      "helpInvocation": "drc /help",
      "result": {
        "status": "failed",
        "timedOut": false,
        "exitCode": 1,
        "durationMs": 33,
        "stdout": null,
        "stderr": "drc needs --deck <file>, the design rules to check against.\nThere is no standard format for one and no deck ships with this tool - a starter deck for sky130 is in wwwroot/resources/GDS Files/sky130A.drc, and docs/DRC.md says how to write one.",
        "outputLimitExceeded": false
      },
      "parsed": true,
      "payload": "drc needs --deck <file>, the design rules to check against.\nThere is no standard format for one and no deck ships with this tool - a starter deck for sky130 is in wwwroot/resources/GDS Files/sky130A.drc, and docs/DRC.md says how to write one.",
      "terminalNonHelp": false
    },
    {
      "command": "layers",
      "helpInvocation": "help layers",
      "result": {
        "status": "ok",
        "timedOut": false,
        "exitCode": 0,
        "durationMs": 35,
        "stdout": "gds - read, check and convert GDSII layout files.\n\nUsage:\n  gds info <file.gds>                  What the file holds: units, structures, elements, layers\n  gds dump <file.gds> [-o <file>]      Every record as text, one per line\n  gds build <file.txt> [-o <file>]     Read that text back into a GDSII file\n  gds validate <path...>               Parse and report; a directory is searched for .gds files\n  gds cells <file.gds> [--tree]        The library's cells: what places what, and what is in each\n  gds nets <file.gds> --at <x,y>       Everything joined to the shape at that point. Needs --layermap\n  gds measure <file.gds> --from --to   The distance between two points, the way the ruler reads it\n  gds drc <file.gds> --deck <file>     Check it against design rules. Exit 3 if any are broken,\n                                       4 if some rule could not be run\n  gds layers <file.gds> [--area]       Layer/datatype pairs, with a count of what is on each, named\n                                       if a layermap says so\n  gds svg <file.gds> [-o <file>]       The layout as an SVG\n  gds model <file.gds> -o <file>       The layout as a 3D model: .stl, .obj, .gltf or .glb\n  gds boolean <file.gds> -o <file>     Combine two layers into a third: --op, --a, --b, --into\n  gds size <file.gds> -o <file>        Move every edge of a layer out or in: --a, --by\n  gds convert <file> -o <file>         Between GDSII, OASIS and DXF\n  gds generate -o <file>               A layout of a chosen size, to measure against\n  gds bench [<file.gds>]               Time parse, flatten, svg and merge over one\n\nOptions:\n  -o, --output <file>   Write here rather than to standard output. \"-\" means standard output.\n      --layermap <file> layers, svg, model: a CSV of layer,datatype,name and more - see below\n      --write-layermap <file>\n                        layers only: this file's own layers as a mapping to edit\n      --layers <list>   svg, model: draw only these, e.g. 65/20,66/44,68\n      --hide <list>     svg, model: draw everything except these\n    \n[truncated]",
        "stderr": null,
        "outputLimitExceeded": false
      },
      "parsed": true,
      "payload": "gds - read, check and convert GDSII layout files.\n\nUsage:\n  gds info <file.gds>                  What the file holds: units, structures, elements, layers\n  gds dump <file.gds> [-o <file>]      Every record as text, one per line\n  gds build <file.txt> [-o <file>]     Read that text back into a GDSII file\n  gds validate <path...>               Parse and report; a directory is searched for .gds files\n  gds cells <file.gds> [--tree]        The library's cells: what places what, and what is in each\n  gds nets <file.gds> --at <x,y>       Everything joined to the shape at that point. Needs --layermap\n  gds measure <file.gds> --from --to   The distance between two points, the way the ruler reads it\n  gds drc <file.gds> --deck <file>     Check it against design rules. Exit 3 if any are broken,\n                                       4 if some rule could not be run\n  gds layers <file.gds> [--area]       Layer/datatype pairs, with a count of what is on each, named\n                                       if a layermap says so\n  gds svg <file.gds> [-o <file>]       The layout as an SVG\n  gds model <file.gds> -o <file>       The layout as a 3D model: .stl, .obj, .gltf or .glb\n  gds boolean <file.gds> -o <file>     Combine two layers into a third: --op, --a, --b, --into\n  gds size <file.gds> -o <file>        Move every edge of a layer out or in: --a, --by\n  gds convert <file> -o <file>         Between GDSII, OASIS and DXF\n  gds generate -o <file>               A layout of a chosen size, to measure against\n  gds bench [<file.gds>]               Time parse, flatten, svg and merge over one\n\nOptions:\n  -o, --output <file>   Write here rather than to standard output. \"-\" means standard output.\n      --layermap <file> layers, svg, model: a CSV of layer,datatype,name and more - see below\n      --write-layermap <file>\n                        layers only: this file's own layers as a mapping to edit\n      --layers <list>   svg, model: draw only these, e.g. 65/20,66/44,68\n      --hide <list>     svg, model: draw everything except these\n    \n[truncated]",
      "terminalNonHelp": false
    },
    {
      "command": "svg",
      "helpInvocation": "help svg",
      "result": {
        "status": "ok",
        "timedOut": false,
        "exitCode": 0,
        "durationMs": 33,
        "stdout": "gds - read, check and convert GDSII layout files.\n\nUsage:\n  gds info <file.gds>                  What the file holds: units, structures, elements, layers\n  gds dump <file.gds> [-o <file>]      Every record as text, one per line\n  gds build <file.txt> [-o <file>]     Read that text back into a GDSII file\n  gds validate <path...>               Parse and report; a directory is searched for .gds files\n  gds cells <file.gds> [--tree]        The library's cells: what places what, and what is in each\n  gds nets <file.gds> --at <x,y>       Everything joined to the shape at that point. Needs --layermap\n  gds measure <file.gds> --from --to   The distance between two points, the way the ruler reads it\n  gds drc <file.gds> --deck <file>     Check it against design rules. Exit 3 if any are broken,\n                                       4 if some rule could not be run\n  gds layers <file.gds> [--area]       Layer/datatype pairs, with a count of what is on each, named\n                                       if a layermap says so\n  gds svg <file.gds> [-o <file>]       The layout as an SVG\n  gds model <file.gds> -o <file>       The layout as a 3D model: .stl, .obj, .gltf or .glb\n  gds boolean <file.gds> -o <file>     Combine two layers into a third: --op, --a, --b, --into\n  gds size <file.gds> -o <file>        Move every edge of a layer out or in: --a, --by\n  gds convert <file> -o <file>         Between GDSII, OASIS and DXF\n  gds generate -o <file>               A layout of a chosen size, to measure against\n  gds bench [<file.gds>]               Time parse, flatten, svg and merge over one\n\nOptions:\n  -o, --output <file>   Write here rather than to standard output. \"-\" means standard output.\n      --layermap <file> layers, svg, model: a CSV of layer,datatype,name and more - see below\n      --write-layermap <file>\n                        layers only: this file's own layers as a mapping to edit\n      --layers <list>   svg, model: draw only these, e.g. 65/20,66/44,68\n      --hide <list>     svg, model: draw everything except these\n    \n[truncated]",
        "stderr": null,
        "outputLimitExceeded": false
      },
      "parsed": true,
      "payload": "gds - read, check and convert GDSII layout files.\n\nUsage:\n  gds info <file.gds>                  What the file holds: units, structures, elements, layers\n  gds dump <file.gds> [-o <file>]      Every record as text, one per line\n  gds build <file.txt> [-o <file>]     Read that text back into a GDSII file\n  gds validate <path...>               Parse and report; a directory is searched for .gds files\n  gds cells <file.gds> [--tree]        The library's cells: what places what, and what is in each\n  gds nets <file.gds> --at <x,y>       Everything joined to the shape at that point. Needs --layermap\n  gds measure <file.gds> --from --to   The distance between two points, the way the ruler reads it\n  gds drc <file.gds> --deck <file>     Check it against design rules. Exit 3 if any are broken,\n                                       4 if some rule could not be run\n  gds layers <file.gds> [--area]       Layer/datatype pairs, with a count of what is on each, named\n                                       if a layermap says so\n  gds svg <file.gds> [-o <file>]       The layout as an SVG\n  gds model <file.gds> -o <file>       The layout as a 3D model: .stl, .obj, .gltf or .glb\n  gds boolean <file.gds> -o <file>     Combine two layers into a third: --op, --a, --b, --into\n  gds size <file.gds> -o <file>        Move every edge of a layer out or in: --a, --by\n  gds convert <file> -o <file>         Between GDSII, OASIS and DXF\n  gds generate -o <file>               A layout of a chosen size, to measure against\n  gds bench [<file.gds>]               Time parse, flatten, svg and merge over one\n\nOptions:\n  -o, --output <file>   Write here rather than to standard output. \"-\" means standard output.\n      --layermap <file> layers, svg, model: a CSV of layer,datatype,name and more - see below\n      --write-layermap <file>\n                        layers only: this file's own layers as a mapping to edit\n      --layers <list>   svg, model: draw only these, e.g. 65/20,66/44,68\n      --hide <list>     svg, model: draw everything except these\n    \n[truncated]",
      "terminalNonHelp": false
    },
    {
      "command": "model",
      "helpInvocation": "help model",
      "result": {
        "status": "ok",
        "timedOut": false,
        "exitCode": 0,
        "durationMs": 35,
        "stdout": "gds - read, check and convert GDSII layout files.\n\nUsage:\n  gds info <file.gds>                  What the file holds: units, structures, elements, layers\n  gds dump <file.gds> [-o <file>]      Every record as text, one per line\n  gds build <file.txt> [-o <file>]     Read that text back into a GDSII file\n  gds validate <path...>               Parse and report; a directory is searched for .gds files\n  gds cells <file.gds> [--tree]        The library's cells: what places what, and what is in each\n  gds nets <file.gds> --at <x,y>       Everything joined to the shape at that point. Needs --layermap\n  gds measure <file.gds> --from --to   The distance between two points, the way the ruler reads it\n  gds drc <file.gds> --deck <file>     Check it against design rules. Exit 3 if any are broken,\n                                       4 if some rule could not be run\n  gds layers <file.gds> [--area]       Layer/datatype pairs, with a count of what is on each, named\n                                       if a layermap says so\n  gds svg <file.gds> [-o <file>]       The layout as an SVG\n  gds model <file.gds> -o <file>       The layout as a 3D model: .stl, .obj, .gltf or .glb\n  gds boolean <file.gds> -o <file>     Combine two layers into a third: --op, --a, --b, --into\n  gds size <file.gds> -o <file>        Move every edge of a layer out or in: --a, --by\n  gds convert <file> -o <file>         Between GDSII, OASIS and DXF\n  gds generate -o <file>               A layout of a chosen size, to measure against\n  gds bench [<file.gds>]               Time parse, flatten, svg and merge over one\n\nOptions:\n  -o, --output <file>   Write here rather than to standard output. \"-\" means standard output.\n      --layermap <file> layers, svg, model: a CSV of layer,datatype,name and more - see below\n      --write-layermap <file>\n                        layers only: this file's own layers as a mapping to edit\n      --layers <list>   svg, model: draw only these, e.g. 65/20,66/44,68\n      --hide <list>     svg, model: draw everything except these\n    \n[truncated]",
        "stderr": null,
        "outputLimitExceeded": false
      },
      "parsed": true,
      "payload": "gds - read, check and convert GDSII layout files.\n\nUsage:\n  gds info <file.gds>                  What the file holds: units, structures, elements, layers\n  gds dump <file.gds> [-o <file>]      Every record as text, one per line\n  gds build <file.txt> [-o <file>]     Read that text back into a GDSII file\n  gds validate <path...>               Parse and report; a directory is searched for .gds files\n  gds cells <file.gds> [--tree]        The library's cells: what places what, and what is in each\n  gds nets <file.gds> --at <x,y>       Everything joined to the shape at that point. Needs --layermap\n  gds measure <file.gds> --from --to   The distance between two points, the way the ruler reads it\n  gds drc <file.gds> --deck <file>     Check it against design rules. Exit 3 if any are broken,\n                                       4 if some rule could not be run\n  gds layers <file.gds> [--area]       Layer/datatype pairs, with a count of what is on each, named\n                                       if a layermap says so\n  gds svg <file.gds> [-o <file>]       The layout as an SVG\n  gds model <file.gds> -o <file>       The layout as a 3D model: .stl, .obj, .gltf or .glb\n  gds boolean <file.gds> -o <file>     Combine two layers into a third: --op, --a, --b, --into\n  gds size <file.gds> -o <file>        Move every edge of a layer out or in: --a, --by\n  gds convert <file> -o <file>         Between GDSII, OASIS and DXF\n  gds generate -o <file>               A layout of a chosen size, to measure against\n  gds bench [<file.gds>]               Time parse, flatten, svg and merge over one\n\nOptions:\n  -o, --output <file>   Write here rather than to standard output. \"-\" means standard output.\n      --layermap <file> layers, svg, model: a CSV of layer,datatype,name and more - see below\n      --write-layermap <file>\n                        layers only: this file's own layers as a mapping to edit\n      --layers <list>   svg, model: draw only these, e.g. 65/20,66/44,68\n      --hide <list>     svg, model: draw everything except these\n    \n[truncated]",
      "terminalNonHelp": false
    },
    {
      "command": "boolean",
      "helpInvocation": "help boolean",
      "result": {
        "status": "ok",
        "timedOut": false,
        "exitCode": 0,
        "durationMs": 43,
        "stdout": "gds - read, check and convert GDSII layout files.\n\nUsage:\n  gds info <file.gds>                  What the file holds: units, structures, elements, layers\n  gds dump <file.gds> [-o <file>]      Every record as text, one per line\n  gds build <file.txt> [-o <file>]     Read that text back into a GDSII file\n  gds validate <path...>               Parse and report; a directory is searched for .gds files\n  gds cells <file.gds> [--tree]        The library's cells: what places what, and what is in each\n  gds nets <file.gds> --at <x,y>       Everything joined to the shape at that point. Needs --layermap\n  gds measure <file.gds> --from --to   The distance between two points, the way the ruler reads it\n  gds drc <file.gds> --deck <file>     Check it against design rules. Exit 3 if any are broken,\n                                       4 if some rule could not be run\n  gds layers <file.gds> [--area]       Layer/datatype pairs, with a count of what is on each, named\n                                       if a layermap says so\n  gds svg <file.gds> [-o <file>]       The layout as an SVG\n  gds model <file.gds> -o <file>       The layout as a 3D model: .stl, .obj, .gltf or .glb\n  gds boolean <file.gds> -o <file>     Combine two layers into a third: --op, --a, --b, --into\n  gds size <file.gds> -o <file>        Move every edge of a layer out or in: --a, --by\n  gds convert <file> -o <file>         Between GDSII, OASIS and DXF\n  gds generate -o <file>               A layout of a chosen size, to measure against\n  gds bench [<file.gds>]               Time parse, flatten, svg and merge over one\n\nOptions:\n  -o, --output <file>   Write here rather than to standard output. \"-\" means standard output.\n      --layermap <file> layers, svg, model: a CSV of layer,datatype,name and more - see below\n      --write-layermap <file>\n                        layers only: this file's own layers as a mapping to edit\n      --layers <list>   svg, model: draw only these, e.g. 65/20,66/44,68\n      --hide <list>     svg, model: draw everything except these\n    \n[truncated]",
        "stderr": null,
        "outputLimitExceeded": false
      },
      "parsed": true,
      "payload": "gds - read, check and convert GDSII layout files.\n\nUsage:\n  gds info <file.gds>                  What the file holds: units, structures, elements, layers\n  gds dump <file.gds> [-o <file>]      Every record as text, one per line\n  gds build <file.txt> [-o <file>]     Read that text back into a GDSII file\n  gds validate <path...>               Parse and report; a directory is searched for .gds files\n  gds cells <file.gds> [--tree]        The library's cells: what places what, and what is in each\n  gds nets <file.gds> --at <x,y>       Everything joined to the shape at that point. Needs --layermap\n  gds measure <file.gds> --from --to   The distance between two points, the way the ruler reads it\n  gds drc <file.gds> --deck <file>     Check it against design rules. Exit 3 if any are broken,\n                                       4 if some rule could not be run\n  gds layers <file.gds> [--area]       Layer/datatype pairs, with a count of what is on each, named\n                                       if a layermap says so\n  gds svg <file.gds> [-o <file>]       The layout as an SVG\n  gds model <file.gds> -o <file>       The layout as a 3D model: .stl, .obj, .gltf or .glb\n  gds boolean <file.gds> -o <file>     Combine two layers into a third: --op, --a, --b, --into\n  gds size <file.gds> -o <file>        Move every edge of a layer out or in: --a, --by\n  gds convert <file> -o <file>         Between GDSII, OASIS and DXF\n  gds generate -o <file>               A layout of a chosen size, to measure against\n  gds bench [<file.gds>]               Time parse, flatten, svg and merge over one\n\nOptions:\n  -o, --output <file>   Write here rather than to standard output. \"-\" means standard output.\n      --layermap <file> layers, svg, model: a CSV of layer,datatype,name and more - see below\n      --write-layermap <file>\n                        layers only: this file's own layers as a mapping to edit\n      --layers <list>   svg, model: draw only these, e.g. 65/20,66/44,68\n      --hide <list>     svg, model: draw everything except these\n    \n[truncated]",
      "terminalNonHelp": false
    },
    {
      "command": "size",
      "helpInvocation": "help size",
      "result": {
        "status": "ok",
        "timedOut": false,
        "exitCode": 0,
        "durationMs": 33,
        "stdout": "gds - read, check and convert GDSII layout files.\n\nUsage:\n  gds info <file.gds>                  What the file holds: units, structures, elements, layers\n  gds dump <file.gds> [-o <file>]      Every record as text, one per line\n  gds build <file.txt> [-o <file>]     Read that text back into a GDSII file\n  gds validate <path...>               Parse and report; a directory is searched for .gds files\n  gds cells <file.gds> [--tree]        The library's cells: what places what, and what is in each\n  gds nets <file.gds> --at <x,y>       Everything joined to the shape at that point. Needs --layermap\n  gds measure <file.gds> --from --to   The distance between two points, the way the ruler reads it\n  gds drc <file.gds> --deck <file>     Check it against design rules. Exit 3 if any are broken,\n                                       4 if some rule could not be run\n  gds layers <file.gds> [--area]       Layer/datatype pairs, with a count of what is on each, named\n                                       if a layermap says so\n  gds svg <file.gds> [-o <file>]       The layout as an SVG\n  gds model <file.gds> -o <file>       The layout as a 3D model: .stl, .obj, .gltf or .glb\n  gds boolean <file.gds> -o <file>     Combine two layers into a third: --op, --a, --b, --into\n  gds size <file.gds> -o <file>        Move every edge of a layer out or in: --a, --by\n  gds convert <file> -o <file>         Between GDSII, OASIS and DXF\n  gds generate -o <file>               A layout of a chosen size, to measure against\n  gds bench [<file.gds>]               Time parse, flatten, svg and merge over one\n\nOptions:\n  -o, --output <file>   Write here rather than to standard output. \"-\" means standard output.\n      --layermap <file> layers, svg, model: a CSV of layer,datatype,name and more - see below\n      --write-layermap <file>\n                        layers only: this file's own layers as a mapping to edit\n      --layers <list>   svg, model: draw only these, e.g. 65/20,66/44,68\n      --hide <list>     svg, model: draw everything except these\n    \n[truncated]",
        "stderr": null,
        "outputLimitExceeded": false
      },
      "parsed": true,
      "payload": "gds - read, check and convert GDSII layout files.\n\nUsage:\n  gds info <file.gds>                  What the file holds: units, structures, elements, layers\n  gds dump <file.gds> [-o <file>]      Every record as text, one per line\n  gds build <file.txt> [-o <file>]     Read that text back into a GDSII file\n  gds validate <path...>               Parse and report; a directory is searched for .gds files\n  gds cells <file.gds> [--tree]        The library's cells: what places what, and what is in each\n  gds nets <file.gds> --at <x,y>       Everything joined to the shape at that point. Needs --layermap\n  gds measure <file.gds> --from --to   The distance between two points, the way the ruler reads it\n  gds drc <file.gds> --deck <file>     Check it against design rules. Exit 3 if any are broken,\n                                       4 if some rule could not be run\n  gds layers <file.gds> [--area]       Layer/datatype pairs, with a count of what is on each, named\n                                       if a layermap says so\n  gds svg <file.gds> [-o <file>]       The layout as an SVG\n  gds model <file.gds> -o <file>       The layout as a 3D model: .stl, .obj, .gltf or .glb\n  gds boolean <file.gds> -o <file>     Combine two layers into a third: --op, --a, --b, --into\n  gds size <file.gds> -o <file>        Move every edge of a layer out or in: --a, --by\n  gds convert <file> -o <file>         Between GDSII, OASIS and DXF\n  gds generate -o <file>               A layout of a chosen size, to measure against\n  gds bench [<file.gds>]               Time parse, flatten, svg and merge over one\n\nOptions:\n  -o, --output <file>   Write here rather than to standard output. \"-\" means standard output.\n      --layermap <file> layers, svg, model: a CSV of layer,datatype,name and more - see below\n      --write-layermap <file>\n                        layers only: this file's own layers as a mapping to edit\n      --layers <list>   svg, model: draw only these, e.g. 65/20,66/44,68\n      --hide <list>     svg, model: draw everything except these\n    \n[truncated]",
      "terminalNonHelp": false
    },
    {
      "command": "convert",
      "helpInvocation": "help convert",
      "result": {
        "status": "ok",
        "timedOut": false,
        "exitCode": 0,
        "durationMs": 31,
        "stdout": "gds - read, check and convert GDSII layout files.\n\nUsage:\n  gds info <file.gds>                  What the file holds: units, structures, elements, layers\n  gds dump <file.gds> [-o <file>]      Every record as text, one per line\n  gds build <file.txt> [-o <file>]     Read that text back into a GDSII file\n  gds validate <path...>               Parse and report; a directory is searched for .gds files\n  gds cells <file.gds> [--tree]        The library's cells: what places what, and what is in each\n  gds nets <file.gds> --at <x,y>       Everything joined to the shape at that point. Needs --layermap\n  gds measure <file.gds> --from --to   The distance between two points, the way the ruler reads it\n  gds drc <file.gds> --deck <file>     Check it against design rules. Exit 3 if any are broken,\n                                       4 if some rule could not be run\n  gds layers <file.gds> [--area]       Layer/datatype pairs, with a count of what is on each, named\n                                       if a layermap says so\n  gds svg <file.gds> [-o <file>]       The layout as an SVG\n  gds model <file.gds> -o <file>       The layout as a 3D model: .stl, .obj, .gltf or .glb\n  gds boolean <file.gds> -o <file>     Combine two layers into a third: --op, --a, --b, --into\n  gds size <file.gds> -o <file>        Move every edge of a layer out or in: --a, --by\n  gds convert <file> -o <file>         Between GDSII, OASIS and DXF\n  gds generate -o <file>               A layout of a chosen size, to measure against\n  gds bench [<file.gds>]               Time parse, flatten, svg and merge over one\n\nOptions:\n  -o, --output <file>   Write here rather than to standard output. \"-\" means standard output.\n      --layermap <file> layers, svg, model: a CSV of layer,datatype,name and more - see below\n      --write-layermap <file>\n                        layers only: this file's own layers as a mapping to edit\n      --layers <list>   svg, model: draw only these, e.g. 65/20,66/44,68\n      --hide <list>     svg, model: draw everything except these\n    \n[truncated]",
        "stderr": null,
        "outputLimitExceeded": false
      },
      "parsed": true,
      "payload": "gds - read, check and convert GDSII layout files.\n\nUsage:\n  gds info <file.gds>                  What the file holds: units, structures, elements, layers\n  gds dump <file.gds> [-o <file>]      Every record as text, one per line\n  gds build <file.txt> [-o <file>]     Read that text back into a GDSII file\n  gds validate <path...>               Parse and report; a directory is searched for .gds files\n  gds cells <file.gds> [--tree]        The library's cells: what places what, and what is in each\n  gds nets <file.gds> --at <x,y>       Everything joined to the shape at that point. Needs --layermap\n  gds measure <file.gds> --from --to   The distance between two points, the way the ruler reads it\n  gds drc <file.gds> --deck <file>     Check it against design rules. Exit 3 if any are broken,\n                                       4 if some rule could not be run\n  gds layers <file.gds> [--area]       Layer/datatype pairs, with a count of what is on each, named\n                                       if a layermap says so\n  gds svg <file.gds> [-o <file>]       The layout as an SVG\n  gds model <file.gds> -o <file>       The layout as a 3D model: .stl, .obj, .gltf or .glb\n  gds boolean <file.gds> -o <file>     Combine two layers into a third: --op, --a, --b, --into\n  gds size <file.gds> -o <file>        Move every edge of a layer out or in: --a, --by\n  gds convert <file> -o <file>         Between GDSII, OASIS and DXF\n  gds generate -o <file>               A layout of a chosen size, to measure against\n  gds bench [<file.gds>]               Time parse, flatten, svg and merge over one\n\nOptions:\n  -o, --output <file>   Write here rather than to standard output. \"-\" means standard output.\n      --layermap <file> layers, svg, model: a CSV of layer,datatype,name and more - see below\n      --write-layermap <file>\n                        layers only: this file's own layers as a mapping to edit\n      --layers <list>   svg, model: draw only these, e.g. 65/20,66/44,68\n      --hide <list>     svg, model: draw everything except these\n    \n[truncated]",
      "terminalNonHelp": false
    },
    {
      "command": "generate",
      "helpInvocation": "help generate",
      "result": {
        "status": "ok",
        "timedOut": false,
        "exitCode": 0,
        "durationMs": 44,
        "stdout": "gds - read, check and convert GDSII layout files.\n\nUsage:\n  gds info <file.gds>                  What the file holds: units, structures, elements, layers\n  gds dump <file.gds> [-o <file>]      Every record as text, one per line\n  gds build <file.txt> [-o <file>]     Read that text back into a GDSII file\n  gds validate <path...>               Parse and report; a directory is searched for .gds files\n  gds cells <file.gds> [--tree]        The library's cells: what places what, and what is in each\n  gds nets <file.gds> --at <x,y>       Everything joined to the shape at that point. Needs --layermap\n  gds measure <file.gds> --from --to   The distance between two points, the way the ruler reads it\n  gds drc <file.gds> --deck <file>     Check it against design rules. Exit 3 if any are broken,\n                                       4 if some rule could not be run\n  gds layers <file.gds> [--area]       Layer/datatype pairs, with a count of what is on each, named\n                                       if a layermap says so\n  gds svg <file.gds> [-o <file>]       The layout as an SVG\n  gds model <file.gds> -o <file>       The layout as a 3D model: .stl, .obj, .gltf or .glb\n  gds boolean <file.gds> -o <file>     Combine two layers into a third: --op, --a, --b, --into\n  gds size <file.gds> -o <file>        Move every edge of a layer out or in: --a, --by\n  gds convert <file> -o <file>         Between GDSII, OASIS and DXF\n  gds generate -o <file>               A layout of a chosen size, to measure against\n  gds bench [<file.gds>]               Time parse, flatten, svg and merge over one\n\nOptions:\n  -o, --output <file>   Write here rather than to standard output. \"-\" means standard output.\n      --layermap <file> layers, svg, model: a CSV of layer,datatype,name and more - see below\n      --write-layermap <file>\n                        layers only: this file's own layers as a mapping to edit\n      --layers <list>   svg, model: draw only these, e.g. 65/20,66/44,68\n      --hide <list>     svg, model: draw everything except these\n    \n[truncated]",
        "stderr": null,
        "outputLimitExceeded": false
      },
      "parsed": true,
      "payload": "gds - read, check and convert GDSII layout files.\n\nUsage:\n  gds info <file.gds>                  What the file holds: units, structures, elements, layers\n  gds dump <file.gds> [-o <file>]      Every record as text, one per line\n  gds build <file.txt> [-o <file>]     Read that text back into a GDSII file\n  gds validate <path...>               Parse and report; a directory is searched for .gds files\n  gds cells <file.gds> [--tree]        The library's cells: what places what, and what is in each\n  gds nets <file.gds> --at <x,y>       Everything joined to the shape at that point. Needs --layermap\n  gds measure <file.gds> --from --to   The distance between two points, the way the ruler reads it\n  gds drc <file.gds> --deck <file>     Check it against design rules. Exit 3 if any are broken,\n                                       4 if some rule could not be run\n  gds layers <file.gds> [--area]       Layer/datatype pairs, with a count of what is on each, named\n                                       if a layermap says so\n  gds svg <file.gds> [-o <file>]       The layout as an SVG\n  gds model <file.gds> -o <file>       The layout as a 3D model: .stl, .obj, .gltf or .glb\n  gds boolean <file.gds> -o <file>     Combine two layers into a third: --op, --a, --b, --into\n  gds size <file.gds> -o <file>        Move every edge of a layer out or in: --a, --by\n  gds convert <file> -o <file>         Between GDSII, OASIS and DXF\n  gds generate -o <file>               A layout of a chosen size, to measure against\n  gds bench [<file.gds>]               Time parse, flatten, svg and merge over one\n\nOptions:\n  -o, --output <file>   Write here rather than to standard output. \"-\" means standard output.\n      --layermap <file> layers, svg, model: a CSV of layer,datatype,name and more - see below\n      --write-layermap <file>\n                        layers only: this file's own layers as a mapping to edit\n      --layers <list>   svg, model: draw only these, e.g. 65/20,66/44,68\n      --hide <list>     svg, model: draw everything except these\n    \n[truncated]",
      "terminalNonHelp": false
    },
    {
      "command": "bench",
      "helpInvocation": "help bench",
      "result": {
        "status": "ok",
        "timedOut": false,
        "exitCode": 0,
        "durationMs": 38,
        "stdout": "gds - read, check and convert GDSII layout files.\n\nUsage:\n  gds info <file.gds>                  What the file holds: units, structures, elements, layers\n  gds dump <file.gds> [-o <file>]      Every record as text, one per line\n  gds build <file.txt> [-o <file>]     Read that text back into a GDSII file\n  gds validate <path...>               Parse and report; a directory is searched for .gds files\n  gds cells <file.gds> [--tree]        The library's cells: what places what, and what is in each\n  gds nets <file.gds> --at <x,y>       Everything joined to the shape at that point. Needs --layermap\n  gds measure <file.gds> --from --to   The distance between two points, the way the ruler reads it\n  gds drc <file.gds> --deck <file>     Check it against design rules. Exit 3 if any are broken,\n                                       4 if some rule could not be run\n  gds layers <file.gds> [--area]       Layer/datatype pairs, with a count of what is on each, named\n                                       if a layermap says so\n  gds svg <file.gds> [-o <file>]       The layout as an SVG\n  gds model <file.gds> -o <file>       The layout as a 3D model: .stl, .obj, .gltf or .glb\n  gds boolean <file.gds> -o <file>     Combine two layers into a third: --op, --a, --b, --into\n  gds size <file.gds> -o <file>        Move every edge of a layer out or in: --a, --by\n  gds convert <file> -o <file>         Between GDSII, OASIS and DXF\n  gds generate -o <file>               A layout of a chosen size, to measure against\n  gds bench [<file.gds>]               Time parse, flatten, svg and merge over one\n\nOptions:\n  -o, --output <file>   Write here rather than to standard output. \"-\" means standard output.\n      --layermap <file> layers, svg, model: a CSV of layer,datatype,name and more - see below\n      --write-layermap <file>\n                        layers only: this file's own layers as a mapping to edit\n      --layers <list>   svg, model: draw only these, e.g. 65/20,66/44,68\n      --hide <list>     svg, model: draw everything except these\n    \n[truncated]",
        "stderr": null,
        "outputLimitExceeded": false
      },
      "parsed": true,
      "payload": "gds - read, check and convert GDSII layout files.\n\nUsage:\n  gds info <file.gds>                  What the file holds: units, structures, elements, layers\n  gds dump <file.gds> [-o <file>]      Every record as text, one per line\n  gds build <file.txt> [-o <file>]     Read that text back into a GDSII file\n  gds validate <path...>               Parse and report; a directory is searched for .gds files\n  gds cells <file.gds> [--tree]        The library's cells: what places what, and what is in each\n  gds nets <file.gds> --at <x,y>       Everything joined to the shape at that point. Needs --layermap\n  gds measure <file.gds> --from --to   The distance between two points, the way the ruler reads it\n  gds drc <file.gds> --deck <file>     Check it against design rules. Exit 3 if any are broken,\n                                       4 if some rule could not be run\n  gds layers <file.gds> [--area]       Layer/datatype pairs, with a count of what is on each, named\n                                       if a layermap says so\n  gds svg <file.gds> [-o <file>]       The layout as an SVG\n  gds model <file.gds> -o <file>       The layout as a 3D model: .stl, .obj, .gltf or .glb\n  gds boolean <file.gds> -o <file>     Combine two layers into a third: --op, --a, --b, --into\n  gds size <file.gds> -o <file>        Move every edge of a layer out or in: --a, --by\n  gds convert <file> -o <file>         Between GDSII, OASIS and DXF\n  gds generate -o <file>               A layout of a chosen size, to measure against\n  gds bench [<file.gds>]               Time parse, flatten, svg and merge over one\n\nOptions:\n  -o, --output <file>   Write here rather than to standard output. \"-\" means standard output.\n      --layermap <file> layers, svg, model: a CSV of layer,datatype,name and more - see below\n      --write-layermap <file>\n                        layers only: this file's own layers as a mapping to edit\n      --layers <list>   svg, model: draw only these, e.g. 65/20,66/44,68\n      --hide <list>     svg, model: draw everything except these\n    \n[truncated]",
      "terminalNonHelp": false
    }
  ]
}
