{
  "opencli": "0.1-draft",
  "info": {
    "title": "phxspec",
    "version": "1.6.11",
    "description": "Run XSpec test suites for XSLT against the PhoenixmlDb engine. No JVM and no Saxon required — the XSpec stylesheets are embedded in the tool."
  },
  "x-inspectra": {
    "artifactSource": "crawled-from-help",
    "generator": "InSpectra.Gen",
    "helpDocumentCount": 1
  },
  "commands": [],
  "options": [
    {
      "name": "--census",
      "recursive": false,
      "hidden": false,
      "description": "Sweep suites through the same runner the single-suite path\nuses, and print a Markdown census (summary, pick-list grouped\nby error code, skips, per-suite detail) to stdout. Each path is\neither a directory (swept recursively for *.xspec) or a single\n.xspec file, so a sweep can be scoped to a chosen subset without\nmoving suites out of the tree they resolve their imports against.",
      "arguments": [
        {
          "name": "PATH",
          "required": true,
          "arity": {
            "minimum": 1,
            "maximum": 1
          }
        }
      ]
    }
  ],
  "arguments": [
    {
      "name": "SUITEXSPEC",
      "required": true,
      "hidden": false,
      "arity": {
        "minimum": 1,
        "maximum": 1
      }
    }
  ]
}
