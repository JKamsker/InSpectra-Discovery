{
  "commands": [
    {
      "command": null,
      "helpInvocation": "--help",
      "result": {
        "status": "ok",
        "timedOut": false,
        "exitCode": 0,
        "durationMs": 919,
        "stdout": "┌──────────────────────────────────────────┐\n  │          Velixir CLI v2.0.5               │\n  │    Deploy any app to the cloud            │\n  └──────────────────────────────────────────┘\n\n  Usage: velixir <command> [options]\n\n  Commands:\n    login                   Sign in (opens your browser)\n    logout                  Clear the stored API key from this machine\n    init                    Create or link a Velixir app (writes .velixir.json)\n    deploy                  Package source, upload, build + deploy\n    deploy --template <slug> Create an app from a starter template and deploy it\n    templates               List the starter templates\n    status                  Check current deployment status\n    apps                    List your apps\n    db <sub>                Manage Postgres databases (run `velixir db` for sub-commands)\n    cache <sub>             Manage Valkey caches (run `velixir cache` for sub-commands)\n    doctor                  Verify prerequisites (credentials, API, project)\n    version                 Print the CLI version\n\n  Deploy options:\n    --app, -a <id>          Target app ID (overrides .velixir.json)\n    --slot, -s <slot>       Target deployment slot (default: production)\n    --template, -t <slug>   Deploy a starter template (nothing local needed)\n    --name, -n <name>       App name for a template deploy (default: the slug)\n    --with-services         Create + connect the database/cache a template uses\n    --no-services           Skip them (the app shows a setup page until one is bound)\n    --source-dir, -d <dir>  Directory to package as source (default: current dir)\n    --no-tail               Enqueue the deploy and return without waiting for it\n\n  Init options:\n    --app, -a <id>          Link to an existing app instead of creating one\n\n  Login options:\n    --key, -k <key>         API key, for CI and headless machines (skips the browser)\n    --no-browser            Paste a key at the prompt instead of approving in the browser\n    --url, -u <url>         API base URL (default: https:/\n[truncated]",
        "stderr": null,
        "outputLimitExceeded": false
      },
      "parsed": true,
      "payload": "┌──────────────────────────────────────────┐\n  │          Velixir CLI v2.0.5               │\n  │    Deploy any app to the cloud            │\n  └──────────────────────────────────────────┘\n\n  Usage: velixir <command> [options]\n\n  Commands:\n    login                   Sign in (opens your browser)\n    logout                  Clear the stored API key from this machine\n    init                    Create or link a Velixir app (writes .velixir.json)\n    deploy                  Package source, upload, build + deploy\n    deploy --template <slug> Create an app from a starter template and deploy it\n    templates               List the starter templates\n    status                  Check current deployment status\n    apps                    List your apps\n    db <sub>                Manage Postgres databases (run `velixir db` for sub-commands)\n    cache <sub>             Manage Valkey caches (run `velixir cache` for sub-commands)\n    doctor                  Verify prerequisites (credentials, API, project)\n    version                 Print the CLI version\n\n  Deploy options:\n    --app, -a <id>          Target app ID (overrides .velixir.json)\n    --slot, -s <slot>       Target deployment slot (default: production)\n    --template, -t <slug>   Deploy a starter template (nothing local needed)\n    --name, -n <name>       App name for a template deploy (default: the slug)\n    --with-services         Create + connect the database/cache a template uses\n    --no-services           Skip them (the app shows a setup page until one is bound)\n    --source-dir, -d <dir>  Directory to package as source (default: current dir)\n    --no-tail               Enqueue the deploy and return without waiting for it\n\n  Init options:\n    --app, -a <id>          Link to an existing app instead of creating one\n\n  Login options:\n    --key, -k <key>         API key, for CI and headless machines (skips the browser)\n    --no-browser            Paste a key at the prompt instead of approving in the browser\n    --url, -u <url>         API base URL (default: https:/\n[truncated]",
      "terminalNonHelp": false
    },
    {
      "command": "login",
      "helpInvocation": "login --help",
      "result": {
        "status": "failed",
        "timedOut": false,
        "exitCode": 1,
        "durationMs": 169,
        "stdout": "API Key: ✗",
        "stderr": "API key is required. Generate one at https://velixir.net/ApiKeys/Create",
        "outputLimitExceeded": false
      },
      "parsed": false,
      "payload": "API Key: ✗",
      "terminalNonHelp": false
    },
    {
      "command": "logout",
      "helpInvocation": "logout --help",
      "result": {
        "status": "ok",
        "timedOut": false,
        "exitCode": 0,
        "durationMs": 25,
        "stdout": "✓ Logged out. Stored API key cleared from ~/.velixir/config.json.",
        "stderr": null,
        "outputLimitExceeded": false
      },
      "parsed": false,
      "payload": "✓ Logged out. Stored API key cleared from ~/.velixir/config.json.",
      "terminalNonHelp": false
    },
    {
      "command": "init",
      "helpInvocation": "init --help",
      "result": {
        "status": "failed",
        "timedOut": false,
        "exitCode": 1,
        "durationMs": 215,
        "stdout": "✗",
        "stderr": "Not logged in. Run 'velixir login' first.",
        "outputLimitExceeded": false
      },
      "parsed": false,
      "payload": "✗",
      "terminalNonHelp": false
    },
    {
      "command": "deploy",
      "helpInvocation": "help deploy",
      "result": {
        "status": "ok",
        "timedOut": false,
        "exitCode": 0,
        "durationMs": 35,
        "stdout": "┌──────────────────────────────────────────┐\n  │          Velixir CLI v2.0.5               │\n  │    Deploy any app to the cloud            │\n  └──────────────────────────────────────────┘\n\n  Usage: velixir <command> [options]\n\n  Commands:\n    login                   Sign in (opens your browser)\n    logout                  Clear the stored API key from this machine\n    init                    Create or link a Velixir app (writes .velixir.json)\n    deploy                  Package source, upload, build + deploy\n    deploy --template <slug> Create an app from a starter template and deploy it\n    templates               List the starter templates\n    status                  Check current deployment status\n    apps                    List your apps\n    db <sub>                Manage Postgres databases (run `velixir db` for sub-commands)\n    cache <sub>             Manage Valkey caches (run `velixir cache` for sub-commands)\n    doctor                  Verify prerequisites (credentials, API, project)\n    version                 Print the CLI version\n\n  Deploy options:\n    --app, -a <id>          Target app ID (overrides .velixir.json)\n    --slot, -s <slot>       Target deployment slot (default: production)\n    --template, -t <slug>   Deploy a starter template (nothing local needed)\n    --name, -n <name>       App name for a template deploy (default: the slug)\n    --with-services         Create + connect the database/cache a template uses\n    --no-services           Skip them (the app shows a setup page until one is bound)\n    --source-dir, -d <dir>  Directory to package as source (default: current dir)\n    --no-tail               Enqueue the deploy and return without waiting for it\n\n  Init options:\n    --app, -a <id>          Link to an existing app instead of creating one\n\n  Login options:\n    --key, -k <key>         API key, for CI and headless machines (skips the browser)\n    --no-browser            Paste a key at the prompt instead of approving in the browser\n    --url, -u <url>         API base URL (default: https:/\n[truncated]",
        "stderr": null,
        "outputLimitExceeded": false
      },
      "parsed": true,
      "payload": "┌──────────────────────────────────────────┐\n  │          Velixir CLI v2.0.5               │\n  │    Deploy any app to the cloud            │\n  └──────────────────────────────────────────┘\n\n  Usage: velixir <command> [options]\n\n  Commands:\n    login                   Sign in (opens your browser)\n    logout                  Clear the stored API key from this machine\n    init                    Create or link a Velixir app (writes .velixir.json)\n    deploy                  Package source, upload, build + deploy\n    deploy --template <slug> Create an app from a starter template and deploy it\n    templates               List the starter templates\n    status                  Check current deployment status\n    apps                    List your apps\n    db <sub>                Manage Postgres databases (run `velixir db` for sub-commands)\n    cache <sub>             Manage Valkey caches (run `velixir cache` for sub-commands)\n    doctor                  Verify prerequisites (credentials, API, project)\n    version                 Print the CLI version\n\n  Deploy options:\n    --app, -a <id>          Target app ID (overrides .velixir.json)\n    --slot, -s <slot>       Target deployment slot (default: production)\n    --template, -t <slug>   Deploy a starter template (nothing local needed)\n    --name, -n <name>       App name for a template deploy (default: the slug)\n    --with-services         Create + connect the database/cache a template uses\n    --no-services           Skip them (the app shows a setup page until one is bound)\n    --source-dir, -d <dir>  Directory to package as source (default: current dir)\n    --no-tail               Enqueue the deploy and return without waiting for it\n\n  Init options:\n    --app, -a <id>          Link to an existing app instead of creating one\n\n  Login options:\n    --key, -k <key>         API key, for CI and headless machines (skips the browser)\n    --no-browser            Paste a key at the prompt instead of approving in the browser\n    --url, -u <url>         API base URL (default: https:/\n[truncated]",
      "terminalNonHelp": false
    },
    {
      "command": "templates",
      "helpInvocation": "templates --help",
      "result": {
        "status": "ok",
        "timedOut": false,
        "exitCode": 0,
        "durationMs": 2159,
        "stdout": "node-http  Node.js 22\n      A zero-dependency HTTP server on Node 22.\n  python-flask  Python 3.13\n      Flask behind gunicorn on Python 3.13.\n  go-http  Go 1.23\n      net/http on Go 1.23, standard library only.\n  dotnet-minimal-api  .NET 10\n      A minimal API on .NET 10.\n  deno-http  Deno\n      Deno.serve with TypeScript, no build step.\n  php-web  PHP 8.4\n      PHP 8.4 behind nginx and FPM.\n  ruby-sinatra  Ruby 3.4\n      Sinatra on Ruby 3.4.\n  rust-http  Rust 1.86\n      A std-only HTTP listener on Rust 1.86.\n  status-page  Go 1.23\n      A public status page for your services, with no dependencies.\n  node-postgres-notes  Node.js 22\n      A notes app wired to managed Postgres.\n      + managed Postgres\n  directus  Node.js 22\n      A headless CMS on a database you own.\n      + managed Postgres\n  n8n  Node.js 24\n      Workflow automation you host yourself.\n      + managed Postgres\n  ai-chat  Node.js 22\n      A private, streaming AI chat for Mistral, OpenAI or any compatible API.\n  background-jobs  Node.js 22\n      A job queue with a live dashboard, on managed Valkey.\n      + managed Valkey\n  discord-bot  Python 3.13\n      A Discord bot with slash commands and a status page that hands you its invite link.\n  django-postgres  Python 3.13\n      A Django guestbook with the admin, on managed Postgres.\n      + managed Postgres\n  fastapi-postgres  Python 3.13\n      A todo API with interactive docs, on managed Postgres.\n      + managed Postgres\n  mcp-server  Node.js 22\n      Your own MCP server for Claude Code, Cursor and VS Code, behind a secret token.\n  nextjs-postgres  Node.js 22\n      A Next.js 15 guestbook on managed Postgres, with Server Actions.\n      + managed Postgres\n  telegram-bot  Python 3.13\n      A Telegram bot on webhooks that sleeps between messages, so it runs on the Free plan.\n  umami  Node.js 22\n      Cookie-free, privacy-friendly web analytics, with every page view kept in your own managed Postgres.\n      + managed Postgres\n  hermes-agent  Python 3.13\n      A sandbox for Nous Research's Hermes Agent: an AI\n[truncated]",
        "stderr": null,
        "outputLimitExceeded": false
      },
      "parsed": false,
      "payload": "node-http  Node.js 22\n      A zero-dependency HTTP server on Node 22.\n  python-flask  Python 3.13\n      Flask behind gunicorn on Python 3.13.\n  go-http  Go 1.23\n      net/http on Go 1.23, standard library only.\n  dotnet-minimal-api  .NET 10\n      A minimal API on .NET 10.\n  deno-http  Deno\n      Deno.serve with TypeScript, no build step.\n  php-web  PHP 8.4\n      PHP 8.4 behind nginx and FPM.\n  ruby-sinatra  Ruby 3.4\n      Sinatra on Ruby 3.4.\n  rust-http  Rust 1.86\n      A std-only HTTP listener on Rust 1.86.\n  status-page  Go 1.23\n      A public status page for your services, with no dependencies.\n  node-postgres-notes  Node.js 22\n      A notes app wired to managed Postgres.\n      + managed Postgres\n  directus  Node.js 22\n      A headless CMS on a database you own.\n      + managed Postgres\n  n8n  Node.js 24\n      Workflow automation you host yourself.\n      + managed Postgres\n  ai-chat  Node.js 22\n      A private, streaming AI chat for Mistral, OpenAI or any compatible API.\n  background-jobs  Node.js 22\n      A job queue with a live dashboard, on managed Valkey.\n      + managed Valkey\n  discord-bot  Python 3.13\n      A Discord bot with slash commands and a status page that hands you its invite link.\n  django-postgres  Python 3.13\n      A Django guestbook with the admin, on managed Postgres.\n      + managed Postgres\n  fastapi-postgres  Python 3.13\n      A todo API with interactive docs, on managed Postgres.\n      + managed Postgres\n  mcp-server  Node.js 22\n      Your own MCP server for Claude Code, Cursor and VS Code, behind a secret token.\n  nextjs-postgres  Node.js 22\n      A Next.js 15 guestbook on managed Postgres, with Server Actions.\n      + managed Postgres\n  telegram-bot  Python 3.13\n      A Telegram bot on webhooks that sleeps between messages, so it runs on the Free plan.\n  umami  Node.js 22\n      Cookie-free, privacy-friendly web analytics, with every page view kept in your own managed Postgres.\n      + managed Postgres\n  hermes-agent  Python 3.13\n      A sandbox for Nous Research's Hermes Agent: an AI\n[truncated]",
      "terminalNonHelp": false
    },
    {
      "command": "status",
      "helpInvocation": "status --help",
      "result": {
        "status": "failed",
        "timedOut": false,
        "exitCode": 1,
        "durationMs": 29,
        "stdout": "✗",
        "stderr": "Not logged in. Run 'velixir login' first.",
        "outputLimitExceeded": false
      },
      "parsed": false,
      "payload": "✗",
      "terminalNonHelp": false
    },
    {
      "command": "apps",
      "helpInvocation": "apps --help",
      "result": {
        "status": "failed",
        "timedOut": false,
        "exitCode": 1,
        "durationMs": 28,
        "stdout": "✗",
        "stderr": "Not logged in. Run 'velixir login' first.",
        "outputLimitExceeded": false
      },
      "parsed": false,
      "payload": "✗",
      "terminalNonHelp": false
    },
    {
      "command": "db",
      "helpInvocation": "db --help",
      "result": {
        "status": "ok",
        "timedOut": false,
        "exitCode": 0,
        "durationMs": 25,
        "stdout": "Usage: velixir db <subcommand> [options]\n\n  Subcommands:\n    create                      Provision a new managed Postgres database\n    list                        List databases on your team\n    connect <id> [--format f]   Print connection string (format: url|adonet, default url)\n    bind <id> --app <app-id>    Bind to an app (env var DATABASE_URL by default; --env to override,\n                                --format adonet for the Npgsql / EF Core keyword form)\n    delete <id> [--yes]         Delete (confirms by re-typing the id unless --yes)",
        "stderr": null,
        "outputLimitExceeded": false
      },
      "parsed": true,
      "payload": "Usage: velixir db <subcommand> [options]\n\n  Subcommands:\n    create                      Provision a new managed Postgres database\n    list                        List databases on your team\n    connect <id> [--format f]   Print connection string (format: url|adonet, default url)\n    bind <id> --app <app-id>    Bind to an app (env var DATABASE_URL by default; --env to override,\n                                --format adonet for the Npgsql / EF Core keyword form)\n    delete <id> [--yes]         Delete (confirms by re-typing the id unless --yes)",
      "terminalNonHelp": false
    },
    {
      "command": "cache",
      "helpInvocation": "cache --help",
      "result": {
        "status": "ok",
        "timedOut": false,
        "exitCode": 0,
        "durationMs": 39,
        "stdout": "Usage: velixir cache <subcommand> [options]\n\n  Subcommands:\n    create                      Provision a new managed Valkey (Redis-compatible) cache\n    list                        List caches on your team\n    connect <id> [--format f]   Print connection string (format: url|adonet, default url)\n    bind <id> --app <app-id>    Bind to an app (env var REDIS_URL by default; --env to override,\n                                --format adonet for the StackExchange.Redis configuration form)\n    delete <id> [--yes]         Delete (confirms by re-typing the id unless --yes)",
        "stderr": null,
        "outputLimitExceeded": false
      },
      "parsed": true,
      "payload": "Usage: velixir cache <subcommand> [options]\n\n  Subcommands:\n    create                      Provision a new managed Valkey (Redis-compatible) cache\n    list                        List caches on your team\n    connect <id> [--format f]   Print connection string (format: url|adonet, default url)\n    bind <id> --app <app-id>    Bind to an app (env var REDIS_URL by default; --env to override,\n                                --format adonet for the StackExchange.Redis configuration form)\n    delete <id> [--yes]         Delete (confirms by re-typing the id unless --yes)",
      "terminalNonHelp": false
    },
    {
      "command": "doctor",
      "helpInvocation": "doctor --help",
      "result": {
        "status": "failed",
        "timedOut": false,
        "exitCode": 1,
        "durationMs": 37,
        "stdout": "● velixir doctor: checking your environment for v2.0.5\n\n  ✗  velixir credentials     not found\n       → Run `velixir login --key <key>` (generate at https://velixir.net/ApiKeys/Create).\n  •  Current directory       no project markers in cwd (run `velixir init` from your project root)\n\n✗",
        "stderr": "One or more checks failed. Fix the items flagged above, then re-run.",
        "outputLimitExceeded": false
      },
      "parsed": false,
      "payload": "● velixir doctor: checking your environment for v2.0.5\n\n  ✗  velixir credentials     not found\n       → Run `velixir login --key <key>` (generate at https://velixir.net/ApiKeys/Create).\n  •  Current directory       no project markers in cwd (run `velixir init` from your project root)\n\n✗",
      "terminalNonHelp": false
    },
    {
      "command": "db create",
      "helpInvocation": "help db create",
      "result": {
        "status": "ok",
        "timedOut": false,
        "exitCode": 0,
        "durationMs": 42,
        "stdout": "┌──────────────────────────────────────────┐\n  │          Velixir CLI v2.0.5               │\n  │    Deploy any app to the cloud            │\n  └──────────────────────────────────────────┘\n\n  Usage: velixir <command> [options]\n\n  Commands:\n    login                   Sign in (opens your browser)\n    logout                  Clear the stored API key from this machine\n    init                    Create or link a Velixir app (writes .velixir.json)\n    deploy                  Package source, upload, build + deploy\n    deploy --template <slug> Create an app from a starter template and deploy it\n    templates               List the starter templates\n    status                  Check current deployment status\n    apps                    List your apps\n    db <sub>                Manage Postgres databases (run `velixir db` for sub-commands)\n    cache <sub>             Manage Valkey caches (run `velixir cache` for sub-commands)\n    doctor                  Verify prerequisites (credentials, API, project)\n    version                 Print the CLI version\n\n  Deploy options:\n    --app, -a <id>          Target app ID (overrides .velixir.json)\n    --slot, -s <slot>       Target deployment slot (default: production)\n    --template, -t <slug>   Deploy a starter template (nothing local needed)\n    --name, -n <name>       App name for a template deploy (default: the slug)\n    --with-services         Create + connect the database/cache a template uses\n    --no-services           Skip them (the app shows a setup page until one is bound)\n    --source-dir, -d <dir>  Directory to package as source (default: current dir)\n    --no-tail               Enqueue the deploy and return without waiting for it\n\n  Init options:\n    --app, -a <id>          Link to an existing app instead of creating one\n\n  Login options:\n    --key, -k <key>         API key, for CI and headless machines (skips the browser)\n    --no-browser            Paste a key at the prompt instead of approving in the browser\n    --url, -u <url>         API base URL (default: https:/\n[truncated]",
        "stderr": null,
        "outputLimitExceeded": false
      },
      "parsed": true,
      "payload": "┌──────────────────────────────────────────┐\n  │          Velixir CLI v2.0.5               │\n  │    Deploy any app to the cloud            │\n  └──────────────────────────────────────────┘\n\n  Usage: velixir <command> [options]\n\n  Commands:\n    login                   Sign in (opens your browser)\n    logout                  Clear the stored API key from this machine\n    init                    Create or link a Velixir app (writes .velixir.json)\n    deploy                  Package source, upload, build + deploy\n    deploy --template <slug> Create an app from a starter template and deploy it\n    templates               List the starter templates\n    status                  Check current deployment status\n    apps                    List your apps\n    db <sub>                Manage Postgres databases (run `velixir db` for sub-commands)\n    cache <sub>             Manage Valkey caches (run `velixir cache` for sub-commands)\n    doctor                  Verify prerequisites (credentials, API, project)\n    version                 Print the CLI version\n\n  Deploy options:\n    --app, -a <id>          Target app ID (overrides .velixir.json)\n    --slot, -s <slot>       Target deployment slot (default: production)\n    --template, -t <slug>   Deploy a starter template (nothing local needed)\n    --name, -n <name>       App name for a template deploy (default: the slug)\n    --with-services         Create + connect the database/cache a template uses\n    --no-services           Skip them (the app shows a setup page until one is bound)\n    --source-dir, -d <dir>  Directory to package as source (default: current dir)\n    --no-tail               Enqueue the deploy and return without waiting for it\n\n  Init options:\n    --app, -a <id>          Link to an existing app instead of creating one\n\n  Login options:\n    --key, -k <key>         API key, for CI and headless machines (skips the browser)\n    --no-browser            Paste a key at the prompt instead of approving in the browser\n    --url, -u <url>         API base URL (default: https:/\n[truncated]",
      "terminalNonHelp": false
    },
    {
      "command": "db list",
      "helpInvocation": "help db list",
      "result": {
        "status": "ok",
        "timedOut": false,
        "exitCode": 0,
        "durationMs": 31,
        "stdout": "┌──────────────────────────────────────────┐\n  │          Velixir CLI v2.0.5               │\n  │    Deploy any app to the cloud            │\n  └──────────────────────────────────────────┘\n\n  Usage: velixir <command> [options]\n\n  Commands:\n    login                   Sign in (opens your browser)\n    logout                  Clear the stored API key from this machine\n    init                    Create or link a Velixir app (writes .velixir.json)\n    deploy                  Package source, upload, build + deploy\n    deploy --template <slug> Create an app from a starter template and deploy it\n    templates               List the starter templates\n    status                  Check current deployment status\n    apps                    List your apps\n    db <sub>                Manage Postgres databases (run `velixir db` for sub-commands)\n    cache <sub>             Manage Valkey caches (run `velixir cache` for sub-commands)\n    doctor                  Verify prerequisites (credentials, API, project)\n    version                 Print the CLI version\n\n  Deploy options:\n    --app, -a <id>          Target app ID (overrides .velixir.json)\n    --slot, -s <slot>       Target deployment slot (default: production)\n    --template, -t <slug>   Deploy a starter template (nothing local needed)\n    --name, -n <name>       App name for a template deploy (default: the slug)\n    --with-services         Create + connect the database/cache a template uses\n    --no-services           Skip them (the app shows a setup page until one is bound)\n    --source-dir, -d <dir>  Directory to package as source (default: current dir)\n    --no-tail               Enqueue the deploy and return without waiting for it\n\n  Init options:\n    --app, -a <id>          Link to an existing app instead of creating one\n\n  Login options:\n    --key, -k <key>         API key, for CI and headless machines (skips the browser)\n    --no-browser            Paste a key at the prompt instead of approving in the browser\n    --url, -u <url>         API base URL (default: https:/\n[truncated]",
        "stderr": null,
        "outputLimitExceeded": false
      },
      "parsed": true,
      "payload": "┌──────────────────────────────────────────┐\n  │          Velixir CLI v2.0.5               │\n  │    Deploy any app to the cloud            │\n  └──────────────────────────────────────────┘\n\n  Usage: velixir <command> [options]\n\n  Commands:\n    login                   Sign in (opens your browser)\n    logout                  Clear the stored API key from this machine\n    init                    Create or link a Velixir app (writes .velixir.json)\n    deploy                  Package source, upload, build + deploy\n    deploy --template <slug> Create an app from a starter template and deploy it\n    templates               List the starter templates\n    status                  Check current deployment status\n    apps                    List your apps\n    db <sub>                Manage Postgres databases (run `velixir db` for sub-commands)\n    cache <sub>             Manage Valkey caches (run `velixir cache` for sub-commands)\n    doctor                  Verify prerequisites (credentials, API, project)\n    version                 Print the CLI version\n\n  Deploy options:\n    --app, -a <id>          Target app ID (overrides .velixir.json)\n    --slot, -s <slot>       Target deployment slot (default: production)\n    --template, -t <slug>   Deploy a starter template (nothing local needed)\n    --name, -n <name>       App name for a template deploy (default: the slug)\n    --with-services         Create + connect the database/cache a template uses\n    --no-services           Skip them (the app shows a setup page until one is bound)\n    --source-dir, -d <dir>  Directory to package as source (default: current dir)\n    --no-tail               Enqueue the deploy and return without waiting for it\n\n  Init options:\n    --app, -a <id>          Link to an existing app instead of creating one\n\n  Login options:\n    --key, -k <key>         API key, for CI and headless machines (skips the browser)\n    --no-browser            Paste a key at the prompt instead of approving in the browser\n    --url, -u <url>         API base URL (default: https:/\n[truncated]",
      "terminalNonHelp": false
    },
    {
      "command": "db connect",
      "helpInvocation": "help db connect",
      "result": {
        "status": "ok",
        "timedOut": false,
        "exitCode": 0,
        "durationMs": 25,
        "stdout": "┌──────────────────────────────────────────┐\n  │          Velixir CLI v2.0.5               │\n  │    Deploy any app to the cloud            │\n  └──────────────────────────────────────────┘\n\n  Usage: velixir <command> [options]\n\n  Commands:\n    login                   Sign in (opens your browser)\n    logout                  Clear the stored API key from this machine\n    init                    Create or link a Velixir app (writes .velixir.json)\n    deploy                  Package source, upload, build + deploy\n    deploy --template <slug> Create an app from a starter template and deploy it\n    templates               List the starter templates\n    status                  Check current deployment status\n    apps                    List your apps\n    db <sub>                Manage Postgres databases (run `velixir db` for sub-commands)\n    cache <sub>             Manage Valkey caches (run `velixir cache` for sub-commands)\n    doctor                  Verify prerequisites (credentials, API, project)\n    version                 Print the CLI version\n\n  Deploy options:\n    --app, -a <id>          Target app ID (overrides .velixir.json)\n    --slot, -s <slot>       Target deployment slot (default: production)\n    --template, -t <slug>   Deploy a starter template (nothing local needed)\n    --name, -n <name>       App name for a template deploy (default: the slug)\n    --with-services         Create + connect the database/cache a template uses\n    --no-services           Skip them (the app shows a setup page until one is bound)\n    --source-dir, -d <dir>  Directory to package as source (default: current dir)\n    --no-tail               Enqueue the deploy and return without waiting for it\n\n  Init options:\n    --app, -a <id>          Link to an existing app instead of creating one\n\n  Login options:\n    --key, -k <key>         API key, for CI and headless machines (skips the browser)\n    --no-browser            Paste a key at the prompt instead of approving in the browser\n    --url, -u <url>         API base URL (default: https:/\n[truncated]",
        "stderr": null,
        "outputLimitExceeded": false
      },
      "parsed": true,
      "payload": "┌──────────────────────────────────────────┐\n  │          Velixir CLI v2.0.5               │\n  │    Deploy any app to the cloud            │\n  └──────────────────────────────────────────┘\n\n  Usage: velixir <command> [options]\n\n  Commands:\n    login                   Sign in (opens your browser)\n    logout                  Clear the stored API key from this machine\n    init                    Create or link a Velixir app (writes .velixir.json)\n    deploy                  Package source, upload, build + deploy\n    deploy --template <slug> Create an app from a starter template and deploy it\n    templates               List the starter templates\n    status                  Check current deployment status\n    apps                    List your apps\n    db <sub>                Manage Postgres databases (run `velixir db` for sub-commands)\n    cache <sub>             Manage Valkey caches (run `velixir cache` for sub-commands)\n    doctor                  Verify prerequisites (credentials, API, project)\n    version                 Print the CLI version\n\n  Deploy options:\n    --app, -a <id>          Target app ID (overrides .velixir.json)\n    --slot, -s <slot>       Target deployment slot (default: production)\n    --template, -t <slug>   Deploy a starter template (nothing local needed)\n    --name, -n <name>       App name for a template deploy (default: the slug)\n    --with-services         Create + connect the database/cache a template uses\n    --no-services           Skip them (the app shows a setup page until one is bound)\n    --source-dir, -d <dir>  Directory to package as source (default: current dir)\n    --no-tail               Enqueue the deploy and return without waiting for it\n\n  Init options:\n    --app, -a <id>          Link to an existing app instead of creating one\n\n  Login options:\n    --key, -k <key>         API key, for CI and headless machines (skips the browser)\n    --no-browser            Paste a key at the prompt instead of approving in the browser\n    --url, -u <url>         API base URL (default: https:/\n[truncated]",
      "terminalNonHelp": false
    },
    {
      "command": "db bind",
      "helpInvocation": "help db bind",
      "result": {
        "status": "ok",
        "timedOut": false,
        "exitCode": 0,
        "durationMs": 25,
        "stdout": "┌──────────────────────────────────────────┐\n  │          Velixir CLI v2.0.5               │\n  │    Deploy any app to the cloud            │\n  └──────────────────────────────────────────┘\n\n  Usage: velixir <command> [options]\n\n  Commands:\n    login                   Sign in (opens your browser)\n    logout                  Clear the stored API key from this machine\n    init                    Create or link a Velixir app (writes .velixir.json)\n    deploy                  Package source, upload, build + deploy\n    deploy --template <slug> Create an app from a starter template and deploy it\n    templates               List the starter templates\n    status                  Check current deployment status\n    apps                    List your apps\n    db <sub>                Manage Postgres databases (run `velixir db` for sub-commands)\n    cache <sub>             Manage Valkey caches (run `velixir cache` for sub-commands)\n    doctor                  Verify prerequisites (credentials, API, project)\n    version                 Print the CLI version\n\n  Deploy options:\n    --app, -a <id>          Target app ID (overrides .velixir.json)\n    --slot, -s <slot>       Target deployment slot (default: production)\n    --template, -t <slug>   Deploy a starter template (nothing local needed)\n    --name, -n <name>       App name for a template deploy (default: the slug)\n    --with-services         Create + connect the database/cache a template uses\n    --no-services           Skip them (the app shows a setup page until one is bound)\n    --source-dir, -d <dir>  Directory to package as source (default: current dir)\n    --no-tail               Enqueue the deploy and return without waiting for it\n\n  Init options:\n    --app, -a <id>          Link to an existing app instead of creating one\n\n  Login options:\n    --key, -k <key>         API key, for CI and headless machines (skips the browser)\n    --no-browser            Paste a key at the prompt instead of approving in the browser\n    --url, -u <url>         API base URL (default: https:/\n[truncated]",
        "stderr": null,
        "outputLimitExceeded": false
      },
      "parsed": true,
      "payload": "┌──────────────────────────────────────────┐\n  │          Velixir CLI v2.0.5               │\n  │    Deploy any app to the cloud            │\n  └──────────────────────────────────────────┘\n\n  Usage: velixir <command> [options]\n\n  Commands:\n    login                   Sign in (opens your browser)\n    logout                  Clear the stored API key from this machine\n    init                    Create or link a Velixir app (writes .velixir.json)\n    deploy                  Package source, upload, build + deploy\n    deploy --template <slug> Create an app from a starter template and deploy it\n    templates               List the starter templates\n    status                  Check current deployment status\n    apps                    List your apps\n    db <sub>                Manage Postgres databases (run `velixir db` for sub-commands)\n    cache <sub>             Manage Valkey caches (run `velixir cache` for sub-commands)\n    doctor                  Verify prerequisites (credentials, API, project)\n    version                 Print the CLI version\n\n  Deploy options:\n    --app, -a <id>          Target app ID (overrides .velixir.json)\n    --slot, -s <slot>       Target deployment slot (default: production)\n    --template, -t <slug>   Deploy a starter template (nothing local needed)\n    --name, -n <name>       App name for a template deploy (default: the slug)\n    --with-services         Create + connect the database/cache a template uses\n    --no-services           Skip them (the app shows a setup page until one is bound)\n    --source-dir, -d <dir>  Directory to package as source (default: current dir)\n    --no-tail               Enqueue the deploy and return without waiting for it\n\n  Init options:\n    --app, -a <id>          Link to an existing app instead of creating one\n\n  Login options:\n    --key, -k <key>         API key, for CI and headless machines (skips the browser)\n    --no-browser            Paste a key at the prompt instead of approving in the browser\n    --url, -u <url>         API base URL (default: https:/\n[truncated]",
      "terminalNonHelp": false
    },
    {
      "command": "db delete",
      "helpInvocation": "help db delete",
      "result": {
        "status": "ok",
        "timedOut": false,
        "exitCode": 0,
        "durationMs": 26,
        "stdout": "┌──────────────────────────────────────────┐\n  │          Velixir CLI v2.0.5               │\n  │    Deploy any app to the cloud            │\n  └──────────────────────────────────────────┘\n\n  Usage: velixir <command> [options]\n\n  Commands:\n    login                   Sign in (opens your browser)\n    logout                  Clear the stored API key from this machine\n    init                    Create or link a Velixir app (writes .velixir.json)\n    deploy                  Package source, upload, build + deploy\n    deploy --template <slug> Create an app from a starter template and deploy it\n    templates               List the starter templates\n    status                  Check current deployment status\n    apps                    List your apps\n    db <sub>                Manage Postgres databases (run `velixir db` for sub-commands)\n    cache <sub>             Manage Valkey caches (run `velixir cache` for sub-commands)\n    doctor                  Verify prerequisites (credentials, API, project)\n    version                 Print the CLI version\n\n  Deploy options:\n    --app, -a <id>          Target app ID (overrides .velixir.json)\n    --slot, -s <slot>       Target deployment slot (default: production)\n    --template, -t <slug>   Deploy a starter template (nothing local needed)\n    --name, -n <name>       App name for a template deploy (default: the slug)\n    --with-services         Create + connect the database/cache a template uses\n    --no-services           Skip them (the app shows a setup page until one is bound)\n    --source-dir, -d <dir>  Directory to package as source (default: current dir)\n    --no-tail               Enqueue the deploy and return without waiting for it\n\n  Init options:\n    --app, -a <id>          Link to an existing app instead of creating one\n\n  Login options:\n    --key, -k <key>         API key, for CI and headless machines (skips the browser)\n    --no-browser            Paste a key at the prompt instead of approving in the browser\n    --url, -u <url>         API base URL (default: https:/\n[truncated]",
        "stderr": null,
        "outputLimitExceeded": false
      },
      "parsed": true,
      "payload": "┌──────────────────────────────────────────┐\n  │          Velixir CLI v2.0.5               │\n  │    Deploy any app to the cloud            │\n  └──────────────────────────────────────────┘\n\n  Usage: velixir <command> [options]\n\n  Commands:\n    login                   Sign in (opens your browser)\n    logout                  Clear the stored API key from this machine\n    init                    Create or link a Velixir app (writes .velixir.json)\n    deploy                  Package source, upload, build + deploy\n    deploy --template <slug> Create an app from a starter template and deploy it\n    templates               List the starter templates\n    status                  Check current deployment status\n    apps                    List your apps\n    db <sub>                Manage Postgres databases (run `velixir db` for sub-commands)\n    cache <sub>             Manage Valkey caches (run `velixir cache` for sub-commands)\n    doctor                  Verify prerequisites (credentials, API, project)\n    version                 Print the CLI version\n\n  Deploy options:\n    --app, -a <id>          Target app ID (overrides .velixir.json)\n    --slot, -s <slot>       Target deployment slot (default: production)\n    --template, -t <slug>   Deploy a starter template (nothing local needed)\n    --name, -n <name>       App name for a template deploy (default: the slug)\n    --with-services         Create + connect the database/cache a template uses\n    --no-services           Skip them (the app shows a setup page until one is bound)\n    --source-dir, -d <dir>  Directory to package as source (default: current dir)\n    --no-tail               Enqueue the deploy and return without waiting for it\n\n  Init options:\n    --app, -a <id>          Link to an existing app instead of creating one\n\n  Login options:\n    --key, -k <key>         API key, for CI and headless machines (skips the browser)\n    --no-browser            Paste a key at the prompt instead of approving in the browser\n    --url, -u <url>         API base URL (default: https:/\n[truncated]",
      "terminalNonHelp": false
    }
  ]
}
