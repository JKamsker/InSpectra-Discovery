{
  "opencli": "0.1-draft",
  "info": {
    "title": "docxport (1.2.2+f70a417c80558a4535ec97eb5f886e4d8bb05ed7)",
    "version": "1.2.2"
  },
  "x-inspectra": {
    "artifactSource": "crawled-from-help",
    "generator": "InSpectra.Discovery",
    "helpDocumentCount": 1
  },
  "commands": [],
  "options": [
    {
      "name": "--format",
      "recursive": false,
      "hidden": false,
      "description": "Output format (default: markdown)\nIf --format is omitted, the format is inferred from -o/--output extension (.md/.html/.txt).",
      "arguments": [
        {
          "name": "FORMAT",
          "required": true,
          "arity": {
            "minimum": 1,
            "maximum": 1
          }
        }
      ]
    },
    {
      "name": "--tracked",
      "recursive": false,
      "hidden": false,
      "description": "Tracked change mode (accept, reject, inline, split). Plain text supports accept/reject.",
      "arguments": [
        {
          "name": "TRACKED",
          "required": true,
          "arity": {
            "minimum": 1,
            "maximum": 1
          }
        }
      ]
    },
    {
      "name": "--plain",
      "recursive": false,
      "hidden": false,
      "description": "Plain Markdown (only for markdown format)\n-o, --output=...  Output file path (default: swaps extension)\n--vars=...     Load DOCVARIABLE values from a JSON or INI file.\n-D name=value  Define a DOCVARIABLE (repeatable). CLI values override --vars.\n--log          Enable info-level logging to stderr.\n--log-level=...  Set log level (trace|debug|info|warn|error|critical|none).\n-v, --version  Show CLI version\n-h, --help     Show this help"
    }
  ],
  "arguments": [
    {
      "name": "INPUTDOCX",
      "required": true,
      "hidden": false,
      "arity": {
        "minimum": 1,
        "maximum": 1
      }
    }
  ]
}
