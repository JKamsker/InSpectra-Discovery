{
  "opencli": "0.1-draft",
  "info": {
    "title": "aimon",
    "version": "0.5.0-alpha.2",
    "description": "CLI for analyzing local Claude Code usage data (tokens, cost, sessions)."
  },
  "x-inspectra": {
    "artifactSource": "startup-hook",
    "generator": "InSpectra.Gen",
    "hookCapture": {
      "cliFramework": "System.CommandLine",
      "frameworkVersion": "2.0.11.0",
      "systemCommandLineVersion": "2.0.11.0",
      "patchTarget": "Parse-postfix ()"
    },
    "cliFramework": "System.CommandLine",
    "cliParsedDescription": "aimon - AI Usage Monitor"
  },
  "options": [
    {
      "name": "--help",
      "recursive": true,
      "hidden": false,
      "description": "Show help and usage information",
      "aliases": [
        "-h",
        "/h",
        "-?",
        "/?"
      ]
    },
    {
      "name": "--version",
      "recursive": false,
      "hidden": false,
      "description": "Show version information"
    }
  ],
  "commands": [
    {
      "name": "today",
      "description": "Show today's usage summary"
    },
    {
      "name": "week",
      "description": "Show last 7 days summary"
    },
    {
      "name": "month",
      "description": "Show last 30 days summary"
    },
    {
      "name": "models",
      "description": "Show model usage distribution"
    },
    {
      "name": "sessions",
      "description": "Show session statistics"
    },
    {
      "name": "hours",
      "description": "Show hourly activity distribution"
    },
    {
      "name": "export",
      "description": "Export usage data",
      "options": [
        {
          "name": "--format",
          "recursive": false,
          "hidden": false,
          "description": "Output format (json or csv) [default: json]",
          "arguments": [
            {
              "name": "FORMAT",
              "required": true,
              "arity": {
                "minimum": 1,
                "maximum": 1
              },
              "type": "String"
            }
          ]
        },
        {
          "name": "--output",
          "recursive": false,
          "hidden": false,
          "description": "Output file path (defaults to stdout)",
          "arguments": [
            {
              "name": "OUTPUT",
              "required": true,
              "arity": {
                "minimum": 1,
                "maximum": 1
              },
              "type": "String"
            }
          ]
        }
      ]
    },
    {
      "name": "watch",
      "description": "Continuously refresh a usage view at a fixed interval",
      "options": [
        {
          "name": "--view",
          "recursive": false,
          "hidden": false,
          "description": "View to refresh: limits, today, week, models, sessions, hours [default: limits]",
          "arguments": [
            {
              "name": "VIEW",
              "required": true,
              "arity": {
                "minimum": 1,
                "maximum": 1
              },
              "type": "String"
            }
          ]
        },
        {
          "name": "--interval",
          "recursive": false,
          "hidden": false,
          "description": "Refresh interval in seconds [default: 2]",
          "arguments": [
            {
              "name": "INTERVAL",
              "required": true,
              "arity": {
                "minimum": 1,
                "maximum": 1
              },
              "type": "Int32"
            }
          ]
        },
        {
          "name": "--session-reset",
          "recursive": false,
          "hidden": false,
          "description": "Real reset time of the current session window as local 'HH:mm' (e.g. 18:30), as shown by Claude itself. Only used when --view limits.",
          "arguments": [
            {
              "name": "SESSION_RESET",
              "required": true,
              "arity": {
                "minimum": 1,
                "maximum": 1
              },
              "type": "String"
            }
          ]
        },
        {
          "name": "--week-reset",
          "recursive": false,
          "hidden": false,
          "description": "Real weekly reset time as 'Ddd HH:mm' (e.g. 'Mon 09:00'), as shown by Claude itself. This is a fixed weekly schedule for your account, so it only needs to be set once. Only used when --view limits.",
          "arguments": [
            {
              "name": "WEEK_RESET",
              "required": true,
              "arity": {
                "minimum": 1,
                "maximum": 1
              },
              "type": "String"
            }
          ]
        },
        {
          "name": "--session-token-progress",
          "recursive": false,
          "hidden": false,
          "description": "Current session usage percentage as shown on Claude's own usage page (Settings > Usage, or /usage in Claude Code), e.g. 32 for '32%'. Combined with the tokens counted so far this session to derive a token budget, which is then used to track the token-progress bar live. Only used when --view limits.",
          "arguments": [
            {
              "name": "SESSION_TOKEN_PROGRESS",
              "required": true,
              "arity": {
                "minimum": 1,
                "maximum": 1
              },
              "type": "Double"
            }
          ]
        },
        {
          "name": "--week-token-progress",
          "recursive": false,
          "hidden": false,
          "description": "Current weekly usage percentage as shown on Claude's own usage page, e.g. 32 for '32%'. Combined with the tokens counted so far this week to derive a token budget. Only used when --view limits.",
          "arguments": [
            {
              "name": "WEEK_TOKEN_PROGRESS",
              "required": true,
              "arity": {
                "minimum": 1,
                "maximum": 1
              },
              "type": "Double"
            }
          ]
        }
      ]
    }
  ]
}
