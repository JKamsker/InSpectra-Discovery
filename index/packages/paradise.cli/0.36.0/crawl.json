{
  "commands": [
    {
      "command": null,
      "helpInvocation": "--help",
      "result": {
        "status": "failed",
        "timedOut": false,
        "exitCode": 2,
        "durationMs": 34,
        "stdout": null,
        "stderr": "usage: paradise <command> <verb> [options]\n\nnew <name> [--output <dir>]   create a project: assets tree, a sample level, .gitignore\n\nassets verify [--fix]         check the assets/ tree: sidecars, identities, validity\n                                --fix repoints reference paths a rename left stale\nassets prefab-check [--fix]   police (or restore) canonical form of *.prefab documents\nassets build [--profile p]    compile assets/ into build/ (or .editor/play with --editor)\nassets clean [--keep-editor]  delete derived output (build/, and .editor/ unless kept)\nassets watch                  keep *.meta in step, rebuilding .editor/play (play mode on)\n                                --no-editor rebuilds build/ instead; the tray toggles this\n                                --dry-run reports without writing; --no-build skips the rebuild\n                                a tray icon (idle/building/failed) on Windows and macOS\n                                --no-tray keeps the console-only behaviour\nassets mv <from> <to>         move a file or directory under assets/ with its sidecars,\n                                rewriting every prefab reference to the new path\nassets catalogue              regenerate the Asset Browser catalogue of prefabs (needs Blender)\n\ntools doctor                  report every build tool: found, version, and how to fix\n                                probes the same root `assets build` does (--project applies)\ntools install <ktx|slang>     install one (may prompt for elevation on Windows)\n\noptions:\n  --project <dir>             the project root (default: found from the working directory)\n  --profile <name>            a build profile declared in project.toml\n                                (omitted: the built-in defaults — toml, full quality)",
        "outputLimitExceeded": false
      },
      "parsed": true,
      "payload": "usage: paradise <command> <verb> [options]\n\nnew <name> [--output <dir>]   create a project: assets tree, a sample level, .gitignore\n\nassets verify [--fix]         check the assets/ tree: sidecars, identities, validity\n                                --fix repoints reference paths a rename left stale\nassets prefab-check [--fix]   police (or restore) canonical form of *.prefab documents\nassets build [--profile p]    compile assets/ into build/ (or .editor/play with --editor)\nassets clean [--keep-editor]  delete derived output (build/, and .editor/ unless kept)\nassets watch                  keep *.meta in step, rebuilding .editor/play (play mode on)\n                                --no-editor rebuilds build/ instead; the tray toggles this\n                                --dry-run reports without writing; --no-build skips the rebuild\n                                a tray icon (idle/building/failed) on Windows and macOS\n                                --no-tray keeps the console-only behaviour\nassets mv <from> <to>         move a file or directory under assets/ with its sidecars,\n                                rewriting every prefab reference to the new path\nassets catalogue              regenerate the Asset Browser catalogue of prefabs (needs Blender)\n\ntools doctor                  report every build tool: found, version, and how to fix\n                                probes the same root `assets build` does (--project applies)\ntools install <ktx|slang>     install one (may prompt for elevation on Windows)\n\noptions:\n  --project <dir>             the project root (default: found from the working directory)\n  --profile <name>            a build profile declared in project.toml\n                                (omitted: the built-in defaults — toml, full quality)",
      "terminalNonHelp": false
    },
    {
      "command": "verify",
      "helpInvocation": "verify --help",
      "result": {
        "status": "failed",
        "timedOut": false,
        "exitCode": 2,
        "durationMs": 35,
        "stdout": null,
        "stderr": "paradise: unknown command 'verify'\nusage: paradise <command> <verb> [options]\n\nnew <name> [--output <dir>]   create a project: assets tree, a sample level, .gitignore\n\nassets verify [--fix]         check the assets/ tree: sidecars, identities, validity\n                                --fix repoints reference paths a rename left stale\nassets prefab-check [--fix]   police (or restore) canonical form of *.prefab documents\nassets build [--profile p]    compile assets/ into build/ (or .editor/play with --editor)\nassets clean [--keep-editor]  delete derived output (build/, and .editor/ unless kept)\nassets watch                  keep *.meta in step, rebuilding .editor/play (play mode on)\n                                --no-editor rebuilds build/ instead; the tray toggles this\n                                --dry-run reports without writing; --no-build skips the rebuild\n                                a tray icon (idle/building/failed) on Windows and macOS\n                                --no-tray keeps the console-only behaviour\nassets mv <from> <to>         move a file or directory under assets/ with its sidecars,\n                                rewriting every prefab reference to the new path\nassets catalogue              regenerate the Asset Browser catalogue of prefabs (needs Blender)\n\ntools doctor                  report every build tool: found, version, and how to fix\n                                probes the same root `assets build` does (--project applies)\ntools install <ktx|slang>     install one (may prompt for elevation on Windows)\n\noptions:\n  --project <dir>             the project root (default: found from the working directory)\n  --profile <name>            a build profile declared in project.toml\n                                (omitted: the built-in defaults — toml, full quality)",
        "outputLimitExceeded": false
      },
      "parsed": true,
      "payload": "paradise: unknown command 'verify'\nusage: paradise <command> <verb> [options]\n\nnew <name> [--output <dir>]   create a project: assets tree, a sample level, .gitignore\n\nassets verify [--fix]         check the assets/ tree: sidecars, identities, validity\n                                --fix repoints reference paths a rename left stale\nassets prefab-check [--fix]   police (or restore) canonical form of *.prefab documents\nassets build [--profile p]    compile assets/ into build/ (or .editor/play with --editor)\nassets clean [--keep-editor]  delete derived output (build/, and .editor/ unless kept)\nassets watch                  keep *.meta in step, rebuilding .editor/play (play mode on)\n                                --no-editor rebuilds build/ instead; the tray toggles this\n                                --dry-run reports without writing; --no-build skips the rebuild\n                                a tray icon (idle/building/failed) on Windows and macOS\n                                --no-tray keeps the console-only behaviour\nassets mv <from> <to>         move a file or directory under assets/ with its sidecars,\n                                rewriting every prefab reference to the new path\nassets catalogue              regenerate the Asset Browser catalogue of prefabs (needs Blender)\n\ntools doctor                  report every build tool: found, version, and how to fix\n                                probes the same root `assets build` does (--project applies)\ntools install <ktx|slang>     install one (may prompt for elevation on Windows)\n\noptions:\n  --project <dir>             the project root (default: found from the working directory)\n  --profile <name>            a build profile declared in project.toml\n                                (omitted: the built-in defaults — toml, full quality)",
      "terminalNonHelp": false
    },
    {
      "command": "prefab-check",
      "helpInvocation": "prefab-check --help",
      "result": {
        "status": "failed",
        "timedOut": false,
        "exitCode": 2,
        "durationMs": 38,
        "stdout": null,
        "stderr": "paradise: unknown command 'prefab-check'\nusage: paradise <command> <verb> [options]\n\nnew <name> [--output <dir>]   create a project: assets tree, a sample level, .gitignore\n\nassets verify [--fix]         check the assets/ tree: sidecars, identities, validity\n                                --fix repoints reference paths a rename left stale\nassets prefab-check [--fix]   police (or restore) canonical form of *.prefab documents\nassets build [--profile p]    compile assets/ into build/ (or .editor/play with --editor)\nassets clean [--keep-editor]  delete derived output (build/, and .editor/ unless kept)\nassets watch                  keep *.meta in step, rebuilding .editor/play (play mode on)\n                                --no-editor rebuilds build/ instead; the tray toggles this\n                                --dry-run reports without writing; --no-build skips the rebuild\n                                a tray icon (idle/building/failed) on Windows and macOS\n                                --no-tray keeps the console-only behaviour\nassets mv <from> <to>         move a file or directory under assets/ with its sidecars,\n                                rewriting every prefab reference to the new path\nassets catalogue              regenerate the Asset Browser catalogue of prefabs (needs Blender)\n\ntools doctor                  report every build tool: found, version, and how to fix\n                                probes the same root `assets build` does (--project applies)\ntools install <ktx|slang>     install one (may prompt for elevation on Windows)\n\noptions:\n  --project <dir>             the project root (default: found from the working directory)\n  --profile <name>            a build profile declared in project.toml\n                                (omitted: the built-in defaults — toml, full quality)",
        "outputLimitExceeded": false
      },
      "parsed": true,
      "payload": "paradise: unknown command 'prefab-check'\nusage: paradise <command> <verb> [options]\n\nnew <name> [--output <dir>]   create a project: assets tree, a sample level, .gitignore\n\nassets verify [--fix]         check the assets/ tree: sidecars, identities, validity\n                                --fix repoints reference paths a rename left stale\nassets prefab-check [--fix]   police (or restore) canonical form of *.prefab documents\nassets build [--profile p]    compile assets/ into build/ (or .editor/play with --editor)\nassets clean [--keep-editor]  delete derived output (build/, and .editor/ unless kept)\nassets watch                  keep *.meta in step, rebuilding .editor/play (play mode on)\n                                --no-editor rebuilds build/ instead; the tray toggles this\n                                --dry-run reports without writing; --no-build skips the rebuild\n                                a tray icon (idle/building/failed) on Windows and macOS\n                                --no-tray keeps the console-only behaviour\nassets mv <from> <to>         move a file or directory under assets/ with its sidecars,\n                                rewriting every prefab reference to the new path\nassets catalogue              regenerate the Asset Browser catalogue of prefabs (needs Blender)\n\ntools doctor                  report every build tool: found, version, and how to fix\n                                probes the same root `assets build` does (--project applies)\ntools install <ktx|slang>     install one (may prompt for elevation on Windows)\n\noptions:\n  --project <dir>             the project root (default: found from the working directory)\n  --profile <name>            a build profile declared in project.toml\n                                (omitted: the built-in defaults — toml, full quality)",
      "terminalNonHelp": false
    },
    {
      "command": "build",
      "helpInvocation": "build --help",
      "result": {
        "status": "failed",
        "timedOut": false,
        "exitCode": 2,
        "durationMs": 35,
        "stdout": null,
        "stderr": "paradise: unknown command 'build'\nusage: paradise <command> <verb> [options]\n\nnew <name> [--output <dir>]   create a project: assets tree, a sample level, .gitignore\n\nassets verify [--fix]         check the assets/ tree: sidecars, identities, validity\n                                --fix repoints reference paths a rename left stale\nassets prefab-check [--fix]   police (or restore) canonical form of *.prefab documents\nassets build [--profile p]    compile assets/ into build/ (or .editor/play with --editor)\nassets clean [--keep-editor]  delete derived output (build/, and .editor/ unless kept)\nassets watch                  keep *.meta in step, rebuilding .editor/play (play mode on)\n                                --no-editor rebuilds build/ instead; the tray toggles this\n                                --dry-run reports without writing; --no-build skips the rebuild\n                                a tray icon (idle/building/failed) on Windows and macOS\n                                --no-tray keeps the console-only behaviour\nassets mv <from> <to>         move a file or directory under assets/ with its sidecars,\n                                rewriting every prefab reference to the new path\nassets catalogue              regenerate the Asset Browser catalogue of prefabs (needs Blender)\n\ntools doctor                  report every build tool: found, version, and how to fix\n                                probes the same root `assets build` does (--project applies)\ntools install <ktx|slang>     install one (may prompt for elevation on Windows)\n\noptions:\n  --project <dir>             the project root (default: found from the working directory)\n  --profile <name>            a build profile declared in project.toml\n                                (omitted: the built-in defaults — toml, full quality)",
        "outputLimitExceeded": false
      },
      "parsed": true,
      "payload": "paradise: unknown command 'build'\nusage: paradise <command> <verb> [options]\n\nnew <name> [--output <dir>]   create a project: assets tree, a sample level, .gitignore\n\nassets verify [--fix]         check the assets/ tree: sidecars, identities, validity\n                                --fix repoints reference paths a rename left stale\nassets prefab-check [--fix]   police (or restore) canonical form of *.prefab documents\nassets build [--profile p]    compile assets/ into build/ (or .editor/play with --editor)\nassets clean [--keep-editor]  delete derived output (build/, and .editor/ unless kept)\nassets watch                  keep *.meta in step, rebuilding .editor/play (play mode on)\n                                --no-editor rebuilds build/ instead; the tray toggles this\n                                --dry-run reports without writing; --no-build skips the rebuild\n                                a tray icon (idle/building/failed) on Windows and macOS\n                                --no-tray keeps the console-only behaviour\nassets mv <from> <to>         move a file or directory under assets/ with its sidecars,\n                                rewriting every prefab reference to the new path\nassets catalogue              regenerate the Asset Browser catalogue of prefabs (needs Blender)\n\ntools doctor                  report every build tool: found, version, and how to fix\n                                probes the same root `assets build` does (--project applies)\ntools install <ktx|slang>     install one (may prompt for elevation on Windows)\n\noptions:\n  --project <dir>             the project root (default: found from the working directory)\n  --profile <name>            a build profile declared in project.toml\n                                (omitted: the built-in defaults — toml, full quality)",
      "terminalNonHelp": false
    },
    {
      "command": "clean",
      "helpInvocation": "clean --help",
      "result": {
        "status": "failed",
        "timedOut": false,
        "exitCode": 2,
        "durationMs": 42,
        "stdout": null,
        "stderr": "paradise: unknown command 'clean'\nusage: paradise <command> <verb> [options]\n\nnew <name> [--output <dir>]   create a project: assets tree, a sample level, .gitignore\n\nassets verify [--fix]         check the assets/ tree: sidecars, identities, validity\n                                --fix repoints reference paths a rename left stale\nassets prefab-check [--fix]   police (or restore) canonical form of *.prefab documents\nassets build [--profile p]    compile assets/ into build/ (or .editor/play with --editor)\nassets clean [--keep-editor]  delete derived output (build/, and .editor/ unless kept)\nassets watch                  keep *.meta in step, rebuilding .editor/play (play mode on)\n                                --no-editor rebuilds build/ instead; the tray toggles this\n                                --dry-run reports without writing; --no-build skips the rebuild\n                                a tray icon (idle/building/failed) on Windows and macOS\n                                --no-tray keeps the console-only behaviour\nassets mv <from> <to>         move a file or directory under assets/ with its sidecars,\n                                rewriting every prefab reference to the new path\nassets catalogue              regenerate the Asset Browser catalogue of prefabs (needs Blender)\n\ntools doctor                  report every build tool: found, version, and how to fix\n                                probes the same root `assets build` does (--project applies)\ntools install <ktx|slang>     install one (may prompt for elevation on Windows)\n\noptions:\n  --project <dir>             the project root (default: found from the working directory)\n  --profile <name>            a build profile declared in project.toml\n                                (omitted: the built-in defaults — toml, full quality)",
        "outputLimitExceeded": false
      },
      "parsed": true,
      "payload": "paradise: unknown command 'clean'\nusage: paradise <command> <verb> [options]\n\nnew <name> [--output <dir>]   create a project: assets tree, a sample level, .gitignore\n\nassets verify [--fix]         check the assets/ tree: sidecars, identities, validity\n                                --fix repoints reference paths a rename left stale\nassets prefab-check [--fix]   police (or restore) canonical form of *.prefab documents\nassets build [--profile p]    compile assets/ into build/ (or .editor/play with --editor)\nassets clean [--keep-editor]  delete derived output (build/, and .editor/ unless kept)\nassets watch                  keep *.meta in step, rebuilding .editor/play (play mode on)\n                                --no-editor rebuilds build/ instead; the tray toggles this\n                                --dry-run reports without writing; --no-build skips the rebuild\n                                a tray icon (idle/building/failed) on Windows and macOS\n                                --no-tray keeps the console-only behaviour\nassets mv <from> <to>         move a file or directory under assets/ with its sidecars,\n                                rewriting every prefab reference to the new path\nassets catalogue              regenerate the Asset Browser catalogue of prefabs (needs Blender)\n\ntools doctor                  report every build tool: found, version, and how to fix\n                                probes the same root `assets build` does (--project applies)\ntools install <ktx|slang>     install one (may prompt for elevation on Windows)\n\noptions:\n  --project <dir>             the project root (default: found from the working directory)\n  --profile <name>            a build profile declared in project.toml\n                                (omitted: the built-in defaults — toml, full quality)",
      "terminalNonHelp": false
    },
    {
      "command": "watch",
      "helpInvocation": "watch --help",
      "result": {
        "status": "failed",
        "timedOut": false,
        "exitCode": 2,
        "durationMs": 40,
        "stdout": null,
        "stderr": "paradise: unknown command 'watch'\nusage: paradise <command> <verb> [options]\n\nnew <name> [--output <dir>]   create a project: assets tree, a sample level, .gitignore\n\nassets verify [--fix]         check the assets/ tree: sidecars, identities, validity\n                                --fix repoints reference paths a rename left stale\nassets prefab-check [--fix]   police (or restore) canonical form of *.prefab documents\nassets build [--profile p]    compile assets/ into build/ (or .editor/play with --editor)\nassets clean [--keep-editor]  delete derived output (build/, and .editor/ unless kept)\nassets watch                  keep *.meta in step, rebuilding .editor/play (play mode on)\n                                --no-editor rebuilds build/ instead; the tray toggles this\n                                --dry-run reports without writing; --no-build skips the rebuild\n                                a tray icon (idle/building/failed) on Windows and macOS\n                                --no-tray keeps the console-only behaviour\nassets mv <from> <to>         move a file or directory under assets/ with its sidecars,\n                                rewriting every prefab reference to the new path\nassets catalogue              regenerate the Asset Browser catalogue of prefabs (needs Blender)\n\ntools doctor                  report every build tool: found, version, and how to fix\n                                probes the same root `assets build` does (--project applies)\ntools install <ktx|slang>     install one (may prompt for elevation on Windows)\n\noptions:\n  --project <dir>             the project root (default: found from the working directory)\n  --profile <name>            a build profile declared in project.toml\n                                (omitted: the built-in defaults — toml, full quality)",
        "outputLimitExceeded": false
      },
      "parsed": true,
      "payload": "paradise: unknown command 'watch'\nusage: paradise <command> <verb> [options]\n\nnew <name> [--output <dir>]   create a project: assets tree, a sample level, .gitignore\n\nassets verify [--fix]         check the assets/ tree: sidecars, identities, validity\n                                --fix repoints reference paths a rename left stale\nassets prefab-check [--fix]   police (or restore) canonical form of *.prefab documents\nassets build [--profile p]    compile assets/ into build/ (or .editor/play with --editor)\nassets clean [--keep-editor]  delete derived output (build/, and .editor/ unless kept)\nassets watch                  keep *.meta in step, rebuilding .editor/play (play mode on)\n                                --no-editor rebuilds build/ instead; the tray toggles this\n                                --dry-run reports without writing; --no-build skips the rebuild\n                                a tray icon (idle/building/failed) on Windows and macOS\n                                --no-tray keeps the console-only behaviour\nassets mv <from> <to>         move a file or directory under assets/ with its sidecars,\n                                rewriting every prefab reference to the new path\nassets catalogue              regenerate the Asset Browser catalogue of prefabs (needs Blender)\n\ntools doctor                  report every build tool: found, version, and how to fix\n                                probes the same root `assets build` does (--project applies)\ntools install <ktx|slang>     install one (may prompt for elevation on Windows)\n\noptions:\n  --project <dir>             the project root (default: found from the working directory)\n  --profile <name>            a build profile declared in project.toml\n                                (omitted: the built-in defaults — toml, full quality)",
      "terminalNonHelp": false
    },
    {
      "command": "mv",
      "helpInvocation": "mv --help",
      "result": {
        "status": "failed",
        "timedOut": false,
        "exitCode": 2,
        "durationMs": 49,
        "stdout": null,
        "stderr": "paradise: unknown command 'mv'\nusage: paradise <command> <verb> [options]\n\nnew <name> [--output <dir>]   create a project: assets tree, a sample level, .gitignore\n\nassets verify [--fix]         check the assets/ tree: sidecars, identities, validity\n                                --fix repoints reference paths a rename left stale\nassets prefab-check [--fix]   police (or restore) canonical form of *.prefab documents\nassets build [--profile p]    compile assets/ into build/ (or .editor/play with --editor)\nassets clean [--keep-editor]  delete derived output (build/, and .editor/ unless kept)\nassets watch                  keep *.meta in step, rebuilding .editor/play (play mode on)\n                                --no-editor rebuilds build/ instead; the tray toggles this\n                                --dry-run reports without writing; --no-build skips the rebuild\n                                a tray icon (idle/building/failed) on Windows and macOS\n                                --no-tray keeps the console-only behaviour\nassets mv <from> <to>         move a file or directory under assets/ with its sidecars,\n                                rewriting every prefab reference to the new path\nassets catalogue              regenerate the Asset Browser catalogue of prefabs (needs Blender)\n\ntools doctor                  report every build tool: found, version, and how to fix\n                                probes the same root `assets build` does (--project applies)\ntools install <ktx|slang>     install one (may prompt for elevation on Windows)\n\noptions:\n  --project <dir>             the project root (default: found from the working directory)\n  --profile <name>            a build profile declared in project.toml\n                                (omitted: the built-in defaults — toml, full quality)",
        "outputLimitExceeded": false
      },
      "parsed": true,
      "payload": "paradise: unknown command 'mv'\nusage: paradise <command> <verb> [options]\n\nnew <name> [--output <dir>]   create a project: assets tree, a sample level, .gitignore\n\nassets verify [--fix]         check the assets/ tree: sidecars, identities, validity\n                                --fix repoints reference paths a rename left stale\nassets prefab-check [--fix]   police (or restore) canonical form of *.prefab documents\nassets build [--profile p]    compile assets/ into build/ (or .editor/play with --editor)\nassets clean [--keep-editor]  delete derived output (build/, and .editor/ unless kept)\nassets watch                  keep *.meta in step, rebuilding .editor/play (play mode on)\n                                --no-editor rebuilds build/ instead; the tray toggles this\n                                --dry-run reports without writing; --no-build skips the rebuild\n                                a tray icon (idle/building/failed) on Windows and macOS\n                                --no-tray keeps the console-only behaviour\nassets mv <from> <to>         move a file or directory under assets/ with its sidecars,\n                                rewriting every prefab reference to the new path\nassets catalogue              regenerate the Asset Browser catalogue of prefabs (needs Blender)\n\ntools doctor                  report every build tool: found, version, and how to fix\n                                probes the same root `assets build` does (--project applies)\ntools install <ktx|slang>     install one (may prompt for elevation on Windows)\n\noptions:\n  --project <dir>             the project root (default: found from the working directory)\n  --profile <name>            a build profile declared in project.toml\n                                (omitted: the built-in defaults — toml, full quality)",
      "terminalNonHelp": false
    },
    {
      "command": "catalogue",
      "helpInvocation": "catalogue --help",
      "result": {
        "status": "failed",
        "timedOut": false,
        "exitCode": 2,
        "durationMs": 34,
        "stdout": null,
        "stderr": "paradise: unknown command 'catalogue'\nusage: paradise <command> <verb> [options]\n\nnew <name> [--output <dir>]   create a project: assets tree, a sample level, .gitignore\n\nassets verify [--fix]         check the assets/ tree: sidecars, identities, validity\n                                --fix repoints reference paths a rename left stale\nassets prefab-check [--fix]   police (or restore) canonical form of *.prefab documents\nassets build [--profile p]    compile assets/ into build/ (or .editor/play with --editor)\nassets clean [--keep-editor]  delete derived output (build/, and .editor/ unless kept)\nassets watch                  keep *.meta in step, rebuilding .editor/play (play mode on)\n                                --no-editor rebuilds build/ instead; the tray toggles this\n                                --dry-run reports without writing; --no-build skips the rebuild\n                                a tray icon (idle/building/failed) on Windows and macOS\n                                --no-tray keeps the console-only behaviour\nassets mv <from> <to>         move a file or directory under assets/ with its sidecars,\n                                rewriting every prefab reference to the new path\nassets catalogue              regenerate the Asset Browser catalogue of prefabs (needs Blender)\n\ntools doctor                  report every build tool: found, version, and how to fix\n                                probes the same root `assets build` does (--project applies)\ntools install <ktx|slang>     install one (may prompt for elevation on Windows)\n\noptions:\n  --project <dir>             the project root (default: found from the working directory)\n  --profile <name>            a build profile declared in project.toml\n                                (omitted: the built-in defaults — toml, full quality)",
        "outputLimitExceeded": false
      },
      "parsed": true,
      "payload": "paradise: unknown command 'catalogue'\nusage: paradise <command> <verb> [options]\n\nnew <name> [--output <dir>]   create a project: assets tree, a sample level, .gitignore\n\nassets verify [--fix]         check the assets/ tree: sidecars, identities, validity\n                                --fix repoints reference paths a rename left stale\nassets prefab-check [--fix]   police (or restore) canonical form of *.prefab documents\nassets build [--profile p]    compile assets/ into build/ (or .editor/play with --editor)\nassets clean [--keep-editor]  delete derived output (build/, and .editor/ unless kept)\nassets watch                  keep *.meta in step, rebuilding .editor/play (play mode on)\n                                --no-editor rebuilds build/ instead; the tray toggles this\n                                --dry-run reports without writing; --no-build skips the rebuild\n                                a tray icon (idle/building/failed) on Windows and macOS\n                                --no-tray keeps the console-only behaviour\nassets mv <from> <to>         move a file or directory under assets/ with its sidecars,\n                                rewriting every prefab reference to the new path\nassets catalogue              regenerate the Asset Browser catalogue of prefabs (needs Blender)\n\ntools doctor                  report every build tool: found, version, and how to fix\n                                probes the same root `assets build` does (--project applies)\ntools install <ktx|slang>     install one (may prompt for elevation on Windows)\n\noptions:\n  --project <dir>             the project root (default: found from the working directory)\n  --profile <name>            a build profile declared in project.toml\n                                (omitted: the built-in defaults — toml, full quality)",
      "terminalNonHelp": false
    }
  ]
}
