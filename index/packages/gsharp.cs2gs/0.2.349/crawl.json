{
  "commands": [
    {
      "command": null,
      "helpInvocation": "--help",
      "result": {
        "status": "ok",
        "timedOut": false,
        "exitCode": 0,
        "durationMs": 33,
        "stdout": "cs2gs - C# to G# migration tool (ADR-0115)\n\nUsage:\n  cs2gs migrate [options]\n  cs2gs report --run <runDir> [--out <file-or-dir>]\n\nmigrate options:\n  --corpus <dir>    Corpus root (default: tools/cs2gs/corpus).\n  --app <id>        Migrate only this app (repeatable, e.g. corpus/L1-Console).\n  --gsc <path>      Override gsc.dll (default: out/bin/<Config>/Compiler/gsc.dll).\n  --out <dir>       Runs root for artifacts (default: ./cs2gs-runs).\n  --config <name>   Build config used to find gsc (default: Release).\n\nreport options:\n  --run <dir>       Existing run directory containing run.json (required).\n  --out <dir>       Output directory for report.html + summary.json (default: the run dir).\n\nA migrate run also writes report.html + summary.json into the run dir automatically (ADR-0115 §F).\n\nExit code is non-zero if any app fails a stage (so CI can gate).",
        "stderr": null,
        "outputLimitExceeded": false
      },
      "parsed": true,
      "payload": "cs2gs - C# to G# migration tool (ADR-0115)\n\nUsage:\n  cs2gs migrate [options]\n  cs2gs report --run <runDir> [--out <file-or-dir>]\n\nmigrate options:\n  --corpus <dir>    Corpus root (default: tools/cs2gs/corpus).\n  --app <id>        Migrate only this app (repeatable, e.g. corpus/L1-Console).\n  --gsc <path>      Override gsc.dll (default: out/bin/<Config>/Compiler/gsc.dll).\n  --out <dir>       Runs root for artifacts (default: ./cs2gs-runs).\n  --config <name>   Build config used to find gsc (default: Release).\n\nreport options:\n  --run <dir>       Existing run directory containing run.json (required).\n  --out <dir>       Output directory for report.html + summary.json (default: the run dir).\n\nA migrate run also writes report.html + summary.json into the run dir automatically (ADR-0115 §F).\n\nExit code is non-zero if any app fails a stage (so CI can gate).",
      "terminalNonHelp": false
    },
    {
      "command": "migrate",
      "helpInvocation": "migrate --help",
      "result": {
        "status": "failed",
        "timedOut": false,
        "exitCode": 1,
        "durationMs": 36,
        "stdout": "cs2gs - C# to G# migration tool (ADR-0115)\n\nUsage:\n  cs2gs migrate [options]\n  cs2gs report --run <runDir> [--out <file-or-dir>]\n\nmigrate options:\n  --corpus <dir>    Corpus root (default: tools/cs2gs/corpus).\n  --app <id>        Migrate only this app (repeatable, e.g. corpus/L1-Console).\n  --gsc <path>      Override gsc.dll (default: out/bin/<Config>/Compiler/gsc.dll).\n  --out <dir>       Runs root for artifacts (default: ./cs2gs-runs).\n  --config <name>   Build config used to find gsc (default: Release).\n\nreport options:\n  --run <dir>       Existing run directory containing run.json (required).\n  --out <dir>       Output directory for report.html + summary.json (default: the run dir).\n\nA migrate run also writes report.html + summary.json into the run dir automatically (ADR-0115 §F).\n\nExit code is non-zero if any app fails a stage (so CI can gate).",
        "stderr": "cs2gs: unknown option '--help'.",
        "outputLimitExceeded": false
      },
      "parsed": true,
      "payload": "cs2gs - C# to G# migration tool (ADR-0115)\n\nUsage:\n  cs2gs migrate [options]\n  cs2gs report --run <runDir> [--out <file-or-dir>]\n\nmigrate options:\n  --corpus <dir>    Corpus root (default: tools/cs2gs/corpus).\n  --app <id>        Migrate only this app (repeatable, e.g. corpus/L1-Console).\n  --gsc <path>      Override gsc.dll (default: out/bin/<Config>/Compiler/gsc.dll).\n  --out <dir>       Runs root for artifacts (default: ./cs2gs-runs).\n  --config <name>   Build config used to find gsc (default: Release).\n\nreport options:\n  --run <dir>       Existing run directory containing run.json (required).\n  --out <dir>       Output directory for report.html + summary.json (default: the run dir).\n\nA migrate run also writes report.html + summary.json into the run dir automatically (ADR-0115 §F).\n\nExit code is non-zero if any app fails a stage (so CI can gate).",
      "terminalNonHelp": false
    },
    {
      "command": "report",
      "helpInvocation": "report --help",
      "result": {
        "status": "failed",
        "timedOut": false,
        "exitCode": 1,
        "durationMs": 35,
        "stdout": "cs2gs - C# to G# migration tool (ADR-0115)\n\nUsage:\n  cs2gs migrate [options]\n  cs2gs report --run <runDir> [--out <file-or-dir>]\n\nmigrate options:\n  --corpus <dir>    Corpus root (default: tools/cs2gs/corpus).\n  --app <id>        Migrate only this app (repeatable, e.g. corpus/L1-Console).\n  --gsc <path>      Override gsc.dll (default: out/bin/<Config>/Compiler/gsc.dll).\n  --out <dir>       Runs root for artifacts (default: ./cs2gs-runs).\n  --config <name>   Build config used to find gsc (default: Release).\n\nreport options:\n  --run <dir>       Existing run directory containing run.json (required).\n  --out <dir>       Output directory for report.html + summary.json (default: the run dir).\n\nA migrate run also writes report.html + summary.json into the run dir automatically (ADR-0115 §F).\n\nExit code is non-zero if any app fails a stage (so CI can gate).",
        "stderr": "cs2gs: unknown option '--help'.",
        "outputLimitExceeded": false
      },
      "parsed": true,
      "payload": "cs2gs - C# to G# migration tool (ADR-0115)\n\nUsage:\n  cs2gs migrate [options]\n  cs2gs report --run <runDir> [--out <file-or-dir>]\n\nmigrate options:\n  --corpus <dir>    Corpus root (default: tools/cs2gs/corpus).\n  --app <id>        Migrate only this app (repeatable, e.g. corpus/L1-Console).\n  --gsc <path>      Override gsc.dll (default: out/bin/<Config>/Compiler/gsc.dll).\n  --out <dir>       Runs root for artifacts (default: ./cs2gs-runs).\n  --config <name>   Build config used to find gsc (default: Release).\n\nreport options:\n  --run <dir>       Existing run directory containing run.json (required).\n  --out <dir>       Output directory for report.html + summary.json (default: the run dir).\n\nA migrate run also writes report.html + summary.json into the run dir automatically (ADR-0115 §F).\n\nExit code is non-zero if any app fails a stage (so CI can gate).",
      "terminalNonHelp": false
    }
  ]
}
