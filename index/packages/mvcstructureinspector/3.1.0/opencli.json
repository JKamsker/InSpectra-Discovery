{
  "opencli": "0.1-draft",
  "info": {
    "title": "MVC Structure Inspector — Static Structural Analysis for ASP.NET Core",
    "version": "3.1.0",
    "description": "A Roslyn 5.0-powered .NET 10 global CLI tool for static structural analysis of ASP.NET Core MVC projects. Performs deep AST traversal with C# 14 support — including delegate signatures, lambda/closure analysis, captured variable detection, and expression body inspection. Generates comprehensive structural reports, gap analysis, snapshot-based drift detection, intelligent .gitignore generation, and OS kernel diagnostics via lambda probes."
  },
  "x-inspectra": {
    "artifactSource": "crawled-from-help",
    "generator": "InSpectra.Discovery",
    "helpDocumentCount": 1,
    "cliParsedTitle": "mvc-inspect"
  },
  "commands": [
    {
      "name": "gitignore",
      "hidden": false,
      "description": "Generate .gitignore with smart project detection"
    },
    {
      "name": "open",
      "hidden": false,
      "description": "Open an existing report with the default viewer"
    },
    {
      "name": "verify",
      "hidden": false,
      "description": "Run self-verification (integrity, security, compatibility)"
    }
  ],
  "options": [
    {
      "name": "--version",
      "recursive": false,
      "hidden": false,
      "description": "Show version and build information",
      "aliases": [
        "-v"
      ]
    },
    {
      "name": "--out",
      "recursive": false,
      "hidden": false,
      "description": "Override the auto output path",
      "arguments": [
        {
          "name": "FILE",
          "required": true,
          "arity": {
            "minimum": 1,
            "maximum": 1
          }
        }
      ]
    },
    {
      "name": "--open",
      "recursive": false,
      "hidden": false,
      "description": "Open the report automatically after saving"
    },
    {
      "name": "--with-proj",
      "recursive": false,
      "hidden": false,
      "description": "Include .csproj and .sln comparison (compare only)"
    },
    {
      "name": "--no-views",
      "recursive": false,
      "hidden": false,
      "description": "Exclude .cshtml files"
    },
    {
      "name": "--cs-only",
      "recursive": false,
      "hidden": false,
      "description": "C# files only"
    },
    {
      "name": "--no-migrations",
      "recursive": false,
      "hidden": false,
      "description": "Exclude Migrations folder",
      "arguments": [
        {
          "name": "NO_MIGRATIONS",
          "required": false,
          "arity": {
            "minimum": 0,
            "maximum": 1
          }
        }
      ]
    },
    {
      "name": "--preview",
      "recursive": false,
      "hidden": false,
      "description": "Preview generated .gitignore without writing to disk"
    },
    {
      "name": "--merge",
      "recursive": false,
      "hidden": false,
      "description": "Merge with existing .gitignore (append unique patterns only)"
    },
    {
      "name": "--add",
      "recursive": false,
      "hidden": false,
      "description": "Add custom patterns (e.g., --add \"*.log\" \"tmp/\" \"secrets/\")\nNotes:\nWhen inspecting a project, a .snapshot.json file is saved alongside the .txt report.\nThis snapshot can later be used with --from-report to track structural drift over time.\nThe gitignore command detects all project ecosystems (including nested/hybrid projects)\nand NEVER overwrites an existing .gitignore — it creates a .gitignore.generated_* file instead.\nUse --merge to safely append missing patterns to an existing .gitignore.\nUse 'verify' to run security and compatibility checks without any access keys.",
      "arguments": [
        {
          "name": "PATTERNS",
          "required": true,
          "arity": {
            "minimum": 1,
            "maximum": 1
          }
        }
      ]
    }
  ]
}
