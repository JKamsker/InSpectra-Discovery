{
  "commands": [
    {
      "command": null,
      "helpInvocation": "--help",
      "result": {
        "status": "ok",
        "timedOut": false,
        "exitCode": 0,
        "durationMs": 148,
        "stdout": "albuild - Business Central dev-loop CLI (an MCP client over the businessdev.ALbuild engine)\n\nUsage: albuild <command> [options]\n\nCommands:\n  doctor                 Check host readiness (PowerShell + module + Docker).\n  setup                  Guided setup: check host, module, albuild.json; print next steps.\n  update                 Update the PowerShell module + MCP server in one go (the CLI updates via 'dotnet tool update').\n  module install         Install the businessdev.ALbuild PowerShell module (PSGallery).\n  module update          Update the businessdev.ALbuild PowerShell module.\n  module status          Show the installed/used businessdev.ALbuild module.\n  init                   Create an albuild.json in the workspace.\n  feeds add              Add a package feed to albuild.json: feeds add <url> [--name --kind apps|symbols|runtime --id-scheme --api-key-env VAR].\n  feeds list             List the feeds in albuild.json (read-only).\n  feeds remove           Remove a feed from albuild.json by url or --name.  [destructive]\n  agents list            List configured MCP agents and their tools.\n  agents add             Add/replace an MCP agent: agents add <name> --tools a,b,c (or *).\n  agents remove          Remove an MCP agent.  [destructive]\n  agents token           Generate a bearer token for an agent (for the MCP HTTP host).\n  artifact find          Resolve a BC artifact URL.\n  container list         List BC containers (read-only).\n  container inspect      Show a container + its installed apps.\n  container new          Provision or warm-reuse a BC container (idempotent).\n  container restart      Restart a container.\n  container remove       Remove a container (destructive; shared -> approval).  [destructive]\n  deps plan              Dry-run dependency resolution for a project (read-only).\n  deps check             Reconcile manifest vs container vs feeds (read-only).\n  deps install           Resolve + install the dependency closure into a container.\n  app build              Compile the workspace fresh (fresh symbo\n[truncated]",
        "stderr": null,
        "outputLimitExceeded": false
      },
      "parsed": true,
      "payload": "albuild - Business Central dev-loop CLI (an MCP client over the businessdev.ALbuild engine)\n\nUsage: albuild <command> [options]\n\nCommands:\n  doctor                 Check host readiness (PowerShell + module + Docker).\n  setup                  Guided setup: check host, module, albuild.json; print next steps.\n  update                 Update the PowerShell module + MCP server in one go (the CLI updates via 'dotnet tool update').\n  module install         Install the businessdev.ALbuild PowerShell module (PSGallery).\n  module update          Update the businessdev.ALbuild PowerShell module.\n  module status          Show the installed/used businessdev.ALbuild module.\n  init                   Create an albuild.json in the workspace.\n  feeds add              Add a package feed to albuild.json: feeds add <url> [--name --kind apps|symbols|runtime --id-scheme --api-key-env VAR].\n  feeds list             List the feeds in albuild.json (read-only).\n  feeds remove           Remove a feed from albuild.json by url or --name.  [destructive]\n  agents list            List configured MCP agents and their tools.\n  agents add             Add/replace an MCP agent: agents add <name> --tools a,b,c (or *).\n  agents remove          Remove an MCP agent.  [destructive]\n  agents token           Generate a bearer token for an agent (for the MCP HTTP host).\n  artifact find          Resolve a BC artifact URL.\n  container list         List BC containers (read-only).\n  container inspect      Show a container + its installed apps.\n  container new          Provision or warm-reuse a BC container (idempotent).\n  container restart      Restart a container.\n  container remove       Remove a container (destructive; shared -> approval).  [destructive]\n  deps plan              Dry-run dependency resolution for a project (read-only).\n  deps check             Reconcile manifest vs container vs feeds (read-only).\n  deps install           Resolve + install the dependency closure into a container.\n  app build              Compile the workspace fresh (fresh symbo\n[truncated]",
      "terminalNonHelp": false
    },
    {
      "command": "doctor",
      "helpInvocation": "doctor --help",
      "result": {
        "status": "failed",
        "timedOut": false,
        "exitCode": 3,
        "durationMs": 13073,
        "stdout": null,
        "stderr": "[ OK ] PowerShell 7.6.3\n[FAIL] businessdev.ALbuild module not found\n[WARN] Docker is in 'linux' mode (BC needs Windows containers)\n[INFO] businessdev.ALbuild latest on PSGallery: 2.2.26743\n[ OK ] albuild CLI up to date (0.1.26744)\n[INFO] albuild-mcp server latest on npm: 0.1.26729 (not installed locally)\n✗ error [HOST_NOT_READY]: Host preflight failed (PowerShell / businessdev.ALbuild module / Docker).\n  Run 'albuild module install', then re-check. See https://docs.365businessdev.com/albuild/mcp-server/get-started.",
        "outputLimitExceeded": false
      },
      "parsed": false,
      "payload": "[ OK ] PowerShell 7.6.3\n[FAIL] businessdev.ALbuild module not found\n[WARN] Docker is in 'linux' mode (BC needs Windows containers)\n[INFO] businessdev.ALbuild latest on PSGallery: 2.2.26743\n[ OK ] albuild CLI up to date (0.1.26744)\n[INFO] albuild-mcp server latest on npm: 0.1.26729 (not installed locally)\n✗ error [HOST_NOT_READY]: Host preflight failed (PowerShell / businessdev.ALbuild module / Docker).\n  Run 'albuild module install', then re-check. See https://docs.365businessdev.com/albuild/mcp-server/get-started.",
      "terminalNonHelp": false
    },
    {
      "command": "setup",
      "helpInvocation": "help setup",
      "result": {
        "status": "failed",
        "timedOut": false,
        "exitCode": 2,
        "durationMs": 59,
        "stdout": "albuild - Business Central dev-loop CLI (an MCP client over the businessdev.ALbuild engine)\n\nUsage: albuild <command> [options]\n\nCommands:\n  doctor                 Check host readiness (PowerShell + module + Docker).\n  setup                  Guided setup: check host, module, albuild.json; print next steps.\n  update                 Update the PowerShell module + MCP server in one go (the CLI updates via 'dotnet tool update').\n  module install         Install the businessdev.ALbuild PowerShell module (PSGallery).\n  module update          Update the businessdev.ALbuild PowerShell module.\n  module status          Show the installed/used businessdev.ALbuild module.\n  init                   Create an albuild.json in the workspace.\n  feeds add              Add a package feed to albuild.json: feeds add <url> [--name --kind apps|symbols|runtime --id-scheme --api-key-env VAR].\n  feeds list             List the feeds in albuild.json (read-only).\n  feeds remove           Remove a feed from albuild.json by url or --name.  [destructive]\n  agents list            List configured MCP agents and their tools.\n  agents add             Add/replace an MCP agent: agents add <name> --tools a,b,c (or *).\n  agents remove          Remove an MCP agent.  [destructive]\n  agents token           Generate a bearer token for an agent (for the MCP HTTP host).\n  artifact find          Resolve a BC artifact URL.\n  container list         List BC containers (read-only).\n  container inspect      Show a container + its installed apps.\n  container new          Provision or warm-reuse a BC container (idempotent).\n  container restart      Restart a container.\n  container remove       Remove a container (destructive; shared -> approval).  [destructive]\n  deps plan              Dry-run dependency resolution for a project (read-only).\n  deps check             Reconcile manifest vs container vs feeds (read-only).\n  deps install           Resolve + install the dependency closure into a container.\n  app build              Compile the workspace fresh (fresh symbo\n[truncated]",
        "stderr": "Unknown command: help setup",
        "outputLimitExceeded": false
      },
      "parsed": false,
      "payload": "albuild - Business Central dev-loop CLI (an MCP client over the businessdev.ALbuild engine)\n\nUsage: albuild <command> [options]\n\nCommands:\n  doctor                 Check host readiness (PowerShell + module + Docker).\n  setup                  Guided setup: check host, module, albuild.json; print next steps.\n  update                 Update the PowerShell module + MCP server in one go (the CLI updates via 'dotnet tool update').\n  module install         Install the businessdev.ALbuild PowerShell module (PSGallery).\n  module update          Update the businessdev.ALbuild PowerShell module.\n  module status          Show the installed/used businessdev.ALbuild module.\n  init                   Create an albuild.json in the workspace.\n  feeds add              Add a package feed to albuild.json: feeds add <url> [--name --kind apps|symbols|runtime --id-scheme --api-key-env VAR].\n  feeds list             List the feeds in albuild.json (read-only).\n  feeds remove           Remove a feed from albuild.json by url or --name.  [destructive]\n  agents list            List configured MCP agents and their tools.\n  agents add             Add/replace an MCP agent: agents add <name> --tools a,b,c (or *).\n  agents remove          Remove an MCP agent.  [destructive]\n  agents token           Generate a bearer token for an agent (for the MCP HTTP host).\n  artifact find          Resolve a BC artifact URL.\n  container list         List BC containers (read-only).\n  container inspect      Show a container + its installed apps.\n  container new          Provision or warm-reuse a BC container (idempotent).\n  container restart      Restart a container.\n  container remove       Remove a container (destructive; shared -> approval).  [destructive]\n  deps plan              Dry-run dependency resolution for a project (read-only).\n  deps check             Reconcile manifest vs container vs feeds (read-only).\n  deps install           Resolve + install the dependency closure into a container.\n  app build              Compile the workspace fresh (fresh symbo\n[truncated]",
      "terminalNonHelp": false
    },
    {
      "command": "update",
      "helpInvocation": "update /help",
      "result": {
        "status": "failed",
        "timedOut": false,
        "exitCode": 2,
        "durationMs": 61,
        "stdout": null,
        "stderr": "✗ error [BAD_COMPONENT]: Unknown component(s): /help.\n  Valid components: module, mcp. Update the CLI itself separately: dotnet tool update -g businessdev.ALbuild.Cli",
        "outputLimitExceeded": false
      },
      "parsed": false,
      "payload": "✗ error [BAD_COMPONENT]: Unknown component(s): /help.\n  Valid components: module, mcp. Update the CLI itself separately: dotnet tool update -g businessdev.ALbuild.Cli",
      "terminalNonHelp": false
    },
    {
      "command": "module install",
      "helpInvocation": "help module install",
      "result": {
        "status": "failed",
        "timedOut": false,
        "exitCode": 2,
        "durationMs": 48,
        "stdout": "albuild - Business Central dev-loop CLI (an MCP client over the businessdev.ALbuild engine)\n\nUsage: albuild <command> [options]\n\nCommands:\n  doctor                 Check host readiness (PowerShell + module + Docker).\n  setup                  Guided setup: check host, module, albuild.json; print next steps.\n  update                 Update the PowerShell module + MCP server in one go (the CLI updates via 'dotnet tool update').\n  module install         Install the businessdev.ALbuild PowerShell module (PSGallery).\n  module update          Update the businessdev.ALbuild PowerShell module.\n  module status          Show the installed/used businessdev.ALbuild module.\n  init                   Create an albuild.json in the workspace.\n  feeds add              Add a package feed to albuild.json: feeds add <url> [--name --kind apps|symbols|runtime --id-scheme --api-key-env VAR].\n  feeds list             List the feeds in albuild.json (read-only).\n  feeds remove           Remove a feed from albuild.json by url or --name.  [destructive]\n  agents list            List configured MCP agents and their tools.\n  agents add             Add/replace an MCP agent: agents add <name> --tools a,b,c (or *).\n  agents remove          Remove an MCP agent.  [destructive]\n  agents token           Generate a bearer token for an agent (for the MCP HTTP host).\n  artifact find          Resolve a BC artifact URL.\n  container list         List BC containers (read-only).\n  container inspect      Show a container + its installed apps.\n  container new          Provision or warm-reuse a BC container (idempotent).\n  container restart      Restart a container.\n  container remove       Remove a container (destructive; shared -> approval).  [destructive]\n  deps plan              Dry-run dependency resolution for a project (read-only).\n  deps check             Reconcile manifest vs container vs feeds (read-only).\n  deps install           Resolve + install the dependency closure into a container.\n  app build              Compile the workspace fresh (fresh symbo\n[truncated]",
        "stderr": "Unknown command: help module install",
        "outputLimitExceeded": false
      },
      "parsed": true,
      "payload": "albuild - Business Central dev-loop CLI (an MCP client over the businessdev.ALbuild engine)\n\nUsage: albuild <command> [options]\n\nCommands:\n  doctor                 Check host readiness (PowerShell + module + Docker).\n  setup                  Guided setup: check host, module, albuild.json; print next steps.\n  update                 Update the PowerShell module + MCP server in one go (the CLI updates via 'dotnet tool update').\n  module install         Install the businessdev.ALbuild PowerShell module (PSGallery).\n  module update          Update the businessdev.ALbuild PowerShell module.\n  module status          Show the installed/used businessdev.ALbuild module.\n  init                   Create an albuild.json in the workspace.\n  feeds add              Add a package feed to albuild.json: feeds add <url> [--name --kind apps|symbols|runtime --id-scheme --api-key-env VAR].\n  feeds list             List the feeds in albuild.json (read-only).\n  feeds remove           Remove a feed from albuild.json by url or --name.  [destructive]\n  agents list            List configured MCP agents and their tools.\n  agents add             Add/replace an MCP agent: agents add <name> --tools a,b,c (or *).\n  agents remove          Remove an MCP agent.  [destructive]\n  agents token           Generate a bearer token for an agent (for the MCP HTTP host).\n  artifact find          Resolve a BC artifact URL.\n  container list         List BC containers (read-only).\n  container inspect      Show a container + its installed apps.\n  container new          Provision or warm-reuse a BC container (idempotent).\n  container restart      Restart a container.\n  container remove       Remove a container (destructive; shared -> approval).  [destructive]\n  deps plan              Dry-run dependency resolution for a project (read-only).\n  deps check             Reconcile manifest vs container vs feeds (read-only).\n  deps install           Resolve + install the dependency closure into a container.\n  app build              Compile the workspace fresh (fresh symbo\n[truncated]",
      "terminalNonHelp": false
    },
    {
      "command": "module update",
      "helpInvocation": "help module update",
      "result": {
        "status": "failed",
        "timedOut": false,
        "exitCode": 2,
        "durationMs": 49,
        "stdout": "albuild - Business Central dev-loop CLI (an MCP client over the businessdev.ALbuild engine)\n\nUsage: albuild <command> [options]\n\nCommands:\n  doctor                 Check host readiness (PowerShell + module + Docker).\n  setup                  Guided setup: check host, module, albuild.json; print next steps.\n  update                 Update the PowerShell module + MCP server in one go (the CLI updates via 'dotnet tool update').\n  module install         Install the businessdev.ALbuild PowerShell module (PSGallery).\n  module update          Update the businessdev.ALbuild PowerShell module.\n  module status          Show the installed/used businessdev.ALbuild module.\n  init                   Create an albuild.json in the workspace.\n  feeds add              Add a package feed to albuild.json: feeds add <url> [--name --kind apps|symbols|runtime --id-scheme --api-key-env VAR].\n  feeds list             List the feeds in albuild.json (read-only).\n  feeds remove           Remove a feed from albuild.json by url or --name.  [destructive]\n  agents list            List configured MCP agents and their tools.\n  agents add             Add/replace an MCP agent: agents add <name> --tools a,b,c (or *).\n  agents remove          Remove an MCP agent.  [destructive]\n  agents token           Generate a bearer token for an agent (for the MCP HTTP host).\n  artifact find          Resolve a BC artifact URL.\n  container list         List BC containers (read-only).\n  container inspect      Show a container + its installed apps.\n  container new          Provision or warm-reuse a BC container (idempotent).\n  container restart      Restart a container.\n  container remove       Remove a container (destructive; shared -> approval).  [destructive]\n  deps plan              Dry-run dependency resolution for a project (read-only).\n  deps check             Reconcile manifest vs container vs feeds (read-only).\n  deps install           Resolve + install the dependency closure into a container.\n  app build              Compile the workspace fresh (fresh symbo\n[truncated]",
        "stderr": "Unknown command: help module update",
        "outputLimitExceeded": false
      },
      "parsed": false,
      "payload": "albuild - Business Central dev-loop CLI (an MCP client over the businessdev.ALbuild engine)\n\nUsage: albuild <command> [options]\n\nCommands:\n  doctor                 Check host readiness (PowerShell + module + Docker).\n  setup                  Guided setup: check host, module, albuild.json; print next steps.\n  update                 Update the PowerShell module + MCP server in one go (the CLI updates via 'dotnet tool update').\n  module install         Install the businessdev.ALbuild PowerShell module (PSGallery).\n  module update          Update the businessdev.ALbuild PowerShell module.\n  module status          Show the installed/used businessdev.ALbuild module.\n  init                   Create an albuild.json in the workspace.\n  feeds add              Add a package feed to albuild.json: feeds add <url> [--name --kind apps|symbols|runtime --id-scheme --api-key-env VAR].\n  feeds list             List the feeds in albuild.json (read-only).\n  feeds remove           Remove a feed from albuild.json by url or --name.  [destructive]\n  agents list            List configured MCP agents and their tools.\n  agents add             Add/replace an MCP agent: agents add <name> --tools a,b,c (or *).\n  agents remove          Remove an MCP agent.  [destructive]\n  agents token           Generate a bearer token for an agent (for the MCP HTTP host).\n  artifact find          Resolve a BC artifact URL.\n  container list         List BC containers (read-only).\n  container inspect      Show a container + its installed apps.\n  container new          Provision or warm-reuse a BC container (idempotent).\n  container restart      Restart a container.\n  container remove       Remove a container (destructive; shared -> approval).  [destructive]\n  deps plan              Dry-run dependency resolution for a project (read-only).\n  deps check             Reconcile manifest vs container vs feeds (read-only).\n  deps install           Resolve + install the dependency closure into a container.\n  app build              Compile the workspace fresh (fresh symbo\n[truncated]",
      "terminalNonHelp": false
    },
    {
      "command": "module status",
      "helpInvocation": "module status --help",
      "result": {
        "status": "ok",
        "timedOut": false,
        "exitCode": 0,
        "durationMs": 883,
        "stdout": "installed: true\nversion: 2.2.26743\ninstalledPath: /tmp/inspectra-help-busine-a7f8be14-87c075a5/xdg-data/powershell/Modules/businessdev.ALbuild/2.2.26743/businessdev.ALbuild.psd1\nresolvedByCli: businessdev.ALbuild\npowershell: pwsh",
        "stderr": null,
        "outputLimitExceeded": false
      },
      "parsed": false,
      "payload": "installed: true\nversion: 2.2.26743\ninstalledPath: /tmp/inspectra-help-busine-a7f8be14-87c075a5/xdg-data/powershell/Modules/businessdev.ALbuild/2.2.26743/businessdev.ALbuild.psd1\nresolvedByCli: businessdev.ALbuild\npowershell: pwsh",
      "terminalNonHelp": true
    },
    {
      "command": "init",
      "helpInvocation": "init --help",
      "result": {
        "status": "ok",
        "timedOut": false,
        "exitCode": 0,
        "durationMs": 58,
        "stdout": "written: /tmp/inspectra-help-busine-a7f8be14-87c075a5/albuild.json\ndryRun: false\nconfig:\n  country: w1\n  artifactType: Sandbox\n  bcVersion: \n  select: Latest\n  testSuite: DEFAULT",
        "stderr": null,
        "outputLimitExceeded": false
      },
      "parsed": false,
      "payload": "written: /tmp/inspectra-help-busine-a7f8be14-87c075a5/albuild.json\ndryRun: false\nconfig:\n  country: w1\n  artifactType: Sandbox\n  bcVersion: \n  select: Latest\n  testSuite: DEFAULT",
      "terminalNonHelp": true
    },
    {
      "command": "feeds add",
      "helpInvocation": "feeds add /help",
      "result": {
        "status": "ok",
        "timedOut": false,
        "exitCode": 0,
        "durationMs": 67,
        "stdout": "file: /tmp/inspectra-help-busine-a7f8be14-87c075a5/albuild.json\naction: added\nfeed: /help\nfeedCount: 1",
        "stderr": null,
        "outputLimitExceeded": false
      },
      "parsed": false,
      "payload": "file: /tmp/inspectra-help-busine-a7f8be14-87c075a5/albuild.json\naction: added\nfeed: /help\nfeedCount: 1",
      "terminalNonHelp": true
    },
    {
      "command": "feeds list",
      "helpInvocation": "feeds list --help",
      "result": {
        "status": "ok",
        "timedOut": false,
        "exitCode": 0,
        "durationMs": 66,
        "stdout": "file: /tmp/inspectra-help-busine-a7f8be14-87c075a5/albuild.json\nfeeds:\n- url=/help  name=  kind=",
        "stderr": null,
        "outputLimitExceeded": false
      },
      "parsed": false,
      "payload": "file: /tmp/inspectra-help-busine-a7f8be14-87c075a5/albuild.json\nfeeds:\n- url=/help  name=  kind=",
      "terminalNonHelp": true
    },
    {
      "command": "feeds remove",
      "helpInvocation": "feeds remove /help",
      "result": {
        "status": "ok",
        "timedOut": false,
        "exitCode": 0,
        "durationMs": 65,
        "stdout": "file: /tmp/inspectra-help-busine-a7f8be14-87c075a5/albuild.json\nremoved: 1\nfeedCount: 0",
        "stderr": null,
        "outputLimitExceeded": false
      },
      "parsed": false,
      "payload": "file: /tmp/inspectra-help-busine-a7f8be14-87c075a5/albuild.json\nremoved: 1\nfeedCount: 0",
      "terminalNonHelp": true
    },
    {
      "command": "agents list",
      "helpInvocation": "agents list --help",
      "result": {
        "status": "ok",
        "timedOut": false,
        "exitCode": 0,
        "durationMs": 54,
        "stdout": "file: /tmp/inspectra-help-busine-a7f8be14-87c075a5/home/.albuild/agents.json\nagents:\n(none)",
        "stderr": null,
        "outputLimitExceeded": false
      },
      "parsed": false,
      "payload": "file: /tmp/inspectra-help-busine-a7f8be14-87c075a5/home/.albuild/agents.json\nagents:\n(none)",
      "terminalNonHelp": true
    },
    {
      "command": "agents add",
      "helpInvocation": "agents add /help",
      "result": {
        "status": "ok",
        "timedOut": false,
        "exitCode": 0,
        "durationMs": 75,
        "stdout": "file: /tmp/inspectra-help-busine-a7f8be14-87c075a5/home/.albuild/agents.json\nagent: /help\ntools:\n- *",
        "stderr": "! Point ALBUILD_MCP_AGENTS_FILE at this file on the MCP host to enforce the roster.",
        "outputLimitExceeded": false
      },
      "parsed": false,
      "payload": "file: /tmp/inspectra-help-busine-a7f8be14-87c075a5/home/.albuild/agents.json\nagent: /help\ntools:\n- *",
      "terminalNonHelp": true
    },
    {
      "command": "agents remove",
      "helpInvocation": "agents remove /help",
      "result": {
        "status": "ok",
        "timedOut": false,
        "exitCode": 0,
        "durationMs": 72,
        "stdout": "file: /tmp/inspectra-help-busine-a7f8be14-87c075a5/home/.albuild/agents.json\nagent: /help\nremoved: true",
        "stderr": null,
        "outputLimitExceeded": false
      },
      "parsed": false,
      "payload": "file: /tmp/inspectra-help-busine-a7f8be14-87c075a5/home/.albuild/agents.json\nagent: /help\nremoved: true",
      "terminalNonHelp": true
    },
    {
      "command": "agents token",
      "helpInvocation": "agents token /help",
      "result": {
        "status": "ok",
        "timedOut": false,
        "exitCode": 0,
        "durationMs": 73,
        "stdout": "agent: /help\ntoken: alb_VShHADtLP2g-zBHua1AnrfTX9T3SAcZD\ntokensFile: /tmp/inspectra-help-busine-a7f8be14-87c075a5/home/.albuild/tokens.json\nenvValue: {\"alb_VShHADtLP2g-zBHua1AnrfTX9T3SAcZD\":\"/help\"}",
        "stderr": "! Give the token to the agent (as its Authorization: Bearer <token>).\n! On the MCP HTTP host set ALBUILD_MCP_TOKENS to the 'envValue' above.",
        "outputLimitExceeded": false
      },
      "parsed": false,
      "payload": "agent: /help\ntoken: alb_VShHADtLP2g-zBHua1AnrfTX9T3SAcZD\ntokensFile: /tmp/inspectra-help-busine-a7f8be14-87c075a5/home/.albuild/tokens.json\nenvValue: {\"alb_VShHADtLP2g-zBHua1AnrfTX9T3SAcZD\":\"/help\"}",
      "terminalNonHelp": true
    },
    {
      "command": "artifact find",
      "helpInvocation": "help artifact find",
      "result": {
        "status": "failed",
        "timedOut": false,
        "exitCode": 2,
        "durationMs": 49,
        "stdout": "albuild - Business Central dev-loop CLI (an MCP client over the businessdev.ALbuild engine)\n\nUsage: albuild <command> [options]\n\nCommands:\n  doctor                 Check host readiness (PowerShell + module + Docker).\n  setup                  Guided setup: check host, module, albuild.json; print next steps.\n  update                 Update the PowerShell module + MCP server in one go (the CLI updates via 'dotnet tool update').\n  module install         Install the businessdev.ALbuild PowerShell module (PSGallery).\n  module update          Update the businessdev.ALbuild PowerShell module.\n  module status          Show the installed/used businessdev.ALbuild module.\n  init                   Create an albuild.json in the workspace.\n  feeds add              Add a package feed to albuild.json: feeds add <url> [--name --kind apps|symbols|runtime --id-scheme --api-key-env VAR].\n  feeds list             List the feeds in albuild.json (read-only).\n  feeds remove           Remove a feed from albuild.json by url or --name.  [destructive]\n  agents list            List configured MCP agents and their tools.\n  agents add             Add/replace an MCP agent: agents add <name> --tools a,b,c (or *).\n  agents remove          Remove an MCP agent.  [destructive]\n  agents token           Generate a bearer token for an agent (for the MCP HTTP host).\n  artifact find          Resolve a BC artifact URL.\n  container list         List BC containers (read-only).\n  container inspect      Show a container + its installed apps.\n  container new          Provision or warm-reuse a BC container (idempotent).\n  container restart      Restart a container.\n  container remove       Remove a container (destructive; shared -> approval).  [destructive]\n  deps plan              Dry-run dependency resolution for a project (read-only).\n  deps check             Reconcile manifest vs container vs feeds (read-only).\n  deps install           Resolve + install the dependency closure into a container.\n  app build              Compile the workspace fresh (fresh symbo\n[truncated]",
        "stderr": "Unknown command: help artifact find",
        "outputLimitExceeded": false
      },
      "parsed": true,
      "payload": "albuild - Business Central dev-loop CLI (an MCP client over the businessdev.ALbuild engine)\n\nUsage: albuild <command> [options]\n\nCommands:\n  doctor                 Check host readiness (PowerShell + module + Docker).\n  setup                  Guided setup: check host, module, albuild.json; print next steps.\n  update                 Update the PowerShell module + MCP server in one go (the CLI updates via 'dotnet tool update').\n  module install         Install the businessdev.ALbuild PowerShell module (PSGallery).\n  module update          Update the businessdev.ALbuild PowerShell module.\n  module status          Show the installed/used businessdev.ALbuild module.\n  init                   Create an albuild.json in the workspace.\n  feeds add              Add a package feed to albuild.json: feeds add <url> [--name --kind apps|symbols|runtime --id-scheme --api-key-env VAR].\n  feeds list             List the feeds in albuild.json (read-only).\n  feeds remove           Remove a feed from albuild.json by url or --name.  [destructive]\n  agents list            List configured MCP agents and their tools.\n  agents add             Add/replace an MCP agent: agents add <name> --tools a,b,c (or *).\n  agents remove          Remove an MCP agent.  [destructive]\n  agents token           Generate a bearer token for an agent (for the MCP HTTP host).\n  artifact find          Resolve a BC artifact URL.\n  container list         List BC containers (read-only).\n  container inspect      Show a container + its installed apps.\n  container new          Provision or warm-reuse a BC container (idempotent).\n  container restart      Restart a container.\n  container remove       Remove a container (destructive; shared -> approval).  [destructive]\n  deps plan              Dry-run dependency resolution for a project (read-only).\n  deps check             Reconcile manifest vs container vs feeds (read-only).\n  deps install           Resolve + install the dependency closure into a container.\n  app build              Compile the workspace fresh (fresh symbo\n[truncated]",
      "terminalNonHelp": false
    },
    {
      "command": "container list",
      "helpInvocation": "help container list",
      "result": {
        "status": "failed",
        "timedOut": false,
        "exitCode": 2,
        "durationMs": 58,
        "stdout": "albuild - Business Central dev-loop CLI (an MCP client over the businessdev.ALbuild engine)\n\nUsage: albuild <command> [options]\n\nCommands:\n  doctor                 Check host readiness (PowerShell + module + Docker).\n  setup                  Guided setup: check host, module, albuild.json; print next steps.\n  update                 Update the PowerShell module + MCP server in one go (the CLI updates via 'dotnet tool update').\n  module install         Install the businessdev.ALbuild PowerShell module (PSGallery).\n  module update          Update the businessdev.ALbuild PowerShell module.\n  module status          Show the installed/used businessdev.ALbuild module.\n  init                   Create an albuild.json in the workspace.\n  feeds add              Add a package feed to albuild.json: feeds add <url> [--name --kind apps|symbols|runtime --id-scheme --api-key-env VAR].\n  feeds list             List the feeds in albuild.json (read-only).\n  feeds remove           Remove a feed from albuild.json by url or --name.  [destructive]\n  agents list            List configured MCP agents and their tools.\n  agents add             Add/replace an MCP agent: agents add <name> --tools a,b,c (or *).\n  agents remove          Remove an MCP agent.  [destructive]\n  agents token           Generate a bearer token for an agent (for the MCP HTTP host).\n  artifact find          Resolve a BC artifact URL.\n  container list         List BC containers (read-only).\n  container inspect      Show a container + its installed apps.\n  container new          Provision or warm-reuse a BC container (idempotent).\n  container restart      Restart a container.\n  container remove       Remove a container (destructive; shared -> approval).  [destructive]\n  deps plan              Dry-run dependency resolution for a project (read-only).\n  deps check             Reconcile manifest vs container vs feeds (read-only).\n  deps install           Resolve + install the dependency closure into a container.\n  app build              Compile the workspace fresh (fresh symbo\n[truncated]",
        "stderr": "Unknown command: help container list",
        "outputLimitExceeded": false
      },
      "parsed": true,
      "payload": "albuild - Business Central dev-loop CLI (an MCP client over the businessdev.ALbuild engine)\n\nUsage: albuild <command> [options]\n\nCommands:\n  doctor                 Check host readiness (PowerShell + module + Docker).\n  setup                  Guided setup: check host, module, albuild.json; print next steps.\n  update                 Update the PowerShell module + MCP server in one go (the CLI updates via 'dotnet tool update').\n  module install         Install the businessdev.ALbuild PowerShell module (PSGallery).\n  module update          Update the businessdev.ALbuild PowerShell module.\n  module status          Show the installed/used businessdev.ALbuild module.\n  init                   Create an albuild.json in the workspace.\n  feeds add              Add a package feed to albuild.json: feeds add <url> [--name --kind apps|symbols|runtime --id-scheme --api-key-env VAR].\n  feeds list             List the feeds in albuild.json (read-only).\n  feeds remove           Remove a feed from albuild.json by url or --name.  [destructive]\n  agents list            List configured MCP agents and their tools.\n  agents add             Add/replace an MCP agent: agents add <name> --tools a,b,c (or *).\n  agents remove          Remove an MCP agent.  [destructive]\n  agents token           Generate a bearer token for an agent (for the MCP HTTP host).\n  artifact find          Resolve a BC artifact URL.\n  container list         List BC containers (read-only).\n  container inspect      Show a container + its installed apps.\n  container new          Provision or warm-reuse a BC container (idempotent).\n  container restart      Restart a container.\n  container remove       Remove a container (destructive; shared -> approval).  [destructive]\n  deps plan              Dry-run dependency resolution for a project (read-only).\n  deps check             Reconcile manifest vs container vs feeds (read-only).\n  deps install           Resolve + install the dependency closure into a container.\n  app build              Compile the workspace fresh (fresh symbo\n[truncated]",
      "terminalNonHelp": false
    },
    {
      "command": "container inspect",
      "helpInvocation": "help container inspect",
      "result": {
        "status": "failed",
        "timedOut": false,
        "exitCode": 2,
        "durationMs": 49,
        "stdout": "albuild - Business Central dev-loop CLI (an MCP client over the businessdev.ALbuild engine)\n\nUsage: albuild <command> [options]\n\nCommands:\n  doctor                 Check host readiness (PowerShell + module + Docker).\n  setup                  Guided setup: check host, module, albuild.json; print next steps.\n  update                 Update the PowerShell module + MCP server in one go (the CLI updates via 'dotnet tool update').\n  module install         Install the businessdev.ALbuild PowerShell module (PSGallery).\n  module update          Update the businessdev.ALbuild PowerShell module.\n  module status          Show the installed/used businessdev.ALbuild module.\n  init                   Create an albuild.json in the workspace.\n  feeds add              Add a package feed to albuild.json: feeds add <url> [--name --kind apps|symbols|runtime --id-scheme --api-key-env VAR].\n  feeds list             List the feeds in albuild.json (read-only).\n  feeds remove           Remove a feed from albuild.json by url or --name.  [destructive]\n  agents list            List configured MCP agents and their tools.\n  agents add             Add/replace an MCP agent: agents add <name> --tools a,b,c (or *).\n  agents remove          Remove an MCP agent.  [destructive]\n  agents token           Generate a bearer token for an agent (for the MCP HTTP host).\n  artifact find          Resolve a BC artifact URL.\n  container list         List BC containers (read-only).\n  container inspect      Show a container + its installed apps.\n  container new          Provision or warm-reuse a BC container (idempotent).\n  container restart      Restart a container.\n  container remove       Remove a container (destructive; shared -> approval).  [destructive]\n  deps plan              Dry-run dependency resolution for a project (read-only).\n  deps check             Reconcile manifest vs container vs feeds (read-only).\n  deps install           Resolve + install the dependency closure into a container.\n  app build              Compile the workspace fresh (fresh symbo\n[truncated]",
        "stderr": "Unknown command: help container inspect",
        "outputLimitExceeded": false
      },
      "parsed": true,
      "payload": "albuild - Business Central dev-loop CLI (an MCP client over the businessdev.ALbuild engine)\n\nUsage: albuild <command> [options]\n\nCommands:\n  doctor                 Check host readiness (PowerShell + module + Docker).\n  setup                  Guided setup: check host, module, albuild.json; print next steps.\n  update                 Update the PowerShell module + MCP server in one go (the CLI updates via 'dotnet tool update').\n  module install         Install the businessdev.ALbuild PowerShell module (PSGallery).\n  module update          Update the businessdev.ALbuild PowerShell module.\n  module status          Show the installed/used businessdev.ALbuild module.\n  init                   Create an albuild.json in the workspace.\n  feeds add              Add a package feed to albuild.json: feeds add <url> [--name --kind apps|symbols|runtime --id-scheme --api-key-env VAR].\n  feeds list             List the feeds in albuild.json (read-only).\n  feeds remove           Remove a feed from albuild.json by url or --name.  [destructive]\n  agents list            List configured MCP agents and their tools.\n  agents add             Add/replace an MCP agent: agents add <name> --tools a,b,c (or *).\n  agents remove          Remove an MCP agent.  [destructive]\n  agents token           Generate a bearer token for an agent (for the MCP HTTP host).\n  artifact find          Resolve a BC artifact URL.\n  container list         List BC containers (read-only).\n  container inspect      Show a container + its installed apps.\n  container new          Provision or warm-reuse a BC container (idempotent).\n  container restart      Restart a container.\n  container remove       Remove a container (destructive; shared -> approval).  [destructive]\n  deps plan              Dry-run dependency resolution for a project (read-only).\n  deps check             Reconcile manifest vs container vs feeds (read-only).\n  deps install           Resolve + install the dependency closure into a container.\n  app build              Compile the workspace fresh (fresh symbo\n[truncated]",
      "terminalNonHelp": false
    },
    {
      "command": "container new",
      "helpInvocation": "help container new",
      "result": {
        "status": "failed",
        "timedOut": false,
        "exitCode": 2,
        "durationMs": 48,
        "stdout": "albuild - Business Central dev-loop CLI (an MCP client over the businessdev.ALbuild engine)\n\nUsage: albuild <command> [options]\n\nCommands:\n  doctor                 Check host readiness (PowerShell + module + Docker).\n  setup                  Guided setup: check host, module, albuild.json; print next steps.\n  update                 Update the PowerShell module + MCP server in one go (the CLI updates via 'dotnet tool update').\n  module install         Install the businessdev.ALbuild PowerShell module (PSGallery).\n  module update          Update the businessdev.ALbuild PowerShell module.\n  module status          Show the installed/used businessdev.ALbuild module.\n  init                   Create an albuild.json in the workspace.\n  feeds add              Add a package feed to albuild.json: feeds add <url> [--name --kind apps|symbols|runtime --id-scheme --api-key-env VAR].\n  feeds list             List the feeds in albuild.json (read-only).\n  feeds remove           Remove a feed from albuild.json by url or --name.  [destructive]\n  agents list            List configured MCP agents and their tools.\n  agents add             Add/replace an MCP agent: agents add <name> --tools a,b,c (or *).\n  agents remove          Remove an MCP agent.  [destructive]\n  agents token           Generate a bearer token for an agent (for the MCP HTTP host).\n  artifact find          Resolve a BC artifact URL.\n  container list         List BC containers (read-only).\n  container inspect      Show a container + its installed apps.\n  container new          Provision or warm-reuse a BC container (idempotent).\n  container restart      Restart a container.\n  container remove       Remove a container (destructive; shared -> approval).  [destructive]\n  deps plan              Dry-run dependency resolution for a project (read-only).\n  deps check             Reconcile manifest vs container vs feeds (read-only).\n  deps install           Resolve + install the dependency closure into a container.\n  app build              Compile the workspace fresh (fresh symbo\n[truncated]",
        "stderr": "Unknown command: help container new",
        "outputLimitExceeded": false
      },
      "parsed": true,
      "payload": "albuild - Business Central dev-loop CLI (an MCP client over the businessdev.ALbuild engine)\n\nUsage: albuild <command> [options]\n\nCommands:\n  doctor                 Check host readiness (PowerShell + module + Docker).\n  setup                  Guided setup: check host, module, albuild.json; print next steps.\n  update                 Update the PowerShell module + MCP server in one go (the CLI updates via 'dotnet tool update').\n  module install         Install the businessdev.ALbuild PowerShell module (PSGallery).\n  module update          Update the businessdev.ALbuild PowerShell module.\n  module status          Show the installed/used businessdev.ALbuild module.\n  init                   Create an albuild.json in the workspace.\n  feeds add              Add a package feed to albuild.json: feeds add <url> [--name --kind apps|symbols|runtime --id-scheme --api-key-env VAR].\n  feeds list             List the feeds in albuild.json (read-only).\n  feeds remove           Remove a feed from albuild.json by url or --name.  [destructive]\n  agents list            List configured MCP agents and their tools.\n  agents add             Add/replace an MCP agent: agents add <name> --tools a,b,c (or *).\n  agents remove          Remove an MCP agent.  [destructive]\n  agents token           Generate a bearer token for an agent (for the MCP HTTP host).\n  artifact find          Resolve a BC artifact URL.\n  container list         List BC containers (read-only).\n  container inspect      Show a container + its installed apps.\n  container new          Provision or warm-reuse a BC container (idempotent).\n  container restart      Restart a container.\n  container remove       Remove a container (destructive; shared -> approval).  [destructive]\n  deps plan              Dry-run dependency resolution for a project (read-only).\n  deps check             Reconcile manifest vs container vs feeds (read-only).\n  deps install           Resolve + install the dependency closure into a container.\n  app build              Compile the workspace fresh (fresh symbo\n[truncated]",
      "terminalNonHelp": false
    },
    {
      "command": "container restart",
      "helpInvocation": "help container restart",
      "result": {
        "status": "failed",
        "timedOut": false,
        "exitCode": 2,
        "durationMs": 58,
        "stdout": "albuild - Business Central dev-loop CLI (an MCP client over the businessdev.ALbuild engine)\n\nUsage: albuild <command> [options]\n\nCommands:\n  doctor                 Check host readiness (PowerShell + module + Docker).\n  setup                  Guided setup: check host, module, albuild.json; print next steps.\n  update                 Update the PowerShell module + MCP server in one go (the CLI updates via 'dotnet tool update').\n  module install         Install the businessdev.ALbuild PowerShell module (PSGallery).\n  module update          Update the businessdev.ALbuild PowerShell module.\n  module status          Show the installed/used businessdev.ALbuild module.\n  init                   Create an albuild.json in the workspace.\n  feeds add              Add a package feed to albuild.json: feeds add <url> [--name --kind apps|symbols|runtime --id-scheme --api-key-env VAR].\n  feeds list             List the feeds in albuild.json (read-only).\n  feeds remove           Remove a feed from albuild.json by url or --name.  [destructive]\n  agents list            List configured MCP agents and their tools.\n  agents add             Add/replace an MCP agent: agents add <name> --tools a,b,c (or *).\n  agents remove          Remove an MCP agent.  [destructive]\n  agents token           Generate a bearer token for an agent (for the MCP HTTP host).\n  artifact find          Resolve a BC artifact URL.\n  container list         List BC containers (read-only).\n  container inspect      Show a container + its installed apps.\n  container new          Provision or warm-reuse a BC container (idempotent).\n  container restart      Restart a container.\n  container remove       Remove a container (destructive; shared -> approval).  [destructive]\n  deps plan              Dry-run dependency resolution for a project (read-only).\n  deps check             Reconcile manifest vs container vs feeds (read-only).\n  deps install           Resolve + install the dependency closure into a container.\n  app build              Compile the workspace fresh (fresh symbo\n[truncated]",
        "stderr": "Unknown command: help container restart",
        "outputLimitExceeded": false
      },
      "parsed": true,
      "payload": "albuild - Business Central dev-loop CLI (an MCP client over the businessdev.ALbuild engine)\n\nUsage: albuild <command> [options]\n\nCommands:\n  doctor                 Check host readiness (PowerShell + module + Docker).\n  setup                  Guided setup: check host, module, albuild.json; print next steps.\n  update                 Update the PowerShell module + MCP server in one go (the CLI updates via 'dotnet tool update').\n  module install         Install the businessdev.ALbuild PowerShell module (PSGallery).\n  module update          Update the businessdev.ALbuild PowerShell module.\n  module status          Show the installed/used businessdev.ALbuild module.\n  init                   Create an albuild.json in the workspace.\n  feeds add              Add a package feed to albuild.json: feeds add <url> [--name --kind apps|symbols|runtime --id-scheme --api-key-env VAR].\n  feeds list             List the feeds in albuild.json (read-only).\n  feeds remove           Remove a feed from albuild.json by url or --name.  [destructive]\n  agents list            List configured MCP agents and their tools.\n  agents add             Add/replace an MCP agent: agents add <name> --tools a,b,c (or *).\n  agents remove          Remove an MCP agent.  [destructive]\n  agents token           Generate a bearer token for an agent (for the MCP HTTP host).\n  artifact find          Resolve a BC artifact URL.\n  container list         List BC containers (read-only).\n  container inspect      Show a container + its installed apps.\n  container new          Provision or warm-reuse a BC container (idempotent).\n  container restart      Restart a container.\n  container remove       Remove a container (destructive; shared -> approval).  [destructive]\n  deps plan              Dry-run dependency resolution for a project (read-only).\n  deps check             Reconcile manifest vs container vs feeds (read-only).\n  deps install           Resolve + install the dependency closure into a container.\n  app build              Compile the workspace fresh (fresh symbo\n[truncated]",
      "terminalNonHelp": false
    },
    {
      "command": "container remove",
      "helpInvocation": "help container remove",
      "result": {
        "status": "failed",
        "timedOut": false,
        "exitCode": 2,
        "durationMs": 54,
        "stdout": "albuild - Business Central dev-loop CLI (an MCP client over the businessdev.ALbuild engine)\n\nUsage: albuild <command> [options]\n\nCommands:\n  doctor                 Check host readiness (PowerShell + module + Docker).\n  setup                  Guided setup: check host, module, albuild.json; print next steps.\n  update                 Update the PowerShell module + MCP server in one go (the CLI updates via 'dotnet tool update').\n  module install         Install the businessdev.ALbuild PowerShell module (PSGallery).\n  module update          Update the businessdev.ALbuild PowerShell module.\n  module status          Show the installed/used businessdev.ALbuild module.\n  init                   Create an albuild.json in the workspace.\n  feeds add              Add a package feed to albuild.json: feeds add <url> [--name --kind apps|symbols|runtime --id-scheme --api-key-env VAR].\n  feeds list             List the feeds in albuild.json (read-only).\n  feeds remove           Remove a feed from albuild.json by url or --name.  [destructive]\n  agents list            List configured MCP agents and their tools.\n  agents add             Add/replace an MCP agent: agents add <name> --tools a,b,c (or *).\n  agents remove          Remove an MCP agent.  [destructive]\n  agents token           Generate a bearer token for an agent (for the MCP HTTP host).\n  artifact find          Resolve a BC artifact URL.\n  container list         List BC containers (read-only).\n  container inspect      Show a container + its installed apps.\n  container new          Provision or warm-reuse a BC container (idempotent).\n  container restart      Restart a container.\n  container remove       Remove a container (destructive; shared -> approval).  [destructive]\n  deps plan              Dry-run dependency resolution for a project (read-only).\n  deps check             Reconcile manifest vs container vs feeds (read-only).\n  deps install           Resolve + install the dependency closure into a container.\n  app build              Compile the workspace fresh (fresh symbo\n[truncated]",
        "stderr": "Unknown command: help container remove",
        "outputLimitExceeded": false
      },
      "parsed": true,
      "payload": "albuild - Business Central dev-loop CLI (an MCP client over the businessdev.ALbuild engine)\n\nUsage: albuild <command> [options]\n\nCommands:\n  doctor                 Check host readiness (PowerShell + module + Docker).\n  setup                  Guided setup: check host, module, albuild.json; print next steps.\n  update                 Update the PowerShell module + MCP server in one go (the CLI updates via 'dotnet tool update').\n  module install         Install the businessdev.ALbuild PowerShell module (PSGallery).\n  module update          Update the businessdev.ALbuild PowerShell module.\n  module status          Show the installed/used businessdev.ALbuild module.\n  init                   Create an albuild.json in the workspace.\n  feeds add              Add a package feed to albuild.json: feeds add <url> [--name --kind apps|symbols|runtime --id-scheme --api-key-env VAR].\n  feeds list             List the feeds in albuild.json (read-only).\n  feeds remove           Remove a feed from albuild.json by url or --name.  [destructive]\n  agents list            List configured MCP agents and their tools.\n  agents add             Add/replace an MCP agent: agents add <name> --tools a,b,c (or *).\n  agents remove          Remove an MCP agent.  [destructive]\n  agents token           Generate a bearer token for an agent (for the MCP HTTP host).\n  artifact find          Resolve a BC artifact URL.\n  container list         List BC containers (read-only).\n  container inspect      Show a container + its installed apps.\n  container new          Provision or warm-reuse a BC container (idempotent).\n  container restart      Restart a container.\n  container remove       Remove a container (destructive; shared -> approval).  [destructive]\n  deps plan              Dry-run dependency resolution for a project (read-only).\n  deps check             Reconcile manifest vs container vs feeds (read-only).\n  deps install           Resolve + install the dependency closure into a container.\n  app build              Compile the workspace fresh (fresh symbo\n[truncated]",
      "terminalNonHelp": false
    },
    {
      "command": "deps plan",
      "helpInvocation": "help deps plan",
      "result": {
        "status": "failed",
        "timedOut": false,
        "exitCode": 2,
        "durationMs": 49,
        "stdout": "albuild - Business Central dev-loop CLI (an MCP client over the businessdev.ALbuild engine)\n\nUsage: albuild <command> [options]\n\nCommands:\n  doctor                 Check host readiness (PowerShell + module + Docker).\n  setup                  Guided setup: check host, module, albuild.json; print next steps.\n  update                 Update the PowerShell module + MCP server in one go (the CLI updates via 'dotnet tool update').\n  module install         Install the businessdev.ALbuild PowerShell module (PSGallery).\n  module update          Update the businessdev.ALbuild PowerShell module.\n  module status          Show the installed/used businessdev.ALbuild module.\n  init                   Create an albuild.json in the workspace.\n  feeds add              Add a package feed to albuild.json: feeds add <url> [--name --kind apps|symbols|runtime --id-scheme --api-key-env VAR].\n  feeds list             List the feeds in albuild.json (read-only).\n  feeds remove           Remove a feed from albuild.json by url or --name.  [destructive]\n  agents list            List configured MCP agents and their tools.\n  agents add             Add/replace an MCP agent: agents add <name> --tools a,b,c (or *).\n  agents remove          Remove an MCP agent.  [destructive]\n  agents token           Generate a bearer token for an agent (for the MCP HTTP host).\n  artifact find          Resolve a BC artifact URL.\n  container list         List BC containers (read-only).\n  container inspect      Show a container + its installed apps.\n  container new          Provision or warm-reuse a BC container (idempotent).\n  container restart      Restart a container.\n  container remove       Remove a container (destructive; shared -> approval).  [destructive]\n  deps plan              Dry-run dependency resolution for a project (read-only).\n  deps check             Reconcile manifest vs container vs feeds (read-only).\n  deps install           Resolve + install the dependency closure into a container.\n  app build              Compile the workspace fresh (fresh symbo\n[truncated]",
        "stderr": "Unknown command: help deps plan",
        "outputLimitExceeded": false
      },
      "parsed": true,
      "payload": "albuild - Business Central dev-loop CLI (an MCP client over the businessdev.ALbuild engine)\n\nUsage: albuild <command> [options]\n\nCommands:\n  doctor                 Check host readiness (PowerShell + module + Docker).\n  setup                  Guided setup: check host, module, albuild.json; print next steps.\n  update                 Update the PowerShell module + MCP server in one go (the CLI updates via 'dotnet tool update').\n  module install         Install the businessdev.ALbuild PowerShell module (PSGallery).\n  module update          Update the businessdev.ALbuild PowerShell module.\n  module status          Show the installed/used businessdev.ALbuild module.\n  init                   Create an albuild.json in the workspace.\n  feeds add              Add a package feed to albuild.json: feeds add <url> [--name --kind apps|symbols|runtime --id-scheme --api-key-env VAR].\n  feeds list             List the feeds in albuild.json (read-only).\n  feeds remove           Remove a feed from albuild.json by url or --name.  [destructive]\n  agents list            List configured MCP agents and their tools.\n  agents add             Add/replace an MCP agent: agents add <name> --tools a,b,c (or *).\n  agents remove          Remove an MCP agent.  [destructive]\n  agents token           Generate a bearer token for an agent (for the MCP HTTP host).\n  artifact find          Resolve a BC artifact URL.\n  container list         List BC containers (read-only).\n  container inspect      Show a container + its installed apps.\n  container new          Provision or warm-reuse a BC container (idempotent).\n  container restart      Restart a container.\n  container remove       Remove a container (destructive; shared -> approval).  [destructive]\n  deps plan              Dry-run dependency resolution for a project (read-only).\n  deps check             Reconcile manifest vs container vs feeds (read-only).\n  deps install           Resolve + install the dependency closure into a container.\n  app build              Compile the workspace fresh (fresh symbo\n[truncated]",
      "terminalNonHelp": false
    },
    {
      "command": "deps check",
      "helpInvocation": "help deps check",
      "result": {
        "status": "failed",
        "timedOut": false,
        "exitCode": 2,
        "durationMs": 52,
        "stdout": "albuild - Business Central dev-loop CLI (an MCP client over the businessdev.ALbuild engine)\n\nUsage: albuild <command> [options]\n\nCommands:\n  doctor                 Check host readiness (PowerShell + module + Docker).\n  setup                  Guided setup: check host, module, albuild.json; print next steps.\n  update                 Update the PowerShell module + MCP server in one go (the CLI updates via 'dotnet tool update').\n  module install         Install the businessdev.ALbuild PowerShell module (PSGallery).\n  module update          Update the businessdev.ALbuild PowerShell module.\n  module status          Show the installed/used businessdev.ALbuild module.\n  init                   Create an albuild.json in the workspace.\n  feeds add              Add a package feed to albuild.json: feeds add <url> [--name --kind apps|symbols|runtime --id-scheme --api-key-env VAR].\n  feeds list             List the feeds in albuild.json (read-only).\n  feeds remove           Remove a feed from albuild.json by url or --name.  [destructive]\n  agents list            List configured MCP agents and their tools.\n  agents add             Add/replace an MCP agent: agents add <name> --tools a,b,c (or *).\n  agents remove          Remove an MCP agent.  [destructive]\n  agents token           Generate a bearer token for an agent (for the MCP HTTP host).\n  artifact find          Resolve a BC artifact URL.\n  container list         List BC containers (read-only).\n  container inspect      Show a container + its installed apps.\n  container new          Provision or warm-reuse a BC container (idempotent).\n  container restart      Restart a container.\n  container remove       Remove a container (destructive; shared -> approval).  [destructive]\n  deps plan              Dry-run dependency resolution for a project (read-only).\n  deps check             Reconcile manifest vs container vs feeds (read-only).\n  deps install           Resolve + install the dependency closure into a container.\n  app build              Compile the workspace fresh (fresh symbo\n[truncated]",
        "stderr": "Unknown command: help deps check",
        "outputLimitExceeded": false
      },
      "parsed": true,
      "payload": "albuild - Business Central dev-loop CLI (an MCP client over the businessdev.ALbuild engine)\n\nUsage: albuild <command> [options]\n\nCommands:\n  doctor                 Check host readiness (PowerShell + module + Docker).\n  setup                  Guided setup: check host, module, albuild.json; print next steps.\n  update                 Update the PowerShell module + MCP server in one go (the CLI updates via 'dotnet tool update').\n  module install         Install the businessdev.ALbuild PowerShell module (PSGallery).\n  module update          Update the businessdev.ALbuild PowerShell module.\n  module status          Show the installed/used businessdev.ALbuild module.\n  init                   Create an albuild.json in the workspace.\n  feeds add              Add a package feed to albuild.json: feeds add <url> [--name --kind apps|symbols|runtime --id-scheme --api-key-env VAR].\n  feeds list             List the feeds in albuild.json (read-only).\n  feeds remove           Remove a feed from albuild.json by url or --name.  [destructive]\n  agents list            List configured MCP agents and their tools.\n  agents add             Add/replace an MCP agent: agents add <name> --tools a,b,c (or *).\n  agents remove          Remove an MCP agent.  [destructive]\n  agents token           Generate a bearer token for an agent (for the MCP HTTP host).\n  artifact find          Resolve a BC artifact URL.\n  container list         List BC containers (read-only).\n  container inspect      Show a container + its installed apps.\n  container new          Provision or warm-reuse a BC container (idempotent).\n  container restart      Restart a container.\n  container remove       Remove a container (destructive; shared -> approval).  [destructive]\n  deps plan              Dry-run dependency resolution for a project (read-only).\n  deps check             Reconcile manifest vs container vs feeds (read-only).\n  deps install           Resolve + install the dependency closure into a container.\n  app build              Compile the workspace fresh (fresh symbo\n[truncated]",
      "terminalNonHelp": false
    },
    {
      "command": "deps install",
      "helpInvocation": "help deps install",
      "result": {
        "status": "failed",
        "timedOut": false,
        "exitCode": 2,
        "durationMs": 54,
        "stdout": "albuild - Business Central dev-loop CLI (an MCP client over the businessdev.ALbuild engine)\n\nUsage: albuild <command> [options]\n\nCommands:\n  doctor                 Check host readiness (PowerShell + module + Docker).\n  setup                  Guided setup: check host, module, albuild.json; print next steps.\n  update                 Update the PowerShell module + MCP server in one go (the CLI updates via 'dotnet tool update').\n  module install         Install the businessdev.ALbuild PowerShell module (PSGallery).\n  module update          Update the businessdev.ALbuild PowerShell module.\n  module status          Show the installed/used businessdev.ALbuild module.\n  init                   Create an albuild.json in the workspace.\n  feeds add              Add a package feed to albuild.json: feeds add <url> [--name --kind apps|symbols|runtime --id-scheme --api-key-env VAR].\n  feeds list             List the feeds in albuild.json (read-only).\n  feeds remove           Remove a feed from albuild.json by url or --name.  [destructive]\n  agents list            List configured MCP agents and their tools.\n  agents add             Add/replace an MCP agent: agents add <name> --tools a,b,c (or *).\n  agents remove          Remove an MCP agent.  [destructive]\n  agents token           Generate a bearer token for an agent (for the MCP HTTP host).\n  artifact find          Resolve a BC artifact URL.\n  container list         List BC containers (read-only).\n  container inspect      Show a container + its installed apps.\n  container new          Provision or warm-reuse a BC container (idempotent).\n  container restart      Restart a container.\n  container remove       Remove a container (destructive; shared -> approval).  [destructive]\n  deps plan              Dry-run dependency resolution for a project (read-only).\n  deps check             Reconcile manifest vs container vs feeds (read-only).\n  deps install           Resolve + install the dependency closure into a container.\n  app build              Compile the workspace fresh (fresh symbo\n[truncated]",
        "stderr": "Unknown command: help deps install",
        "outputLimitExceeded": false
      },
      "parsed": true,
      "payload": "albuild - Business Central dev-loop CLI (an MCP client over the businessdev.ALbuild engine)\n\nUsage: albuild <command> [options]\n\nCommands:\n  doctor                 Check host readiness (PowerShell + module + Docker).\n  setup                  Guided setup: check host, module, albuild.json; print next steps.\n  update                 Update the PowerShell module + MCP server in one go (the CLI updates via 'dotnet tool update').\n  module install         Install the businessdev.ALbuild PowerShell module (PSGallery).\n  module update          Update the businessdev.ALbuild PowerShell module.\n  module status          Show the installed/used businessdev.ALbuild module.\n  init                   Create an albuild.json in the workspace.\n  feeds add              Add a package feed to albuild.json: feeds add <url> [--name --kind apps|symbols|runtime --id-scheme --api-key-env VAR].\n  feeds list             List the feeds in albuild.json (read-only).\n  feeds remove           Remove a feed from albuild.json by url or --name.  [destructive]\n  agents list            List configured MCP agents and their tools.\n  agents add             Add/replace an MCP agent: agents add <name> --tools a,b,c (or *).\n  agents remove          Remove an MCP agent.  [destructive]\n  agents token           Generate a bearer token for an agent (for the MCP HTTP host).\n  artifact find          Resolve a BC artifact URL.\n  container list         List BC containers (read-only).\n  container inspect      Show a container + its installed apps.\n  container new          Provision or warm-reuse a BC container (idempotent).\n  container restart      Restart a container.\n  container remove       Remove a container (destructive; shared -> approval).  [destructive]\n  deps plan              Dry-run dependency resolution for a project (read-only).\n  deps check             Reconcile manifest vs container vs feeds (read-only).\n  deps install           Resolve + install the dependency closure into a container.\n  app build              Compile the workspace fresh (fresh symbo\n[truncated]",
      "terminalNonHelp": false
    },
    {
      "command": "app build",
      "helpInvocation": "app build --help",
      "result": {
        "status": "ok",
        "timedOut": false,
        "exitCode": 0,
        "durationMs": 41195,
        "stdout": "targetVersion: 28.3.52162.52870\nartifactUrl: https://bcartifacts-exdbf9fwegejdqak.b02.azurefd.net/sandbox/28.3.52162.52870/w1\noutputFolder: /tmp/inspectra-help-busine-a7f8be14-87c075a5/tmp/albuild-build-9410eabae5314d2d9721b9428fd169b1\nprojects:\n(none)\ndependencies:\n(none)\nexternalDeps:\n(none)",
        "stderr": "Compiling fresh, locally (artifact symbols + feed deps -> alc)...\n→ build-app ...\n  Resolving artifact (Sandbox/w1//Latest)...\n  Downloading artifact https://bcartifacts-exdbf9fwegejdqak.b02.azurefd.net/sandbox/28.3.52162.52870/w1 ...\n  Downloading artifact https://bcartifacts-exdbf9fwegejdqak.b02.azurefd.net/sandbox/28.3.52162.52870/platform ...\n  Staged the test toolkit symbols (116 app(s)) into '/tmp/inspectra-help-busine-a7f8be14-87c075a5/xdg-data/ALbuild/artifacts/sandbox/28.3.52162.52870/platform/.albuild-toolkit-symbols'.\n  Build BC version: 28.3.52162.52870\n  Local dependency folder '/tmp/inspectra-help-busine-a7f8be14-87c075a5/.dependencies' not found; skipping.\n  dotnet tool install --global Microsoft.Dynamics.BusinessCentral.Development.Tools --ignore-failed-sources --add-source https://api.nuget.org/v3/index.json:\n  Tools directory '/tmp/inspectra-help-busine-a7f8be14-87c075a5/dotnet-home/.dotnet/tools' is not currently on the PATH environment variable.\n  If you are using bash, you can add it to your profile by running the following command:\n  cat << \\EOF >> ~/.bash_profile\n  # Add .NET Core SDK tools\n  export PATH=\"$PATH:/tmp/inspectra-help-busine-a7f8be14-87c075a5/dotnet-home/.dotnet/tools\"\n  EOF\n  You can add it to the current session by running the following command:\n  export PATH=\"$PATH:/tmp/inspectra-help-busine-a7f8be14-87c075a5/dotnet-home/.dotnet/tools\"\n  You can invoke the tool using the following command: al\n  Tool 'microsoft.dynamics.businesscentral.development.tools' (version '17.0.34.45391') was successfully installed.\n  AL Tool 'Microsoft.Dynamics.BusinessCentral.Development.Tools' install step completed (dotnet exit 0).\n  Build order: \n✓ build-app · 41s",
        "outputLimitExceeded": false
      },
      "parsed": false,
      "payload": "targetVersion: 28.3.52162.52870\nartifactUrl: https://bcartifacts-exdbf9fwegejdqak.b02.azurefd.net/sandbox/28.3.52162.52870/w1\noutputFolder: /tmp/inspectra-help-busine-a7f8be14-87c075a5/tmp/albuild-build-9410eabae5314d2d9721b9428fd169b1\nprojects:\n(none)\ndependencies:\n(none)\nexternalDeps:\n(none)",
      "terminalNonHelp": true
    },
    {
      "command": "app publish",
      "helpInvocation": "help app publish",
      "result": {
        "status": "failed",
        "timedOut": false,
        "exitCode": 2,
        "durationMs": 48,
        "stdout": "albuild - Business Central dev-loop CLI (an MCP client over the businessdev.ALbuild engine)\n\nUsage: albuild <command> [options]\n\nCommands:\n  doctor                 Check host readiness (PowerShell + module + Docker).\n  setup                  Guided setup: check host, module, albuild.json; print next steps.\n  update                 Update the PowerShell module + MCP server in one go (the CLI updates via 'dotnet tool update').\n  module install         Install the businessdev.ALbuild PowerShell module (PSGallery).\n  module update          Update the businessdev.ALbuild PowerShell module.\n  module status          Show the installed/used businessdev.ALbuild module.\n  init                   Create an albuild.json in the workspace.\n  feeds add              Add a package feed to albuild.json: feeds add <url> [--name --kind apps|symbols|runtime --id-scheme --api-key-env VAR].\n  feeds list             List the feeds in albuild.json (read-only).\n  feeds remove           Remove a feed from albuild.json by url or --name.  [destructive]\n  agents list            List configured MCP agents and their tools.\n  agents add             Add/replace an MCP agent: agents add <name> --tools a,b,c (or *).\n  agents remove          Remove an MCP agent.  [destructive]\n  agents token           Generate a bearer token for an agent (for the MCP HTTP host).\n  artifact find          Resolve a BC artifact URL.\n  container list         List BC containers (read-only).\n  container inspect      Show a container + its installed apps.\n  container new          Provision or warm-reuse a BC container (idempotent).\n  container restart      Restart a container.\n  container remove       Remove a container (destructive; shared -> approval).  [destructive]\n  deps plan              Dry-run dependency resolution for a project (read-only).\n  deps check             Reconcile manifest vs container vs feeds (read-only).\n  deps install           Resolve + install the dependency closure into a container.\n  app build              Compile the workspace fresh (fresh symbo\n[truncated]",
        "stderr": "Unknown command: help app publish",
        "outputLimitExceeded": false
      },
      "parsed": true,
      "payload": "albuild - Business Central dev-loop CLI (an MCP client over the businessdev.ALbuild engine)\n\nUsage: albuild <command> [options]\n\nCommands:\n  doctor                 Check host readiness (PowerShell + module + Docker).\n  setup                  Guided setup: check host, module, albuild.json; print next steps.\n  update                 Update the PowerShell module + MCP server in one go (the CLI updates via 'dotnet tool update').\n  module install         Install the businessdev.ALbuild PowerShell module (PSGallery).\n  module update          Update the businessdev.ALbuild PowerShell module.\n  module status          Show the installed/used businessdev.ALbuild module.\n  init                   Create an albuild.json in the workspace.\n  feeds add              Add a package feed to albuild.json: feeds add <url> [--name --kind apps|symbols|runtime --id-scheme --api-key-env VAR].\n  feeds list             List the feeds in albuild.json (read-only).\n  feeds remove           Remove a feed from albuild.json by url or --name.  [destructive]\n  agents list            List configured MCP agents and their tools.\n  agents add             Add/replace an MCP agent: agents add <name> --tools a,b,c (or *).\n  agents remove          Remove an MCP agent.  [destructive]\n  agents token           Generate a bearer token for an agent (for the MCP HTTP host).\n  artifact find          Resolve a BC artifact URL.\n  container list         List BC containers (read-only).\n  container inspect      Show a container + its installed apps.\n  container new          Provision or warm-reuse a BC container (idempotent).\n  container restart      Restart a container.\n  container remove       Remove a container (destructive; shared -> approval).  [destructive]\n  deps plan              Dry-run dependency resolution for a project (read-only).\n  deps check             Reconcile manifest vs container vs feeds (read-only).\n  deps install           Resolve + install the dependency closure into a container.\n  app build              Compile the workspace fresh (fresh symbo\n[truncated]",
      "terminalNonHelp": false
    },
    {
      "command": "app unpublish",
      "helpInvocation": "help app unpublish",
      "result": {
        "status": "failed",
        "timedOut": false,
        "exitCode": 2,
        "durationMs": 58,
        "stdout": "albuild - Business Central dev-loop CLI (an MCP client over the businessdev.ALbuild engine)\n\nUsage: albuild <command> [options]\n\nCommands:\n  doctor                 Check host readiness (PowerShell + module + Docker).\n  setup                  Guided setup: check host, module, albuild.json; print next steps.\n  update                 Update the PowerShell module + MCP server in one go (the CLI updates via 'dotnet tool update').\n  module install         Install the businessdev.ALbuild PowerShell module (PSGallery).\n  module update          Update the businessdev.ALbuild PowerShell module.\n  module status          Show the installed/used businessdev.ALbuild module.\n  init                   Create an albuild.json in the workspace.\n  feeds add              Add a package feed to albuild.json: feeds add <url> [--name --kind apps|symbols|runtime --id-scheme --api-key-env VAR].\n  feeds list             List the feeds in albuild.json (read-only).\n  feeds remove           Remove a feed from albuild.json by url or --name.  [destructive]\n  agents list            List configured MCP agents and their tools.\n  agents add             Add/replace an MCP agent: agents add <name> --tools a,b,c (or *).\n  agents remove          Remove an MCP agent.  [destructive]\n  agents token           Generate a bearer token for an agent (for the MCP HTTP host).\n  artifact find          Resolve a BC artifact URL.\n  container list         List BC containers (read-only).\n  container inspect      Show a container + its installed apps.\n  container new          Provision or warm-reuse a BC container (idempotent).\n  container restart      Restart a container.\n  container remove       Remove a container (destructive; shared -> approval).  [destructive]\n  deps plan              Dry-run dependency resolution for a project (read-only).\n  deps check             Reconcile manifest vs container vs feeds (read-only).\n  deps install           Resolve + install the dependency closure into a container.\n  app build              Compile the workspace fresh (fresh symbo\n[truncated]",
        "stderr": "Unknown command: help app unpublish",
        "outputLimitExceeded": false
      },
      "parsed": true,
      "payload": "albuild - Business Central dev-loop CLI (an MCP client over the businessdev.ALbuild engine)\n\nUsage: albuild <command> [options]\n\nCommands:\n  doctor                 Check host readiness (PowerShell + module + Docker).\n  setup                  Guided setup: check host, module, albuild.json; print next steps.\n  update                 Update the PowerShell module + MCP server in one go (the CLI updates via 'dotnet tool update').\n  module install         Install the businessdev.ALbuild PowerShell module (PSGallery).\n  module update          Update the businessdev.ALbuild PowerShell module.\n  module status          Show the installed/used businessdev.ALbuild module.\n  init                   Create an albuild.json in the workspace.\n  feeds add              Add a package feed to albuild.json: feeds add <url> [--name --kind apps|symbols|runtime --id-scheme --api-key-env VAR].\n  feeds list             List the feeds in albuild.json (read-only).\n  feeds remove           Remove a feed from albuild.json by url or --name.  [destructive]\n  agents list            List configured MCP agents and their tools.\n  agents add             Add/replace an MCP agent: agents add <name> --tools a,b,c (or *).\n  agents remove          Remove an MCP agent.  [destructive]\n  agents token           Generate a bearer token for an agent (for the MCP HTTP host).\n  artifact find          Resolve a BC artifact URL.\n  container list         List BC containers (read-only).\n  container inspect      Show a container + its installed apps.\n  container new          Provision or warm-reuse a BC container (idempotent).\n  container restart      Restart a container.\n  container remove       Remove a container (destructive; shared -> approval).  [destructive]\n  deps plan              Dry-run dependency resolution for a project (read-only).\n  deps check             Reconcile manifest vs container vs feeds (read-only).\n  deps install           Resolve + install the dependency closure into a container.\n  app build              Compile the workspace fresh (fresh symbo\n[truncated]",
      "terminalNonHelp": false
    },
    {
      "command": "app test",
      "helpInvocation": "help app test",
      "result": {
        "status": "failed",
        "timedOut": false,
        "exitCode": 2,
        "durationMs": 49,
        "stdout": "albuild - Business Central dev-loop CLI (an MCP client over the businessdev.ALbuild engine)\n\nUsage: albuild <command> [options]\n\nCommands:\n  doctor                 Check host readiness (PowerShell + module + Docker).\n  setup                  Guided setup: check host, module, albuild.json; print next steps.\n  update                 Update the PowerShell module + MCP server in one go (the CLI updates via 'dotnet tool update').\n  module install         Install the businessdev.ALbuild PowerShell module (PSGallery).\n  module update          Update the businessdev.ALbuild PowerShell module.\n  module status          Show the installed/used businessdev.ALbuild module.\n  init                   Create an albuild.json in the workspace.\n  feeds add              Add a package feed to albuild.json: feeds add <url> [--name --kind apps|symbols|runtime --id-scheme --api-key-env VAR].\n  feeds list             List the feeds in albuild.json (read-only).\n  feeds remove           Remove a feed from albuild.json by url or --name.  [destructive]\n  agents list            List configured MCP agents and their tools.\n  agents add             Add/replace an MCP agent: agents add <name> --tools a,b,c (or *).\n  agents remove          Remove an MCP agent.  [destructive]\n  agents token           Generate a bearer token for an agent (for the MCP HTTP host).\n  artifact find          Resolve a BC artifact URL.\n  container list         List BC containers (read-only).\n  container inspect      Show a container + its installed apps.\n  container new          Provision or warm-reuse a BC container (idempotent).\n  container restart      Restart a container.\n  container remove       Remove a container (destructive; shared -> approval).  [destructive]\n  deps plan              Dry-run dependency resolution for a project (read-only).\n  deps check             Reconcile manifest vs container vs feeds (read-only).\n  deps install           Resolve + install the dependency closure into a container.\n  app build              Compile the workspace fresh (fresh symbo\n[truncated]",
        "stderr": "Unknown command: help app test",
        "outputLimitExceeded": false
      },
      "parsed": true,
      "payload": "albuild - Business Central dev-loop CLI (an MCP client over the businessdev.ALbuild engine)\n\nUsage: albuild <command> [options]\n\nCommands:\n  doctor                 Check host readiness (PowerShell + module + Docker).\n  setup                  Guided setup: check host, module, albuild.json; print next steps.\n  update                 Update the PowerShell module + MCP server in one go (the CLI updates via 'dotnet tool update').\n  module install         Install the businessdev.ALbuild PowerShell module (PSGallery).\n  module update          Update the businessdev.ALbuild PowerShell module.\n  module status          Show the installed/used businessdev.ALbuild module.\n  init                   Create an albuild.json in the workspace.\n  feeds add              Add a package feed to albuild.json: feeds add <url> [--name --kind apps|symbols|runtime --id-scheme --api-key-env VAR].\n  feeds list             List the feeds in albuild.json (read-only).\n  feeds remove           Remove a feed from albuild.json by url or --name.  [destructive]\n  agents list            List configured MCP agents and their tools.\n  agents add             Add/replace an MCP agent: agents add <name> --tools a,b,c (or *).\n  agents remove          Remove an MCP agent.  [destructive]\n  agents token           Generate a bearer token for an agent (for the MCP HTTP host).\n  artifact find          Resolve a BC artifact URL.\n  container list         List BC containers (read-only).\n  container inspect      Show a container + its installed apps.\n  container new          Provision or warm-reuse a BC container (idempotent).\n  container restart      Restart a container.\n  container remove       Remove a container (destructive; shared -> approval).  [destructive]\n  deps plan              Dry-run dependency resolution for a project (read-only).\n  deps check             Reconcile manifest vs container vs feeds (read-only).\n  deps install           Resolve + install the dependency closure into a container.\n  app build              Compile the workspace fresh (fresh symbo\n[truncated]",
      "terminalNonHelp": false
    },
    {
      "command": "app deploy",
      "helpInvocation": "help app deploy",
      "result": {
        "status": "failed",
        "timedOut": false,
        "exitCode": 2,
        "durationMs": 48,
        "stdout": "albuild - Business Central dev-loop CLI (an MCP client over the businessdev.ALbuild engine)\n\nUsage: albuild <command> [options]\n\nCommands:\n  doctor                 Check host readiness (PowerShell + module + Docker).\n  setup                  Guided setup: check host, module, albuild.json; print next steps.\n  update                 Update the PowerShell module + MCP server in one go (the CLI updates via 'dotnet tool update').\n  module install         Install the businessdev.ALbuild PowerShell module (PSGallery).\n  module update          Update the businessdev.ALbuild PowerShell module.\n  module status          Show the installed/used businessdev.ALbuild module.\n  init                   Create an albuild.json in the workspace.\n  feeds add              Add a package feed to albuild.json: feeds add <url> [--name --kind apps|symbols|runtime --id-scheme --api-key-env VAR].\n  feeds list             List the feeds in albuild.json (read-only).\n  feeds remove           Remove a feed from albuild.json by url or --name.  [destructive]\n  agents list            List configured MCP agents and their tools.\n  agents add             Add/replace an MCP agent: agents add <name> --tools a,b,c (or *).\n  agents remove          Remove an MCP agent.  [destructive]\n  agents token           Generate a bearer token for an agent (for the MCP HTTP host).\n  artifact find          Resolve a BC artifact URL.\n  container list         List BC containers (read-only).\n  container inspect      Show a container + its installed apps.\n  container new          Provision or warm-reuse a BC container (idempotent).\n  container restart      Restart a container.\n  container remove       Remove a container (destructive; shared -> approval).  [destructive]\n  deps plan              Dry-run dependency resolution for a project (read-only).\n  deps check             Reconcile manifest vs container vs feeds (read-only).\n  deps install           Resolve + install the dependency closure into a container.\n  app build              Compile the workspace fresh (fresh symbo\n[truncated]",
        "stderr": "Unknown command: help app deploy",
        "outputLimitExceeded": false
      },
      "parsed": true,
      "payload": "albuild - Business Central dev-loop CLI (an MCP client over the businessdev.ALbuild engine)\n\nUsage: albuild <command> [options]\n\nCommands:\n  doctor                 Check host readiness (PowerShell + module + Docker).\n  setup                  Guided setup: check host, module, albuild.json; print next steps.\n  update                 Update the PowerShell module + MCP server in one go (the CLI updates via 'dotnet tool update').\n  module install         Install the businessdev.ALbuild PowerShell module (PSGallery).\n  module update          Update the businessdev.ALbuild PowerShell module.\n  module status          Show the installed/used businessdev.ALbuild module.\n  init                   Create an albuild.json in the workspace.\n  feeds add              Add a package feed to albuild.json: feeds add <url> [--name --kind apps|symbols|runtime --id-scheme --api-key-env VAR].\n  feeds list             List the feeds in albuild.json (read-only).\n  feeds remove           Remove a feed from albuild.json by url or --name.  [destructive]\n  agents list            List configured MCP agents and their tools.\n  agents add             Add/replace an MCP agent: agents add <name> --tools a,b,c (or *).\n  agents remove          Remove an MCP agent.  [destructive]\n  agents token           Generate a bearer token for an agent (for the MCP HTTP host).\n  artifact find          Resolve a BC artifact URL.\n  container list         List BC containers (read-only).\n  container inspect      Show a container + its installed apps.\n  container new          Provision or warm-reuse a BC container (idempotent).\n  container restart      Restart a container.\n  container remove       Remove a container (destructive; shared -> approval).  [destructive]\n  deps plan              Dry-run dependency resolution for a project (read-only).\n  deps check             Reconcile manifest vs container vs feeds (read-only).\n  deps install           Resolve + install the dependency closure into a container.\n  app build              Compile the workspace fresh (fresh symbo\n[truncated]",
      "terminalNonHelp": false
    },
    {
      "command": "app results",
      "helpInvocation": "help app results",
      "result": {
        "status": "failed",
        "timedOut": false,
        "exitCode": 2,
        "durationMs": 49,
        "stdout": "albuild - Business Central dev-loop CLI (an MCP client over the businessdev.ALbuild engine)\n\nUsage: albuild <command> [options]\n\nCommands:\n  doctor                 Check host readiness (PowerShell + module + Docker).\n  setup                  Guided setup: check host, module, albuild.json; print next steps.\n  update                 Update the PowerShell module + MCP server in one go (the CLI updates via 'dotnet tool update').\n  module install         Install the businessdev.ALbuild PowerShell module (PSGallery).\n  module update          Update the businessdev.ALbuild PowerShell module.\n  module status          Show the installed/used businessdev.ALbuild module.\n  init                   Create an albuild.json in the workspace.\n  feeds add              Add a package feed to albuild.json: feeds add <url> [--name --kind apps|symbols|runtime --id-scheme --api-key-env VAR].\n  feeds list             List the feeds in albuild.json (read-only).\n  feeds remove           Remove a feed from albuild.json by url or --name.  [destructive]\n  agents list            List configured MCP agents and their tools.\n  agents add             Add/replace an MCP agent: agents add <name> --tools a,b,c (or *).\n  agents remove          Remove an MCP agent.  [destructive]\n  agents token           Generate a bearer token for an agent (for the MCP HTTP host).\n  artifact find          Resolve a BC artifact URL.\n  container list         List BC containers (read-only).\n  container inspect      Show a container + its installed apps.\n  container new          Provision or warm-reuse a BC container (idempotent).\n  container restart      Restart a container.\n  container remove       Remove a container (destructive; shared -> approval).  [destructive]\n  deps plan              Dry-run dependency resolution for a project (read-only).\n  deps check             Reconcile manifest vs container vs feeds (read-only).\n  deps install           Resolve + install the dependency closure into a container.\n  app build              Compile the workspace fresh (fresh symbo\n[truncated]",
        "stderr": "Unknown command: help app results",
        "outputLimitExceeded": false
      },
      "parsed": true,
      "payload": "albuild - Business Central dev-loop CLI (an MCP client over the businessdev.ALbuild engine)\n\nUsage: albuild <command> [options]\n\nCommands:\n  doctor                 Check host readiness (PowerShell + module + Docker).\n  setup                  Guided setup: check host, module, albuild.json; print next steps.\n  update                 Update the PowerShell module + MCP server in one go (the CLI updates via 'dotnet tool update').\n  module install         Install the businessdev.ALbuild PowerShell module (PSGallery).\n  module update          Update the businessdev.ALbuild PowerShell module.\n  module status          Show the installed/used businessdev.ALbuild module.\n  init                   Create an albuild.json in the workspace.\n  feeds add              Add a package feed to albuild.json: feeds add <url> [--name --kind apps|symbols|runtime --id-scheme --api-key-env VAR].\n  feeds list             List the feeds in albuild.json (read-only).\n  feeds remove           Remove a feed from albuild.json by url or --name.  [destructive]\n  agents list            List configured MCP agents and their tools.\n  agents add             Add/replace an MCP agent: agents add <name> --tools a,b,c (or *).\n  agents remove          Remove an MCP agent.  [destructive]\n  agents token           Generate a bearer token for an agent (for the MCP HTTP host).\n  artifact find          Resolve a BC artifact URL.\n  container list         List BC containers (read-only).\n  container inspect      Show a container + its installed apps.\n  container new          Provision or warm-reuse a BC container (idempotent).\n  container restart      Restart a container.\n  container remove       Remove a container (destructive; shared -> approval).  [destructive]\n  deps plan              Dry-run dependency resolution for a project (read-only).\n  deps check             Reconcile manifest vs container vs feeds (read-only).\n  deps install           Resolve + install the dependency closure into a container.\n  app build              Compile the workspace fresh (fresh symbo\n[truncated]",
      "terminalNonHelp": false
    },
    {
      "command": "coverage convert",
      "helpInvocation": "help coverage convert",
      "result": {
        "status": "failed",
        "timedOut": false,
        "exitCode": 2,
        "durationMs": 48,
        "stdout": "albuild - Business Central dev-loop CLI (an MCP client over the businessdev.ALbuild engine)\n\nUsage: albuild <command> [options]\n\nCommands:\n  doctor                 Check host readiness (PowerShell + module + Docker).\n  setup                  Guided setup: check host, module, albuild.json; print next steps.\n  update                 Update the PowerShell module + MCP server in one go (the CLI updates via 'dotnet tool update').\n  module install         Install the businessdev.ALbuild PowerShell module (PSGallery).\n  module update          Update the businessdev.ALbuild PowerShell module.\n  module status          Show the installed/used businessdev.ALbuild module.\n  init                   Create an albuild.json in the workspace.\n  feeds add              Add a package feed to albuild.json: feeds add <url> [--name --kind apps|symbols|runtime --id-scheme --api-key-env VAR].\n  feeds list             List the feeds in albuild.json (read-only).\n  feeds remove           Remove a feed from albuild.json by url or --name.  [destructive]\n  agents list            List configured MCP agents and their tools.\n  agents add             Add/replace an MCP agent: agents add <name> --tools a,b,c (or *).\n  agents remove          Remove an MCP agent.  [destructive]\n  agents token           Generate a bearer token for an agent (for the MCP HTTP host).\n  artifact find          Resolve a BC artifact URL.\n  container list         List BC containers (read-only).\n  container inspect      Show a container + its installed apps.\n  container new          Provision or warm-reuse a BC container (idempotent).\n  container restart      Restart a container.\n  container remove       Remove a container (destructive; shared -> approval).  [destructive]\n  deps plan              Dry-run dependency resolution for a project (read-only).\n  deps check             Reconcile manifest vs container vs feeds (read-only).\n  deps install           Resolve + install the dependency closure into a container.\n  app build              Compile the workspace fresh (fresh symbo\n[truncated]",
        "stderr": "Unknown command: help coverage convert",
        "outputLimitExceeded": false
      },
      "parsed": true,
      "payload": "albuild - Business Central dev-loop CLI (an MCP client over the businessdev.ALbuild engine)\n\nUsage: albuild <command> [options]\n\nCommands:\n  doctor                 Check host readiness (PowerShell + module + Docker).\n  setup                  Guided setup: check host, module, albuild.json; print next steps.\n  update                 Update the PowerShell module + MCP server in one go (the CLI updates via 'dotnet tool update').\n  module install         Install the businessdev.ALbuild PowerShell module (PSGallery).\n  module update          Update the businessdev.ALbuild PowerShell module.\n  module status          Show the installed/used businessdev.ALbuild module.\n  init                   Create an albuild.json in the workspace.\n  feeds add              Add a package feed to albuild.json: feeds add <url> [--name --kind apps|symbols|runtime --id-scheme --api-key-env VAR].\n  feeds list             List the feeds in albuild.json (read-only).\n  feeds remove           Remove a feed from albuild.json by url or --name.  [destructive]\n  agents list            List configured MCP agents and their tools.\n  agents add             Add/replace an MCP agent: agents add <name> --tools a,b,c (or *).\n  agents remove          Remove an MCP agent.  [destructive]\n  agents token           Generate a bearer token for an agent (for the MCP HTTP host).\n  artifact find          Resolve a BC artifact URL.\n  container list         List BC containers (read-only).\n  container inspect      Show a container + its installed apps.\n  container new          Provision or warm-reuse a BC container (idempotent).\n  container restart      Restart a container.\n  container remove       Remove a container (destructive; shared -> approval).  [destructive]\n  deps plan              Dry-run dependency resolution for a project (read-only).\n  deps check             Reconcile manifest vs container vs feeds (read-only).\n  deps install           Resolve + install the dependency closure into a container.\n  app build              Compile the workspace fresh (fresh symbo\n[truncated]",
      "terminalNonHelp": false
    },
    {
      "command": "coverage summary",
      "helpInvocation": "help coverage summary",
      "result": {
        "status": "failed",
        "timedOut": false,
        "exitCode": 2,
        "durationMs": 54,
        "stdout": "albuild - Business Central dev-loop CLI (an MCP client over the businessdev.ALbuild engine)\n\nUsage: albuild <command> [options]\n\nCommands:\n  doctor                 Check host readiness (PowerShell + module + Docker).\n  setup                  Guided setup: check host, module, albuild.json; print next steps.\n  update                 Update the PowerShell module + MCP server in one go (the CLI updates via 'dotnet tool update').\n  module install         Install the businessdev.ALbuild PowerShell module (PSGallery).\n  module update          Update the businessdev.ALbuild PowerShell module.\n  module status          Show the installed/used businessdev.ALbuild module.\n  init                   Create an albuild.json in the workspace.\n  feeds add              Add a package feed to albuild.json: feeds add <url> [--name --kind apps|symbols|runtime --id-scheme --api-key-env VAR].\n  feeds list             List the feeds in albuild.json (read-only).\n  feeds remove           Remove a feed from albuild.json by url or --name.  [destructive]\n  agents list            List configured MCP agents and their tools.\n  agents add             Add/replace an MCP agent: agents add <name> --tools a,b,c (or *).\n  agents remove          Remove an MCP agent.  [destructive]\n  agents token           Generate a bearer token for an agent (for the MCP HTTP host).\n  artifact find          Resolve a BC artifact URL.\n  container list         List BC containers (read-only).\n  container inspect      Show a container + its installed apps.\n  container new          Provision or warm-reuse a BC container (idempotent).\n  container restart      Restart a container.\n  container remove       Remove a container (destructive; shared -> approval).  [destructive]\n  deps plan              Dry-run dependency resolution for a project (read-only).\n  deps check             Reconcile manifest vs container vs feeds (read-only).\n  deps install           Resolve + install the dependency closure into a container.\n  app build              Compile the workspace fresh (fresh symbo\n[truncated]",
        "stderr": "Unknown command: help coverage summary",
        "outputLimitExceeded": false
      },
      "parsed": true,
      "payload": "albuild - Business Central dev-loop CLI (an MCP client over the businessdev.ALbuild engine)\n\nUsage: albuild <command> [options]\n\nCommands:\n  doctor                 Check host readiness (PowerShell + module + Docker).\n  setup                  Guided setup: check host, module, albuild.json; print next steps.\n  update                 Update the PowerShell module + MCP server in one go (the CLI updates via 'dotnet tool update').\n  module install         Install the businessdev.ALbuild PowerShell module (PSGallery).\n  module update          Update the businessdev.ALbuild PowerShell module.\n  module status          Show the installed/used businessdev.ALbuild module.\n  init                   Create an albuild.json in the workspace.\n  feeds add              Add a package feed to albuild.json: feeds add <url> [--name --kind apps|symbols|runtime --id-scheme --api-key-env VAR].\n  feeds list             List the feeds in albuild.json (read-only).\n  feeds remove           Remove a feed from albuild.json by url or --name.  [destructive]\n  agents list            List configured MCP agents and their tools.\n  agents add             Add/replace an MCP agent: agents add <name> --tools a,b,c (or *).\n  agents remove          Remove an MCP agent.  [destructive]\n  agents token           Generate a bearer token for an agent (for the MCP HTTP host).\n  artifact find          Resolve a BC artifact URL.\n  container list         List BC containers (read-only).\n  container inspect      Show a container + its installed apps.\n  container new          Provision or warm-reuse a BC container (idempotent).\n  container restart      Restart a container.\n  container remove       Remove a container (destructive; shared -> approval).  [destructive]\n  deps plan              Dry-run dependency resolution for a project (read-only).\n  deps check             Reconcile manifest vs container vs feeds (read-only).\n  deps install           Resolve + install the dependency closure into a container.\n  app build              Compile the workspace fresh (fresh symbo\n[truncated]",
      "terminalNonHelp": false
    },
    {
      "command": "coverage threshold",
      "helpInvocation": "help coverage threshold",
      "result": {
        "status": "failed",
        "timedOut": false,
        "exitCode": 2,
        "durationMs": 54,
        "stdout": "albuild - Business Central dev-loop CLI (an MCP client over the businessdev.ALbuild engine)\n\nUsage: albuild <command> [options]\n\nCommands:\n  doctor                 Check host readiness (PowerShell + module + Docker).\n  setup                  Guided setup: check host, module, albuild.json; print next steps.\n  update                 Update the PowerShell module + MCP server in one go (the CLI updates via 'dotnet tool update').\n  module install         Install the businessdev.ALbuild PowerShell module (PSGallery).\n  module update          Update the businessdev.ALbuild PowerShell module.\n  module status          Show the installed/used businessdev.ALbuild module.\n  init                   Create an albuild.json in the workspace.\n  feeds add              Add a package feed to albuild.json: feeds add <url> [--name --kind apps|symbols|runtime --id-scheme --api-key-env VAR].\n  feeds list             List the feeds in albuild.json (read-only).\n  feeds remove           Remove a feed from albuild.json by url or --name.  [destructive]\n  agents list            List configured MCP agents and their tools.\n  agents add             Add/replace an MCP agent: agents add <name> --tools a,b,c (or *).\n  agents remove          Remove an MCP agent.  [destructive]\n  agents token           Generate a bearer token for an agent (for the MCP HTTP host).\n  artifact find          Resolve a BC artifact URL.\n  container list         List BC containers (read-only).\n  container inspect      Show a container + its installed apps.\n  container new          Provision or warm-reuse a BC container (idempotent).\n  container restart      Restart a container.\n  container remove       Remove a container (destructive; shared -> approval).  [destructive]\n  deps plan              Dry-run dependency resolution for a project (read-only).\n  deps check             Reconcile manifest vs container vs feeds (read-only).\n  deps install           Resolve + install the dependency closure into a container.\n  app build              Compile the workspace fresh (fresh symbo\n[truncated]",
        "stderr": "Unknown command: help coverage threshold",
        "outputLimitExceeded": false
      },
      "parsed": true,
      "payload": "albuild - Business Central dev-loop CLI (an MCP client over the businessdev.ALbuild engine)\n\nUsage: albuild <command> [options]\n\nCommands:\n  doctor                 Check host readiness (PowerShell + module + Docker).\n  setup                  Guided setup: check host, module, albuild.json; print next steps.\n  update                 Update the PowerShell module + MCP server in one go (the CLI updates via 'dotnet tool update').\n  module install         Install the businessdev.ALbuild PowerShell module (PSGallery).\n  module update          Update the businessdev.ALbuild PowerShell module.\n  module status          Show the installed/used businessdev.ALbuild module.\n  init                   Create an albuild.json in the workspace.\n  feeds add              Add a package feed to albuild.json: feeds add <url> [--name --kind apps|symbols|runtime --id-scheme --api-key-env VAR].\n  feeds list             List the feeds in albuild.json (read-only).\n  feeds remove           Remove a feed from albuild.json by url or --name.  [destructive]\n  agents list            List configured MCP agents and their tools.\n  agents add             Add/replace an MCP agent: agents add <name> --tools a,b,c (or *).\n  agents remove          Remove an MCP agent.  [destructive]\n  agents token           Generate a bearer token for an agent (for the MCP HTTP host).\n  artifact find          Resolve a BC artifact URL.\n  container list         List BC containers (read-only).\n  container inspect      Show a container + its installed apps.\n  container new          Provision or warm-reuse a BC container (idempotent).\n  container restart      Restart a container.\n  container remove       Remove a container (destructive; shared -> approval).  [destructive]\n  deps plan              Dry-run dependency resolution for a project (read-only).\n  deps check             Reconcile manifest vs container vs feeds (read-only).\n  deps install           Resolve + install the dependency closure into a container.\n  app build              Compile the workspace fresh (fresh symbo\n[truncated]",
      "terminalNonHelp": false
    },
    {
      "command": "coverage delta",
      "helpInvocation": "help coverage delta",
      "result": {
        "status": "failed",
        "timedOut": false,
        "exitCode": 2,
        "durationMs": 48,
        "stdout": "albuild - Business Central dev-loop CLI (an MCP client over the businessdev.ALbuild engine)\n\nUsage: albuild <command> [options]\n\nCommands:\n  doctor                 Check host readiness (PowerShell + module + Docker).\n  setup                  Guided setup: check host, module, albuild.json; print next steps.\n  update                 Update the PowerShell module + MCP server in one go (the CLI updates via 'dotnet tool update').\n  module install         Install the businessdev.ALbuild PowerShell module (PSGallery).\n  module update          Update the businessdev.ALbuild PowerShell module.\n  module status          Show the installed/used businessdev.ALbuild module.\n  init                   Create an albuild.json in the workspace.\n  feeds add              Add a package feed to albuild.json: feeds add <url> [--name --kind apps|symbols|runtime --id-scheme --api-key-env VAR].\n  feeds list             List the feeds in albuild.json (read-only).\n  feeds remove           Remove a feed from albuild.json by url or --name.  [destructive]\n  agents list            List configured MCP agents and their tools.\n  agents add             Add/replace an MCP agent: agents add <name> --tools a,b,c (or *).\n  agents remove          Remove an MCP agent.  [destructive]\n  agents token           Generate a bearer token for an agent (for the MCP HTTP host).\n  artifact find          Resolve a BC artifact URL.\n  container list         List BC containers (read-only).\n  container inspect      Show a container + its installed apps.\n  container new          Provision or warm-reuse a BC container (idempotent).\n  container restart      Restart a container.\n  container remove       Remove a container (destructive; shared -> approval).  [destructive]\n  deps plan              Dry-run dependency resolution for a project (read-only).\n  deps check             Reconcile manifest vs container vs feeds (read-only).\n  deps install           Resolve + install the dependency closure into a container.\n  app build              Compile the workspace fresh (fresh symbo\n[truncated]",
        "stderr": "Unknown command: help coverage delta",
        "outputLimitExceeded": false
      },
      "parsed": true,
      "payload": "albuild - Business Central dev-loop CLI (an MCP client over the businessdev.ALbuild engine)\n\nUsage: albuild <command> [options]\n\nCommands:\n  doctor                 Check host readiness (PowerShell + module + Docker).\n  setup                  Guided setup: check host, module, albuild.json; print next steps.\n  update                 Update the PowerShell module + MCP server in one go (the CLI updates via 'dotnet tool update').\n  module install         Install the businessdev.ALbuild PowerShell module (PSGallery).\n  module update          Update the businessdev.ALbuild PowerShell module.\n  module status          Show the installed/used businessdev.ALbuild module.\n  init                   Create an albuild.json in the workspace.\n  feeds add              Add a package feed to albuild.json: feeds add <url> [--name --kind apps|symbols|runtime --id-scheme --api-key-env VAR].\n  feeds list             List the feeds in albuild.json (read-only).\n  feeds remove           Remove a feed from albuild.json by url or --name.  [destructive]\n  agents list            List configured MCP agents and their tools.\n  agents add             Add/replace an MCP agent: agents add <name> --tools a,b,c (or *).\n  agents remove          Remove an MCP agent.  [destructive]\n  agents token           Generate a bearer token for an agent (for the MCP HTTP host).\n  artifact find          Resolve a BC artifact URL.\n  container list         List BC containers (read-only).\n  container inspect      Show a container + its installed apps.\n  container new          Provision or warm-reuse a BC container (idempotent).\n  container restart      Restart a container.\n  container remove       Remove a container (destructive; shared -> approval).  [destructive]\n  deps plan              Dry-run dependency resolution for a project (read-only).\n  deps check             Reconcile manifest vs container vs feeds (read-only).\n  deps install           Resolve + install the dependency closure into a container.\n  app build              Compile the workspace fresh (fresh symbo\n[truncated]",
      "terminalNonHelp": false
    },
    {
      "command": "coverage merge",
      "helpInvocation": "help coverage merge",
      "result": {
        "status": "failed",
        "timedOut": false,
        "exitCode": 2,
        "durationMs": 48,
        "stdout": "albuild - Business Central dev-loop CLI (an MCP client over the businessdev.ALbuild engine)\n\nUsage: albuild <command> [options]\n\nCommands:\n  doctor                 Check host readiness (PowerShell + module + Docker).\n  setup                  Guided setup: check host, module, albuild.json; print next steps.\n  update                 Update the PowerShell module + MCP server in one go (the CLI updates via 'dotnet tool update').\n  module install         Install the businessdev.ALbuild PowerShell module (PSGallery).\n  module update          Update the businessdev.ALbuild PowerShell module.\n  module status          Show the installed/used businessdev.ALbuild module.\n  init                   Create an albuild.json in the workspace.\n  feeds add              Add a package feed to albuild.json: feeds add <url> [--name --kind apps|symbols|runtime --id-scheme --api-key-env VAR].\n  feeds list             List the feeds in albuild.json (read-only).\n  feeds remove           Remove a feed from albuild.json by url or --name.  [destructive]\n  agents list            List configured MCP agents and their tools.\n  agents add             Add/replace an MCP agent: agents add <name> --tools a,b,c (or *).\n  agents remove          Remove an MCP agent.  [destructive]\n  agents token           Generate a bearer token for an agent (for the MCP HTTP host).\n  artifact find          Resolve a BC artifact URL.\n  container list         List BC containers (read-only).\n  container inspect      Show a container + its installed apps.\n  container new          Provision or warm-reuse a BC container (idempotent).\n  container restart      Restart a container.\n  container remove       Remove a container (destructive; shared -> approval).  [destructive]\n  deps plan              Dry-run dependency resolution for a project (read-only).\n  deps check             Reconcile manifest vs container vs feeds (read-only).\n  deps install           Resolve + install the dependency closure into a container.\n  app build              Compile the workspace fresh (fresh symbo\n[truncated]",
        "stderr": "Unknown command: help coverage merge",
        "outputLimitExceeded": false
      },
      "parsed": true,
      "payload": "albuild - Business Central dev-loop CLI (an MCP client over the businessdev.ALbuild engine)\n\nUsage: albuild <command> [options]\n\nCommands:\n  doctor                 Check host readiness (PowerShell + module + Docker).\n  setup                  Guided setup: check host, module, albuild.json; print next steps.\n  update                 Update the PowerShell module + MCP server in one go (the CLI updates via 'dotnet tool update').\n  module install         Install the businessdev.ALbuild PowerShell module (PSGallery).\n  module update          Update the businessdev.ALbuild PowerShell module.\n  module status          Show the installed/used businessdev.ALbuild module.\n  init                   Create an albuild.json in the workspace.\n  feeds add              Add a package feed to albuild.json: feeds add <url> [--name --kind apps|symbols|runtime --id-scheme --api-key-env VAR].\n  feeds list             List the feeds in albuild.json (read-only).\n  feeds remove           Remove a feed from albuild.json by url or --name.  [destructive]\n  agents list            List configured MCP agents and their tools.\n  agents add             Add/replace an MCP agent: agents add <name> --tools a,b,c (or *).\n  agents remove          Remove an MCP agent.  [destructive]\n  agents token           Generate a bearer token for an agent (for the MCP HTTP host).\n  artifact find          Resolve a BC artifact URL.\n  container list         List BC containers (read-only).\n  container inspect      Show a container + its installed apps.\n  container new          Provision or warm-reuse a BC container (idempotent).\n  container restart      Restart a container.\n  container remove       Remove a container (destructive; shared -> approval).  [destructive]\n  deps plan              Dry-run dependency resolution for a project (read-only).\n  deps check             Reconcile manifest vs container vs feeds (read-only).\n  deps install           Resolve + install the dependency closure into a container.\n  app build              Compile the workspace fresh (fresh symbo\n[truncated]",
      "terminalNonHelp": false
    },
    {
      "command": "quality",
      "helpInvocation": "quality --help",
      "result": {
        "status": "failed",
        "timedOut": false,
        "exitCode": 2,
        "durationMs": 56,
        "stdout": null,
        "stderr": "✗ error [MISSING_OPTION]: --workspace is required for 'quality'.\n  Pass --workspace <value>.",
        "outputLimitExceeded": false
      },
      "parsed": false,
      "payload": "✗ error [MISSING_OPTION]: --workspace is required for 'quality'.\n  Pass --workspace <value>.",
      "terminalNonHelp": false
    },
    {
      "command": "project order",
      "helpInvocation": "help project order",
      "result": {
        "status": "failed",
        "timedOut": false,
        "exitCode": 2,
        "durationMs": 48,
        "stdout": "albuild - Business Central dev-loop CLI (an MCP client over the businessdev.ALbuild engine)\n\nUsage: albuild <command> [options]\n\nCommands:\n  doctor                 Check host readiness (PowerShell + module + Docker).\n  setup                  Guided setup: check host, module, albuild.json; print next steps.\n  update                 Update the PowerShell module + MCP server in one go (the CLI updates via 'dotnet tool update').\n  module install         Install the businessdev.ALbuild PowerShell module (PSGallery).\n  module update          Update the businessdev.ALbuild PowerShell module.\n  module status          Show the installed/used businessdev.ALbuild module.\n  init                   Create an albuild.json in the workspace.\n  feeds add              Add a package feed to albuild.json: feeds add <url> [--name --kind apps|symbols|runtime --id-scheme --api-key-env VAR].\n  feeds list             List the feeds in albuild.json (read-only).\n  feeds remove           Remove a feed from albuild.json by url or --name.  [destructive]\n  agents list            List configured MCP agents and their tools.\n  agents add             Add/replace an MCP agent: agents add <name> --tools a,b,c (or *).\n  agents remove          Remove an MCP agent.  [destructive]\n  agents token           Generate a bearer token for an agent (for the MCP HTTP host).\n  artifact find          Resolve a BC artifact URL.\n  container list         List BC containers (read-only).\n  container inspect      Show a container + its installed apps.\n  container new          Provision or warm-reuse a BC container (idempotent).\n  container restart      Restart a container.\n  container remove       Remove a container (destructive; shared -> approval).  [destructive]\n  deps plan              Dry-run dependency resolution for a project (read-only).\n  deps check             Reconcile manifest vs container vs feeds (read-only).\n  deps install           Resolve + install the dependency closure into a container.\n  app build              Compile the workspace fresh (fresh symbo\n[truncated]",
        "stderr": "Unknown command: help project order",
        "outputLimitExceeded": false
      },
      "parsed": true,
      "payload": "albuild - Business Central dev-loop CLI (an MCP client over the businessdev.ALbuild engine)\n\nUsage: albuild <command> [options]\n\nCommands:\n  doctor                 Check host readiness (PowerShell + module + Docker).\n  setup                  Guided setup: check host, module, albuild.json; print next steps.\n  update                 Update the PowerShell module + MCP server in one go (the CLI updates via 'dotnet tool update').\n  module install         Install the businessdev.ALbuild PowerShell module (PSGallery).\n  module update          Update the businessdev.ALbuild PowerShell module.\n  module status          Show the installed/used businessdev.ALbuild module.\n  init                   Create an albuild.json in the workspace.\n  feeds add              Add a package feed to albuild.json: feeds add <url> [--name --kind apps|symbols|runtime --id-scheme --api-key-env VAR].\n  feeds list             List the feeds in albuild.json (read-only).\n  feeds remove           Remove a feed from albuild.json by url or --name.  [destructive]\n  agents list            List configured MCP agents and their tools.\n  agents add             Add/replace an MCP agent: agents add <name> --tools a,b,c (or *).\n  agents remove          Remove an MCP agent.  [destructive]\n  agents token           Generate a bearer token for an agent (for the MCP HTTP host).\n  artifact find          Resolve a BC artifact URL.\n  container list         List BC containers (read-only).\n  container inspect      Show a container + its installed apps.\n  container new          Provision or warm-reuse a BC container (idempotent).\n  container restart      Restart a container.\n  container remove       Remove a container (destructive; shared -> approval).  [destructive]\n  deps plan              Dry-run dependency resolution for a project (read-only).\n  deps check             Reconcile manifest vs container vs feeds (read-only).\n  deps install           Resolve + install the dependency closure into a container.\n  app build              Compile the workspace fresh (fresh symbo\n[truncated]",
      "terminalNonHelp": false
    },
    {
      "command": "job get",
      "helpInvocation": "help job get",
      "result": {
        "status": "failed",
        "timedOut": false,
        "exitCode": 2,
        "durationMs": 50,
        "stdout": "albuild - Business Central dev-loop CLI (an MCP client over the businessdev.ALbuild engine)\n\nUsage: albuild <command> [options]\n\nCommands:\n  doctor                 Check host readiness (PowerShell + module + Docker).\n  setup                  Guided setup: check host, module, albuild.json; print next steps.\n  update                 Update the PowerShell module + MCP server in one go (the CLI updates via 'dotnet tool update').\n  module install         Install the businessdev.ALbuild PowerShell module (PSGallery).\n  module update          Update the businessdev.ALbuild PowerShell module.\n  module status          Show the installed/used businessdev.ALbuild module.\n  init                   Create an albuild.json in the workspace.\n  feeds add              Add a package feed to albuild.json: feeds add <url> [--name --kind apps|symbols|runtime --id-scheme --api-key-env VAR].\n  feeds list             List the feeds in albuild.json (read-only).\n  feeds remove           Remove a feed from albuild.json by url or --name.  [destructive]\n  agents list            List configured MCP agents and their tools.\n  agents add             Add/replace an MCP agent: agents add <name> --tools a,b,c (or *).\n  agents remove          Remove an MCP agent.  [destructive]\n  agents token           Generate a bearer token for an agent (for the MCP HTTP host).\n  artifact find          Resolve a BC artifact URL.\n  container list         List BC containers (read-only).\n  container inspect      Show a container + its installed apps.\n  container new          Provision or warm-reuse a BC container (idempotent).\n  container restart      Restart a container.\n  container remove       Remove a container (destructive; shared -> approval).  [destructive]\n  deps plan              Dry-run dependency resolution for a project (read-only).\n  deps check             Reconcile manifest vs container vs feeds (read-only).\n  deps install           Resolve + install the dependency closure into a container.\n  app build              Compile the workspace fresh (fresh symbo\n[truncated]",
        "stderr": "Unknown command: help job get",
        "outputLimitExceeded": false
      },
      "parsed": true,
      "payload": "albuild - Business Central dev-loop CLI (an MCP client over the businessdev.ALbuild engine)\n\nUsage: albuild <command> [options]\n\nCommands:\n  doctor                 Check host readiness (PowerShell + module + Docker).\n  setup                  Guided setup: check host, module, albuild.json; print next steps.\n  update                 Update the PowerShell module + MCP server in one go (the CLI updates via 'dotnet tool update').\n  module install         Install the businessdev.ALbuild PowerShell module (PSGallery).\n  module update          Update the businessdev.ALbuild PowerShell module.\n  module status          Show the installed/used businessdev.ALbuild module.\n  init                   Create an albuild.json in the workspace.\n  feeds add              Add a package feed to albuild.json: feeds add <url> [--name --kind apps|symbols|runtime --id-scheme --api-key-env VAR].\n  feeds list             List the feeds in albuild.json (read-only).\n  feeds remove           Remove a feed from albuild.json by url or --name.  [destructive]\n  agents list            List configured MCP agents and their tools.\n  agents add             Add/replace an MCP agent: agents add <name> --tools a,b,c (or *).\n  agents remove          Remove an MCP agent.  [destructive]\n  agents token           Generate a bearer token for an agent (for the MCP HTTP host).\n  artifact find          Resolve a BC artifact URL.\n  container list         List BC containers (read-only).\n  container inspect      Show a container + its installed apps.\n  container new          Provision or warm-reuse a BC container (idempotent).\n  container restart      Restart a container.\n  container remove       Remove a container (destructive; shared -> approval).  [destructive]\n  deps plan              Dry-run dependency resolution for a project (read-only).\n  deps check             Reconcile manifest vs container vs feeds (read-only).\n  deps install           Resolve + install the dependency closure into a container.\n  app build              Compile the workspace fresh (fresh symbo\n[truncated]",
      "terminalNonHelp": false
    },
    {
      "command": "job cancel",
      "helpInvocation": "help job cancel",
      "result": {
        "status": "failed",
        "timedOut": false,
        "exitCode": 2,
        "durationMs": 49,
        "stdout": "albuild - Business Central dev-loop CLI (an MCP client over the businessdev.ALbuild engine)\n\nUsage: albuild <command> [options]\n\nCommands:\n  doctor                 Check host readiness (PowerShell + module + Docker).\n  setup                  Guided setup: check host, module, albuild.json; print next steps.\n  update                 Update the PowerShell module + MCP server in one go (the CLI updates via 'dotnet tool update').\n  module install         Install the businessdev.ALbuild PowerShell module (PSGallery).\n  module update          Update the businessdev.ALbuild PowerShell module.\n  module status          Show the installed/used businessdev.ALbuild module.\n  init                   Create an albuild.json in the workspace.\n  feeds add              Add a package feed to albuild.json: feeds add <url> [--name --kind apps|symbols|runtime --id-scheme --api-key-env VAR].\n  feeds list             List the feeds in albuild.json (read-only).\n  feeds remove           Remove a feed from albuild.json by url or --name.  [destructive]\n  agents list            List configured MCP agents and their tools.\n  agents add             Add/replace an MCP agent: agents add <name> --tools a,b,c (or *).\n  agents remove          Remove an MCP agent.  [destructive]\n  agents token           Generate a bearer token for an agent (for the MCP HTTP host).\n  artifact find          Resolve a BC artifact URL.\n  container list         List BC containers (read-only).\n  container inspect      Show a container + its installed apps.\n  container new          Provision or warm-reuse a BC container (idempotent).\n  container restart      Restart a container.\n  container remove       Remove a container (destructive; shared -> approval).  [destructive]\n  deps plan              Dry-run dependency resolution for a project (read-only).\n  deps check             Reconcile manifest vs container vs feeds (read-only).\n  deps install           Resolve + install the dependency closure into a container.\n  app build              Compile the workspace fresh (fresh symbo\n[truncated]",
        "stderr": "Unknown command: help job cancel",
        "outputLimitExceeded": false
      },
      "parsed": true,
      "payload": "albuild - Business Central dev-loop CLI (an MCP client over the businessdev.ALbuild engine)\n\nUsage: albuild <command> [options]\n\nCommands:\n  doctor                 Check host readiness (PowerShell + module + Docker).\n  setup                  Guided setup: check host, module, albuild.json; print next steps.\n  update                 Update the PowerShell module + MCP server in one go (the CLI updates via 'dotnet tool update').\n  module install         Install the businessdev.ALbuild PowerShell module (PSGallery).\n  module update          Update the businessdev.ALbuild PowerShell module.\n  module status          Show the installed/used businessdev.ALbuild module.\n  init                   Create an albuild.json in the workspace.\n  feeds add              Add a package feed to albuild.json: feeds add <url> [--name --kind apps|symbols|runtime --id-scheme --api-key-env VAR].\n  feeds list             List the feeds in albuild.json (read-only).\n  feeds remove           Remove a feed from albuild.json by url or --name.  [destructive]\n  agents list            List configured MCP agents and their tools.\n  agents add             Add/replace an MCP agent: agents add <name> --tools a,b,c (or *).\n  agents remove          Remove an MCP agent.  [destructive]\n  agents token           Generate a bearer token for an agent (for the MCP HTTP host).\n  artifact find          Resolve a BC artifact URL.\n  container list         List BC containers (read-only).\n  container inspect      Show a container + its installed apps.\n  container new          Provision or warm-reuse a BC container (idempotent).\n  container restart      Restart a container.\n  container remove       Remove a container (destructive; shared -> approval).  [destructive]\n  deps plan              Dry-run dependency resolution for a project (read-only).\n  deps check             Reconcile manifest vs container vs feeds (read-only).\n  deps install           Resolve + install the dependency closure into a container.\n  app build              Compile the workspace fresh (fresh symbo\n[truncated]",
      "terminalNonHelp": false
    },
    {
      "command": "run pipeline",
      "helpInvocation": "run pipeline /help",
      "result": {
        "status": "failed",
        "timedOut": false,
        "exitCode": 3,
        "durationMs": 3851,
        "stdout": null,
        "stderr": "Pipeline 1/3: building fresh, locally…\n→ build-app ...\n  Resolving artifact (Sandbox/w1//Latest)...\n  Build BC version: 28.3.52162.52870\n  Local dependency folder '/tmp/inspectra-help-busine-a7f8be14-87c075a5/.dependencies' not found; skipping.\n  AL Tool already available: /tmp/inspectra-help-busine-a7f8be14-87c075a5/dotnet-home/.dotnet/tools/al.\n  Build order: \n✓ build-app · 3s\n  Pipeline 2/3: publishing app(s)…\n  Pipeline 3/3: deploy + test…\n→ deploy-and-test ...\n✗ error [NO_CREDENTIALS]: No credentials for container '/help' to run its tests.\n  Pass --username/--password (or --password-env <VAR>), or create the container with 'albuild container new'.",
        "outputLimitExceeded": false
      },
      "parsed": false,
      "payload": "Pipeline 1/3: building fresh, locally…\n→ build-app ...\n  Resolving artifact (Sandbox/w1//Latest)...\n  Build BC version: 28.3.52162.52870\n  Local dependency folder '/tmp/inspectra-help-busine-a7f8be14-87c075a5/.dependencies' not found; skipping.\n  AL Tool already available: /tmp/inspectra-help-busine-a7f8be14-87c075a5/dotnet-home/.dotnet/tools/al.\n  Build order: \n✓ build-app · 3s\n  Pipeline 2/3: publishing app(s)…\n  Pipeline 3/3: deploy + test…\n→ deploy-and-test ...\n✗ error [NO_CREDENTIALS]: No credentials for container '/help' to run its tests.\n  Pass --username/--password (or --password-env <VAR>), or create the container with 'albuild container new'.",
      "terminalNonHelp": true
    },
    {
      "command": "demo progress",
      "helpInvocation": "help demo progress",
      "result": {
        "status": "failed",
        "timedOut": false,
        "exitCode": 2,
        "durationMs": 52,
        "stdout": "albuild - Business Central dev-loop CLI (an MCP client over the businessdev.ALbuild engine)\n\nUsage: albuild <command> [options]\n\nCommands:\n  doctor                 Check host readiness (PowerShell + module + Docker).\n  setup                  Guided setup: check host, module, albuild.json; print next steps.\n  update                 Update the PowerShell module + MCP server in one go (the CLI updates via 'dotnet tool update').\n  module install         Install the businessdev.ALbuild PowerShell module (PSGallery).\n  module update          Update the businessdev.ALbuild PowerShell module.\n  module status          Show the installed/used businessdev.ALbuild module.\n  init                   Create an albuild.json in the workspace.\n  feeds add              Add a package feed to albuild.json: feeds add <url> [--name --kind apps|symbols|runtime --id-scheme --api-key-env VAR].\n  feeds list             List the feeds in albuild.json (read-only).\n  feeds remove           Remove a feed from albuild.json by url or --name.  [destructive]\n  agents list            List configured MCP agents and their tools.\n  agents add             Add/replace an MCP agent: agents add <name> --tools a,b,c (or *).\n  agents remove          Remove an MCP agent.  [destructive]\n  agents token           Generate a bearer token for an agent (for the MCP HTTP host).\n  artifact find          Resolve a BC artifact URL.\n  container list         List BC containers (read-only).\n  container inspect      Show a container + its installed apps.\n  container new          Provision or warm-reuse a BC container (idempotent).\n  container restart      Restart a container.\n  container remove       Remove a container (destructive; shared -> approval).  [destructive]\n  deps plan              Dry-run dependency resolution for a project (read-only).\n  deps check             Reconcile manifest vs container vs feeds (read-only).\n  deps install           Resolve + install the dependency closure into a container.\n  app build              Compile the workspace fresh (fresh symbo\n[truncated]",
        "stderr": "Unknown command: help demo progress",
        "outputLimitExceeded": false
      },
      "parsed": true,
      "payload": "albuild - Business Central dev-loop CLI (an MCP client over the businessdev.ALbuild engine)\n\nUsage: albuild <command> [options]\n\nCommands:\n  doctor                 Check host readiness (PowerShell + module + Docker).\n  setup                  Guided setup: check host, module, albuild.json; print next steps.\n  update                 Update the PowerShell module + MCP server in one go (the CLI updates via 'dotnet tool update').\n  module install         Install the businessdev.ALbuild PowerShell module (PSGallery).\n  module update          Update the businessdev.ALbuild PowerShell module.\n  module status          Show the installed/used businessdev.ALbuild module.\n  init                   Create an albuild.json in the workspace.\n  feeds add              Add a package feed to albuild.json: feeds add <url> [--name --kind apps|symbols|runtime --id-scheme --api-key-env VAR].\n  feeds list             List the feeds in albuild.json (read-only).\n  feeds remove           Remove a feed from albuild.json by url or --name.  [destructive]\n  agents list            List configured MCP agents and their tools.\n  agents add             Add/replace an MCP agent: agents add <name> --tools a,b,c (or *).\n  agents remove          Remove an MCP agent.  [destructive]\n  agents token           Generate a bearer token for an agent (for the MCP HTTP host).\n  artifact find          Resolve a BC artifact URL.\n  container list         List BC containers (read-only).\n  container inspect      Show a container + its installed apps.\n  container new          Provision or warm-reuse a BC container (idempotent).\n  container restart      Restart a container.\n  container remove       Remove a container (destructive; shared -> approval).  [destructive]\n  deps plan              Dry-run dependency resolution for a project (read-only).\n  deps check             Reconcile manifest vs container vs feeds (read-only).\n  deps install           Resolve + install the dependency closure into a container.\n  app build              Compile the workspace fresh (fresh symbo\n[truncated]",
      "terminalNonHelp": false
    },
    {
      "command": "mcp install",
      "helpInvocation": "help mcp install",
      "result": {
        "status": "failed",
        "timedOut": false,
        "exitCode": 2,
        "durationMs": 48,
        "stdout": "albuild - Business Central dev-loop CLI (an MCP client over the businessdev.ALbuild engine)\n\nUsage: albuild <command> [options]\n\nCommands:\n  doctor                 Check host readiness (PowerShell + module + Docker).\n  setup                  Guided setup: check host, module, albuild.json; print next steps.\n  update                 Update the PowerShell module + MCP server in one go (the CLI updates via 'dotnet tool update').\n  module install         Install the businessdev.ALbuild PowerShell module (PSGallery).\n  module update          Update the businessdev.ALbuild PowerShell module.\n  module status          Show the installed/used businessdev.ALbuild module.\n  init                   Create an albuild.json in the workspace.\n  feeds add              Add a package feed to albuild.json: feeds add <url> [--name --kind apps|symbols|runtime --id-scheme --api-key-env VAR].\n  feeds list             List the feeds in albuild.json (read-only).\n  feeds remove           Remove a feed from albuild.json by url or --name.  [destructive]\n  agents list            List configured MCP agents and their tools.\n  agents add             Add/replace an MCP agent: agents add <name> --tools a,b,c (or *).\n  agents remove          Remove an MCP agent.  [destructive]\n  agents token           Generate a bearer token for an agent (for the MCP HTTP host).\n  artifact find          Resolve a BC artifact URL.\n  container list         List BC containers (read-only).\n  container inspect      Show a container + its installed apps.\n  container new          Provision or warm-reuse a BC container (idempotent).\n  container restart      Restart a container.\n  container remove       Remove a container (destructive; shared -> approval).  [destructive]\n  deps plan              Dry-run dependency resolution for a project (read-only).\n  deps check             Reconcile manifest vs container vs feeds (read-only).\n  deps install           Resolve + install the dependency closure into a container.\n  app build              Compile the workspace fresh (fresh symbo\n[truncated]",
        "stderr": "Unknown command: help mcp install",
        "outputLimitExceeded": false
      },
      "parsed": true,
      "payload": "albuild - Business Central dev-loop CLI (an MCP client over the businessdev.ALbuild engine)\n\nUsage: albuild <command> [options]\n\nCommands:\n  doctor                 Check host readiness (PowerShell + module + Docker).\n  setup                  Guided setup: check host, module, albuild.json; print next steps.\n  update                 Update the PowerShell module + MCP server in one go (the CLI updates via 'dotnet tool update').\n  module install         Install the businessdev.ALbuild PowerShell module (PSGallery).\n  module update          Update the businessdev.ALbuild PowerShell module.\n  module status          Show the installed/used businessdev.ALbuild module.\n  init                   Create an albuild.json in the workspace.\n  feeds add              Add a package feed to albuild.json: feeds add <url> [--name --kind apps|symbols|runtime --id-scheme --api-key-env VAR].\n  feeds list             List the feeds in albuild.json (read-only).\n  feeds remove           Remove a feed from albuild.json by url or --name.  [destructive]\n  agents list            List configured MCP agents and their tools.\n  agents add             Add/replace an MCP agent: agents add <name> --tools a,b,c (or *).\n  agents remove          Remove an MCP agent.  [destructive]\n  agents token           Generate a bearer token for an agent (for the MCP HTTP host).\n  artifact find          Resolve a BC artifact URL.\n  container list         List BC containers (read-only).\n  container inspect      Show a container + its installed apps.\n  container new          Provision or warm-reuse a BC container (idempotent).\n  container restart      Restart a container.\n  container remove       Remove a container (destructive; shared -> approval).  [destructive]\n  deps plan              Dry-run dependency resolution for a project (read-only).\n  deps check             Reconcile manifest vs container vs feeds (read-only).\n  deps install           Resolve + install the dependency closure into a container.\n  app build              Compile the workspace fresh (fresh symbo\n[truncated]",
      "terminalNonHelp": false
    },
    {
      "command": "mcp serve",
      "helpInvocation": "mcp serve --help",
      "result": {
        "status": "timed-out",
        "timedOut": true,
        "exitCode": null,
        "durationMs": 60097,
        "stdout": null,
        "stderr": "Starting MCP server (http://127.0.0.1:5020/mcp) — Ctrl+C to stop.\n[albuild-mcp] WARN Docker: Docker is in 'linux' container mode. BC images are Windows-only.\n[albuild-mcp]   -> Switch Docker to Windows containers (or point the MCP at a remote Windows Docker host).\n[albuild-mcp] HTTP transport ready at http://127.0.0.1:5020/mcp (1 agent token(s) configured).",
        "outputLimitExceeded": false
      },
      "parsed": false,
      "payload": "Starting MCP server (http://127.0.0.1:5020/mcp) — Ctrl+C to stop.\n[albuild-mcp] WARN Docker: Docker is in 'linux' container mode. BC images are Windows-only.\n[albuild-mcp]   -> Switch Docker to Windows containers (or point the MCP at a remote Windows Docker host).\n[albuild-mcp] HTTP transport ready at http://127.0.0.1:5020/mcp (1 agent token(s) configured).",
      "terminalNonHelp": false
    },
    {
      "command": "mcp service",
      "helpInvocation": "mcp service --help",
      "result": {
        "status": "failed",
        "timedOut": false,
        "exitCode": 2,
        "durationMs": 52,
        "stdout": null,
        "stderr": "✗ error [USAGE]: Usage: albuild mcp service install|uninstall [--name <svc>]\n  Wraps the MCP HTTP server as a Windows service via NSSM.",
        "outputLimitExceeded": false
      },
      "parsed": true,
      "payload": "✗ error [USAGE]: Usage: albuild mcp service install|uninstall [--name <svc>]\n  Wraps the MCP HTTP server as a Windows service via NSSM.",
      "terminalNonHelp": false
    },
    {
      "command": "mcp logs",
      "helpInvocation": "help mcp logs",
      "result": {
        "status": "failed",
        "timedOut": false,
        "exitCode": 2,
        "durationMs": 54,
        "stdout": "albuild - Business Central dev-loop CLI (an MCP client over the businessdev.ALbuild engine)\n\nUsage: albuild <command> [options]\n\nCommands:\n  doctor                 Check host readiness (PowerShell + module + Docker).\n  setup                  Guided setup: check host, module, albuild.json; print next steps.\n  update                 Update the PowerShell module + MCP server in one go (the CLI updates via 'dotnet tool update').\n  module install         Install the businessdev.ALbuild PowerShell module (PSGallery).\n  module update          Update the businessdev.ALbuild PowerShell module.\n  module status          Show the installed/used businessdev.ALbuild module.\n  init                   Create an albuild.json in the workspace.\n  feeds add              Add a package feed to albuild.json: feeds add <url> [--name --kind apps|symbols|runtime --id-scheme --api-key-env VAR].\n  feeds list             List the feeds in albuild.json (read-only).\n  feeds remove           Remove a feed from albuild.json by url or --name.  [destructive]\n  agents list            List configured MCP agents and their tools.\n  agents add             Add/replace an MCP agent: agents add <name> --tools a,b,c (or *).\n  agents remove          Remove an MCP agent.  [destructive]\n  agents token           Generate a bearer token for an agent (for the MCP HTTP host).\n  artifact find          Resolve a BC artifact URL.\n  container list         List BC containers (read-only).\n  container inspect      Show a container + its installed apps.\n  container new          Provision or warm-reuse a BC container (idempotent).\n  container restart      Restart a container.\n  container remove       Remove a container (destructive; shared -> approval).  [destructive]\n  deps plan              Dry-run dependency resolution for a project (read-only).\n  deps check             Reconcile manifest vs container vs feeds (read-only).\n  deps install           Resolve + install the dependency closure into a container.\n  app build              Compile the workspace fresh (fresh symbo\n[truncated]",
        "stderr": "Unknown command: help mcp logs",
        "outputLimitExceeded": false
      },
      "parsed": true,
      "payload": "albuild - Business Central dev-loop CLI (an MCP client over the businessdev.ALbuild engine)\n\nUsage: albuild <command> [options]\n\nCommands:\n  doctor                 Check host readiness (PowerShell + module + Docker).\n  setup                  Guided setup: check host, module, albuild.json; print next steps.\n  update                 Update the PowerShell module + MCP server in one go (the CLI updates via 'dotnet tool update').\n  module install         Install the businessdev.ALbuild PowerShell module (PSGallery).\n  module update          Update the businessdev.ALbuild PowerShell module.\n  module status          Show the installed/used businessdev.ALbuild module.\n  init                   Create an albuild.json in the workspace.\n  feeds add              Add a package feed to albuild.json: feeds add <url> [--name --kind apps|symbols|runtime --id-scheme --api-key-env VAR].\n  feeds list             List the feeds in albuild.json (read-only).\n  feeds remove           Remove a feed from albuild.json by url or --name.  [destructive]\n  agents list            List configured MCP agents and their tools.\n  agents add             Add/replace an MCP agent: agents add <name> --tools a,b,c (or *).\n  agents remove          Remove an MCP agent.  [destructive]\n  agents token           Generate a bearer token for an agent (for the MCP HTTP host).\n  artifact find          Resolve a BC artifact URL.\n  container list         List BC containers (read-only).\n  container inspect      Show a container + its installed apps.\n  container new          Provision or warm-reuse a BC container (idempotent).\n  container restart      Restart a container.\n  container remove       Remove a container (destructive; shared -> approval).  [destructive]\n  deps plan              Dry-run dependency resolution for a project (read-only).\n  deps check             Reconcile manifest vs container vs feeds (read-only).\n  deps install           Resolve + install the dependency closure into a container.\n  app build              Compile the workspace fresh (fresh symbo\n[truncated]",
      "terminalNonHelp": false
    },
    {
      "command": "mcp status",
      "helpInvocation": "help mcp status",
      "result": {
        "status": "failed",
        "timedOut": false,
        "exitCode": 2,
        "durationMs": 49,
        "stdout": "albuild - Business Central dev-loop CLI (an MCP client over the businessdev.ALbuild engine)\n\nUsage: albuild <command> [options]\n\nCommands:\n  doctor                 Check host readiness (PowerShell + module + Docker).\n  setup                  Guided setup: check host, module, albuild.json; print next steps.\n  update                 Update the PowerShell module + MCP server in one go (the CLI updates via 'dotnet tool update').\n  module install         Install the businessdev.ALbuild PowerShell module (PSGallery).\n  module update          Update the businessdev.ALbuild PowerShell module.\n  module status          Show the installed/used businessdev.ALbuild module.\n  init                   Create an albuild.json in the workspace.\n  feeds add              Add a package feed to albuild.json: feeds add <url> [--name --kind apps|symbols|runtime --id-scheme --api-key-env VAR].\n  feeds list             List the feeds in albuild.json (read-only).\n  feeds remove           Remove a feed from albuild.json by url or --name.  [destructive]\n  agents list            List configured MCP agents and their tools.\n  agents add             Add/replace an MCP agent: agents add <name> --tools a,b,c (or *).\n  agents remove          Remove an MCP agent.  [destructive]\n  agents token           Generate a bearer token for an agent (for the MCP HTTP host).\n  artifact find          Resolve a BC artifact URL.\n  container list         List BC containers (read-only).\n  container inspect      Show a container + its installed apps.\n  container new          Provision or warm-reuse a BC container (idempotent).\n  container restart      Restart a container.\n  container remove       Remove a container (destructive; shared -> approval).  [destructive]\n  deps plan              Dry-run dependency resolution for a project (read-only).\n  deps check             Reconcile manifest vs container vs feeds (read-only).\n  deps install           Resolve + install the dependency closure into a container.\n  app build              Compile the workspace fresh (fresh symbo\n[truncated]",
        "stderr": "Unknown command: help mcp status",
        "outputLimitExceeded": false
      },
      "parsed": true,
      "payload": "albuild - Business Central dev-loop CLI (an MCP client over the businessdev.ALbuild engine)\n\nUsage: albuild <command> [options]\n\nCommands:\n  doctor                 Check host readiness (PowerShell + module + Docker).\n  setup                  Guided setup: check host, module, albuild.json; print next steps.\n  update                 Update the PowerShell module + MCP server in one go (the CLI updates via 'dotnet tool update').\n  module install         Install the businessdev.ALbuild PowerShell module (PSGallery).\n  module update          Update the businessdev.ALbuild PowerShell module.\n  module status          Show the installed/used businessdev.ALbuild module.\n  init                   Create an albuild.json in the workspace.\n  feeds add              Add a package feed to albuild.json: feeds add <url> [--name --kind apps|symbols|runtime --id-scheme --api-key-env VAR].\n  feeds list             List the feeds in albuild.json (read-only).\n  feeds remove           Remove a feed from albuild.json by url or --name.  [destructive]\n  agents list            List configured MCP agents and their tools.\n  agents add             Add/replace an MCP agent: agents add <name> --tools a,b,c (or *).\n  agents remove          Remove an MCP agent.  [destructive]\n  agents token           Generate a bearer token for an agent (for the MCP HTTP host).\n  artifact find          Resolve a BC artifact URL.\n  container list         List BC containers (read-only).\n  container inspect      Show a container + its installed apps.\n  container new          Provision or warm-reuse a BC container (idempotent).\n  container restart      Restart a container.\n  container remove       Remove a container (destructive; shared -> approval).  [destructive]\n  deps plan              Dry-run dependency resolution for a project (read-only).\n  deps check             Reconcile manifest vs container vs feeds (read-only).\n  deps install           Resolve + install the dependency closure into a container.\n  app build              Compile the workspace fresh (fresh symbo\n[truncated]",
      "terminalNonHelp": false
    },
    {
      "command": "mcp doctor",
      "helpInvocation": "help mcp doctor",
      "result": {
        "status": "failed",
        "timedOut": false,
        "exitCode": 2,
        "durationMs": 50,
        "stdout": "albuild - Business Central dev-loop CLI (an MCP client over the businessdev.ALbuild engine)\n\nUsage: albuild <command> [options]\n\nCommands:\n  doctor                 Check host readiness (PowerShell + module + Docker).\n  setup                  Guided setup: check host, module, albuild.json; print next steps.\n  update                 Update the PowerShell module + MCP server in one go (the CLI updates via 'dotnet tool update').\n  module install         Install the businessdev.ALbuild PowerShell module (PSGallery).\n  module update          Update the businessdev.ALbuild PowerShell module.\n  module status          Show the installed/used businessdev.ALbuild module.\n  init                   Create an albuild.json in the workspace.\n  feeds add              Add a package feed to albuild.json: feeds add <url> [--name --kind apps|symbols|runtime --id-scheme --api-key-env VAR].\n  feeds list             List the feeds in albuild.json (read-only).\n  feeds remove           Remove a feed from albuild.json by url or --name.  [destructive]\n  agents list            List configured MCP agents and their tools.\n  agents add             Add/replace an MCP agent: agents add <name> --tools a,b,c (or *).\n  agents remove          Remove an MCP agent.  [destructive]\n  agents token           Generate a bearer token for an agent (for the MCP HTTP host).\n  artifact find          Resolve a BC artifact URL.\n  container list         List BC containers (read-only).\n  container inspect      Show a container + its installed apps.\n  container new          Provision or warm-reuse a BC container (idempotent).\n  container restart      Restart a container.\n  container remove       Remove a container (destructive; shared -> approval).  [destructive]\n  deps plan              Dry-run dependency resolution for a project (read-only).\n  deps check             Reconcile manifest vs container vs feeds (read-only).\n  deps install           Resolve + install the dependency closure into a container.\n  app build              Compile the workspace fresh (fresh symbo\n[truncated]",
        "stderr": "Unknown command: help mcp doctor",
        "outputLimitExceeded": false
      },
      "parsed": false,
      "payload": "albuild - Business Central dev-loop CLI (an MCP client over the businessdev.ALbuild engine)\n\nUsage: albuild <command> [options]\n\nCommands:\n  doctor                 Check host readiness (PowerShell + module + Docker).\n  setup                  Guided setup: check host, module, albuild.json; print next steps.\n  update                 Update the PowerShell module + MCP server in one go (the CLI updates via 'dotnet tool update').\n  module install         Install the businessdev.ALbuild PowerShell module (PSGallery).\n  module update          Update the businessdev.ALbuild PowerShell module.\n  module status          Show the installed/used businessdev.ALbuild module.\n  init                   Create an albuild.json in the workspace.\n  feeds add              Add a package feed to albuild.json: feeds add <url> [--name --kind apps|symbols|runtime --id-scheme --api-key-env VAR].\n  feeds list             List the feeds in albuild.json (read-only).\n  feeds remove           Remove a feed from albuild.json by url or --name.  [destructive]\n  agents list            List configured MCP agents and their tools.\n  agents add             Add/replace an MCP agent: agents add <name> --tools a,b,c (or *).\n  agents remove          Remove an MCP agent.  [destructive]\n  agents token           Generate a bearer token for an agent (for the MCP HTTP host).\n  artifact find          Resolve a BC artifact URL.\n  container list         List BC containers (read-only).\n  container inspect      Show a container + its installed apps.\n  container new          Provision or warm-reuse a BC container (idempotent).\n  container restart      Restart a container.\n  container remove       Remove a container (destructive; shared -> approval).  [destructive]\n  deps plan              Dry-run dependency resolution for a project (read-only).\n  deps check             Reconcile manifest vs container vs feeds (read-only).\n  deps install           Resolve + install the dependency closure into a container.\n  app build              Compile the workspace fresh (fresh symbo\n[truncated]",
      "terminalNonHelp": false
    },
    {
      "command": "mcp list-tools",
      "helpInvocation": "help mcp list-tools",
      "result": {
        "status": "failed",
        "timedOut": false,
        "exitCode": 2,
        "durationMs": 62,
        "stdout": "albuild - Business Central dev-loop CLI (an MCP client over the businessdev.ALbuild engine)\n\nUsage: albuild <command> [options]\n\nCommands:\n  doctor                 Check host readiness (PowerShell + module + Docker).\n  setup                  Guided setup: check host, module, albuild.json; print next steps.\n  update                 Update the PowerShell module + MCP server in one go (the CLI updates via 'dotnet tool update').\n  module install         Install the businessdev.ALbuild PowerShell module (PSGallery).\n  module update          Update the businessdev.ALbuild PowerShell module.\n  module status          Show the installed/used businessdev.ALbuild module.\n  init                   Create an albuild.json in the workspace.\n  feeds add              Add a package feed to albuild.json: feeds add <url> [--name --kind apps|symbols|runtime --id-scheme --api-key-env VAR].\n  feeds list             List the feeds in albuild.json (read-only).\n  feeds remove           Remove a feed from albuild.json by url or --name.  [destructive]\n  agents list            List configured MCP agents and their tools.\n  agents add             Add/replace an MCP agent: agents add <name> --tools a,b,c (or *).\n  agents remove          Remove an MCP agent.  [destructive]\n  agents token           Generate a bearer token for an agent (for the MCP HTTP host).\n  artifact find          Resolve a BC artifact URL.\n  container list         List BC containers (read-only).\n  container inspect      Show a container + its installed apps.\n  container new          Provision or warm-reuse a BC container (idempotent).\n  container restart      Restart a container.\n  container remove       Remove a container (destructive; shared -> approval).  [destructive]\n  deps plan              Dry-run dependency resolution for a project (read-only).\n  deps check             Reconcile manifest vs container vs feeds (read-only).\n  deps install           Resolve + install the dependency closure into a container.\n  app build              Compile the workspace fresh (fresh symbo\n[truncated]",
        "stderr": "Unknown command: help mcp list-tools",
        "outputLimitExceeded": false
      },
      "parsed": true,
      "payload": "albuild - Business Central dev-loop CLI (an MCP client over the businessdev.ALbuild engine)\n\nUsage: albuild <command> [options]\n\nCommands:\n  doctor                 Check host readiness (PowerShell + module + Docker).\n  setup                  Guided setup: check host, module, albuild.json; print next steps.\n  update                 Update the PowerShell module + MCP server in one go (the CLI updates via 'dotnet tool update').\n  module install         Install the businessdev.ALbuild PowerShell module (PSGallery).\n  module update          Update the businessdev.ALbuild PowerShell module.\n  module status          Show the installed/used businessdev.ALbuild module.\n  init                   Create an albuild.json in the workspace.\n  feeds add              Add a package feed to albuild.json: feeds add <url> [--name --kind apps|symbols|runtime --id-scheme --api-key-env VAR].\n  feeds list             List the feeds in albuild.json (read-only).\n  feeds remove           Remove a feed from albuild.json by url or --name.  [destructive]\n  agents list            List configured MCP agents and their tools.\n  agents add             Add/replace an MCP agent: agents add <name> --tools a,b,c (or *).\n  agents remove          Remove an MCP agent.  [destructive]\n  agents token           Generate a bearer token for an agent (for the MCP HTTP host).\n  artifact find          Resolve a BC artifact URL.\n  container list         List BC containers (read-only).\n  container inspect      Show a container + its installed apps.\n  container new          Provision or warm-reuse a BC container (idempotent).\n  container restart      Restart a container.\n  container remove       Remove a container (destructive; shared -> approval).  [destructive]\n  deps plan              Dry-run dependency resolution for a project (read-only).\n  deps check             Reconcile manifest vs container vs feeds (read-only).\n  deps install           Resolve + install the dependency closure into a container.\n  app build              Compile the workspace fresh (fresh symbo\n[truncated]",
      "terminalNonHelp": false
    },
    {
      "command": "mcp call",
      "helpInvocation": "help mcp call",
      "result": {
        "status": "failed",
        "timedOut": false,
        "exitCode": 2,
        "durationMs": 48,
        "stdout": "albuild - Business Central dev-loop CLI (an MCP client over the businessdev.ALbuild engine)\n\nUsage: albuild <command> [options]\n\nCommands:\n  doctor                 Check host readiness (PowerShell + module + Docker).\n  setup                  Guided setup: check host, module, albuild.json; print next steps.\n  update                 Update the PowerShell module + MCP server in one go (the CLI updates via 'dotnet tool update').\n  module install         Install the businessdev.ALbuild PowerShell module (PSGallery).\n  module update          Update the businessdev.ALbuild PowerShell module.\n  module status          Show the installed/used businessdev.ALbuild module.\n  init                   Create an albuild.json in the workspace.\n  feeds add              Add a package feed to albuild.json: feeds add <url> [--name --kind apps|symbols|runtime --id-scheme --api-key-env VAR].\n  feeds list             List the feeds in albuild.json (read-only).\n  feeds remove           Remove a feed from albuild.json by url or --name.  [destructive]\n  agents list            List configured MCP agents and their tools.\n  agents add             Add/replace an MCP agent: agents add <name> --tools a,b,c (or *).\n  agents remove          Remove an MCP agent.  [destructive]\n  agents token           Generate a bearer token for an agent (for the MCP HTTP host).\n  artifact find          Resolve a BC artifact URL.\n  container list         List BC containers (read-only).\n  container inspect      Show a container + its installed apps.\n  container new          Provision or warm-reuse a BC container (idempotent).\n  container restart      Restart a container.\n  container remove       Remove a container (destructive; shared -> approval).  [destructive]\n  deps plan              Dry-run dependency resolution for a project (read-only).\n  deps check             Reconcile manifest vs container vs feeds (read-only).\n  deps install           Resolve + install the dependency closure into a container.\n  app build              Compile the workspace fresh (fresh symbo\n[truncated]",
        "stderr": "Unknown command: help mcp call",
        "outputLimitExceeded": false
      },
      "parsed": true,
      "payload": "albuild - Business Central dev-loop CLI (an MCP client over the businessdev.ALbuild engine)\n\nUsage: albuild <command> [options]\n\nCommands:\n  doctor                 Check host readiness (PowerShell + module + Docker).\n  setup                  Guided setup: check host, module, albuild.json; print next steps.\n  update                 Update the PowerShell module + MCP server in one go (the CLI updates via 'dotnet tool update').\n  module install         Install the businessdev.ALbuild PowerShell module (PSGallery).\n  module update          Update the businessdev.ALbuild PowerShell module.\n  module status          Show the installed/used businessdev.ALbuild module.\n  init                   Create an albuild.json in the workspace.\n  feeds add              Add a package feed to albuild.json: feeds add <url> [--name --kind apps|symbols|runtime --id-scheme --api-key-env VAR].\n  feeds list             List the feeds in albuild.json (read-only).\n  feeds remove           Remove a feed from albuild.json by url or --name.  [destructive]\n  agents list            List configured MCP agents and their tools.\n  agents add             Add/replace an MCP agent: agents add <name> --tools a,b,c (or *).\n  agents remove          Remove an MCP agent.  [destructive]\n  agents token           Generate a bearer token for an agent (for the MCP HTTP host).\n  artifact find          Resolve a BC artifact URL.\n  container list         List BC containers (read-only).\n  container inspect      Show a container + its installed apps.\n  container new          Provision or warm-reuse a BC container (idempotent).\n  container restart      Restart a container.\n  container remove       Remove a container (destructive; shared -> approval).  [destructive]\n  deps plan              Dry-run dependency resolution for a project (read-only).\n  deps check             Reconcile manifest vs container vs feeds (read-only).\n  deps install           Resolve + install the dependency closure into a container.\n  app build              Compile the workspace fresh (fresh symbo\n[truncated]",
      "terminalNonHelp": false
    },
    {
      "command": "mcp configure",
      "helpInvocation": "mcp configure --help",
      "result": {
        "status": "ok",
        "timedOut": false,
        "exitCode": 0,
        "durationMs": 61,
        "stdout": "client: claude-code\nwritten: /tmp/inspectra-help-busine-a7f8be14-87c075a5/.mcp.json\ndryRun: false\nprofileSaved: \nmcpJson:\n  mcpServers:\n    albuild:\n      command: npx\n      args:\n- -y\n- @365businessdev/albuild-mcp\n      env:\n        ALBUILD_MCP_AGENT: claude-code",
        "stderr": null,
        "outputLimitExceeded": false
      },
      "parsed": false,
      "payload": "client: claude-code\nwritten: /tmp/inspectra-help-busine-a7f8be14-87c075a5/.mcp.json\ndryRun: false\nprofileSaved: \nmcpJson:\n  mcpServers:\n    albuild:\n      command: npx\n      args:\n- -y\n- @365businessdev/albuild-mcp\n      env:\n        ALBUILD_MCP_AGENT: claude-code",
      "terminalNonHelp": true
    },
    {
      "command": "mcp default",
      "helpInvocation": "mcp default --help",
      "result": {
        "status": "ok",
        "timedOut": false,
        "exitCode": 0,
        "durationMs": 57,
        "stdout": "defaultServer: \nenvOverride: \nconfigPath: /tmp/inspectra-help-busine-a7f8be14-87c075a5/home/.albuild/config.json\nservers:\n(none)",
        "stderr": "! No default set — commands run local unless you pass --server. Set one with 'albuild mcp default <name>'.",
        "outputLimitExceeded": false
      },
      "parsed": false,
      "payload": "defaultServer: \nenvOverride: \nconfigPath: /tmp/inspectra-help-busine-a7f8be14-87c075a5/home/.albuild/config.json\nservers:\n(none)",
      "terminalNonHelp": true
    }
  ]
}
