{
  "opencli": "0.1-draft",
  "info": {
    "title": "fsgg-coord-engine — the typed coordination engine (ADR-0034), and the client it becomes (ADR-0040).",
    "version": "0.85.1",
    "description": "FS-GG typed coordination engine — the schedulability model behind `fsgg-coord` as one total function over a board-state snapshot, plus the typed IO layer that drives it. It IS the coordination tool (ADR-0034/ADR-0040): the `fsgg-coord` shim resolves and execs it; there is no bash left to shadow."
  },
  "x-inspectra": {
    "artifactSource": "crawled-from-help",
    "generator": "InSpectra.Gen",
    "helpDocumentCount": 1,
    "cliParsedDescription": "`scripts/fsgg-coord` is the client you run today; this is the engine it shells out to, and — at the\nPhase D shim — the engine it BECOMES. The DECISION commands read state on stdin and touch nothing. The\nCLIENT commands read and write GitHub through the typed IO layer."
  },
  "commands": [],
  "options": [
    {
      "name": "--remove",
      "recursive": false,
      "hidden": false,
      "description": "preserve every other live edge and fail stale on change",
      "aliases": [
        "--add"
      ],
      "arguments": [
        {
          "name": "REFS",
          "required": true,
          "arity": {
            "minimum": 1,
            "maximum": 1
          }
        }
      ]
    },
    {
      "name": "--replace",
      "recursive": false,
      "hidden": false,
      "description": "explicitly replace the set or clear it\nset-field --batch <ref> Field=Value ...    write N fields in ONE aliased mutation (#448)",
      "aliases": [
        "--clear"
      ],
      "arguments": [
        {
          "name": "REFS",
          "required": true,
          "arity": {
            "minimum": 1,
            "maximum": 1
          }
        }
      ]
    },
    {
      "name": "--json",
      "recursive": false,
      "hidden": false,
      "description": "one object, rather than prose over two streams (#1517)\noverlap <ref> --active | <a> <b>           does an item's touch-set collide? (repo-scoped, #353)"
    }
  ]
}
