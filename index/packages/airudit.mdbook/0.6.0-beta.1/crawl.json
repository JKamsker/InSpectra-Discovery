{
  "commands": [
    {
      "command": null,
      "helpInvocation": "--help",
      "result": {
        "status": "ok",
        "timedOut": false,
        "exitCode": 0,
        "durationMs": 60,
        "stdout": "Airudit.MdBook – Usage\n\nThis command will generate HTML files for each specified markdown file. \n\ncommand usage: \n    mdbook {file path}+ [options]\n\nOutput modes (default: write X.md.html next to each source):\n    --Export <dir>        Copy the generated pages into <dir> (mirrored paths)\n    --Single-File <file>  Combine every page into one self-contained HTML file\n    --Side                Also write the in-place files. They are on by default but\n                          suppressed once --Export or --Single-File is given; pass\n                          --Side to keep writing them as well.\n\nSingle-file options: \n    --ByLang              With --Single-File, write one combined file per detected\n                          language. Put \"{lang}\" in the file path (e.g. book.{lang}.html);\n                          otherwise \".{lang}\" is inserted before the extension.\n\nRendering: \n    --Template <file>     HTML template file path, or a builtin: name (see below)\n    --Copyright <str>     Copyright notice, exposed to the template as {{{Copyright}}}\n    --Embed               Inline local images as data: URIs so the output is self-\n                          contained. On automatically with --Single-File.\n    --No-Embed            Keep images as external references even with --Single-File.\n    --No-Highlight        Do not syntax-highlight fenced code blocks (on by default).\n\nDiagrams (mermaid, plantuml, …; pre-rendered to inline SVG): \n    --Diagrams <none|docker|kroki>\n                          How to render diagram fences. Default none: they stay plain\n                          and a setup hint is shown.\n                          docker: one-shot local images (mermaid, plantuml) — offline.\n                          kroki:  a Kroki server (needs --KrokiUrl) — all diagram types.\n    --KrokiUrl <url>      Base URL of the Kroki server, e.g. https://kroki.io/ (a public\n                          server uploads your diagram source — see the diagrams help page).\n\nOutput & info: \n    --Verbose, -v         Print a per-page trace wh\n[truncated]",
        "stderr": null,
        "outputLimitExceeded": false
      },
      "parsed": true,
      "payload": "Airudit.MdBook – Usage\n\nThis command will generate HTML files for each specified markdown file. \n\ncommand usage: \n    mdbook {file path}+ [options]\n\nOutput modes (default: write X.md.html next to each source):\n    --Export <dir>        Copy the generated pages into <dir> (mirrored paths)\n    --Single-File <file>  Combine every page into one self-contained HTML file\n    --Side                Also write the in-place files. They are on by default but\n                          suppressed once --Export or --Single-File is given; pass\n                          --Side to keep writing them as well.\n\nSingle-file options: \n    --ByLang              With --Single-File, write one combined file per detected\n                          language. Put \"{lang}\" in the file path (e.g. book.{lang}.html);\n                          otherwise \".{lang}\" is inserted before the extension.\n\nRendering: \n    --Template <file>     HTML template file path, or a builtin: name (see below)\n    --Copyright <str>     Copyright notice, exposed to the template as {{{Copyright}}}\n    --Embed               Inline local images as data: URIs so the output is self-\n                          contained. On automatically with --Single-File.\n    --No-Embed            Keep images as external references even with --Single-File.\n    --No-Highlight        Do not syntax-highlight fenced code blocks (on by default).\n\nDiagrams (mermaid, plantuml, …; pre-rendered to inline SVG): \n    --Diagrams <none|docker|kroki>\n                          How to render diagram fences. Default none: they stay plain\n                          and a setup hint is shown.\n                          docker: one-shot local images (mermaid, plantuml) — offline.\n                          kroki:  a Kroki server (needs --KrokiUrl) — all diagram types.\n    --KrokiUrl <url>      Base URL of the Kroki server, e.g. https://kroki.io/ (a public\n                          server uploads your diagram source — see the diagrams help page).\n\nOutput & info: \n    --Verbose, -v         Print a per-page trace wh\n[truncated]",
      "terminalNonHelp": false
    }
  ]
}
