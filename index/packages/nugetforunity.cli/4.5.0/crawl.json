{
  "documentCount": 1,
  "commandCount": 1,
  "captureCount": 1,
  "commands": [
    {
      "command": null,
      "helpInvocation": "--help",
      "result": {
        "status": "failed",
        "timedOut": false,
        "exitCode": 1,
        "durationMs": 547,
        "stdout": "Description:\n    A .NET Core Global Tool (Command-Line-Tool) that allows restoring NuGet packages managed by NuGetForUnity before starting Unity e.g. before building a Unity project on a build server. The tool only restores the NuGet packages, they are managed by the NuGetForUnity Unity Editor plug-in, so you still need to install the NuGetForUnity tool in Unity to install new packages and to ensure that e.g. the import settings of the DLL's are changed to comply with Unity.\n\nUsage:\n    nugetforunity restore <PROJECT_PATH> [options]\n\nArguments:\n    <PROJECT_PATH>  The path to the Unity project, should be the root path where e.g. the 'ProjectSettings' folder is located. If not specified, the command will use the current directory. [default: /tmp/inspectra-help-nugetforunity.cli-4.5.0-5bdbc9ca47634371a023affa1cd9a935]\n\nOptions:\n    -?, -h, --help  Show command line help.",
        "stderr": null
      },
      "parsed": true,
      "payload": "Description:\n    A .NET Core Global Tool (Command-Line-Tool) that allows restoring NuGet packages managed by NuGetForUnity before starting Unity e.g. before building a Unity project on a build server. The tool only restores the NuGet packages, they are managed by the NuGetForUnity Unity Editor plug-in, so you still need to install the NuGetForUnity tool in Unity to install new packages and to ensure that e.g. the import settings of the DLL's are changed to comply with Unity.\n\nUsage:\n    nugetforunity restore <PROJECT_PATH> [options]\n\nArguments:\n    <PROJECT_PATH>  The path to the Unity project, should be the root path where e.g. the 'ProjectSettings' folder is located. If not specified, the command will use the current directory. [default: /tmp/inspectra-help-nugetforunity.cli-4.5.0-5bdbc9ca47634371a023affa1cd9a935]\n\nOptions:\n    -?, -h, --help  Show command line help."
    }
  ]
}
