{
  "commands": [
    {
      "command": null,
      "helpInvocation": "--help",
      "result": {
        "status": "ok",
        "timedOut": false,
        "exitCode": 0,
        "durationMs": 21,
        "stdout": "edifact — read and validate UN/EDIFACT against the published directories\n\nCommands\n  ingest <path-to-distribution>   Read a UNECE release directory into a compiled pack.\n    --out <directory>             Where packs are written. Default: as resolved below.\n    --syntax <directory>          Where the ISO 9735 definitions live.\n    --no-strict                   Write a pack even if something went unaccounted for.\n    --include-prose               Keep the directory's descriptive text. Writes to\n                                  <pack-id>-prose, which .gitignore keeps out of the repo.\n\n  inspect <pack-id>               Print a compiled pack's manifest.\n    --packs <directory>           Where packs live.\n\n  validate <path-to-interchange>  Read an interchange and report what is wrong with it.\n    --pack <pack-id>              Which release to validate against. Default: whichever\n                                  one UNH says the interchange was written against.\n    --packs <directory>           Where packs live.\n    --syntax <directory>          Where the ISO 9735 definitions live.\n    --guide <path>                An implementation guide to layer over the release.\n                                  Repeatable; the order is the precedence order.\n    --tree                        Print each message's structure as well.\n    --quiet                       Report only errors, not warnings.\n    --round-trip                  Also write the interchange back out and report whether\n                                  it is reproduced character for character.\n\n  project <path-to-interchange>   Write each message of an interchange as a document.\n  project --message <key>         Write a message type and everything it names.\n    --pack <pack-id>              Which release. Worked out from UNH for a file;\n                                  required for --message.\n    --packs <directory>           Where packs live.\n    --syntax <directory>          Where the ISO 9735 definitions live.\n    --guide <path>                An implementation \n[truncated]",
        "stderr": null,
        "outputLimitExceeded": false
      },
      "parsed": true,
      "payload": "edifact — read and validate UN/EDIFACT against the published directories\n\nCommands\n  ingest <path-to-distribution>   Read a UNECE release directory into a compiled pack.\n    --out <directory>             Where packs are written. Default: as resolved below.\n    --syntax <directory>          Where the ISO 9735 definitions live.\n    --no-strict                   Write a pack even if something went unaccounted for.\n    --include-prose               Keep the directory's descriptive text. Writes to\n                                  <pack-id>-prose, which .gitignore keeps out of the repo.\n\n  inspect <pack-id>               Print a compiled pack's manifest.\n    --packs <directory>           Where packs live.\n\n  validate <path-to-interchange>  Read an interchange and report what is wrong with it.\n    --pack <pack-id>              Which release to validate against. Default: whichever\n                                  one UNH says the interchange was written against.\n    --packs <directory>           Where packs live.\n    --syntax <directory>          Where the ISO 9735 definitions live.\n    --guide <path>                An implementation guide to layer over the release.\n                                  Repeatable; the order is the precedence order.\n    --tree                        Print each message's structure as well.\n    --quiet                       Report only errors, not warnings.\n    --round-trip                  Also write the interchange back out and report whether\n                                  it is reproduced character for character.\n\n  project <path-to-interchange>   Write each message of an interchange as a document.\n  project --message <key>         Write a message type and everything it names.\n    --pack <pack-id>              Which release. Worked out from UNH for a file;\n                                  required for --message.\n    --packs <directory>           Where packs live.\n    --syntax <directory>          Where the ISO 9735 definitions live.\n    --guide <path>                An implementation \n[truncated]",
      "terminalNonHelp": false
    },
    {
      "command": "ingest",
      "helpInvocation": "ingest -h",
      "result": {
        "status": "failed",
        "timedOut": false,
        "exitCode": 2,
        "durationMs": 35,
        "stdout": "No UN/EDIFACT distribution at /tmp/inspectra-help-edifac-118681ac-a8337543/-h.",
        "stderr": null,
        "outputLimitExceeded": false
      },
      "parsed": false,
      "payload": "No UN/EDIFACT distribution at /tmp/inspectra-help-edifac-118681ac-a8337543/-h.",
      "terminalNonHelp": true
    },
    {
      "command": "inspect",
      "helpInvocation": "inspect -h",
      "result": {
        "status": "failed",
        "timedOut": false,
        "exitCode": 4,
        "durationMs": 31,
        "stdout": "Nothing at packs/compiled to read. Looked, in order:\n  --packs                          not given\n  EDIFACT_PACKS                    not set\n  beside Edifact.slnx or .edifact  no marker at or above /tmp/inspectra-help-edifac-118681ac-a8337543\n  per-user                         /tmp/inspectra-help-edifac-118681ac-a8337543/xdg-data/edifact/packs/compiled  (no such directory)\n  beside the tool                  /tmp/inspectra-help-edifac-118681ac-a8337543/tool/.store/edifact.cli/1.0.0-preview.2/edifact.cli/1.0.0-preview.2/tools/net10.0/any/packs/compiled  (no such directory)",
        "stderr": null,
        "outputLimitExceeded": false
      },
      "parsed": false,
      "payload": "Nothing at packs/compiled to read. Looked, in order:\n  --packs                          not given\n  EDIFACT_PACKS                    not set\n  beside Edifact.slnx or .edifact  no marker at or above /tmp/inspectra-help-edifac-118681ac-a8337543\n  per-user                         /tmp/inspectra-help-edifac-118681ac-a8337543/xdg-data/edifact/packs/compiled  (no such directory)\n  beside the tool                  /tmp/inspectra-help-edifac-118681ac-a8337543/tool/.store/edifact.cli/1.0.0-preview.2/edifact.cli/1.0.0-preview.2/tools/net10.0/any/packs/compiled  (no such directory)",
      "terminalNonHelp": true
    },
    {
      "command": "validate",
      "helpInvocation": "help validate",
      "result": {
        "status": "ok",
        "timedOut": false,
        "exitCode": 0,
        "durationMs": 23,
        "stdout": "edifact — read and validate UN/EDIFACT against the published directories\n\nCommands\n  ingest <path-to-distribution>   Read a UNECE release directory into a compiled pack.\n    --out <directory>             Where packs are written. Default: as resolved below.\n    --syntax <directory>          Where the ISO 9735 definitions live.\n    --no-strict                   Write a pack even if something went unaccounted for.\n    --include-prose               Keep the directory's descriptive text. Writes to\n                                  <pack-id>-prose, which .gitignore keeps out of the repo.\n\n  inspect <pack-id>               Print a compiled pack's manifest.\n    --packs <directory>           Where packs live.\n\n  validate <path-to-interchange>  Read an interchange and report what is wrong with it.\n    --pack <pack-id>              Which release to validate against. Default: whichever\n                                  one UNH says the interchange was written against.\n    --packs <directory>           Where packs live.\n    --syntax <directory>          Where the ISO 9735 definitions live.\n    --guide <path>                An implementation guide to layer over the release.\n                                  Repeatable; the order is the precedence order.\n    --tree                        Print each message's structure as well.\n    --quiet                       Report only errors, not warnings.\n    --round-trip                  Also write the interchange back out and report whether\n                                  it is reproduced character for character.\n\n  project <path-to-interchange>   Write each message of an interchange as a document.\n  project --message <key>         Write a message type and everything it names.\n    --pack <pack-id>              Which release. Worked out from UNH for a file;\n                                  required for --message.\n    --packs <directory>           Where packs live.\n    --syntax <directory>          Where the ISO 9735 definitions live.\n    --guide <path>                An implementation \n[truncated]",
        "stderr": null,
        "outputLimitExceeded": false
      },
      "parsed": true,
      "payload": "edifact — read and validate UN/EDIFACT against the published directories\n\nCommands\n  ingest <path-to-distribution>   Read a UNECE release directory into a compiled pack.\n    --out <directory>             Where packs are written. Default: as resolved below.\n    --syntax <directory>          Where the ISO 9735 definitions live.\n    --no-strict                   Write a pack even if something went unaccounted for.\n    --include-prose               Keep the directory's descriptive text. Writes to\n                                  <pack-id>-prose, which .gitignore keeps out of the repo.\n\n  inspect <pack-id>               Print a compiled pack's manifest.\n    --packs <directory>           Where packs live.\n\n  validate <path-to-interchange>  Read an interchange and report what is wrong with it.\n    --pack <pack-id>              Which release to validate against. Default: whichever\n                                  one UNH says the interchange was written against.\n    --packs <directory>           Where packs live.\n    --syntax <directory>          Where the ISO 9735 definitions live.\n    --guide <path>                An implementation guide to layer over the release.\n                                  Repeatable; the order is the precedence order.\n    --tree                        Print each message's structure as well.\n    --quiet                       Report only errors, not warnings.\n    --round-trip                  Also write the interchange back out and report whether\n                                  it is reproduced character for character.\n\n  project <path-to-interchange>   Write each message of an interchange as a document.\n  project --message <key>         Write a message type and everything it names.\n    --pack <pack-id>              Which release. Worked out from UNH for a file;\n                                  required for --message.\n    --packs <directory>           Where packs live.\n    --syntax <directory>          Where the ISO 9735 definitions live.\n    --guide <path>                An implementation \n[truncated]",
      "terminalNonHelp": false
    },
    {
      "command": "project",
      "helpInvocation": "project",
      "result": {
        "status": "failed",
        "timedOut": false,
        "exitCode": 2,
        "durationMs": 24,
        "stdout": "usage: edifact project <path-to-interchange> [--xml] [--out <directory>]\n       edifact project --message <key> --pack <pack-id> [--out <directory>]\n         [--packs <directory>] [--syntax <directory>] [--guide <path>]...",
        "stderr": null,
        "outputLimitExceeded": false
      },
      "parsed": true,
      "payload": "usage: edifact project <path-to-interchange> [--xml] [--out <directory>]\n       edifact project --message <key> --pack <pack-id> [--out <directory>]\n         [--packs <directory>] [--syntax <directory>] [--guide <path>]...",
      "terminalNonHelp": false
    }
  ]
}
