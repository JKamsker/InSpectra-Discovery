{
  "opencli": "0.1-draft",
  "info": {
    "title": "AutoMappic.Cli",
    "version": "0.5.1",
    "description": "CLI tool for AutoMappic: mapping validation and visualization."
  },
  "x-inspectra": {
    "artifactSource": "startup-hook",
    "generator": "InSpectra.Discovery",
    "hookCapture": {
      "cliFramework": "System.CommandLine",
      "frameworkVersion": "2.0.0.0",
      "systemCommandLineVersion": "2.0.0.0",
      "patchTarget": "Parse-postfix ()"
    },
    "cliFramework": "System.CommandLine",
    "cliParsedDescription": "AutoMappic CLI - Mapping validation and visualization tool."
  },
  "commands": [
    {
      "name": "validate",
      "description": "Validates all mapping declarations in a project.",
      "arguments": [
        {
          "name": "project",
          "description": "The path to the .csproj file.",
          "required": true,
          "arity": {
            "minimum": 1,
            "maximum": 1
          },
          "type": "String"
        }
      ]
    },
    {
      "name": "visualize",
      "description": "Generates a mapping graph visualization.",
      "arguments": [
        {
          "name": "project",
          "description": "The path to the .csproj file.",
          "required": true,
          "arity": {
            "minimum": 1,
            "maximum": 1
          },
          "type": "String"
        }
      ]
    }
  ]
}
