{
  "commands": [
    {
      "command": null,
      "helpInvocation": "--help",
      "result": {
        "status": "ok",
        "timedOut": false,
        "exitCode": 0,
        "durationMs": 25,
        "stdout": "cordango — author Cordango apps as semantic source.\n\n  cordango new <first-app>\n      Create a workspace and its first app. Needs an empty directory.\n  cordango add app <name>\n      Add another app to this workspace. One workspace holds many.\n  cordango configure [--target <id>]\n      Decide once where this workspace's apps are meant to run, and write it\n      into cordango.yaml. Asks, when there is somebody to ask. `--show` prints\n      what is set.\n  cordango import <app.definition.json> [--app <name>]\n      Bring an App Definition on disk in as source files you can edit. No\n      connection needed.\n  cordango check [--app <key>] [--target <id>]\n      Parse, lower and validate. No model, no database. With --target, also\n      ask whether that generator can build it.\n  cordango validate [--app <key>] [--target <id>]\n      The same command; the word for asking about a target.\n  cordango targets\n      What this build can generate, and what each target deliberately will\n      not.\n  cordango build [--app <key>]\n      Do what `cordango configure` said: write .cordango/build/<key>/\n      artifacts, and, when a target is configured, generate each application\n      into generated/<key>/. Deterministic.\n  cordango build --target <id> [--app <key>]\n      Generate with a target this once, whatever the configuration says. Add\n      --seed <n> for a different dataset, --allow-incomplete to accept a build\n      the generator cannot finish, and --runtime source to check the\n      Cordango.Standalone source in as a sibling project instead of restoring\n      the package.\n  cordango inspect [path] [--app <key>]\n      Describe the workspace, one app, or one aggregate.\n  cordango vocabulary [<name>]\n      What may be written: Cord's words, or one construct's schema.\n  cordango apply <ops.json> --app <key> --scope <kind[:key]>\n      Apply semantic operations and rewrite the affected source files.\n  cordango fmt [--app <key>]\n      Rewrite every .cordango.yaml file in canonical form.\n  cordango doctor\n      Check the workspace for p\n[truncated]",
        "stderr": null,
        "outputLimitExceeded": false
      },
      "parsed": true,
      "payload": "cordango — author Cordango apps as semantic source.\n\n  cordango new <first-app>\n      Create a workspace and its first app. Needs an empty directory.\n  cordango add app <name>\n      Add another app to this workspace. One workspace holds many.\n  cordango configure [--target <id>]\n      Decide once where this workspace's apps are meant to run, and write it\n      into cordango.yaml. Asks, when there is somebody to ask. `--show` prints\n      what is set.\n  cordango import <app.definition.json> [--app <name>]\n      Bring an App Definition on disk in as source files you can edit. No\n      connection needed.\n  cordango check [--app <key>] [--target <id>]\n      Parse, lower and validate. No model, no database. With --target, also\n      ask whether that generator can build it.\n  cordango validate [--app <key>] [--target <id>]\n      The same command; the word for asking about a target.\n  cordango targets\n      What this build can generate, and what each target deliberately will\n      not.\n  cordango build [--app <key>]\n      Do what `cordango configure` said: write .cordango/build/<key>/\n      artifacts, and, when a target is configured, generate each application\n      into generated/<key>/. Deterministic.\n  cordango build --target <id> [--app <key>]\n      Generate with a target this once, whatever the configuration says. Add\n      --seed <n> for a different dataset, --allow-incomplete to accept a build\n      the generator cannot finish, and --runtime source to check the\n      Cordango.Standalone source in as a sibling project instead of restoring\n      the package.\n  cordango inspect [path] [--app <key>]\n      Describe the workspace, one app, or one aggregate.\n  cordango vocabulary [<name>]\n      What may be written: Cord's words, or one construct's schema.\n  cordango apply <ops.json> --app <key> --scope <kind[:key]>\n      Apply semantic operations and rewrite the affected source files.\n  cordango fmt [--app <key>]\n      Rewrite every .cordango.yaml file in canonical form.\n  cordango doctor\n      Check the workspace for p\n[truncated]",
      "terminalNonHelp": false
    }
  ]
}
