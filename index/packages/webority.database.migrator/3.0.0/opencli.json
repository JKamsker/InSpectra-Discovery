{
  "opencli": "0.1-draft",
  "info": {
    "title": "webority-db-migrator - shared schema runner (DbUp), SQL Server or PostgreSQL",
    "version": "3.0.0",
    "description": "Shared DbUp-based schema runner for every Webority database project, SQL Server or PostgreSQL. Run-once journaled Scripts/ + run-always StoredProcedures/, tracked in dbo.SchemaVersions on SQL Server and public.SchemaVersions on PostgreSQL, both stamped in UTC with a content checksum."
  },
  "x-inspectra": {
    "artifactSource": "crawled-from-help",
    "generator": "InSpectra.Gen",
    "helpDocumentCount": 1
  },
  "commands": [],
  "options": [
    {
      "name": "--connection",
      "recursive": false,
      "hidden": false,
      "description": "Connection string (or set MIGRATOR_CONNECTION env var)",
      "aliases": [
        "-c"
      ],
      "arguments": [
        {
          "name": "STRING",
          "required": true,
          "arity": {
            "minimum": 1,
            "maximum": 1
          }
        }
      ]
    },
    {
      "name": "--provider",
      "recursive": false,
      "hidden": false,
      "description": "sqlserver (default) or postgres",
      "aliases": [
        "-p"
      ],
      "arguments": [
        {
          "name": "DB",
          "required": true,
          "arity": {
            "minimum": 1,
            "maximum": 1
          }
        }
      ]
    },
    {
      "name": "--db-root",
      "recursive": false,
      "hidden": false,
      "description": "DB project folder with Scripts/ and/or StoredProcedures/ (default: .)",
      "aliases": [
        "-r"
      ],
      "arguments": [
        {
          "name": "DIR",
          "required": false,
          "arity": {
            "minimum": 0,
            "maximum": 1
          }
        }
      ]
    },
    {
      "name": "--what-if",
      "recursive": false,
      "hidden": false,
      "description": "List pending run-once scripts, run the applied-script guard below,\nand exit. Changes nothing.",
      "aliases": [
        "-w"
      ]
    },
    {
      "name": "--mark-applied",
      "recursive": false,
      "hidden": false,
      "description": "Baseline: record existing run-once scripts as applied WITHOUT executing",
      "aliases": [
        "-m"
      ]
    },
    {
      "name": "--force",
      "recursive": false,
      "hidden": false,
      "description": "Allow --mark-applied against a DB whose journal already has entries\n(only valid with --mark-applied)",
      "aliases": [
        "-f"
      ]
    },
    {
      "name": "--tx-per-script",
      "recursive": false,
      "hidden": false,
      "description": "Wrap each script in its own transaction (default: off)",
      "aliases": [
        "-x"
      ]
    },
    {
      "name": "--timeout",
      "recursive": false,
      "hidden": false,
      "description": "Per-script command timeout (default: 600)",
      "aliases": [
        "-t"
      ],
      "arguments": [
        {
          "name": "SECONDS",
          "required": false,
          "arity": {
            "minimum": 0,
            "maximum": 1
          }
        }
      ]
    },
    {
      "name": "--accept-checksum",
      "recursive": false,
      "hidden": false,
      "description": "Accept the current content of one already-applied script (repeatable)\nThat rewrites the stored hash for that one script from the file on disk, printing the old and new\nvalues. There is deliberately no switch to turn the guard off everywhere.\nWhat the command line will not accept:\nA bare word with no option in front of it, so a connection string passed without --connection\nstops the run instead of being dropped in favour of MIGRATOR_CONNECTION. A value that is itself\nan option, so --connection --what-if is a typo rather than a connection string. --force without\n--mark-applied, which is the only thing it relaxes. --what-if together with --mark-applied,\nwhich ask for opposite things.\nExit codes:\n0  success\n1  migration failure\n2  usage/config error",
      "arguments": [
        {
          "name": "ACCEPT_CHECKSUM",
          "required": true,
          "arity": {
            "minimum": 1,
            "maximum": 1
          }
        }
      ]
    },
    {
      "name": "--help",
      "recursive": false,
      "hidden": false,
      "description": "Show this help and exit\nThe applied-script guard:\nEvery run-once script's content is hashed into SchemaVersions.Checksum when it is applied. On a\nlater run, a script whose content no longer matches its stored hash fails the run, because an\napplied script is immutable by convention and an edit to one is silently ignored on every\ndatabase that already ran it.\nLine endings and a trailing newline are ignored, so the same script on Windows and Linux agrees.\nIf the change IS intended, review it and accept it by journal name:",
      "aliases": [
        "-h"
      ]
    }
  ]
}
