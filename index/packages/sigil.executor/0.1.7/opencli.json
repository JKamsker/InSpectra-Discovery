{
  "opencli": "0.1-draft",
  "info": {
    "title": "sigil",
    "version": "0.1.7",
    "description": "Cross-platform test executor for Sigil visual marker-based UI testing. Compile and run automated UI tests using visual markers."
  },
  "x-inspectra": {
    "artifactSource": "startup-hook",
    "generator": "InSpectra.Discovery",
    "hookCapture": {
      "cliFramework": "System.CommandLine",
      "frameworkVersion": "2.0.0.0",
      "systemCommandLineVersion": "2.0.0.0",
      "patchTarget": "Parse-postfix ()"
    },
    "cliFramework": "System.CommandLine",
    "cliParsedDescription": "Visual marker-based UI testing for Blazor. https://usesigil.dev"
  },
  "commands": [
    {
      "name": "hash",
      "description": "Display the encoding for a marker ID",
      "arguments": [
        {
          "name": "id",
          "description": "The marker ID to hash",
          "required": true,
          "arity": {
            "minimum": 1,
            "maximum": 1
          },
          "type": "String"
        }
      ]
    },
    {
      "name": "run",
      "description": "Execute Sigil test scripts (.sigil or .txt)",
      "arguments": [
        {
          "name": "scripts",
          "description": "The script file(s) to run (.sigil or .txt)",
          "required": false,
          "arity": {
            "minimum": 0,
            "maximum": 100000
          },
          "type": "FileInfo[]"
        }
      ]
    },
    {
      "name": "watch",
      "description": "Run a script and re-run when file changes (requires subscription)",
      "arguments": [
        {
          "name": "script",
          "description": "The .sigil script file to watch",
          "required": true,
          "arity": {
            "minimum": 1,
            "maximum": 1
          },
          "type": "FileInfo"
        }
      ]
    },
    {
      "name": "login",
      "description": "Login with your Sigil account email",
      "arguments": [
        {
          "name": "email",
          "description": "Your Sigil account email (will prompt if not provided)",
          "required": false,
          "arity": {
            "minimum": 0,
            "maximum": 1
          },
          "type": "String"
        }
      ]
    },
    {
      "name": "status",
      "description": "Show subscription status"
    },
    {
      "name": "logout",
      "description": "Clear saved login"
    }
  ]
}
