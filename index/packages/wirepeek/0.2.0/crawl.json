{
  "commands": [
    {
      "command": null,
      "helpInvocation": "--help",
      "result": {
        "status": "ok",
        "timedOut": false,
        "exitCode": 0,
        "durationMs": 40,
        "stdout": "WirePeek — local HTTP(S) capture proxy\r\n\r\nUsage: wirepeek [options]\r\n\r\nOptions:\r\n  --ui-port <port>     Port for the web UI (default 5266)\r\n  --proxy-port <port>  Port the capture proxy listens on (default 8888)\r\n  -h, --help           Show this help\r\n\r\nUnrecognized --options are passed through to the ASP.NET Core host\r\n(e.g. --environment Development).",
        "stderr": null,
        "outputLimitExceeded": false
      },
      "parsed": true,
      "payload": "WirePeek — local HTTP(S) capture proxy\r\n\r\nUsage: wirepeek [options]\r\n\r\nOptions:\r\n  --ui-port <port>     Port for the web UI (default 5266)\r\n  --proxy-port <port>  Port the capture proxy listens on (default 8888)\r\n  -h, --help           Show this help\r\n\r\nUnrecognized --options are passed through to the ASP.NET Core host\r\n(e.g. --environment Development).",
      "terminalNonHelp": false
    }
  ]
}
